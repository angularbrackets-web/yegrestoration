# BK-10 — Cutover: booking becomes the quote path, the form becomes messages

**Status:** plan-reviewed 2026-08-11 — 1 blocker, 3 should-fixes, 3 nits, all
accepted and folded in below. The blocker was a verification trap on the
ticket's own highest-weighted risk: the failure truth table and send-then-stamp
ordering were specced into the route bodies, where the repo's mute mechanics
(`BOOKING_NOTIFY_DISABLED` silences injected seams too — the recorded BK-08
lesson) make the success arms unobservable, so the asserts could never go red.
The decision logic now lives lib-level behind injectable deps and the
verification names, per assert, whether it runs lib-level or route-level.
Should-fixes: the truth table gained its missing fourth arm (`skipped`),
migration 004 got explicit apply-before-deploy sequencing with the window's
failure mode stated, and the CTA sweep learned the blog posts' entity spelling
(`&#35;contact`) that a literal `#contact` grep cannot see. The reviewer also
confirmed every file:behaviour claim in the draft, including the three places
this ticket corrects stale ROADMAP counts.
**Phase:** P5 · **Tier:** Reviewed · **Depends on:** BK-05 (booking live on
production), BK-13 (navbar CTA already points at `/book/`) · **Blocks:** BK-11
(launch checks assume the cutover's tracking shape).

**Scope amendment, client-decided 2026-08-11:** the ROADMAP scoped this ticket
as "booking replaces the contact form; `leads` read-only." The client wants a
written channel to survive for questions, so the form is **demoted and
reframed, not deleted**: quotes and assessments have exactly one door
(`/book/`), the form becomes a general "send us a message" channel living only
on `/contact/`, and `leads` therefore **stays writable indefinitely**. The
ROADMAP P5 row and the read-only comment in `scripts/migrations/001-leads.ts`
are updated to match. Decided in conversation with the user; the funnel logic
(one quote path, one question path, one emergency path — each visitor has an
obvious door) is recorded here so a future copy edit doesn't quietly recreate
two quote doors.

Reviewed because it rewrites the site's primary conversion path, changes a
public write path (`api/contact.ts` — schema, DB semantics, send handling),
retires a live Google Ads conversion tag, and touches customer-facing email
templates. Risk areas, in the order a reviewer should weight them:

1. **Silent lead loss.** `leads.service` is `TEXT NOT NULL`
   (`scripts/migrations/001-leads.ts`) and the route swallows DB failures —
   making `service` optional without the migration means an omitted service
   violates the constraint, the catch eats it, the email still sends, and the
   visitor sees success while the row never lands. The migration, the zod
   shape, and the insert semantics must move together. Likewise the ROADMAP
   trap this ticket owns: **Resend resolves with `{ data: null, error }` and
   never throws**, so today a bounced key or an outage reaches the visitor as
   "message sent." After demotion the form is the only written channel — a
   false success costs the lead permanently.
2. **Conversion-tracking cutover.** The Ads form-conversion block is retired
   from code while the account-side action goes Secondary (user checklist
   below — never deleted). `generate_lead` survives on the message form. Any
   surviving `PUBLIC_*` read must stay a literal `import.meta.env.PUBLIC_X`
   expression (Known trap: helper-wrapped reads compile to `undefined` in the
   client bundle).
3. **Site-wide CTA repoint fan-out.** Quote CTAs live on the homepage, every
   service page, the insurance page, about, three blog posts, the mobile call
   bar, the footer, and `servicePath()`'s fallback. A missed site is a dead
   anchor or a two-door funnel; a new unslashed link is a 308. The existing
   slash scan **cannot see** several of the exact literals this ticket fixes
   (see Verification — the scan regex is extended first, red-observed).
4. **Customer-facing email copy.** The five reply templates are quote-framed,
   interpolate a possibly-absent service label unguarded (`String.replace`
   with null writes the literal text "null" into a customer email), and tell
   customers to call **(780) 244-4747 — a number the client does not
   recognize**. The advertised line is (780) 479-3285. Every admin reply since
   the templates shipped has carried a wrong number; this ticket fixes it.

## Goal

`/book/` is live and proven, but the site still routes quote intent at a
callback form: every "Get a quote" CTA anchors to the form, the form's email
arrives as "New Quote Request," and Ads optimizes toward form submits. After
this ticket: **booking is the one quote path** (every quote/assessment CTA
points at `/book/`, prefilled where intent is already declared), **the form is
the one question path** ("Have a question or want a quote? Send us a message,"
on `/contact/` only), **phone is the one emergency path** (promoted, not
buried, on `/book/` itself), and the admin leads surface — which now lives
forever — gets the fixes it has been owed: checked sends, slashed links,
timezone-correct dates, the right phone number.

## Scope

1. **Migration 004** — `ALTER TABLE leads ALTER COLUMN service DROP NOT NULL`,
   idempotent per the migration framework's shape, ledger row written last.
   `src/lib/db.ts`: `Lead.service` becomes `string | null`; `created_at` /
   `replied_at` correct from `string` to `Date` / `Date | null` per the
   documented driver behaviour (timestamptz arrives as `Date`; every consumer
   wraps in `new Date(...)`, which takes either — the Known-trap entry closes).
2. **`src/lib/contact-message.ts`** — the send path AND the decision logic,
   seam-shaped like `booking-notify.ts` so a script can drive every arm
   lib-level (plan-review blocker: decision logic in a route body is
   unobservable — the mute silences injected seams too, the recorded BK-08
   lesson at the top of `verify-booking-admin-db.ts`): validation schema,
   subject builder, HTML/text builders, and a `handleContactMessage(input,
   deps: { insert, send, … })` that owns the **full truth table** — the
   route becomes a thin shell mapping its outcome to a Response. The table,
   all four arms pinned: send ok → `ok` (insert failure logged, not fatal);
   send failed / resolved `{ error }` → `failed` (500); missing key →
   `failed`, logged, before any send; **`skipped`** (the
   `BOOKING_NOTIFY_DISABLED` mute) → treated as `ok`, test-only by
   construction — the arm route-level verification actually sees, defined
   here rather than left to the keyboard. Schema changes: `service`
   optional-or-empty (the
   `.optional().or(z.literal(''))` shape the email field already uses — the
   Svelte form initializes `service: ''`), `message` becomes **required**
   (it is a message form), deprecated `z.string().email()` replaced with the
   current spelling while the line is being rewritten. Subject: "New message —
   {label}" with service, "New message" without. `escapeHtml` reused from the
   booking email module (the 4-char local copy retires).
3. **`src/pages/api/contact.ts`** — a thin shell over
   `handleContactMessage`: parse, wire real deps (`readEnv('RESEND_API_KEY')`
   — bare `import.meta.env` reads block tsx-driven verification and are the
   BK-12 rule), map outcome → Response. The truth-table *rationale* (why
   send is the source of truth): the office reads leads by email, so the
   visitor's success message must match what the inbox saw — a DB-write
   failure is a logged, best-effort-record loss; a send failure is a 500
   even when the row landed (a retried duplicate is visible in admin and
   costs nothing; a false success costs the lead). **Sequencing:** migration
   004 is applied to production **before** this deploys (BK-05 precedent).
   The window's failure mode under the new client, old schema — omitted
   service → constraint violation → logged loudly, email still sends,
   visitor still sees success, admin row lost — is tolerable for the
   minutes it lasts and is why the order is migration first, deploy second.
4. **`src/components/ContactForm.svelte`** — reframed: heading/copy "Have a
   question or want a quote? Send us a message"; service dropdown optional
   ("Not sure / just a question" as the empty choice); message required;
   success copy "Message sent — we'll get back to you the same business day"
   (replaces the 15-minute promise — see Assumptions); fetch goes to
   **`/api/contact/`** slashed (the ROADMAP's named pre-existing 308);
   `defaultService` prop deleted (its only pass site loses the form; the
   `state_referenced_locally` warning dies with it); the five
   `a11y_label_has_associated_control` warnings fixed with `for`/`id` pairs;
   tracking: the Ads `conversion` block **removed**, GA4 `generate_lead`
   kept (service param now optional). `.form-field` hooks kept so the
   `/contact/` GSAP stagger still animates.
5. **CTA repoint sweep — every quote/assessment door becomes `/book/`:**
   - `src/sections/ServiceSection.astro` — both `#contact` CTAs → `/book/`.
   - `src/components/ServiceHero.astro` `#quote` → `/book/?service={id}`.
   - `src/pages/[service].astro` — `<ContactForm>` section becomes a booking
     CTA section (heading kept, copy adjusted, button `/book/?service={id}`);
     the `#quote` id stays so stray anchors still land.
   - `src/pages/insurance-claims.astro` — form section likewise →
     `/book/?route=insurance`; its `#quote` link at the top repoints; hero
     phone primacy untouched (step 01 is "call first" and stays).
   - `src/pages/index.astro` / `ContactSection.astro` — **left info column
     stays** (phone/email/hours/service-area, the "we answer every call live"
     card — the homepage's NAP block); the form column becomes a booking CTA
     card; the `#contact` id stays; the GSAP block updates to the new nodes.
     The form itself now renders only on `/contact/` (a
     `showForm`-style split or a dedicated variant — implementer's choice,
     but `/contact/` keeps the real form).
   - `src/components/MobileCallBar.astro` — `formAnchor` pass sites become
     `/book/` links, label "Book Assessment" (honest about the destination;
     "Get Help Now" belongs to the phone button), stale `#quote` default
     removed (prop required or defaulted to `/book/`).
   - `src/components/VideoReel.svelte` `#contact` → `/book/`.
   - `src/layouts/BlogLayout.astro` CTA and the three blog MDX `/#contact`
     links (`signs-of-hidden-mold…`, `edmonton-spring-flooding…`,
     `what-to-do-after-house-fire…`) → `/book/`.
   - `src/pages/about.astro` — both "Get Free Assessment" CTAs → `/book/`.
   - `src/data/services.ts` `servicePath()` fallback → `/book/` (BookingForm's
     dropdown covers all services + other, so unpaged services land coherently).
   - `src/components/Footer.astro` — nav gains a "Book an Assessment" →
     `/book/` link; the bottom "request a callback" CTA keeps pointing at
     `/contact/` (callback framing is message framing).
6. **`/book/` page hardening — the escape hatches:**
   - Emergency block at the top: "Water coming in right now? Don't book — call
     (780) 479-3285. We answer live, 24/7."
   - Escape hatch under the picker: "Not ready to book? Call us or
     [send a message](/contact/)."
   - Explicit **all-days-full state**: "We're fully booked for the next two
     weeks — call or send a message and we'll fit you in" (replaces the
     wrong-state "try another day" copy when every day is full).
   - A "closed Fridays" hint line so the missing column reads as policy, not
     a bug; an out-of-area line ("Serving Edmonton and area — outside? Call
     first"); tenant helper copy on the payment-route step ("Renting? Choose
     insurance and mention it in the description"); "Other Emergency" service
     option relabelled "Other / not sure" with an inline "if this can't wait,
     call now" note.
   - `?service=` and `?route=` query-param prefill in `BookingForm.svelte`
     (client-side read; unknown values ignored, nothing trusted beyond
     selecting an option that already exists in the dropdowns).
   - `noindex` comes off `book.astro` and the sitemap exclusion for `/book/`
     is removed — **`/book/confirmed/` keeps both by design** (the config
     comment's "both come off together" edits to match). Breadcrumb +
     `WebPage` JSON-LD added to match every other page.
   - GA4 funnel events so a post-cutover CPA regression is not a black box:
     `booking_availability_error` and `booking_availability_empty` (fired
     from the existing error/empty states; no new PII, no new deps).
7. **`/contact/` page reframe** — meta description and `ContactPage` JSON-LD
   stop selling a 15-minute quote form; intro copy matches the message
   framing; phone stays first for emergencies; the page keeps the real form.
   `src/pages/llms.txt.ts` — `/book/` becomes the named assessment/booking
   path; the `/contact/` description line reframes to questions/messages;
   **and the per-service "(request via …/contact/)" pointer stamped onto
   every unpaged service line becomes `/book/`** (plan-review nit — that
   line is a quote pointer, not a contact pointer).
8. **Admin leads surface (lives forever now):**
   - Slashed-path constants join `booking-admin.ts` (`adminLeadPath(id)`,
     the reply endpoint) and every unslashed site rewires: the two page
     hrefs, the reply form action, and **all four** `Location` headers in
     `api/admin/reply.ts` (the ROADMAP says three; there are four).
   - The three bare `toLocale*` sites on the leads pages switch to
     `formatAdminTimestamp` (the wrong-day-pattern trap closes).
   - `api/admin/reply.ts`: the send+stamp flow extracts into a
     deps-injectable helper (the `notifyAndStamp` shape from
     `booking-notify.ts` — plan-review blocker, same reasoning as scope 2)
     with `readEnv` in the route shell; the Resend **resolved** result
     checked; **send-then-stamp ordering** — `replied_at`/`status` update
     only after a successful send (today a failed send still stamps
     `replied` and shows `?success=1`); a **muted** (`skipped`) send is
     treated as sent — stamp and `?success=1`, test-only by construction —
     so route-level runs exercise the success path end to end; failure
     redirects to `?error=sendfailed`, which the detail page's existing
     error banner learns to render; the bare `throw` on a missing key
     becomes a logged redirect, matching every other arm.
   - `src/lib/reply-templates.ts`: **(780) 244-4747 → (780) 479-3285 in all
     four sites**; templates reframed off "Quote Request" language; service
     interpolation guarded (absent service → "your restoration needs", never
     the literal "null"). The leads pages' own display of a NULL service
     gets the same guard (`admin/index.astro`, `admin/leads/[id].astro` —
     render "not specified" rather than relying on Astro's null-renders-empty
     behaviour), so AC1's "no 'null' anywhere" is designed, not incidental.
9. **a11y debt in touched files** — Navbar.svelte's two warnings (the `<nav>`
   listener, the dialog tabindex) and VideoReel.svelte's one (the mouseenter
   `<div>`), since both files are already in this diff; Navbar's dead
   `#contact` handler (no nav link passes it since BK-13) removed.
10. **Docs and env bookkeeping** — `PUBLIC_AW_FORM_LABEL` removed from
    `env.d.ts` and `.env.example`; `docs/google-ads-tracking-setup.md` updated
    (step 5, the table row, the env line, the verification steps that expect a
    form-submit conversion) plus a dated cutover note; ROADMAP: P5 row
    amended, owned traps closed (Resend swallow, 308 family, leads
    `toLocale*`, `Lead` timestamps, ContactForm/Navbar/VideoReel warnings),
    stale counts fixed (ContactForm has 5 label warnings, not 4; reply.ts has
    four Locations, not three), `001-leads.ts` read-only comment updated, and
    a record of the 244-4747 defect (fabricated-constant class — a phone
    number no one recognizes shipped in customer email; the trap is "copy
    that states contact details must be checked against `BUSINESS`").
11. **Verification** — see below; includes extending the slash scan so the
    literals this ticket fixes are actually observable.

## Out of scope

- **Google Calendar integration** — separate decision pending the client
  (levels sketched for the user 2026-08-11; likely a small invite-email
  ticket beside BK-06 if wanted).
- **BK-06** (reminders; Twilio number (780) 720-8856 pending verification)
  and **BK-11** (launch checks; the GA4-timezone and Ads-account audit items
  land there).
- **Deleting the `leads` table or the reply feature** — both live on.
- **Blog-page bare `toLocale*` dates** (prerendered, date-only — recorded in
  Known traps, owner BK-11/none), **BrandLogo's 4 `state_referenced_locally`
  warnings** (untouched file, recorded), **`is:inline` JSON-LD hints**
  (recorded).
- **Redirects** — no URLs change; none needed.

## Design notes

- **One door per intent.** Quote → `/book/`; question → message form;
  emergency → phone. The reframe must not leave the form *looking* like the
  lower-commitment quote path or visitors will route around booking — that is
  why the copy says "or want a quote" (the shopper's word) while every CTA
  labelled quote/assessment goes to booking.
- **Send is the source of truth for the contact route** because the office
  works from the inbox; the DB row is the admin-surface record. The truth
  table in scope 3 is deliberate, not defensive coding — the reviewer should
  challenge the table, not the absence of one.
- **The Ads action is demoted account-side, not deleted, and not by us** —
  deleting destroys history; Secondary preserves comparability while removing
  it from Smart Bidding. Code retires the tag; the user runs the checklist.
- **Query-param prefill trusts nothing**: `?service=`/`?route=` only ever
  select an option that already exists in the form's own lists; any other
  value is ignored. No write path reads them.
- **`#contact` / `#quote` ids survive** even where CTAs repoint, so stale
  links (bookmarks, cached SERPs, the blog posts until their edit deploys)
  degrade to landing on a relevant section instead of nowhere.

## Acceptance criteria

1. A message with no service lands: row inserted with NULL service, email
   subject "New message", admin list and detail render it without "null"
   anywhere, and a reply built from any template contains neither "null" nor
   (780) 244-4747.
2. The contact route's truth table holds: send-ok → `ok: true` (DB failure
   logged, not fatal); send-failed / `error` in the resolved value / missing
   key → 500 and **no** false success. The reply route stamps
   `replied_at`/`status` only on a successful send and answers `?error=sendfailed`
   (rendered by the page) otherwise. Both routes read the key via `readEnv`.
3. No `#contact`/`#quote`-anchored or `/contact/`-pointing **quote/assessment
   CTA** remains anywhere in `src/` or `src/content/`; `servicePath()`
   fallback is `/book/`; every new link is slashed; the form posts to
   `/api/contact/` slashed.
4. `/book/` renders the emergency block, the not-ready escape hatch, the
   all-full state, the Friday hint; honours `?service=` and `?route=`
   prefill; is indexed (no `noindex`, present in the sitemap) while
   `/book/confirmed/` remains excluded and noindexed.
5. The Ads `conversion` event no longer fires from the form;
   `generate_lead` still does; `PUBLIC_AW_FORM_LABEL` is gone from
   `env.d.ts`/`.env.example`; the booking conversion path is untouched
   (`PUBLIC_AW_BOOKING_LABEL` still read as a literal expression).
6. Admin leads pages and reply flow: slashed everywhere (zero 308s on the
   click path), dates render in America/Edmonton via `formatAdminTimestamp`,
   migration 004 applied, `Lead` typing matches driver behaviour.
7. `svelte-check` warning count drops from 13 to 4 (BrandLogo's remain, by
   scope); no new warnings.
8. `npm run typecheck` and `npm run build` pass.

## Verification

- **Slash-scan extension first, red before use**: `verify-booking-admin.ts`'s
  regex currently cannot match `href="/admin"`, `'/admin?error=…'`, or the
  query-string template Locations — the exact literals this ticket fixes
  (found by this ticket's pre-review; the ROADMAP's "one-line addition"
  claim was wrong and is corrected). Extend the scan (bare `/admin`,
  query-string forms) or pin those spellings by literal needle; either way
  each fixed site must be observable: re-break one representative of each
  spelling class and watch red.
- **`verify:contact` (new)** — split by where each assert can actually go
  red (the plan-review blocker's rule):
  - **Lib-level, injected deps, no mute in play** (the BK-08 pattern —
    `deps.send` is the seam, so `BOOKING_NOTIFY_DISABLED` never enters):
    every truth-table arm — resolved-`{error}` → failed, thrown send →
    failed, missing key → failed-before-send, send ok + insert failure →
    ok-with-loud-log, send ok → ok — plus schema shapes (empty-vs-absent
    service, required message) and subject both ways.
  - **Route-level, dynamic import against the dev branch under
    `BOOKING_NOTIFY_DISABLED=1`** (BK-02 pattern: `DATABASE_URL` swap,
    refuse without `DATABASE_URL_DEV`, own fixture cleanup): the `skipped`
    arm maps to `ok: true` (the pinned test-only row), NULL-service insert
    lands post-migration, validation arms, no email escapes the mute.
- **`verify:booking:admin:db` extension** — same split: send-then-stamp
  ordering **lib-level through the extracted helper's injected send**
  (failed send leaves `replied_at` NULL and status unchanged; successful
  send stamps once; muted send stamps — the pinned row); route-level under
  mute: the `?error=sendfailed` and `?success=1` redirect arms,
  template-null guard, the four Locations slashed.
- **CTA sweep assert** — a scan (new or extended script) over `src/` +
  `src/content/` for `#contact`, `#quote`, **their HTML-entity spellings
  (`&#35;contact` / `&#35;quote` — the three blog MDX links use the entity
  form and a literal grep cannot see them; the plan reviewer's own first
  pass missed them exactly this way)**, and quote-labelled CTAs pointing at
  `/contact/`, with an allowlist for the deliberate survivors (the ids
  themselves, the footer callback CTA, `/contact/` links that are
  message-framed). Red: reintroduce one repointed CTA in plain spelling
  **and one in entity spelling**.
- **Tracking**: build output grep — `dist/client` carries no
  `PUBLIC_AW_FORM_LABEL` substitution and no `conversion` send_to from the
  form bundle; `generate_lead` still present; booking bundle still carries
  the inlined booking label (the BK-04 build-check pattern).
- **Prefill**: BookingForm unit-drives `?service=mold` / `?route=insurance` /
  `?service=garbage` (ignored) through whatever seam the implementation
  exposes.
- Migration: `migrate:status` on dev branch before/after; NULL-service
  insert red-observed pre-migration (the swallowed-constraint failure seen
  once, deliberately, on the dev branch). Production sequencing per scope 3:
  004 applied before deploy, recorded in the gate table like BK-05's 003.
- Gates: `npm run typecheck`, `npm run build`, `verify:contact`,
  `verify:booking:admin` (+scan extension), `verify:booking:admin:db`,
  `verify:booking:files`, `verify:booking:smoke` (regression — the booking
  path must not notice this ticket).
- Post-deploy (user): submit a test message with no service picked; see it
  in admin without "null"; reply with a template and confirm the email
  carries 479-3285; click a service-page CTA → `/book/` with the service
  preselected; confirm `/book/` appears in the live sitemap.

## User checklist (Ads account — not code, not the implementer's)

1. Google Ads → Goals → Conversions: set the **form** conversion action to
   **Secondary** (do not delete — history and comparability die with it).
2. Confirm **"Assessment booked" is Primary** and give it a value meaningfully
   above the call action's $1 (a booked assessment is worth more than a call;
   exact value is yours).
3. Audit sitelinks/callouts/final URLs for anything promising a "quote form"
   or pointing at `#quote`/`#contact` anchors.
4. Expect a 1–2 week Smart Bidding learning period after the tag change; add
   a GA4 property annotation on cutover day ("form demoted to messages;
   quote path = /book/") so before/after comparisons stay readable.
   (GA4 timezone check stays in BK-11.)

## Assumptions log

Settled without the user; each is a reviewer's or the user's to overturn.

- **The 15-minute promise retires with the quote form** (user delegated
  2026-08-11). Reasoning: it was a *callback* promise attached to quote
  requests; quotes now confirm instantly via booking, and pinning a
  15-minute SLA to a general message channel is a commitment the office
  hasn't made. Phone keeps "we answer every call live"; the form promises
  "same business day." One copy line to revert if the client disagrees.
- **`message` becomes required** — a message form with an optional message
  collects empty rows; name+phone-only outreach is what the phone line is for.
- **Homepage keeps its info block; the form column becomes a booking CTA
  card** (user delegated 2026-08-11). The NAP-adjacent block and emergency
  card are the homepage's local-SEO and trust surface; only the form leaves.
- **Send is the source of truth** for both public and reply routes (design
  note above).
- **`generate_lead` keeps its name** across the reframe; the GA4 annotation
  marks the semantic change (quote request → message). Renaming would break
  every existing report for a purity win.
- **Funnel events are the two named ones only** — availability error/empty.
  Per-CTA click events are deferred; GA4's enhanced measurement plus the two
  failure events bound the diagnosis space at this volume.
- **One commit, not two.** The plan reviewer noted a clean seam (admin-leads
  surface could land before the public cutover); kept whole because the
  halves share `db.ts`, the verify scripts, and AC7's warning arithmetic,
  and the P2 retro already rejected size caps for exactly this shape.
- **(780) 720-8856 is recorded as the planned Twilio/SMS number** (user,
  2026-08-11) — BK-06's concern, noted in ROADMAP with it.

## Gate results

| Gate | Result |
| --- | --- |
| `npm run typecheck` | pending |
| `npm run build` | pending |
| `verify:contact` (new) | pending |
| `verify:booking:admin` (+scan extension) | pending |
| `verify:booking:admin:db` (extended) | pending |
| CTA sweep + tracking build grep | pending |
| regressions (files, smoke) | pending |
| migration 004 on dev branch | pending |

## Red-observed

Pending implementation.
