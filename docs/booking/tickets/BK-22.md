# BK-22 — Mandatory email and at least one photo/video on the public form

**Status:** reviewed 2026-08-13, committed. Implementation review by a fresh
agent: **no blockers, no should-fixes, three nits** (below), one out-of-scope
defect recorded in the ROADMAP's Known traps. All nine gates re-run first-hand
by the reviewer rather than read off the table, all green; four red modes
re-observed first-hand, one of them a mode the implementer never broke.

*Previously:* implemented 2026-08-13. All nine gates green, eight red-first
failure modes observed (the table's seven, with (c) split in two — see the gate
table). 16 files, +857/−62 (the "14 files, +739/−49" written here at
implementation time was stale before the ticket and ROADMAP edits landed), left
uncommitted for the reviewer session. Four things diverged from the plan and
are recorded in the Assumptions log: the six-site typecheck claim, red row
(f)'s expected script, the island's own email rule, and
`verify-booking-smoke.ts`.

*Previously:* approved 2026-08-12 — plan-reviewed by a fresh agent (4 blockers,
7 should-fixes, 8 nits, all folded in; see Plan review below), and the one
open question answered by the user: `DRAFT_RATE_LIMIT_PER_HOUR` **20 → 50**.
**Phase:** P7 · **Tier:** Reviewed (public write path; changes what the
booking endpoint accepts) · **Depends on:** nothing — BK-21 landed
(`90c65a9`). *(The draft claimed a dependency on BK-21 because "both touch
`booking-email.ts`". That was false: this ticket touches no email file. Moot
now, corrected for the record.)* · **Blocks:** nothing — BK-23 is independent
and either order would work. This is first because it filters time-wasters
*immediately*, while confirmation is still instant.

## Why

Client decision, 2026-08-12 (ROADMAP P7): "drowning in time-waster bookings",
wants harder requirements to book. Two new requirements on the public form —
**email** and **at least one photo or video** — plus the standing exemption:
"if we enter ourselves we will ask them to text pictures/videos. It won't go
to review process." The 100 MB/file limit stays ("should be fine for now").

This ticket is deliberately the *whole* of the requirement change and none of
the review lifecycle. When it lands, a public booking still confirms
instantly; it just cannot be made without an address to email and something to
look at. BK-23 flips the flow afterwards.

## What the code does today (read, not recalled — verified twice, 2026-08-12)

- `booking-payload.ts:140` — `const email = optional('email', 'Email')`;
  `BookingPayload.email` is `string | null`. The header comment at `:87-91`
  argues the point: "email is optional here anyway — the phone number is how
  this business actually calls people back." That comment is now wrong and is
  part of the diff.
- `booking-form.ts:172-173` — the island's mirror of the same rule:
  format-checked when present, never required.
- `booking-form.ts:205` — "Step 3 collects only optional things: photos and
  consent." Step 3 returns no errors at all.
- `booking-admin-entry.ts:151,197` — `parseAdminEntry` wraps
  `parseBookingPayload` and **forwards its caller's `ParseOptions` verbatim**
  as the second argument. This is the seam the exemption goes through; there
  is no second copy of the validation to keep in step.
- Files: `/api/booking/upload-token/` writes the `appointment_files` row at
  **token-mint time**, before any bytes exist (`upload-token.ts:113`).
  `upload_state` becomes `'uploaded'` only when Vercel's `onUploadCompleted`
  webhook fires — which **never fires on localhost** and "can lag" in
  production (`booking-commit.ts:36-39`). `insertBooking` therefore claims
  files "regardless of `upload_state`".
- `BookingForm.svelte:389` — the payload's `draft_token` is
  `attachments[0]?.draftToken ?? null`, so a form with no attachments sends no
  token and the endpoint never resolves a `draftId`.
- `BookingForm.svelte:396-401` — the island already logs when the server's
  `filesAttached` disagrees with the number of uploads the browser saw
  succeed. The divergence below is known territory, not a new discovery.
- `createDraftSession` (`booking-form.ts:510-537`) does **not** cache a failed
  mint — `current` stays falsy on a `null` result, so the next file selection
  retries. A transient draft failure self-heals today.

## The three design questions this ticket has to answer

### Q1 — What counts as "has a photo", server-side?

The server's only evidence is `appointment_files` rows. Three candidate rules:

| Rule | False-rejects | False-accepts | Dev |
| --- | --- | --- | --- |
| any row for the draft | none | upload minted a token then failed | works |
| row with `upload_state='uploaded'` | webhook lag rejects a real customer | none | **impossible — never fires on localhost** |
| `head()` the blob at commit | Blob outage rejects | none | needs a live store |

**Decided: any row**, i.e. `COUNT(*) FROM appointment_files WHERE
draft_id = $1 AND appointment_id IS NULL`. It behaves identically in dev and
production; the `upload_state` rule is not merely stricter but *unrunnable
locally*, which would make every dev booking impossible and the verify story a
fiction. `head()` adds a network hop inside `POST_COMMIT_BUDGET_MS` to a path
already running four queries. The one false-accept — token minted, PUT failed
— lands on the customer you least want to bounce.

**Corrected from the draft (plan review S2):** the draft claimed this is
"byte-for-byte the same predicate `insertBooking` already claims by, so the
gate can never disagree". It is not. `insertBooking` holds its own copy of the
predicate inside the CTE (`booking-commit.ts:75-82`), plus `AND EXISTS (SELECT
1 FROM new_appt)`, and the two are evaluated at different instants against a
table the cleanup cron writes to. A named `countUnclaimedFiles` beside it is a
**second hand-written copy** — the exact failure mode the ticket cites
`claimedFilePathname` to avoid. The divergence risk is small, but it is not
zero and the ticket must not claim impossibility it does not have. It is
managed by a shared fixture, not by wishing: see Verification.

### Q2 — What happens when photos are *impossible* to attach?

`BookingForm.svelte:232-263` has a deliberate degradation: if minting a draft
fails, the island shows "Photos can't be attached right now. **You can still
book** — or call us", and the booking proceeds. Its JSDoc says the quiet part:
"A failure here **must never block the booking** … an appointment without
photos is still an appointment." Two live causes — `/api/booking/draft/`
answers 429 after `DRAFT_RATE_LIMIT_PER_HOUR = 20` mints per IP per hour, and
a Blob/env misconfiguration.

After this ticket that JSDoc is false, and the branch becomes: photos
impossible ⇒ **booking impossible**. That is a new way to lose a customer.

**Decided:**

1. **Two messages, not one.** `mintDraft` (`BookingForm.svelte:239-263`)
   currently collapses every non-ok status into a single sentence. Split it:
   - **429 — retryable.** "Too many people on this connection right now — wait
     a moment and try again." No phone fallback; `createDraftSession` does not
     cache the failure, so the next tap genuinely works. Telling this visitor
     to abandon the form would be wrong.
   - **anything else — hard failure.** Photos are required and cannot be
     attached, so **call or text `PHONE_DISPLAY`** and the office will book it
     for them. That fallback is real, not a brush-off: the office path is the
     admin entry, which is exempt by the client's own instruction.
2. `booking-config.ts:118` — `DRAFT_RATE_LIMIT_PER_HOUR` **20 → 50**
   (user decision, 2026-08-12; reasoning under Settled below).
3. Do **not** add a "couldn't attach? book anyway" bypass. It reopens exactly
   the hole the client asked to close and is trivially reachable by anyone who
   blocks one request. (Plan review agrees; not to be relitigated.)

### Q3 — What happens when the *only* upload fails or is skipped? (added by plan review B4)

The draft missed this, and it is more common than everything in Q2 combined.
`skipUpload` (`BookingForm.svelte:348-358`) exists for a stated reason:

> "The submit button waits for uploads to finish, so a 100 MB video stalled on
> hotel wifi — **exactly the case this form is built for** — would otherwise
> trap the visitor with no exit but a reload that loses everything they typed.
> A failed upload never blocks; only a hanging one did."

A strict client rule makes that last sentence false. Attach one video on a
flaky connection, watch it stall, hit **Skip** → `status: 'failed'`,
done-count 0, step 3 blocks, no way out. Note the asymmetry: the **server
would accept that booking** — the `appointment_files` row was written at
token-mint time, before the PUT — and only the client refuses.

**Decided: the client counts attachments in `{done, failed}`, not `done`
alone.** A client stricter than the server manufactures a dead end the server
would have allowed. Under this rule the requirement still filters the person
who attaches nothing — the client's actual target — and not the person whose
wifi dropped. `Skip`'s semantics are unchanged, so its JSDoc stays true.

The alternative (Skip becomes "cancel", Retry the only way forward) is
stricter and costs real bookings on bad connections; recorded as the option
not taken, and reversible if the client ever asks for the harder filter.

## Scope

**Validation core**

1. `booking-payload.ts` — `ParseOptions` gains **`entry: 'public' | 'admin'`,
   non-optional**. When `'public'` and email is absent:
   `add('email', 'Email is required.')`. Keep `BookingPayload.email` as
   `string | null` — admin rows genuinely have none, the column stays
   nullable, and **this ticket ships no migration**.
   - A discriminator, not an `isAdmin`/`requireEmail` boolean, for two
     reasons. A boolean's permissive value is `false`, so anything yielding
     `undefined` — a `Partial`, an `as ParseOptions` cast, a spread from a
     stale object — **fails open** and silently exempts the public form. And
     BK-23 needs this exact discriminator anyway (`source='web'` starts
     `pending`, `source='admin'` starts `booked`), so a boolean would be
     replaced one ticket later.
   - **On the rule this appears to break.** `booking-admin-entry.ts:15-24`
     says the admin bypass "is expressed by *not calling* `isSlotBookable`,
     never by a flag threaded through it: an `isAdmin` parameter on a shared
     predicate is how the public path accidentally inherits the bypass." This
     ticket threads a discriminator through a shared parser, so it must argue
     the difference rather than sail past it: the slot bypass is expressible
     structurally because it is a *whole predicate* admin skips, whereas email
     is one field inside a parse both paths need in full. Step 2 is what keeps
     the rule's *intent* — the public path cannot inherit the exemption
     because it cannot name it.
2. `booking-admin-entry.ts` — `parseAdminEntry` takes
   `Omit<ParseOptions, 'entry'>` and **hard-codes `entry: 'admin'` itself** at
   the `parseBookingPayload` call (`:197`), instead of forwarding the caller's
   options verbatim.
   - This is the plan review's B3, and it kills both failure modes: the admin
     route physically cannot supply `entry` (the type forbids it), and no
     caller of `parseAdminEntry` can make it behave like the public form.
     Forwarding instead would leave the admin route's own `ParseOptions`
     carrying a **dead and misleading** field — an `entry: 'public'` there
     would read as meaningful and do nothing.
   - Comment it with the client's exemption so nobody "fixes" the asymmetry.
3. `create.ts:65` — pass `entry: 'public'`.
4. `booking-payload.ts:87-91` — rewrite the `isPlausibleEmail` header comment,
   which currently argues email is optional.
5. **Every `ParseOptions` construction site must be updated — there are six,
   not two** (plan review B3): `src/pages/api/booking/create.ts:65`,
   `src/pages/api/admin/appointments/create.ts:57`,
   `scripts/verify-booking-payload.ts:10`, `scripts/verify-booking-form.ts:187`,
   `scripts/verify-booking-admin-entry.ts:61`, `scripts/verify-booking-ics.ts:448`.
   `astro check` covers `scripts/`, so all six break typecheck — that is the
   mechanism, and it is why `entry` is non-optional.

**The file requirement, server side**

6. `create.ts` — after `verifyDraftToken` resolves `draftId`, count the
   draft's unclaimed files and reject with a 422 field error on `files` when
   the count is 0 — same envelope as a parse error
   (`{ error, fields: [{ field: 'files', message: … }] }`), so the island's
   existing 422 handling routes it without a new branch.
   - `draftToken === null` ⇒ no query, straight to the same error. This is the
     common case, since the island only sends a token when it has an
     attachment.
   - **Fold the COUNT into the existing `Promise.all`** with the
     booked/blackout queries (`create.ts:95`) rather than adding a serial
     round trip. Evaluate the file error *before* `isSlotBookable`, so a
     photo-less booking reads "add a photo" rather than a slot message.
   - Extract the count as a named function in `booking-commit.ts` beside
     `insertBooking` (the `claimedFilePathname` precedent,
     `booking-files.ts:168`) so a verify script drives the *same statement*.

**The file requirement, form side**

7. `booking-form.ts` — `validateStep` gains a fourth parameter carrying the
   count of attachments in `{done, failed}` (per Q3). Non-optional, so every
   call site is typecheck-forced to answer. Step 3 returns
   `add('files', 'Add at least one photo or video.')` when it is 0, and its
   "Step 3 collects only optional things" comment is rewritten.
8. `booking-form.ts:120` — add `files: 3` to `FIELD_STEPS`. Without it,
   `stepForErrors` returns `undefined` for the server's new field,
   `mapCommitResponse` (`:456-463`) falls through to `formError`, and the
   visitor gets "Please check your details and try again" with nothing
   highlighted.
9. `BookingForm.svelte` — **the submit button stays enabled** and
   `validateStep` produces the `files` error, which renders where
   `photoNotice` renders. Do *not* disable submit: every other required field
   on this form (name, phone, address) allows submit and renders a field
   error, and the existing `busy` derived (`:136`) disables only for the
   genuinely transient `uploadsInFlight > 0`. A permanently disabled button
   with no explanation is a dead screen.
   - Specify precedence when both `errors.files` and `photoNotice` are set:
     `photoNotice` is cleared on every new selection (`:269`), and two red
     lines stacked in one box read as two problems.
   - Email label loses `(optional)` (`:630`) and gains the required marking
     the other required fields use.
   - Step 3's "Photos are optional, and they help us arrive with the right
     equipment" (`:758`) becomes a requirement line that still says *why*.
10. `BookingForm.svelte:232-263` — the draft-mint failure path per Q2,
    including **the JSDoc at `:232-238`**, whose "must never block the
    booking … an appointment without photos is still an appointment" is the
    sentence this ticket falsifies and the one most likely to be left behind.
11. `src/sections/ContactSection.astro:263` — "Photos optional, and they help
    us arrive ready" is server-rendered customer-facing copy about this form,
    on the homepage. After this ticket it is false, it is outside
    `BookingForm.svelte`, and **no gate sees it** (`verify-cutover.ts` sweeps
    CTAs and paths, not claims).
12. One GA4 funnel event on the Q2 hard-failure branch, matching the
    `booking_availability_error` precedent (`BookingForm.svelte:177,182`) —
    "a visitor who cannot see any times is a lost quote that leaves no other
    trace" applies exactly to a visitor turned away by the photo requirement,
    and the client will ask whether the filter cost him real jobs. Per the
    ROADMAP's Known trap, these count **attempts, not visitors**; the event
    name must not read as a visitor count.

**Verify scripts that this change breaks (plan review B1 — the draft missed
this entirely)**

13. `scripts/verify-booking-commit.ts` — its `payload()` helper (`:150-161`)
    carries **no `draft_token`**, and 13 of its 16 `post(payload(…))` call
    sites would flip from their asserted status to a 422 on `files`. Two that
    matter most:
    - `:328-336` — the endpoint-level concurrency hammer, 12 racers on one
      slot. All 12 would 422 and `conflicted.length === HAMMER_N - 1` fails.
      **The endpoint-level half of the double-booking guard would stop being
      tested at all** — the worst possible silent loss in this codebase.
    - `:496-498` — `check(none.status === 201, 'a booking with no token must
      still succeed')` is a **live green assertion asserting the exact
      opposite of AC2**. It must be inverted by this ticket, not deleted.
    `payload()` gains a draft token and a seeded file row per logical test.
    (`payload()` already carries `email: 'verify@example.com'`, so the email
    half of this ticket breaks nothing here — the files half breaks all of it.)
14. `scripts/verify-booking-form.ts:105` — `fieldsFor(3, values()).length === 0`
    ("step 3 must never block on consent") changes meaning under the new
    signature. **Rewritten, not deleted**, so the CASL point it makes — consent
    never blocks a booking — is not lost with it.

**Copy** — implementer drafts, client edits later (standing rule). Customer-
visible, so it goes past the client before launch alongside BK-26's checklist.

## Settled — `DRAFT_RATE_LIMIT_PER_HOUR` 20 → 50 (user, 2026-08-12)

`DRAFT_RATE_LIMIT_PER_HOUR` was 20 per IP per hour, and after this ticket it
gates the ability to book at all — one shared connection (an office, a
building, a coffee shop) burning it means nobody behind it can book.

It is also a storage-cost ceiling, which is why the number went to the user
rather than being picked here: each draft independently permits 10 files /
300 MB (`upload-token.ts:106-111`), so worst-case blob burn from a single IP
is ~6 GB/hour at 20, ~15 GB/hour at 50, ~30 GB/hour at the 100 the draft
originally proposed. The island mints exactly one draft per form session
(memoised, `booking-form.ts:510`), so the unit really is *booking sessions per
hour per NAT*.

**50** — headroom for any plausible shared connection without tripling the
abuse ceiling. The draft's 100 was not taken.

## Out of scope

- **No migration.** `appointments.email` stays nullable for admin rows. The
  `pending`/`declined` statuses and the index rewrite are BK-23's.
- No change to `booking-email.ts:221`'s `if (!input.email) return null` — still
  correct, still reachable via admin rows.
- BK-21's "customer is NOT reachable by email" line stays as it is. After this
  ticket it can only be produced by an admin row, which is the case it was
  always about.
- `ContactSection.astro:262` — "Instant confirmation — no waiting for a
  callback" also goes false, but **that is BK-23's to rewrite**, not this
  ticket's. Named here so BK-23's implementer finds it.
- The 100 MB/file, 10-file, 300 MB-total limits are untouched.
- The Ads conversion still fires at submission (P7 decision).
- No retroactive treatment of existing email-less rows.

## Acceptance criteria

1. A public POST with no email is rejected 422 with a `field: 'email'` error;
   the same body with an email is accepted.
2. A public POST with no `draft_token` is rejected 422 with a `field: 'files'`
   error.
3. A public POST whose draft has zero unclaimed `appointment_files` rows is
   rejected 422 on `files`; one row is enough to pass, **whatever its
   `upload_state`**; a row already claimed by another appointment does **not**
   count.
4. An admin entry with neither email nor files still commits, and lands
   `source='admin'`.
5. The island shows a `files` error on step 3 when nothing is attached, with
   the submit button **still enabled**; a server `files` error routes the
   visitor back to step 3.
6. An attachment that failed or was skipped satisfies the client-side rule
   (Q3), and a booking made that way commits.
7. When the draft mint fails 429 the visitor is told to retry; on any other
   failure they are told to call.

## Verification

- `verify:booking:payload` — `entry: 'public'` rejects an absent email,
  accepts a present one; `entry: 'admin'` accepts absent.
- `verify:booking:form` — `validateStep` step 3 with 0 and with 1
  `{done, failed}` attachment; `stepForErrors([{field:'files'}]) === 3`; the
  rewritten consent assertion (item 14).
- `verify:booking:admin:entry` — the exemption end-to-end: no email, still ok.
- **`verify:booking:commit` is the home for the DB-backed arms** (plan review
  S1 — the draft proposed `admin:db` or `smoke`, both wrong: the first drives
  admin write routes, the second is one happy-path booking through a spawned
  dev server). This script already swaps `DATABASE_URL` to the dev branch,
  imports `POST` from the real route (`:68`), issues real draft tokens, and
  seeds `appointment_files` rows **including already-claimed ones**
  (`:440-470`) — the exact third fixture AC3 needs. Same edit pass as item 13.
  - The fixture that manages Q1/S2's two-copies risk: one draft with one
    claimed row **and** one unclaimed row → the count sees 1 **and**
    `insertBooking` claims 1. Dropping `appointment_id IS NULL` from *either*
    copy then goes red.
- Regressions: `verify:contact`, `verify:booking:smoke`, `verify:cutover`.

**Red-first — seven failure modes** (the draft had five and, as the plan
review noted, none of them covered the thing the ticket itself calls its
highest-value assertion):

| # | Break at the production target | Expected red |
| --- | --- | --- |
| a | the email requirement in `parseBookingPayload` | `verify:booking:payload` |
| b | the zero-file rejection in `create.ts` | `verify:booking:commit` |
| c | `appointment_id IS NULL` in the count | `verify:booking:commit` (claimed-row arm) |
| d | the step-3 client gate | `verify:booking:form` |
| e | `files: 3` in `FIELD_STEPS` | `verify:booking:form` |
| f | `entry: 'public'` → `'admin'` at `create.ts:65` | `verify:booking:payload` red, admin arm still green |
| g | `entry: 'admin'` → `'public'` inside `parseAdminEntry` | `verify:booking:admin:entry` red, public arm still green |

(f) and (g) are the pair that pins the exemption's *polarity*. Breaking (a)
alone makes both directions red and proves nothing about which way the
discriminator points.

## Risk areas, for the reviewers to weight

- **Public write path.** The endpoint's accept/reject contract changes.
- **The exemption's polarity.** Get it backwards and either the office cannot
  enter a phone-in booking, or the public form's new requirements evaporate.
  Red rows (f) and (g) exist for exactly this.
- **`verify-booking-commit.ts` is load-bearing and this ticket edits it.** The
  concurrency hammer is the only endpoint-level test of the double-booking
  guard. An edit that makes it pass for the wrong reason is the most expensive
  mistake available here.
- **Availability, not correctness, is the likely failure.** Every branch added
  here is a new way for a real customer to be told no. Q2 and Q3 are where
  that lives.
- No concurrency change: the partial unique index and the CTE are untouched.
- No PII change: no new column, no new field, nothing new in an email.

## Plan review (2026-08-12, fresh agent)

Verdict: not ready to approve — 4 blockers, 7 should-fixes, 8 nits. **All
folded in above; none refused.** The reviewer confirmed the core design (email
required in the parser, ≥1 `appointment_files` row at commit, admin exempt),
endorsed Q1's rule and the no-bypass decision, and independently re-derived
the `FIELD_STEPS` catch as genuine.

- **B1** — `verify-booking-commit.ts` is broken by this change and appeared
  nowhere in the draft, including a live assertion asserting the opposite of
  AC2 and the 12-racer concurrency hammer. → Scope 13, Verification,
  Risk areas. *Independently confirmed by the reviewer session at `:150-161`,
  `:328-336`, `:496-498`.*
- **B2** — no red-first row for the admin exemption, the ticket's own
  self-declared highest-value assertion. → red rows (f) and (g).
- **B3** — Scope steps 1 and 4 described two mutually exclusive designs, and
  the implied one contradicts `booking-admin-entry.ts:15-24`'s written rule
  against threading flags through shared predicates; the boolean also fails
  open on `undefined`; six `ParseOptions` sites, not two. → Scope 1, 2, 5.
  *Confirmed: `:197` forwards `options` verbatim; all six sites verified.*
- **B4** — Q2 missed the common case: the only upload fails or is skipped, and
  the client would block a booking the server would accept. → Q3.
  *Confirmed: `skipUpload`'s JSDoc at `:348-358` says so verbatim.*
- **S1–S7** — DB arm belongs in `verify-booking-commit`; the "same predicate"
  overclaim; `ContactSection.astro:263`; 429 vs hard failure; the rate-limit
  raise is the user's cost decision; submit stays enabled; a funnel event for
  the new abandonment mode. All folded in.
- **N1–N8** — line-reference corrections (`booking-form.ts:172-173`,
  `BookingForm.svelte:396-401`, `:232-238`), the false BK-21 dependency, the
  "one office behind one NAT" phrase belonging to `BookingForm.svelte:236` and
  not `booking-config.ts:118`, the unassertable AC2 clause (dropped), email
  staying `null` and never `''` on the failure path, and
  `verify-booking-form.ts:105` being rewritten rather than deleted. All applied.

## Assumptions log

- Q1, Q2 and Q3 are settled without the user — implementation questions
  beneath the client's decision, not restatements of it. **Except**
  `DRAFT_RATE_LIMIT_PER_HOUR`, which is a cost decision and is the Open
  question above.
- Q3's stricter alternative (Skip becomes "cancel") is recorded as the option
  not taken. If the client wants the harder filter it is a small change, but
  it costs bookings on bad connections and nobody has asked for it.
- "At least one photo **or video**" is one combined requirement, not one of
  each — the client's phrasing throughout is "pictures/videos" as a single ask.
- The client's exemption covers admin entry as a whole: nothing in the admin
  path requires an email or a file after this ticket.
- `email` stays `null` and never `''` on the failure path. The existing
  `required()` helper returns `''` on failure (`booking-payload.ts:118,122`);
  reusing it for email would put an empty string where the type says
  `string | null`. Unreachable today (errors short-circuit at `:196`) but one
  refactor from being live.

### Settled during implementation (2026-08-13)

- **Scope 5's "all six `ParseOptions` sites break typecheck" is false, and the
  design is better than the sentence.** Only **three** break — `create.ts`,
  `verify-booking-payload.ts:10`, `verify-booking-form.ts:187`. The other three
  (`api/admin/appointments/create.ts:57`, `verify-booking-admin-entry.ts:61`,
  `verify-booking-ics.ts:448`) all reach the parser through `parseAdminEntry`,
  whose parameter is `Omit<ParseOptions, 'entry'>` per Scope 2 — so they are
  *forbidden* from supplying the field rather than *forced* to. The two scope
  items were written at different moments and only the "all six" phrasing is
  stale. What actually holds is stronger than what was claimed: every site that
  calls `parseBookingPayload` directly must answer, and every site that goes
  through the office door cannot. Confirmed by running `astro check` with the
  change in place — one error, at `verify-booking-form.ts:59`, and none at the
  three admin-mediated sites.
- **Red row (f) named the wrong script, and the arm it needed did not exist.**
  The table expected `verify:booking:payload` to go red when `create.ts`'s
  `entry` is flipped, but that script drives the parser directly and never
  imports the route — it stayed green under the break, as did
  `verify:booking:admin:entry`. Nothing in the suite read that line. Added an
  endpoint-level arm to `verify-booking-commit.ts` (a public POST with the
  `email` key deleted must 422 naming `email`), which is also **AC1's endpoint
  half** — the Verification section only ever listed parser-level arms for the
  email rule. (f) is red there now.
- **The island's own email rule was implemented, though Scope only names the
  label.** `booking-form.ts`'s `validateStep` step 2 required email to match
  `parseBookingPayload`'s public arm. Scope 9 changes the label and "What the
  code does today" flags `:172-173` as current behaviour, but no scope item
  says "make it required client-side". Left permissive it would have let a
  visitor walk to step 3 and be bounced back by a 422 for a field the page had
  just told them was optional. The module's own header says it mirrors the
  server deliberately. `verify-booking-form.ts`'s "email is optional" assertion
  was inverted rather than deleted.
- **`verify-booking-smoke.ts` is broken by this change and is not in Scope.**
  Its "rejections still reject over HTTP" block re-posts the happy path's
  payload to the same slot to assert a 409 — but that draft's only file was
  claimed by the booking one block earlier, so the repeat now 422s on `files`
  and never reaches the slot logic. Observed red (2 fails) before the fix. The
  block mints a second draft and inserts one `appointment_files` row directly
  rather than uploading a second real blob: the row is what the server counts,
  and a blob would cost storage to prove nothing this block is about. A
  `repeatDraftId` cleanup arm was added beside the existing one. This is the
  same family as plan-review B1 — a verify script the change breaks — and B1
  found `verify-booking-commit.ts` but not this one.
- **The draft fixtures in `verify-booking-commit.ts` are a pre-seeded pool
  (96 drafts, one file each, one batched INSERT), handed out by `payload()`.**
  The alternative — minting inside `payload()` — makes it async, and it is
  called inside `Array.from(...)` and inside loops, so every call site would
  have been rewritten to buy nothing. `payloadWithoutFiles()` is a named
  builder rather than `payload(slot, { draft_token: undefined })`, because that
  spelling depends on `JSON.stringify` dropping undefined keys — a fact about
  the serializer standing in for a statement of intent. `SLOTS_NEEDED` 24 → 40.
- **`photoNotice` beats `errors.files` when both are set.** Scope 9 asks for
  precedence to be specified without choosing. `photoNotice` is the specific
  complaint — *this* file was rejected, or photos cannot be attached at all and
  here is the phone number — and `errors.files` is the generic requirement.
  Showing the generic line over the Q2 hard-failure message would replace "call
  us and we'll book it" with "add a photo", which is advice the visitor cannot
  take. It is cleared on every new selection, so the generic line returns as
  soon as the specific one stops applying.
- **The GA4 event is `booking_photo_upload_unavailable`**, added to
  `BookingFunnelEvent` beside the two BK-10 events. It fires on the hard branch
  only — a 429 is retryable and self-heals, and counting it would drown the
  signal the client will actually ask about. Named as a condition, never a
  headcount, per the ROADMAP's "attempts, not visitors" trap; `createDraftSession`
  retries on the next selection, so one stuck visitor can raise it several times.
- **`verify-booking-admin-entry.ts` carries a compile-time tie**
  (`Parameters<typeof parseAdminEntry>[1]`) so that widening the parameter back
  to a full `ParseOptions` stops `npm run typecheck`. A runtime assertion could
  never catch that — it is the `AppointmentFile['size_bytes']` technique from
  BK-09, applied to the seam this ticket's exemption rides on.

## Gate results

| Gate | Result |
| --- | --- |
| `npm run typecheck` | ✅ 0 errors, 24 hints; `svelte-check` 0 errors, the 4 pre-existing `BrandLogo.svelte` warnings untouched |
| `npm run build` | ✅ Complete |
| `verify:booking:payload` | ✅ all checks passed (new block: "the admin exemption and its polarity") |
| `verify:booking:form` | ✅ all checks passed |
| `verify:booking:admin:entry` | ✅ all checks passed (new block: "the office is exempt") |
| `verify:booking:commit` | ✅ all checks passed (new block: "a public booking must carry a file") |
| `verify:booking:smoke` | ✅ all checks passed — **after a fix; it was red first, see Assumptions** |
| `verify:contact` | ✅ passed (regression) |
| `verify:cutover` | ✅ passed (regression) |
| red-observed (a)–(g) | ✅ eight modes, each broken at the production target and restored **by edit** — never `git checkout`; full set re-run green afterwards |

**Red-observed — one row per failure mode.**

| # | Break | Observed |
| --- | --- | --- |
| a | dropped the `entry === 'public'` email rule in `parseBookingPayload` | `verify:booking:payload` → 3 fails, all public-arm; the admin arm stayed green |
| b | `if (attachedFiles === 0)` → `if (false && …)` in `create.ts` | `verify:booking:commit` → 7 fails across all four rejection arms, incl. "nothing in this block may create a row (11 → 12)" |
| c1 | dropped `appointment_id IS NULL` from **`countUnclaimedFiles`** | `verify:booking:commit` → 1 fail, exactly the spent-draft arm ("a draft whose only file is already claimed must 422 (got 201)") |
| c2 | dropped `appointment_id IS NULL` from **`insertBooking`'s `claimed` CTE** | `verify:booking:commit` → 2 fails, exactly the mixed-draft arm ("only the unclaimed row may be claimed (got 2)", "the donor's file must not be stolen") |
| d | dropped the step-3 file gate in `validateStep` | `verify:booking:form` → 1 fail |
| e | dropped `files: 3` from `FIELD_STEPS` | `verify:booking:form` → 1 fail ("files routes to step 3") |
| f | `entry: 'public'` → `'admin'` at `create.ts` | `verify:booking:commit` → 2 fails; `verify:booking:payload` **and** `verify:booking:admin:entry` both stayed green |
| g | `entry: 'admin'` → `'public'` inside `parseAdminEntry` | `verify:booking:admin:entry` → 3 fails; `verify:booking:payload` stayed green |

(c) is two rows because **one arm cannot catch both copies of the predicate.**
The count only ever decides 0-vs-nonzero, so dropping `IS NULL` there changes 1
to 2 and the mixed-draft arm never notices; it is caught only where the correct
answer is 0. The CTE's copy is the mirror image. The two fixtures were built as
a pair for exactly this and the red pass is what proved the pair is necessary
rather than decorative.

(f) and (g) are the polarity pair, and they behaved as intended: each turned
red in one script while the *other* door's script stayed green. Breaking (a)
alone leaves both doors permissive and proves nothing about direction.

## Implementation review (2026-08-13, fresh agent)

**Verdict: approved. No blockers, no should-fixes, three nits.** Every gate was
re-run rather than read, and every claim the implementer marked as a divergence
from the plan was re-derived by breaking the code, not by reading the sentence.

**Gates, re-run first-hand:** `typecheck` 0 errors / 24 hints / the 4
pre-existing `BrandLogo.svelte` warnings; `build` Complete; `verify:booking:`
`payload`, `form`, `admin:entry`, `commit`, `smoke` all green;
`verify:contact`, `verify:cutover` green.

**Reds re-observed first-hand** — one row per mode, each broken at the
production target and restored by edit:

| # | Break | Observed |
| --- | --- | --- |
| f | `entry: 'public'` → `'admin'` at `create.ts` | `verify:booking:commit` → 2 fails ("a public POST with no email must 422 (got 201)"); `verify:booking:payload` **and** `verify:booking:admin:entry` both stayed green |
| c1 | dropped `appointment_id IS NULL` from `countUnclaimedFiles` | `verify:booking:commit` → 1 fail, only the spent-draft arm |
| c2 | dropped `appointment_id IS NULL` from `insertBooking`'s `claimed` CTE | `verify:booking:commit` → 2 fails, only the mixed-draft arm |
| **h** | **`COUNT(*)::int` → `COUNT(*)`** in `countUnclaimedFiles` — **a mode the implementer never broke** | `verify:booking:commit` → 2 fails ("a token with no files must 422 (got 201)", "a draft whose only file is already claimed must 422 (got 201)") |

(h) is the `size_bytes` bigint-as-string trap the function's own JSDoc claims to
be guarding against: without the cast the driver returns `'0'`, `'0' === 0` is
false, and **every** valid draft token clears the gate regardless of whether it
holds a file. The claim is load-bearing and it is covered — but nothing in the
implementer's red table exercised it, so it is added here as row (h).

**The four disputed conclusions, each checked independently:**

1. **Scope 5's "all six sites break typecheck" — the implementer is right, and
   the mechanism is slightly finer than either sentence says.** Verified by
   mutating all six at once and running `astro check`: **5 errors**, not 6.
   Three sites are *forced* to supply `entry` (`ts(2741)`/`ts(2345)` at
   `create.ts:64`, `verify-booking-payload.ts:16`, `verify-booking-form.ts:229`)
   and two are *forbidden* (`ts(2353)` "'entry' does not exist in type
   `Omit<ParseOptions, 'entry'>`" at `api/admin/appointments/create.ts:58` and
   `verify-booking-ics.ts:448`). The sixth — `verify-booking-admin-entry.ts`'s
   `OPTS` — is neither, because excess-property checking only fires on a fresh
   object literal at the call site and `OPTS` is a hoisted variable. Harmless: a
   stale `entry` there is dead, since `parseAdminEntry` spreads
   `{ ...options, entry: 'admin' }` and the hard-coded value wins. Recorded as
   nit 1.
2. **Red row (f) — confirmed, exactly as the Assumptions log describes.**
   Flipping `create.ts` to `entry: 'admin'` left `verify:booking:payload` and
   `verify:booking:admin:entry` **both green** and turned only
   `verify:booking:commit` red, on the new endpoint arm. That arm really is the
   only thing in the suite reading that line, and it really is AC1's endpoint
   half, which the Verification section never had.
3. **`verify-booking-smoke.ts` — the fix restores the block's subject rather
   than making it pass.** "Rejections still reject over HTTP" asserts
   `repeat.status === 409` **and** `body.code === 'slot_unavailable'`, a pair
   only the slot logic can produce; a 422 on `files` satisfies neither. The
   second arm in the block (`name: ''`, `phone: 'x'` → 422 naming `name`) never
   depended on files at all, since a parse error precedes the file gate.
4. **Red row (c)'s split into two modes — confirmed by breaking each copy
   separately.** See rows c1 and c2 above: one fail in the spent-draft arm, two
   in the mixed-draft arm, disjoint. Neither arm catches the other's copy. The
   pair is necessary, not decorative.

**The concurrency hammer conflicts on the slot, not on the new file gate.**
Checked directly rather than inferred: a temporary probe inside the hammer block
asserted that the 12 racers carry **12 distinct draft tokens** and that all 12
drafts hold **exactly one unclaimed `appointment_files` row** at the instant the
requests go out. Both passed, and the block still reported 1×201 / 11×409 / 1
row, with its pre-existing `slot_taken` assertion proving at least one loser
reaches the partial unique index rather than the precheck. The probe was removed
and the file restored byte-for-byte. `payload()`'s pool hands every call site a
private draft, so no two racers can collide on a shared draft, and a racer that
failed the file gate would answer 422 — a status the block rejects outright.

**Dev branch clean after the DB-touching scripts.** Post-run counts on the dev
branch: 0 appointments, 0 `appointment_files` (0 orphaned, 0 under `bk02/`, 0
under `smoke/`), 0 `bk02-` rate-limit buckets, 0 blackout rows. The new
`repeatDraftId` cleanup arm in the smoke script works. **No secrets in the
diff** — every token in the verify scripts is minted at runtime; no keys,
connection strings, or real PII added.

**Nits (not fixed, not blocking):**

1. `verify-booking-admin-entry.ts`'s `OPTS` can silently carry a dead `entry`
   field, as above. The compile-time tie at `:259` does still do its stated job:
   widening `parseAdminEntry`'s parameter back to a full `ParseOptions` was
   verified to stop `npm run typecheck` (`ts(2741)` on that very line).
2. `RETRY_PHOTO_NOTICE` is declared *after* `mintDraft`, which reads it. Safe —
   `mintDraft` is only ever called from an event handler, long after the
   instance script has run — but it reads backwards.
3. `ContactSection.astro`'s replacement line, "Send a photo or two — we arrive
   with the right equipment", is true but does not say the photo is *required*.
   Copy goes past the client before launch under the standing rule; flagged for
   that pass.

**Out of scope, recorded in the ROADMAP's Known traps (severity medium):** Q2
names a Blob/env misconfiguration as one of its two live causes, but Scope 10
scoped the remedy to `mintDraft`, and `/api/booking/draft/` never touches Blob.
A Blob failure therefore lands at `/api/booking/upload-token/` or the PUT, where
the island shows "That upload didn't finish.", the client's `{done, failed}`
count lets the visitor submit, the server finds no row, and the 422 returns them
to step 3 reading "Add at least one photo or video" beside a visible
attachment — with no phone fallback and **no `booking_photo_upload_unavailable`
event**. The implementation matches the approved Scope; the gap is in the
ticket, so it is recorded rather than fixed inline, per the out-of-scope rule.
