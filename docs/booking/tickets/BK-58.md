# BK-58 — Two false trust claims are live on every page, and one asserts an accreditation the firm does not hold

**Status:** ✅ **COMMITTED AND DEPLOYED, 2026-09-10. Gates green, all six
red-first rows observed, AND VERIFIED ON THE LIVE SITE — `§9`.** The user
approved the wording (decision 42) and authorised the push. **`0c058ae`.**
🔴 **Twelve sites, not eleven — `§7a-1`.** *(— was: IMPLEMENTED not committed;
before that APPROVED; before that DRAFT.)*

**Phase:** free-booking changeover · **Tier:** **Light** — copy and config only.
Gates only, per `CLAUDE.md`. 🛑 **No adversarial pass** — `CLAUDE.md` excludes
copy-only tickets explicitly, and this ticket **removes** claims rather than
adding a money path. **Risk areas:** customer-facing truthfulness on **all 17
built pages**; a **third-party accreditation** claim; **structured data** a
search engine consumes.

⚠️ **THIS IS NOT GATE WORK AND DOES NOT REOPEN DECISION 24.** Decision 24 barred
a *third gate ticket*. This is the copy defect surfaced by decisions 38 and 39,
recorded in `ROADMAP.md` Known traps as *"owner: BK-58"*, and it is the same
shape as BK-57 — a live falsehood with its own small ticket.

---

## 1 · The defect, measured

**Source of truth: the user, 2026-09-09.** Decision 38, verbatim:
***"this firm's founding year is 2026, but founders have experience since
2008."*** Decision 39: **IICRC Certified Firm ✅ and Licensed & Insured ✅ are
substantiated; BBB Accredited is NOT.**

**Measured against `dist/` after `npm run build`, 2026-09-09:**

| Claim, as it reaches a browser | Occurrences | Pages |
| --- | --- | --- |
| `2008` in any grammar | **57** | 🔴 **17 of 17** |
| the PHRASE *"Since 2008"* | 10 | 5 of 17 |
| 🔴 **`"foundingDate": "2008"` in JSON-LD** | **17** | 🔴 **17 of 17** |
| 🔴 **`BBB Accredited`** | **23** | 🔴 **17 of 17** |

🔴 **THE FIRM WAS FOUNDED 2026. Every one of these asserts an 18-year-old
entity.**

## 2 · 🔴 Why this is not a five-file edit — **the claim has TWO grammars and only one is prose**

**A grep for the sentence finds five pages. The structured-data assertion is on
all seventeen**, because `seo.ts:65` emits `foundingDate` site-wide and
`Footer.astro:31` renders the BBB badge on every page.

⚠️ ***A human auditing the rendered text would clear twelve pages that are making
the claim to Google.*** **This is `CLAUDE.md`'s copy-inventory trap in its
structured-data form**, and it is why `§3` is organised by SITE rather than by
page.

🛑 **A LIVE `curl` OF THE HOMEPAGE RETURNS ZERO HITS FOR *"Since 2008"* AND IS
NOT EVIDENCE OF ABSENCE** — that page carries the claim only as `foundingDate`.
*Measured, and briefly misread that way while writing the ROADMAP entry.*
**Probe `dist/` after a build, in BOTH grammars, or the probe answers the wrong
question.**

## 3 · ✅ THE COPY — **APPROVED IN FULL, decision 42, 2026-09-10**

⚠️ **The principle behind every line: the false element is the SUBJECT, not the
number.** *"Serving Edmonton Since 2008"* is false of a 2026 firm **at every
year**. **A true form exists — attach the years to the PEOPLE and their work,
never to the company.**

### 3a · The founding-year claim — ⛔ ~~11~~ **TWELVE source sites**

🔴 **THE TWELFTH IS `llms.txt.ts:37` AND IT WAS FOUND DURING IMPLEMENTATION, NOT HERE — `§7a-1`.** *This heading said eleven for a day after the ticket's own Status line said twelve.*

| # | Site | NOW | PROPOSED |
| --- | --- | --- | --- |
| 1 | `TrustStrip.astro:6` | *"Serving Edmonton Since 2008"* | *"Crews With Experience Since 2008"* |
| 2 | `StatsBar.astro:5` | `18` `yrs` · *"Serving Alberta since 2008"* | `18` `yrs` · *"Restoration experience on every crew"* — ⚠️ **the `18` stays and becomes TRUE**, because it now counts experience |
| 3 | `services.ts:25` | `foundingYear: 2008` | ⛔ **DELETE.** 🔴 **Verified unconsumed** — `grep -rn foundingYear src/` returns this definition and no reader |
| 4 | `services.ts:30` `BUSINESS.description` | *"…company serving Edmonton and surrounding Alberta communities **since 2008**. IICRC-certified crews handle…"* | *"…company serving Edmonton and surrounding Alberta communities. Our IICRC-certified crews have handled water damage, fire and smoke damage, mold removal, sewage cleanup, and storm damage repair **since 2008** — with written scopes, a written guarantee, and direct insurance billing."* |
| 5 | `seo.ts:65` | `foundingDate: '2008'` | ⛔ **DELETE, do not correct.** *Decision 38 is explicit, and it matches `PREPAY-PLAN`'s pre-existing do-regardless item 4* |
| 6 | `about.astro:11` metaTitle | *"About YEG Restoration \| Edmonton Restoration Since 2008"* **(55)** | *"About YEG Restoration \| Edmonton Restoration Specialists"* **(56)** |
| 7 | `about.astro:13` metaDescription | **(139)** *"…serving Edmonton 24/7 **since 2008**. Written scopes, direct insurance billing…"* | **(149)** *"Locally owned, IICRC-certified restoration serving Edmonton 24/7, by crews doing this work since 2008. Direct insurance billing. Call (780) 479-3285."* |
| 8 | `about.astro:111` `<h1>` | *"Edmonton's Restoration Team Since 2008"* | *"Edmonton's Restoration Team"* |
| 9 | `about.astro:152` badge | *"Since 2008"* | *"Experience Since 2008"* |
| 10 | `about.astro:169` decorative word | `2008` *(opacity 0.05 background)* | `EDMONTON` — ⚠️ **it is decorative and it is still an assertion of the founding year** |
| 🆕 **12** | 🔴 **`llms.txt.ts:37`** — `…communities … since ${BUSINESS.foundingYear}` | the FIRM's age, on **the AI-discovery surface** | *"- IICRC-certified crews with restoration experience since 2008, written scopes, written guarantee"* — ⚠️ **merged with the bullet below it, which already began *"IICRC-certified crews"*** |
| 11 | `about.astro:193` body | *"**We've been** locally owned and operated in Edmonton **since 2008**."* | *"We're locally owned and operated in Edmonton, and our people have been doing this work since 2008."* |

🔴 **SITE 4 PROPAGATES.** `services.ts:26-27`'s own comment: *"Reused verbatim in
schema, meta tags, and the about page… Edit here, never inline."* **`about.astro`
carries a comment requiring a verbatim match.** ☐ **Both must be re-verified
after the edit, or the ticket splits one string into two that disagree.**

### 3b · The BBB claim — **4 source sites, all REMOVALS**

| # | Site | Action |
| --- | --- | --- |
| 12 | `Footer.astro:31` | remove `'BBB Accredited'` from the badge array — 🔴 **this is the site-wide one** |
| 13 | `TrustStrip.astro:4` | remove `'BBB Accredited'` |
| 14 | `about.astro:52-53` | remove the whole object, incl. *"Accredited by the Better Business Bureau."* |
| 15 | `about.astro:152` | remove `'BBB Accredited'` from the badge array |

### 3c · The hours claim — **1 source site.** Decision 35

🔴 **VERIFIED FROM `booking-config.ts`, NOT FROM THE TICKET THAT DESCRIBED IT:**
`SLOT_START_TIMES` is `11:30 … 15:30` **(five)**, `SLOT_MINUTES = 30` so the last
appointment ends **16:00**, and **`CLOSED_WEEKDAYS = [5]` — Friday.**
**5 × 6 = 30 slots a week.** ⚠️ **`MIN_NOTICE_DAYS = 1`, so same-day booking is
impossible** — a second thing *"Open 24/7"* misleads about.

| # | Site | NOW | PROPOSED |
| --- | --- | --- | --- |
| 16 | `ContactSection.astro:177`, under a **`Hours`** label | *"Open 24/7 — including holidays"* | **two lines:** *"Emergency line: 24/7, including holidays"* · *"Assessments: 11:30am–4:00pm, closed Fridays"* |

✅ **EVERY OTHER `24/7` ON THE SITE IS TRUE AND MUST NOT BE TOUCHED.** They are
all about the **phone**. *The defect is this one instance, because a `Hours`
label makes it a claim about BOOKING.*

## 4 · ✅ WHAT MUST NOT CHANGE

- **`IICRC Certified` / `IICRC Certified Firm`** — substantiated, decision 39.
- **`Licensed & Insured`** — substantiated, decision 39.
- **Every `24/7` claim about the phone** — true, decision 35.
- ⚠️ **`Direct Insurance Billing` (18 sites)** — **OUT OF SCOPE, see `§7`.**

☑️ **ASK THE CLIENT FOR THE IICRC REGISTRATION NUMBER AND THE LICENCE NUMBER.**
Both badges stand on the user's word and **nothing in this repo evidences
either.** ***Decision 38 is exactly what an unevidenced claim looks like the
first time somebody checks it.***

## 5 · The gate

**Ticket verification, on top of `npm run typecheck` and `npm run build`:**

| # | Assertion | Why this shape |
| --- | --- | --- |
| **G1** | **`/"foundingDate"/` matches ZERO built files** | the site-wide grammar |
| **G2** | **`/BBB/i` matches ZERO built files** | ⚠️ **`/BBB/`, not `/BBB Accredited/`** — *a ban shaped for the exact phrase misses "Better Business Bureau", which `about.astro:53` spells out* |
| **G3** | ⛔ ~~`/Since\s+2008/i` matches ZERO built files~~ 🔴 **RED ON ARRIVAL — the APPROVED copy contains *"Crews With Experience Since 2008"*.** ✅ **AS SHIPPED: every `since <year>` SENTENCE must anchor the year to EXPERIENCE.** *Two earlier versions tuned a character DISTANCE and both failed — `§7a-3`* | the prose grammar |
| **G4** | 🔴 **POSITIVE TWIN — `/experience since 2008/i` matches ≥1 built file** | ***`CLAUDE.md`: a negative check with no positive twin passes on an empty document.*** **G1–G3 are all satisfiable by deleting every page.** G4 is the control that proves the corpus rendered AND that the TRUE form survived |
| **G5** | 🔴 **POSITIVE CONTROL — `/IICRC/` and `/Licensed &amp; Insured/` each match ≥1 built file** | proves the badge arrays still render, so G2 did not pass by emptying them |
| **G6** | **`ContactSection`'s `Hours` block matches `/closed Fridays/i` and NOT `/Open 24\/7/`** ⚠️ **SHIPPED AS THREE — `G6a` ban · `G6b` positive twin · `G6c` a control that the TRUE 24/7 PHONE claim survives.** *One `G6` could not express the third* | the pair, not the ban alone |
| **G7** | **`BUSINESS.description` appears verbatim in `about.astro`'s rendered output** | site 4's propagation |

**Red-first — one row per failure mode, scored on `(exit code, summary line)`,
never a `✗` count:**

| # | Break | Expect |
| --- | --- | --- |
| **R1** | restore `foundingDate: '2008'` | G1 red |
| **R2** | restore `'BBB Accredited'` to `Footer.astro` only | G2 red — **proves the site-wide site is covered** |
| **R3** | restore *"Better Business Bureau"* prose to `about.astro` **without** the badge | G2 red — 🔴 **this is the row that proves G2 is not phrase-shaped** |
| **R4** | delete the `experience since 2008` line | **G4 red while G1–G3 stay GREEN** — 🔴 ***the row that demonstrates why the twin exists*** |
| **R5** | empty the badge arrays entirely | **G5 red while G2 stays green** |
| **R6** | restore *"Open 24/7"* to the `Hours` block | G6 red |

## 6 · Sequencing — **why now, against decision 36**

⚠️ **This IS a funnel-affecting copy change and the one-change-per-week
constraint governs it.** It ships now anyway, and the reason is stated so it is
not re-litigated:

- **The constraint exists so the pricing flip's effect stays readable.**
- **BK-53 and BK-55 are ~38 open items from approval**, so **the window is empty
  this week and will have closed long before the flip lands.**
- 🔴 **Deferring is what would collide.** *And it leaves an unearned
  accreditation claim live in the meantime.*

**Build order:** its own commit, on `main`, **not** on `bk51-gated`. 🛑 **It must
NOT ride the flip's push.**

## 7 · Out of scope — recorded, not fixed

- ⚠️ **`Direct Insurance Billing` — 18 sites.** Decision 37: billing is **both,
  per job.** As a **capability** the claim is true; `HeroSection.astro:54` states
  it of ***your*** claim, unconditionally — *"we bill your insurance directly."*
  🔴 **An overclaim, not plainly false. It needs its own judgement and its own
  ticket.**
- **`seo.ts:70`'s placeholder postal code `T5J 0N3`** — a shipped LAUNCH BLOCKER
  live in structured data, already recorded in `PREPAY-PLAN`. **Adjacent, in the
  same file, and NOT this ticket's.**
- ☐ **`about.astro:193` and `BUSINESS.description` both promise *"a written
  scope"*.** *Read as a claim about a JOB, not about the free assessment, so
  decision 20 and BK-57 do not reach it.* **Reviewer: confirm that reading.**

## 7a · 🔴 WHAT THE IMPLEMENTATION FOUND THAT THIS TICKET GOT WRONG

⚠️ **Three corrections, all found by the checks rather than by re-reading. Recorded
because each is a rule this project already owns, recurring.**

### 🔴 1 · `A4` WAS FALSE — `foundingYear` HAD A READER, and it was a **17th site**

**`A4` said *"`foundingYear` in `services.ts` has no reader — verified by grep."***
⛔ **It has one: `llms.txt.ts:37`, `…communities … since ${BUSINESS.foundingYear}`.**

🔴 **THE GREP FOUND IT AND I READ ABSENCE INTO A TRUNCATED LINE.** The output was
piped through `cut -c1-140`; the interpolation sits at **column ~150**. *The
verification and the misreading were the same command.*

⚠️ **So `§3a` was ELEVEN sites and the claim had TWELVE.** **`llms.txt` is the
AI-discovery surface** — the one place whose entire purpose is being read by a
machine. ✅ **Fixed:** the year moves onto the crews, and **`llms.txt` is now
named in the gate's corpus** rather than assumed to be HTML.
***→ Never size a claim from a truncated grep.***

### 🔴 2 · THE `Licensed & Insured` INVENTORY WAS **FOUR**; IT IS **FIVE**

**`Footer.astro:260` carries it as `Licensed &amp; Insured`** — the ENTITY
spelling — in the footer's legal line. **A grep for `Licensed & Insured` cannot
match it.**

🔴 **IT WAS FOUND BY A RED-FIRST ROW THAT REFUSED TO GO RED.** `R5` removed the
badge from all four known sites and **`G5` stayed GREEN** — correctly, because a
fifth instance still shipped. ***A control that will not go red is telling you
your inventory is wrong.***

⚠️ **`CLAUDE.md`'s copy-inventory trap, in its ENTITY-SPELLING form** — the same
shape as `verify-cutover.ts`'s own `](/&#35;contact)` finding. ✅ **Both claims
re-audited in both grammars afterwards: `BBB|Better Business` = **0**.**

### 🔴 3 · `G3` WAS RED ON ARRIVAL **TWICE**, AND THE SECOND WAS THE FIRST'S FIX

| Version | Shape | Fired on | Why |
| --- | --- | --- | --- |
| **v1** | `(?:serving\|operated\|…)[^.<]{0,60}since\s+\d{4}` | *"…operated in Edmonton, and our people have been doing this work **since 2008**."* | **TRUE, and decision 42's approved wording.** 55 characters apart, **different clauses** |
| **v2** | an anchor required within **70 chars before** | the shared entity description | its anchor *"crews"* is **~103 chars** ahead |
| ✅ **v3** | **the SENTENCE containing the year must anchor it** | — | ✅ green |

🔴 **BOTH FAILING VERSIONS WERE TUNING A DISTANCE, AND DISTANCE CANNOT TELL ONE
CLAUSE FROM ANOTHER.** **The unit that can is the SENTENCE** — *which is what
BK-53 reached independently after trying the block and the whole file (R12-0).*
⚠️ **In both cases the fix was to the SHAPE, never to the copy.**

### ⚠️ 4 · The harness threw away a build exit code, and it mattered

**`R5` v2 reported `exit=0 · 13/13` on a landed break.** The build had been run
with its exit code **discarded**; had it failed, `dist/` would have been **stale**
and the gate would have graded **the previous build**. *It had not failed — but
the run could not prove that, so the row was re-run asserting it.*
***A red-first row over `dist/` must assert the BUILD succeeded, not only that the
script produced a summary line.***

## 8 · Gate results — **2026-09-10**

| Gate | Result |
| --- | --- |
| `npm run typecheck` | ✅ **0 errors** *(4 pre-existing warnings, 1 file, unrelated)* |
| `npm run build` | ✅ **PASS** |
| `npm run verify:trust` | ✅ **13/13, exit 0** |

**RED-FIRST — six rows, each scored on `(exit code, summary line)`, each restored
from a `cp` FILE BACKUP, and each asserting the break LANDED first:**

| # | Break | Result | Observed |
| --- | --- | --- | --- |
| **R1** | restore `foundingDate: '2008'` | `exit=1 · 12/13` | ✅ **G1 red** |
| **R2** | restore `'BBB Accredited'` to the FOOTER only | `exit=1 · 12/13` | ✅ **G2 red** — *the site-wide site is covered* |
| **R3** | restore *"Better Business Bureau"* PROSE, no badge | `exit=1 · 12/13` | ✅ **G2 red** — 🔴 ***the row that proves G2 is not phrase-shaped*** |
| **R4** | delete every `since 2008` from source — *the "fix G3 by removing the year" move* | `exit=1 · 12/13` | ✅ **G4 red while G1–G3 stayed GREEN** — 🔴 ***the row that demonstrates why the twin exists*** |
| **R5** | empty `Licensed & Insured` from every site | `exit=1 · 12/13` | ✅ **G5 red** — ⚠️ **only on the THIRD attempt: v1 CRASHED (`exit=2`, no summary line) and v2's break was INCOMPLETE. Neither was scored.** *See `§7a-2`* |
| **R6** | restore *"Open 24/7"* to the `Hours` block | `exit=1 · 12/13` | ✅ **G6a red** |

✅ **Baseline before and after: `exit=0 · 13/13`.** ✅ **Break residue checked by
grep after restore: `foundingDate` 0, `BBB` 0, `Better Business` 0, `Open 24/7` 0,
`Licensed & Insured` 5, `IICRC` 25.**

## 8a · Assumptions

| # | Assumption | Basis |
| --- | --- | --- |
| **A1** | The firm was founded **2026**; 2008 is the founders' experience | **User, decision 38, verbatim** |
| **A2** | IICRC and Licensed & Insured are substantiable; BBB is not | **User, decision 39.** ⚠️ **Asserted, not evidenced — `§4`** |
| **A3** | The emergency phone genuinely is answered 24/7 | **User, decision 35** |
| ⛔ **A4 — FALSE, WITHDRAWN 2026-09-10** | ⛔ ~~`foundingYear` has no reader~~ 🔴 **`llms.txt.ts:37` reads it.** *The grep that "verified" it was truncated at 140 columns and the interpolation sits at ~150.* **`§3a` was eleven sites; the claim had twelve** | **`§7a-1`** |
| **A5** | The grid is 11:30–16:00, Fridays closed, 1 day minimum notice | **Verified in `booking-config.ts`, 2026-09-10 — not inherited from the question that raised it** |

## 9 · ✅ VERIFIED ON THE LIVE SITE — 2026-09-10, after the deploy reached `Ready`

🔴 **`dist/` is not production. This is `curl` against `www.yegrestoration.ca`,
over TEN pages, in BOTH grammars** — because the whole point of `§2` is that one
grammar is invisible to a prose check.

**Pages probed:** `/` · `/about/` · `/contact/` · the four service pages ·
`/insurance-claims/` · `/book/` · `/404.html`

| Claim | Live count | Want |
| --- | --- | --- |
| `bbb\|better business` *(case-insensitive)* | **0** | 0 |
| `"foundingDate"` | **0** | 0 |
| `Open 24/7` | **0** | 0 |

**And the POSITIVE CONTROLS, which are what prove the removals were surgical and
not a blank page:**

| Control | Live |
| --- | --- |
| *"this work since 2008"* on `/about/` | ✅ **3** |
| *"Crews With Experience Since 2008"* on a service page | ✅ **1** |
| `IICRC` on the homepage | ✅ **8** |
| `Licensed &amp; Insured` on the homepage | ✅ **2** |
| *"closed Fridays"* on `/contact/` | ✅ **1** |
| `llms.txt` carries the experience claim, not a company date | ✅ **both lines** |

✅ **BK-48's rule is satisfied: the item is closed BY AN ARTIFACT, not by a
configuration change.** *The artifact is the live response body, and it was
fetched after the deploy reported `Ready` — not before.*
