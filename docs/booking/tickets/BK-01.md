# BK-01 — Availability computation + `GET /api/booking/availability/`

**Status:** plan-reviewed — revised 2026-08-06, ready to implement
**Phase:** P2 · **Tier:** Standard · **Depends on:** P1

Tier is Standard rather than Heavy despite being a public endpoint: it is
read-only, writes nothing, and exposes only free/busy counts — no PII, no
customer data, no irreversible effect.

## Goal

Give the date/time picker (BK-03) a single source of truth for what can be
booked, computed server-side from the same rules the commit endpoint (BK-02)
will validate against.

## Scope

1. `src/lib/booking-availability.ts` — a **pure** function that takes an
   explicit clock plus already-fetched booked slots and blackout days, and
   returns the availability for the booking window. No DB, no `new Date()`
   default inside the computation, no environment access. This is what makes it
   testable without a database.

   Its input contract is **primitives only**, never raw driver rows:
   `{ now: Date, blackoutDays: DateKey[], bookedSlotMs: number[] }`. Slot
   matching is by epoch millisecond, never by string equality on an ISO
   rendering.
2. `src/pages/api/booking/availability.ts` — a thin endpoint: read the two
   tables for the window, **normalize at the SQL boundary** (below), hand the
   primitives to the pure function, serialise, set cache headers. Carries
   `export const prerender = false` — `astro.config.mjs` sets
   `output: 'static'`, so a GET route without it is baked at build time and
   would serve a frozen calendar with neither the cache header nor the 500 path
   ever in effect.
3. `scripts/verify-availability.ts` — property checks over the pure function
   with an injected clock, in the shape of the existing `verify-slots.ts`.
4. Add `verify:availability` to package.json scripts.
5. Correct the row-type comment in `src/lib/db.ts` to record what the driver
   actually returns (below), so BK-02, BK-07 and BK-08 don't walk into it.

### Driver deserialization — the trap this ticket has to dodge

`src/lib/db.ts` types `Appointment.slot_start` and `BlackoutDate.day` as
`string`. **They are not.** Verified directly against the installed driver:

```
neon.types.getTypeParser(1082)('2026-08-10')            -> Date 2026-08-10T06:00:00.000Z
neon.types.getTypeParser(1184)('2026-08-10 17:30:00+00') -> Date 2026-08-10T17:30:00.000Z
```

Both come back as `Date` objects. So `blackoutSet.has(row.day)` and
`row.slot_start === slot.toISOString()` both typecheck and both silently never
match — blackout days would be offered as bookable and booked slots shown free.
Nothing in P1 hit this because no existing code reads either column into JS.

`timestamptz` (1184) round-trips correctly, so `slot_start` may be consumed as
a `Date` and compared with `.getTime()`. `date` (1082) does **not**: it is
timezone-naive and gets parsed at the *server's* local midnight, which on
Vercel (UTC) then converts back to the **previous** calendar day in Edmonton.
So blackouts must be selected as `day::text` and kept as `DateKey` strings
end to end. Do not "fix" this with `localDateKey(row.day)`.

## Out of scope

- Any UI. The picker is BK-03.
- Creating a booking, or holding/reserving a slot. BK-02.
- Admin-visible availability (admin can book outside the public window).
- Blackout-date management UI. BK-08.

## Design notes

- **Availability is advisory, not a reservation.** Two visitors can both be shown
  the same free slot. The partial unique index in BK-02 is the only real guard,
  and BK-02 must translate its conflict into a friendly "just taken" response.
  Do not add locking here.
- Reuse `bookableDateRange`, `slotStartsForDate`, `isClosedWeekday`, and
  `isWithinBookingWindow` from `booking-time.ts`. Do not re-derive the grid.
- One query for booked slots (`status <> 'cancelled'`, matching the partial
  unique index) and one for blackout days. Not one per day.
- **Query bounds are calendar-based, not `now + 14 × DAY_MS`.**
  `latestBookableDate` returns a *date*, whose 15:30 slot can sit well past 14×24h
  from now — a naive instant bound would miss a day-14 afternoon booking and show
  it free (and drift by an hour across DST). Bound the booked-slot query at
  `zonedTimeToUtc(localDateKey(now), '00:00')` through
  `zonedTimeToUtc(addDays(latestBookableDate(now), 1), '00:00')`, and the
  blackout query on the two `DateKey` endpoints of `bookableDateRange(now)`.
- Response must not be cached by the CDN — a slot booked a second ago has to
  disappear. `Cache-Control: no-store`.
- Endpoint path needs its trailing slash, and a `BOOKING_AVAILABILITY_ENDPOINT`
  constant alongside the other two in `booking-config.ts`.

## Acceptance criteria

1. Returns every date in the booking window, each with its bookable slots, so the
   picker can render and disable days without a second call.
2. A slot is excluded when any of these hold: the weekday is closed (Friday), the
   day has a `blackout_dates` row, a non-cancelled appointment already occupies
   that `slot_start`, or the slot falls outside the 4-hour/14-day window.
3. A cancelled appointment's slot is offered again.
4. Slot instants are correct across both DST transitions — the local wall clock
   stays 11:30–15:30 on either side of the change.
5. Times are returned as both a UTC ISO instant and a pre-formatted
   America/Edmonton label, so the client never re-derives the timezone.
6. Blackout *reasons* are never in the response. Blocked days are simply empty.
7. Response carries `Cache-Control: no-store`.
8. A failed DB query answers **500 with an error body** and logs the cause. It
   never degrades to an empty or partial calendar.
9. A day whose every slot is taken still appears, with an empty slot list.
10. The pure function is deterministic for a given clock and input set.

## Verification

- `npm run typecheck` and `npm run build` clean.
- `npm run verify:availability` — asserts, against a fixed injected clock:
  15 dates returned (today through day 14 inclusive), closed Fridays, blackout
  exclusion, booked-slot exclusion, cancelled-slot re-offer, the 4-hour boundary
  on both sides, the 14-day horizon on both sides, and both 2026 DST transitions.
- **Round-trip through the real DB**, because the pure-function tests cannot
  catch a deserialization or query-bounds mistake: insert one blackout row and
  one booked appointment (including one at the day-14 15:30 edge), hit the
  actual endpoint, confirm all of them disappear from the response, then clean
  up the rows.
- `curl -i` with the `Cache-Control: no-store` header visible in the output (AC7).
- One run against a broken `DATABASE_URL`, confirming 500 + error body and no
  empty-calendar fallback (AC8).
- Confirm the route is dynamic: no prerendered `availability` asset in the build
  output.

## Risks

- Getting "now" from the server while the visitor's device clock differs — the
  picker must render what the server sent rather than filtering again locally,
  or the two will disagree at the 4-hour boundary. Flag this for BK-03.
- The window is computed at request time, so a form left open past midnight can
  submit a stale slot. BK-02 revalidates; the picker should refetch on submit.

## Assumptions log

- **Full-window response, not just free days.** The picker needs to grey out
  closed and full days, so returning every date with a possibly-empty slot list
  is cheaper than a second call. 15 dates (today through day 14 inclusive,
  as `bookableDateRange` produces) × 5 slots — trivial payload.
- **No rate limiting on this endpoint.** It is read-only and hits two indexed
  queries. Revisit if it is ever abused; `rate-limit.ts` is available.
- **DB failure is a 500, not an empty calendar.** A silent empty response is
  indistinguishable from "fully booked", which would quietly turn away every
  visitor while looking healthy. Fail loudly; the picker shows an error state.
- **No `reason` field for excluded slots.** Telling the public "blacked out" vs
  "already booked" leaks the schedule. Absent means unavailable, full stop.

## Open questions

None blocking. Nothing here is a business decision.

## Review record

**Plan review** — 2026-08-06, fresh agent, verdict NEEDS REVISION. All six
findings accepted, none disputed; each was re-verified against the repo before
the ticket was changed.

| Finding | Disposition |
| --- | --- |
| BLOCKER — driver returns `Date` where `db.ts` claims `string`; matching silently never fires | Fixed: primitives-only input contract, `day::text` at the SQL boundary, real-DB round-trip added to verification, db.ts comment corrected |
| SHOULD-FIX — booked-slot query bounds unspecified; `now + 14×DAY_MS` misses day-14 afternoons | Fixed: calendar-based bounds spelled out in Design notes |
| SHOULD-FIX — no `prerender = false`; static output would bake the endpoint | Fixed: in scope, plus a build-output check |
| SHOULD-FIX — AC7/AC8 asserted by nothing | Fixed: `curl -i` and broken-`DATABASE_URL` runs added |
| NIT — "~14 dates" is 15 | Fixed |
| NIT — "importable from a Svelte island" invites the client re-filtering the Risks section warns against | Fixed: rationale is now testability only |

- Implementation review: _pending_
- Disputed blockers: _none_
