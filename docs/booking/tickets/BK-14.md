# BK-14 — Calendar invites: bookings land on the office Google Calendar

**Status:** plan-reviewed 2026-08-11 — 1 blocker, 6 should-fixes, 4 nits, all
accepted and folded in below. The blocker would have shipped the feature
half-broken invisibly: the draft defined the invite `Message` but no exported
function that can send one (`deliver` is module-private), and the workaround
an implementer would improvise — wrapping it in a `NotificationPlan` —
produces an idempotency key byte-identical across create, cancel, and
un-cancel, so Resend would collapse a CANCEL into a duplicate of the REQUEST
and **the calendar event would never clear**, with every gate green because
injected fake senders bypass `createResendSender` entirely. Scope 3 now adds
the exported seamed sender with a per-transition key. Sharpest should-fixes:
`update.ts` cannot learn the prior status from `RETURNING id` (the boundary
detection is now a pinned single-statement self-join), and ORGANIZER
consistency — which Gmail's CANCEL matching depends on — is now a bare-address
constant asserted stable across kinds. The reviewer verified every file:line
and installed-source claim in the draft.
**Phase:** P6 · **Tier:** Reviewed · **Depends on:** BK-05 (the notification
pipeline this rides), BK-08 (the admin create/cancel routes it hooks) ·
**Blocks:** nothing.

Client-requested 2026-08-11 ("can we connect the booking directly to Google
Calendar?"), scoped in conversation with the user to **level 1** of the three
levels offered: bookings *appear* on the office calendar as standard email
invites, cancellations clear them. Level 2 (Google Calendar API,
self-maintaining events) is the recorded upgrade path; level 3 (the calendar
*blocking* availability) is declined — blackout dates are the manual version
and fit one crew.

Reviewed because it adds mail to two write paths (one of which sends nothing
today), extends the `Message` contract every notification flows through, and
amends a deliberately-tested invariant (admin actions never mail the office).
Risk areas, in the order a reviewer should weight them:

1. **The whitelist no-op.** `createResendSender` maps `Message` fields to the
   SDK by an explicit object literal (`booking-notify.ts:100-110`) — adding
   `attachments` to the `Message` type without editing that literal ships a
   silently attachment-less email, and every verify script would stay green
   because their injected fake senders *do* see the field. The real mapping
   is behind the SDK boundary, so it gets a source-level pin (the BK-09
   precedent for behaviour that cannot be driven without a live call),
   stated honestly as such.
2. **PII in a calendar artifact.** The internal notification email carries
   insurer/policy/claim by design; the ICS must **never** — a calendar event
   syncs to phones, shared calendars, and third-party apps, so it gets the
   SMS-grade rule (`policy_number`/`claim_number` never in ICS output, any
   audience), not the internal-email one. This restates the locked PII rule
   for a new artifact class before the artifact exists.
3. **The amended invariant.** Admin actions currently never mail the office
   (`booking-admin-notify.ts:9-11`, asserted at
   `verify-booking-admin-entry.ts:474-486` — "the office doesn't need to be
   told what it just did"). A phone-in booking typed into admin is exactly
   the case where the office *does* want a machine artifact of what it just
   did — that is the feature. The invariant narrows to "no admin action
   sends a *notification* email to the office"; the calendar invite is a
   calendar artifact, deliberately tiny, and the assertion is rewritten to
   pin the narrowed rule (exactly one office mail per admin create, and it
   is the invite; zero on every other admin action except cancel/un-cancel).
4. **Lifecycle correctness.** Cancel must send `METHOD:CANCEL` with the
   *same UID* and a *higher SEQUENCE* or Google ignores it; un-cancel must
   re-issue `METHOD:REQUEST`, same UID, higher still. Nothing stores a
   sequence — see Mechanism for the stateless answer.

## Goal

Every booking — public `/book/` commit or admin phone-in entry — arrives at
`info@yegrestoration.ca` as a standard iCalendar invite that lands on the
office Google Calendar in one click (or automatically, per Gmail settings).
Cancelling in admin clears the calendar event; un-cancelling restores it.
No new credentials, no Google API: RFC 5545 over the Resend pipeline that
already exists and is already muted, seamed, and verified.

## Mechanism — pinned against installed source and the RFC, not recall

**Resend 6.17.1** (`dist/index.d.mts`, read by this ticket's recon):
`CreateEmailBaseOptions.attachments?: Attachment[]` (:520), max 40 MB/email;
`Attachment` takes `content?: string | Buffer`, `filename?`, `contentType?`
(:602-620). `invalid_attachment` is an existing error code and flows through
the current resolved-`{error}` mapping unchanged. The batch API excludes
attachments; not used here.

**RFC 5545 facts the builder must honour** (the verify script asserts each):

- CRLF line endings; lines folded at 75 octets with a leading space on
  continuations; text values escape `\` `;` `,` and newlines.
- `METHOD:REQUEST` in VCALENDAR for create/restore, `METHOD:CANCEL` +
  `STATUS:CANCELLED` for cancel. Same `UID` across the lifecycle:
  `booking-<id>@yegrestoration.ca` (the SERIAL id is the only reference
  that exists — recon confirmed no human-facing reference column).
- **`SEQUENCE` is stateless by design**: seconds-since-epoch of the send
  instant. RFC 5545 requires only that it increases per revision; deriving
  it from the injected `now` means cancel > create and re-book > cancel
  with **no new column and no migration** — the alternative (a
  `calendar_sequence` column bumped per transition) buys nothing but a
  migration and a race. Two bounds, documented rather than defended
  (plan review): 1-second granularity means a cancel→un-cancel inside the
  same second yields an equal SEQUENCE and the restoring REQUEST reads as
  stale — implausible through two form submits and redirects, accepted;
  and iCalendar INTEGER is int32 (RFC 5545 §3.3.8), which
  seconds-since-epoch crosses on 2038-01-19 — a dated, documented horizon,
  not a defect this ticket pays to avoid.
- **Idempotency keys must not collapse the lifecycle** (the plan-review
  blocker). `createResendSender`'s key is `<prefix>:<to>`; a fixed
  per-booking prefix makes create, cancel, and un-cancel byte-identical
  keys, and Resend's dedupe would swallow every transition after the
  first. Invite sends therefore use a **per-transition prefix embedding
  the SEQUENCE** — `booking-<id>-<kind>-<seq>` — which keeps retry
  dedupe within a transition and never across transitions.
- **ORGANIZER is a bare address and identical across the lifecycle**
  (plan review): `ORGANIZER:mailto:` takes the plain address, not the
  friendly-name `BOOKING_EMAIL_FROM` form, and Gmail distrusts a CANCEL
  whose organizer doesn't match the event's — so `ICS_ORGANIZER` is its
  own booking-config constant, aligned with the sending identity and
  asserted equal across REQUEST and CANCEL.
- Required VEVENT/VCALENDAR properties include `VERSION:2.0` and
  `DTSTAMP` (from the injected `now`) — both on the verify list, not
  assumed.
- `DTSTART`/`DTEND` as UTC instants (`...Z` form) — no VTIMEZONE block
  needed, and Google renders in the viewer's zone. `DTEND = slot_start +
  SLOT_MINUTES` (no `slot_end` column exists; the CHECK pins 30).
- `ORGANIZER:mailto:` the noreply identity; `ATTENDEE:mailto:` the office.
  `SUMMARY: Assessment — {name} ({serviceLabel})`, `LOCATION` from the
  row's address/city/postal (the `address` column exists and is NOT NULL),
  `DESCRIPTION`: phone, service, and the visit-length line — **and never
  policy/claim** (risk area 2).

**Attachment shape**: `{ filename: 'assessment-<id>.ics', content: <ics
text>, contentType: 'text/calendar; charset=utf-8; method=REQUEST' }` (or
`method=CANCEL`) — the `method=` parameter is the conventional signal for
invite rendering; Gmail's actual behaviour is heuristic and undocumented,
which is why AC5's post-deploy render check is the honest pin, not this
sentence. (`Attachment` type at `index.d.mts:522-526` / `:602-620`.)

## Scope

1. **`src/lib/booking-ics.ts`** — the pure builder, no env, no SDK, no DB
   (importable under `tsx` like `booking-email.ts`):
   `buildBookingIcs(event, kind: 'request' | 'cancel', now: Date) → string`
   over `{ id, name, serviceLabel, phone, address, city, postalCode,
   slotStart: Date }`, plus `icsAttachment(ics, kind, id)` returning the
   Resend attachment object. Folding, escaping, CRLF, UID, SEQUENCE-from-now
   all live here. Constants (`ICS_UID_DOMAIN = 'yegrestoration.ca'`,
   `ICS_PRODID`, and **`ICS_ORGANIZER = 'noreply@yegrestoration.ca'`** —
   the bare address, per Mechanism) join `booking-config.ts` — **not**
   `services.ts`, which
   imports `.jpg` and cannot load under `tsx` (recon flag; the same reason
   `booking-config.ts` duplicates `BUSINESS`).
2. **`src/lib/booking-email.ts`** — `Message` gains
   `attachments?: EmailAttachment[]`; `BookingNotificationInput` gains
   `slotStart: Date` and `now: Date` (only the display `slotLabel` exists
   today — an ICS needs the instant); `internalNotification()` attaches the
   REQUEST ICS to the internal message. The customer message is untouched
   (see Out of scope). **The required-field ripple, enumerated** (plan-review
   S3 — typecheck catches each, but a Reviewed plan names the production
   route it forces changes into): `planForPayload` and `planForAppointment`
   in `booking-admin-notify.ts` (the latter gains a `now` parameter it
   lacks), **`api/admin/appointments/resend.ts`** (whose `plan.internal`
   will now carry a fresh-SEQUENCE ICS that is never delivered — the resend
   path sends customer-only; harmless, stated), `scripts/verify-env.ts`'s
   hand-built input, and `verify-booking-admin-db.ts`'s.
3. **`src/lib/booking-notify.ts`** — two changes (the plan-review blocker
   replaced the draft's "nothing else changes"):
   - `createResendSender`'s field whitelist forwards `attachments` (risk
     area 1; source-pinned).
   - **`sendCalendarInvite(message, keyParts, deps: NotifyDeps) →
     SendOutcome`**, exported: mute check first (same `mailDisabled`),
     missing key → `'failed'` logged, real wiring builds the sender with
     the **per-transition idempotency prefix**
     `booking-<id>-<kind>-<seq>` (Mechanism — a fixed per-booking key
     would make Resend collapse a CANCEL into the earlier REQUEST), and
     the injected `deps.send` seam is what every lib-level assertion
     drives. The existing functions — mute, key handling, per-booking
     idempotency for *notifications*, outcomes — are untouched.
4. **`src/lib/booking-admin-notify.ts`** — `planCalendarInvite(event, kind,
   now) → Message`: a deliberately tiny office-only email ("Calendar:
   assessment #<id> — <name>", one-line body, ICS attached), defined over
   **the same plain event record the scope-1 builder takes** — not the
   `Appointment` row, because its create-path caller holds only
   `BookingPayload` + the returned id (plan-review S2). Two mappers mirror
   the existing pair: `inviteEventFromPayload(payload, id)` and
   `inviteEventFromAppointment(row)`. The narrowed invariant lives in this
   module's header where the old one does.
5. **`src/pages/api/booking/create.ts`** — passes `slotStart`/`now` into the
   plan (the payload already carries `payload.slotStart`; it just never
   reached the plan). The public path's office invite is the attachment on
   the internal notification — no second email.
6. **`src/pages/api/admin/appointments/create.ts`** — after a successful
   insert, send `planCalendarInvite(…, 'request', now)` best-effort:
   independent of the `sendConfirmation` checkbox (the invite is for the
   office, the checkbox is about the customer), muted by
   `BOOKING_NOTIFY_DISABLED` like every send, failure logged, entry flash
   unaffected.
7. **`src/pages/api/admin/appointments/update.ts`** — net-new mail wiring
   (this route sends nothing today): when a status edit **crosses the
   cancelled boundary** — into `cancelled` sends `'cancel'`, out of
   `cancelled` back to `booked` sends `'request'` (same UID, later
   SEQUENCE) — wrapped in `withDeadline(…, POST_COMMIT_BUDGET_MS)` like the
   public path, best-effort, logged, redirect flashes unchanged. Re-submits
   that keep the same status, and `completed`/`no_show` edits, send
   nothing. The un-cancel 23505 conflict arm returns before any send.
   **Boundary detection is pinned** (plan-review S1 — today's statement
   `RETURNING id` cannot distinguish a re-submit from an un-cancel, and
   the route's own docstring forbids SELECT-then-UPDATE): the single
   UPDATE widens to a self-join against a pre-update snapshot —
   `UPDATE appointments SET … FROM (SELECT id, status AS prev_status,
   name, address, city, postal_code, service, slot_start FROM appointments
   WHERE id = …) old WHERE appointments.id = old.id RETURNING
   appointments.status, old.*` — one statement, prior status and every ICS
   field in one round trip, no race.
8. **`scripts/verify-booking-ics.ts`** + `npm run verify:booking:ics` — the
   builder exhaustively (see Verification), plus the whitelist source pin.
9. **Extensions**: `verify-booking-email.ts` (internal message carries
   exactly one ICS attachment, REQUEST-typed; customer message carries
   none), `verify-booking-admin-entry.ts` (the narrowed invariant, red-
   observed in both directions), `verify-booking-admin-db.ts` (the
   cancel/un-cancel lib arms with a widened capture — it records only
   `{to}` today — and the update route under mute).
10. **ROADMAP** — the P6 row's status line (the section already exists);
    the admin-notify invariant's wording updated where the roadmap states
    it.

## Out of scope

- **Customer-facing ICS.** Deferred deliberately: a customer invite raises
  its own lifecycle questions (cancellation is phone-in by locked decision —
  mailing a customer a CANCEL artifact is new customer-facing behaviour) and
  the client asked for *his* calendar. Recorded as the natural follow-up if
  he asks; the builder is audience-agnostic so it costs one plan change
  later.
- **Google Calendar API / two-way sync** — levels 2 and 3, recorded above.
- **Reschedule invites** — no reschedule path exists (`slot_start` is
  unmodifiable by design; rescheduling is cancel + re-enter, which this
  ticket already covers as CANCEL + fresh REQUEST).
- **The reminder job (BK-06)** — untouched; SMS and calendar are separate
  artifacts.

## Design notes

- **One mechanism per path, no double mail.** Public bookings: the ICS rides
  the internal notification that already fires. Admin creates and
  cancel/un-cancel: the tiny invite-only email, because nothing else fires
  there and a full lead-notification to the office about its own typing is
  exactly what the old invariant rightly forbade.
- **Best-effort everywhere.** No booking, entry, or status edit ever fails
  or blocks on an invite; a missed invite is an office inconvenience
  recoverable by hand, and the failure is in the function log. This is the
  public path's existing posture extended to the new sends.
- **Stateless SEQUENCE** — Mechanism; the reviewer should challenge the
  derivation, not the absence of a column.
- **The ICS never carries policy/claim for any audience**, even though
  today's only audience is the office that already receives them in the
  same email's body. The rule is about where calendar artifacts travel,
  and it must hold before a customer variant ever exists.

## Acceptance criteria

1. A public booking's internal notification carries exactly one `.ics`
   attachment: METHOD:REQUEST, UID `booking-<id>@yegrestoration.ca`,
   DTSTART/DTEND spanning the slot's 30 minutes in UTC `Z` form, LOCATION
   from the row's address, SUMMARY with name and service — and no policy or
   claim number anywhere in the ICS, for a fully-insured booking. The
   customer confirmation carries no attachment.
2. An admin manual entry sends exactly one office email — the calendar
   invite — regardless of the customer-confirmation checkbox; every other
   admin action except a cancelled-boundary status edit sends the office
   nothing (the narrowed invariant, asserted in both directions).
3. A status edit into `cancelled` sends METHOD:CANCEL + STATUS:CANCELLED
   with the same UID and a SEQUENCE strictly greater than the create's;
   un-cancelling sends a fresh REQUEST, same UID, greater still. Re-submits
   and completed/no_show edits send nothing; the 23505 conflict arm sends
   nothing.
4. All invite sends are best-effort: with the send failing or the key
   unset, booking commit, admin entry, and status edits all succeed exactly
   as today, with the failure logged.
5. The builder's output is RFC-honest: CRLF, 75-octet folding, escaped
   text, and it survives a real Gmail render (post-deploy check).
6. `createResendSender` forwards `attachments` to the SDK (source-pinned),
   and `BOOKING_NOTIFY_DISABLED` mutes every new send.
7. `npm run typecheck` and `npm run build` pass; existing notification
   verify scripts pass unmodified in behaviour (extended, not weakened).

## Verification

- **`verify:booking:ics` (new)** — lib-level, no DB, no env: folding at 75
  octets (a long address red-observes it), escaping (`,` `;` `\` newline),
  CRLF-only output, `VERSION:2.0` and `DTSTAMP` present (DTSTAMP from the
  injected `now`), UID **and ORGANIZER** stability across kinds (Gmail's
  CANCEL matching needs both), SEQUENCE monotonicity (later `now` →
  strictly greater, cancel > create), METHOD/STATUS pairing, UTC `Z`
  DTSTART/DTEND arithmetic (slot + 30), LOCATION/SUMMARY content, and the
  **PII red**: an event built from a fully-insured booking, policy and
  claim in the input, asserts neither string appears in the ICS —
  red-observed by adding policy to DESCRIPTION in the builder. Also
  `sendCalendarInvite` through the `deps.send` seam: mute → `'skipped'`
  with the seam uncalled, missing key → `'failed'`, and the
  **per-transition idempotency prefix** — the real prefix construction is
  a shared symbol the script drives with two transitions and asserts
  inequality (a fixed key is the blocker's regression). Plus two source
  pins, honestly weak per risk area 1: `booking-notify.ts` forwards
  `attachments` in the sender literal (red by removing the field), and
  the two route call sites exist (`create.ts`, `update.ts`) — the routes'
  positive wiring is otherwise observable only post-deploy (plan-review
  S5: a route-level run under mute cannot distinguish "muted" from
  "never wired").
- **`verify:booking:email` extension** — plan-level through the existing
  capture seams: internal message has exactly one attachment,
  `text/calendar; …method=REQUEST`, filename `assessment-<id>.ics`;
  customer message has none; muted arm attaches nothing because nothing
  sends.
- **`verify:booking:admin:entry` extension** — the narrowed invariant, both
  directions, honestly located (plan-review S5): **lib-level** through the
  B1 seam — `planCalendarInvite` + `sendCalendarInvite` produce exactly one
  office-addressed message and it carries the ICS (red: make it a
  lead-notification; red: make it produce nothing — the old assertion
  would have stayed green on that second break, which is why it is
  rewritten); **route wiring** by the source pin above plus the
  post-deploy check, stated as such rather than claimed as behavioural.
- **`verify:booking:admin:db` extension** — capture widened to full
  `Message`; lib-level: `planCalendarInvite` for both kinds against a real
  dev-branch row; route-level under mute: status edit into cancelled and
  back, asserting redirects unchanged and no send escaping the mute; the
  conflict arm sends nothing.
- Red-first throughout: every new assert sees red by breaking the
  production target; one entry per failure mode in the table below.
- Gates: `npm run typecheck`, `npm run build`, `verify:booking:ics` (new),
  `verify:booking:email`, `verify:booking:admin:entry`,
  `verify:booking:admin:db`, regressions `verify:contact`,
  `verify:booking:smoke`, `verify:cutover`.
- Post-deploy (user): book a test assessment on production → the invite
  arrives at info@ and renders as a Gmail invite → add it to the calendar →
  cancel the booking in admin → the calendar event is removed/struck
  (Gmail applies the CANCEL) → delete the test row. Also one admin manual
  entry → invite arrives without the confirmation checkbox ticked.

## Assumptions log

Settled without the user; each is a reviewer's or the user's to overturn.

- **The admin-mail invariant narrows rather than holds.** Risk area 3; the
  feature is impossible without it, and the narrowed form is asserted more
  precisely than the old one was.
- **Office-only this ticket; customer ICS deferred** — Out of scope.
- **SEQUENCE from the clock, no column** — Mechanism.
- **Cancel/un-cancel invites are best-effort with no UI flash.** The
  office is the audience and is standing in admin performing the action;
  a failed CANCEL is visible in the log and recoverable by deleting the
  event by hand. A flash would add a UI state for a self-correcting case.
- **`now` is injected as a `Date`** everywhere (the BK-09 precedent), so
  DTSTAMP and SEQUENCE come from one instant per send.
- **No new environment variables.**

## Gate results

| Gate | Result |
| --- | --- |
| `npm run typecheck` | pending |
| `npm run build` | pending |
| `verify:booking:ics` (new) | pending |
| `verify:booking:email` (extended) | pending |
| `verify:booking:admin:entry` (extended) | pending |
| `verify:booking:admin:db` (extended) | pending |
| regressions (contact, smoke, cutover) | pending |

## Red-observed

Pending implementation.
