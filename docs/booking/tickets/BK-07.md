# BK-07 — Appointments list + detail in `/admin`

**Status:** committed 2026-08-10 as `ef6d76e`, deployed to production. The
admin outage is over: `GET /admin/login/` answers **200** (it 302-looped
before this deploy) and every protected path redirects there in one hop.
Implementation review returned 0 blockers, 2 should-fixes, 4 nits; both
should-fixes fixed with their own red runs (R17, R18), 3 nits taken, 1
recorded. **One open item:** a real login landing on `/admin/` is unverified —
it needs the actual `ADMIN_PASSWORD`, which the implementer does not have.
Plan-reviewed 2026-08-10 — 1 blocker, 2 should-fixes, 3 nits, all
accepted and folded in below. The blocker: admin login is a redirect loop in
live production under `trailingSlash: 'always'` (independently confirmed by
curl: `/admin/login` 308 → `/admin/login/` 302 → `/admin/login` …), so the
surface this ticket builds on has been unreachable since ~2026-07-04. Fix
brought into scope (item 0) per the reviewer's option (a).
**Phase:** P4 · **Tier:** Reviewed · **Depends on:** BK-05 (stamp columns) ·
**Blocks:** BK-08 (edits happen on the detail page this ticket builds), BK-09
(the file section this ticket stubs is where the proxy links land)

Reviewed because it renders PII and insurance identifiers onto a new surface.
Risk areas, in the order a reviewer should weight them:

1. **Auth coverage — now including making auth work at all.** Every new path
   must sit behind the middleware. It matches on the `/admin` prefix, so
   `/admin/appointments` and `/admin/appointments/[id]` are covered by
   construction — the reviewer's job is to confirm nothing here escapes that
   prefix (no new API route, no public-path addition, no data in a client
   island). Plan review found the flip side broken: the middleware's
   `PUBLIC_PATHS` holds only unslashed paths while production 308-normalizes
   every request to the slashed form, so login loops and **production admin
   has been unreachable since the trailing-slash change (2026-07-04)**. The
   fix (scope item 0) edits path matching in the auth middleware, which cuts
   both ways: the reviewer must confirm the normalization cannot classify a
   *protected* path as public.
2. **Timezone rendering.** `slot_start` is a UTC instant and the server runs in
   UTC. A bare `toLocaleString()` or `toLocaleDateString('en-CA', …)` without
   `timeZone` renders the *server's* zone — off by 6 or 7 hours depending on
   DST, which for a 3:30 p.m. appointment shows the wrong day's evening. The
   existing leads page does exactly this for `created_at`, where nobody
   notices; copied onto `slot_start` it sends a crew on the wrong day.
3. **PII stays server-rendered.** No appointment data may enter a Svelte
   island or client script — the page is plain `.astro`, HTML only, like the
   leads pages.
4. **Read-only against production.** This ticket runs two new SELECTs on the
   list page and two on the detail page (appointments + files), and writes
   nothing.

## Goal

Bookings are live (soft-launched 2026-08-10) and the office's only view of
them is the internal email. If that email fails — the exact failure BK-05
built `internal_notified_at` to record — the appointment exists only as a
database row, and nobody is watching the database. The stamp columns are
currently surfaced nowhere, so the failure they record is still invisible.

Give the office a read-only view: what's coming, everything about each visit,
and a loud flag when a notification never went out.

## Scope

0. **Fix admin auth under `trailingSlash: 'always'`** (plan-review blocker).
   - `src/lib/auth.ts` gains a pure, `tsx`-importable
     `isPublicAdminPath(pathname)` that answers true for the three public
     paths in either slash form and false for everything else — including
     tricky near-misses (`/admin/login-x`, `/admin/login//`,
     `/api/admin/login/extra`). The middleware uses it instead of the raw
     `Set` lookup.
   - The middleware's unauthenticated redirect targets `/admin/login/`
     (slashed) so it no longer buys an extra 308.
   - `login.astro`'s form action, `AdminLayout.astro`'s logout action, and
     `login.ts`'s success/failure redirect `Location`s all move to slashed
     form (`/api/admin/login/`, `/api/admin/logout/`, `/admin/`,
     `/admin/login/?error=1`), per the Known trap that unslashed forms 308.
   - Post-deploy check: unauthenticated `GET /admin/login/` answers 200 (it
     answers 302-loop today), and a real login lands on `/admin/`.
1. **`src/lib/booking-admin.ts`** — pure helpers, no env, no DB, importable
   under `tsx`:
   - `partitionAppointments(appointments, now)` →
     `{ upcoming, past }` — upcoming = `slot_start >= now` and status
     `booked`, ascending (soonest first); past = everything else,
     descending (most recent first). Cancelled and no-show rows are *past*
     regardless of date: they need no crew, but they stay visible because a
     cancelled slot that rebooks is tomorrow's confusion.
   - `notificationFlags(appointment)` → the "office was never told" /
     "customer never confirmed" conditions in one place: internal flag when
     `internal_notified_at` is null; customer flag when
     `confirmation_sent_at` is null **and** `email` is non-null (a customer
     who gave no email was never owed one — not a failure). Both flags only
     for `source = 'web'` rows — admin-entered appointments (BK-08) never
     had notifications to fail.
2. **`src/pages/admin/appointments/index.astro`** — `prerender = false`.
   Two queries: `SELECT * FROM appointments`, partitioned by the helper, and
   a grouped file count (`SELECT appointment_id, COUNT(*)::int FROM
   appointment_files WHERE appointment_id IS NOT NULL GROUP BY
   appointment_id`) joined in application code. Claimed rows count
   regardless of `upload_state`, matching the commit path's claim rule (the
   Known trap: `onUploadCompleted` never fires on localhost). Two sections:
   - **Upcoming** — the operational view: slot (via `formatSlot`, labelled
     Edmonton time), name, phone, service label, city, payment route badge,
     file count, and the notification warning flag when either flag is up.
   - **Past** — collapsed visual weight: slot, name, service, status badge
     (completed / cancelled / no_show / booked-but-elapsed).
   Row click → detail. Status badge colors follow the leads page's pattern.
3. **`src/pages/admin/appointments/[id].astro`** — `prerender = false`,
   parameterized SELECT by id (the id is cast/validated before the query;
   non-numeric or missing → 404). Everything on the row, grouped:
   - **Visit** — slot via `formatSlot` + Edmonton label, duration, status,
     pipeline stage, source.
   - **Customer** — name, phone (as `tel:` link), email (as `mailto:` when
     present), address / city / postal code, description.
   - **Insurance** — insurer, policy, claim; section absent entirely on the
     private route. This is the sanctioned admin surface for these fields
     (the `002-booking` column comment: email and admin panel only).
   - **Notifications** — the two BK-05 stamps rendered as sent-at times
     (Edmonton) or as explicit warnings from `notificationFlags`.
     `sms_consent_at` and `reminder_sent_at` render **neutrally**, never as
     warnings: a null consent means "no consent" (the locked rule — the
     timestamp *is* the consent, absence is a valid state), and a null
     reminder is expected on every row until BK-06 ships and the slot
     approaches. "No consent" / "not sent" in plain style.
   - **Files** — count and per-file name/size/state from `appointment_files`
     (`WHERE appointment_id = …`), no links: viewing is BK-09, and the
     section says so. Blob pathnames/URLs are not rendered even as text.
   - **Notes** — `admin_notes` verbatim (editing is BK-08).
4. **`src/layouts/AdminLayout.astro`** — a two-link nav (Leads ·
   Appointments) so both surfaces are reachable; current-page state. The
   30-second `refresh` meta the leads page uses is applied to the
   appointments list too.
5. **`src/pages/admin/index.astro`** — unchanged except whatever the shared
   nav needs; the leads list stays the `/admin` landing page until BK-10
   decides otherwise.
6. **`scripts/verify-booking-admin.ts`** + `npm run verify:booking:admin` —
   the pure helpers under `tsx`, red-first per rule.

## Out of scope

- **Any write**: status/stage edits, cancellation, blackout dates, notes
  editing, manual entry — all BK-08.
- **Viewing file contents** — BK-09 (private Blob store; direct URLs 403).
- **Resending a failed confirmation** — BK-08 at the earliest, needs a write
  and a send.
- **Pagination and search.** One crew, ≤5 slots/day, 14-day booking window:
  the table stays small for months. Recorded here so its absence is a
  decision, not an oversight.
- **The leads page's own unzoned `created_at` rendering** — pre-existing,
  cosmetic there; reviewer agreed it's worth recording, now in ROADMAP Known
  traps with owner BK-10.

## Design notes

- **Every new internal link is slashed** (`/admin/appointments/`,
  `/admin/appointments/${id}/`, nav links) — the site convention; the
  existing admin's unslashed links each eat a 308 and are the pattern not to
  copy.
- **Astro pages, zero client JS**, matching the existing admin exactly. The
  data never leaves the server-rendered HTML, which is what keeps risk area 3
  a non-issue by construction.
- **`formatSlot` is the only slot renderer.** It already exists, is
  Edmonton-aware, and is what the emails use — the admin page showing the
  same string the customer received is a feature, not a shortcut. Dates that
  are *not* slots (created_at, stamps) render through one small local helper
  that passes `timeZone: TIMEZONE` explicitly, so no bare `toLocale*` call
  appears anywhere in the new pages.
- **The warning flags are computed, not styled inline** — `notificationFlags`
  exists so "what counts as a failed notification" is a pure function a
  script can pin down, including the no-email-given and admin-source cases
  where a missing stamp is *not* a failure. The subtlety is exactly the kind
  of condition that rots when written as template ternaries.
- **`SELECT *` on the list page, matching the leads page.** The table is
  small (see pagination note) and the alternative — a column list that must
  chase the schema — is the kind of duplication BK-05's stamp columns would
  already have broken once.
- **Detail query is by validated integer id.** The Neon driver
  parameterizes, so this is defense in depth plus a clean 404 for
  `/admin/appointments/abc`.

## Acceptance criteria

1. Admin auth works end to end on production paths: unauthenticated
   `GET /admin/login/` answers 200 (no loop); a correct password lands on
   `/admin/`; unauthenticated `GET /admin/appointments/` redirects to
   `/admin/login/`; `isPublicAdminPath` answers true for the three public
   paths in both slash forms and false for protected paths and near-misses.
2. The list shows upcoming booked appointments soonest-first with slot
   rendered in Edmonton time and labelled as such; a 3:30 p.m. Edmonton
   booking renders as 3:30 p.m., never as the UTC wall-clock (9:30/10:30
   p.m.) or the wrong day.
3. `partitionAppointments`: upcoming excludes cancelled/no-show/completed
   and past slots; ordering is ascending upcoming, descending past; the
   boundary instant (`slot_start === now`) is upcoming.
4. `notificationFlags`: internal flag iff `internal_notified_at` is null and
   source is web; customer flag iff `confirmation_sent_at` is null, email
   non-null, and source is web; admin-source rows never flag.
5. The detail page shows insurance identifiers on the insurance route and no
   insurance section on the private route.
6. The detail page shows both notification stamps (time or warning), SMS
   consent, and the file list with no Blob URL or pathname in the HTML.
7. A non-existent or non-numeric id answers 404, not 500.
8. `npm run typecheck` and `npm run build` pass.

## Verification

- `npm run verify:booking:admin` — AC3 and AC4 exhaustively (all
  status × source × email × stamp combinations), plus the boundary case;
  `isPublicAdminPath` per AC1's matrix; and a **scripted source scan**
  asserting no bare `toLocale*` without `timeZone` in the two new pages (its
  red run: plant one). New script: red-first entries owed for each failure
  mode.
- AC2, AC5–AC7 are server-rendered DOM: no browser-test infrastructure
  (BK-03/04/05 precedent), so the reviewer reads the pages; AC2's timezone
  claim is additionally covered by the scripted source scan above.
- AC1's live half is a post-deploy curl check (`GET /admin/login/` → 200),
  recorded in the gate table when run.
- `npm run typecheck`, `npm run build`.
- Regression: `verify:booking:email`, `verify:booking:payload` (nothing here
  should touch them; green confirms it).

## Assumptions log

Settled without the user; each is a reviewer's or the user's to overturn.

- **Read-only is the whole ticket.** The ROADMAP scopes edits to BK-08; the
  detail page lays out where they will go but ships none.
- **Leads stays the `/admin` landing page.** Appointments is one click away
  behind shared nav. BK-10 (leads read-only) is the natural point to swap
  the default.
- **Cancelled/no-show visibility**: shown in Past with status badges, never
  hidden. Nobody asked for hiding; recoverable information beats tidiness in
  an ops view.
- **The internal-notification flag is the loud one** (office never told =
  phantom crew visit); the customer flag is informational. Both render, the
  visual weight differs.
- **`SELECT *` and no pagination** per Design notes / Out of scope.
- **No new environment variables.**

Added during implementation:

- **`logout.ts`'s redirect `Location` was slashed too.** Scope item 0 named
  the login page's action, the layout's logout action and `login.ts`'s two
  `Location`s, but not this one — same failure family, one character, and
  leaving it would have been the only unslashed redirect left in the flow.
- **"Row click → detail" ships as links inside the row** (the slot and a
  `View →` link, the leads page's pattern) rather than a clickable `<tr>`.
  A whole-row click needs a client script, and risk area 3 bans client JS on
  this surface. Overturning this means overturning that.
- **The shared helpers live in `booking-admin.ts`, not as page-local
  helpers.** The Design notes call `formatAdminTimestamp` "one small local
  helper"; putting it in the pure module instead lets the scripted gate
  assert what it *renders* (3:30 p.m. Edmonton for a 21:30Z instant) rather
  than only that the source text mentions `timeZone`. Same for the label maps
  and `formatFileSize`.
- **The verify script pins `process.env.TZ = 'UTC'`.** Vercel runs UTC; a
  developer's laptop runs Edmonton, where a formatter missing its `timeZone`
  produces the right answer by accident and the assertion can never fail.
  Red run R12 confirms the pin works.
- **`size_bytes` is coerced through `Number()`.** It is `BIGINT`, which the
  driver returns as a string whatever `db.ts` says — `upload-token.ts`
  already wraps the same column. Recorded as a doc-level defect in ROADMAP
  Known traps (owner BK-09).
- **Astro's CSRF origin check answers 403 to a form POST with no `Origin`
  header.** Found while driving the login flow with `fetch`; browsers always
  send one, and the same POST with `Origin` set returns the expected 302.
  A test artifact, not a defect — recorded so the next person to curl
  `/api/admin/login/` does not read the 403 as an auth failure.

## Gate results

| Gate | Result |
| --- | --- |
| `npm run typecheck` | ✅ pass — 0 errors (astro check: 0 errors / 24 hints; svelte-check: 0 errors / 13 warnings, all pre-existing and none in the new files) |
| `npm run build` | ✅ pass — generated routes are `^/admin/appointments/$` and `^/admin/appointments/([^/]+?)/$`, both slashed, which is the trailing-slash behaviour item 0 exists for |
| `npm run verify:booking:admin` | ✅ pass |
| `npm run verify:booking:email` (regression) | ✅ pass |
| `npm run verify:booking:payload` (regression) | ✅ pass |
| Local dev-server exercise (evidence, not a committed gate) | ✅ 47/47 — see below |
| Post-deploy: `GET https://www.yegrestoration.ca/admin/login/` → 200 | ✅ **200** (it 302-looped before this deploy — the outage is over) |
| Post-deploy: a real login lands on `/admin/` | ⏳ awaiting the user — the implementer has no `ADMIN_PASSWORD` (`.env.local` holds it as `""`). Every observable half is verified below |

**Post-deploy checks — commit `ef6d76e`, run against
`https://www.yegrestoration.ca` on 2026-08-10.** The deploy was polled until
it landed; the first poll still answered 302, the next 200, which is the
outage ending on the exact request the ticket named.

| Request | Before | After |
| --- | --- | --- |
| `GET /admin/login/` | 302 loop | **200** |
| `GET /admin/login` (the loop's entry point) | 308 → 302 → 308 → … | **308 → 200, one redirect, terminates** |
| `GET /admin/` | loop | **302 → `/admin/login/`**, single hop |
| `GET /admin/appointments/` | — | **302 → `/admin/login/`** (the new surface is gated) |
| `GET /admin/appointments/1/` | — | **302 → `/admin/login/`** (so is the detail page) |
| `GET /admin/leads/1/` | loop | **302 → `/admin/login/`** |
| `GET /admin/login-x/` | — | **404** — the near-miss is not public |
| `GET /admin/login//` | — | 308 → `/admin/login/`; Vercel collapses the double slash at the edge, so it never reaches the middleware |
| `POST /api/admin/login/`, wrong password, browser-style `Origin` | 308 → 401 | **302 → `/admin/login/?error=1`** — the endpoint runs instead of 401ing, which is the POST half of the loop fixed |

The last row is the strongest evidence available without the password: the
login POST reaching its own handler and returning the *wrong-password*
redirect is exactly what the 308-into-401 prevented. What remains for the user
is only that the **correct** password lands on `/admin/`.

**Local dev-server exercise.** A throwaway script (scratchpad, not committed)
started `astro dev` against the Neon **dev branch**, inserted three fixture
appointments — insurance with both stamps and a file, private with
`internal_notified_at` null, and an admin-source cancelled row — drove the real
login flow, asserted on the rendered HTML, then deleted them (0 fixtures left
behind, verified by query). It covers the ACs the Verification section assigns
to the reviewer's eyes, from the server's actual output:

- AC1: `GET /admin/login/` 200; `GET /admin/appointments/` 302 → `/admin/login/`
  (one hop, no loop); wrong password → `/admin/login/?error=1`; correct password
  → `/admin/` with the session cookie; `GET /admin/` 200 with the shared nav.
- AC2: the 21:30Z slot renders `Jul 14 · 3:30 p.m.`, never `9:30 p.m.`, under a
  page that says "Edmonton time"; upcoming is soonest-first.
- AC5: policy, claim and insurer render on the insurance route; the private
  route has no insurance section at all.
- AC6: both stamps render (time or warning), consent and reminder render
  neutrally, the file shows `basement.jpg · 5.0 MB`, and neither the Blob URL
  nor the pathname sentinel appears anywhere in the HTML.
- AC7: `abc`, `0`, `-1`, `12abc`, `9999999999999` and a valid-but-absent id all
  answer 404; the detail page is 302-gated without a session.
- The list page carries no insurance identifiers and no addresses.

Local dev needs an `ADMIN_PASSWORD` for any of this: `.env.local` holds it as
`""` (the `vercel env pull` trap) and `import.meta.env` wins over `process.env`
for the middleware's read. A temporary value was written to `.env.local` for the
run and the file restored byte-for-byte afterwards.

## Red-observed

Every assertion below was seen red by breaking the production target — never
the script — and restored; all five files verified byte-identical to their
pre-break state afterwards, and the suite re-ran green.

| # | What was broken | What went red |
| --- | --- | --- |
| R1 | `isPublicAdminPath` loses its slash normalization (the shipped bug, exactly) | the three slashed public paths reported not-public — the production loop reproduced in the script |
| R2 | its exact match loosened to `startsWith` | 8 near-misses (`/admin/login-x`, `/admin/login//`, `/api/admin/login/extra`, …) reported public |
| R3 | `ADMIN_LOGIN_PATH` set back to `/admin/login` | "the unauthenticated redirect target is slashed" |
| R4 | `login.astro`'s action back to `/api/admin/login` | the slashed-action source scan |
| R5 | the `status === 'booked'` test dropped from the upcoming split | completed / cancelled / no_show rows at or after the cutoff landed in upcoming |
| R6 | `slot_start >= now` tightened to `>` | the boundary instant fell into past (AC3's named case) |
| R7 | upcoming sorted descending | "upcoming is ascending — soonest first" |
| R8 | past sorted ascending | "past is descending — most recent first" |
| R9 | `internalMissing` hardcoded false | 4 matrix cells plus "a web booking nobody told the office about is a failure" |
| R10 | the customer flag stops checking whether an email was given | the no-email and whitespace-email cells — a customer who was never owed a confirmation reported as a failure |
| R11 | the `source !== 'web'` early return removed | every admin-source cell flagged |
| R12 | `timeZone` dropped from `formatAdminTimestamp` | rendered `9:30 p.m.` for a 3:30 p.m. slot, `Aug 24` for an Aug 23 evening, and `10:30 p.m.` for the winter/MST case — this is also the evidence that the script's UTC pin works |
| R13 | a bare `toLocaleDateString` planted on the list page | the source scan named the call and the file. The same call *with* `timeZone` was then planted and stayed green, so the scan is a timezone rule and not a keyword ban |
| R14 | `{file.url}` rendered in the detail page's file list | the Blob-location scan |
| R15 | a `process.env` read added to `booking-admin.ts` | the purity check that makes the whole script possible under `tsx` |
| R16 | `formatFileSize` stops coercing its argument | a `BIGINT` arriving as a string rendered as "unknown size" |
| R17 | `src/middleware.ts` restored to its shipped form — the raw `Set.has(pathname)` lookup and the unslashed redirect — with `auth.ts` left correct | the new call-site assertions (both needles, plus "no unslashed `/admin/login` literal of its own"). Before R17 this exact break left every gate green while production looped |
| R18 | a read of `sms_consent_at` added inside `notificationFlags` that changes **no** behaviour | "notificationFlags does not read sms_consent_at" — the behavioural matrix stays green, so this is the assertion carrying that rule alone |

## Implementation review — 2026-08-10

Fresh reviewer, given only the ticket, the diff and the gate output. Verdict:
**no blockers**, 2 should-fixes, 4 nits.

The reviewer independently regenerated `.vercel/output/config.json` and
confirmed from it that production routes *only* the slashed forms (there is no
catch-all to `_render`, and the 308 rule sits before `handle: filesystem`), so
the loop fix is real rather than argued; exercised `isPublicAdminPath` against
23 adversarial inputs including `/admin/login/./`, `/admin//login/`,
`/admin/login%2F`, `/ADMIN/LOGIN/` and a trailing space, all correctly
protected; confirmed no `toLocale*` in the new pages, no client JS, no Blob URL
or pathname, and no write anywhere in the diff.

| # | Finding | Resolution |
| --- | --- | --- |
| 1 | **SHOULD-FIX** — the script pinned `auth.ts` but never the *call site*. Reverting `src/middleware.ts` to `PUBLIC_PATHS.has(pathname)` left typecheck, build and `verify:booking:admin` all green while `/admin` looped in production again. The outage never lived in the helper. | **Fixed.** `src/middleware.ts` added to the slashed-source scan with both needles, plus an assertion that it holds no unslashed `/admin/login` literal of its own. Red run R17 — and the reviewer is right that this was the one assertion the ticket most needed. |
| 2 | **SHOULD-FIX** — vacuous assertion: `Object.keys(appt({}))` read the *script's own fixture builder*, whose keys are fixed in a literal three lines above. It could never report the change its comment claimed to guard. | **Fixed.** Replaced with a scan of `notificationFlags`' body in `booking-admin.ts` for the two column names. Red run R18 breaks behaviour-neutrally, so only this assertion can catch it. |
| 3 | **NIT** — the page scans skipped `AdminLayout.astro`, also new code here; and the Blob regex would miss `file['url']` or a destructure. | **Taken.** `AdminLayout.astro` added to the scanned set; the leak scan now covers dotted, bracket and destructured spellings. |
| 4 | **NIT** — `/^[0-9]{1,10}$/` accepts `/admin/appointments/0000000012/`, a second URL for one row. | **Taken.** Now `/^[1-9][0-9]{0,9}$/`; `0` and leading-zero forms 404. |
| 5 | **NIT** — the notification warning is upcoming-only, so a failed notification loses its list-page flag the moment the slot elapses. | **Recorded, not changed.** Scope item 2 spells out Past's columns (slot, name, service, status) and the reviewer agrees this is a spec consequence rather than a deviation. The detail page still shows it, and the flag's purpose is to catch a phantom crew visit *before* it happens. Reopening it is BK-08's call, alongside resend. |
| 6 | **NIT** — the leads pages' own unslashed links were left unrecorded, unlike `size_bytes`. | **Taken.** Now a ROADMAP Known trap with severity and owner BK-10. |
