# BK-53 — GATE 1: take every price off the site, and stop promising a refund

**Status:** 📝 **REVISION 7 WRITTEN 2026-09-06.** ⛔ **NOT APPROVED — not yet
validated.** Owed: traceability audit, adversarial pre-read, plan review.
✅ Nothing blocked on the user.
🔴 **R7-2 is the one to read first: the gate has NO refund or credit ban, and the
ticket's own title is *"stop promising a refund"*.** Executed — all four money
promises match zero of the ten free-claim shapes and zero of the four amount
shapes, and `verify-cutover.ts` contains no refund pin to inherit. **The break is
one `git revert` of a constant.**
🔴 **R7-1: the radio prices were DROPPED by revision 6** after being a blocker in
three rounds. 🔴 **R7-3: red-first rows 1 and 2 were GREEN** — the rows that
validate the coupling design were the one break it could not see.
**R7-0 is the absorption pass** the audit prescribed: enumerate the replaced
section's rows against the new one and state which verdicts changed.
Previously: 📝 **REVISION 6 WRITTEN 2026-09-05 — a REWRITE of the copy
and gate sections, not another delta layer.** ⛔ **NOT APPROVED — plan review
owed on revision 6.** ✅ Nothing is blocked on the user (decisions 27–29).
🔴 **R6-1 is the finding that changes the gate, and it was MEASURED in a sandbox,
not argued: `verify-cutover.ts` stays FULLY GREEN while `$399`/`$699`/`$1,199`
vanish from `dist/`, because every terms pin in it matches an identifier NAME,
never a value.** Three other scripts crash — one exiting 1 with **zero `✗` and no
summary line**, CLAUDE.md's BK-33 trap live in this repo.
**READ `# 🟪 REVISION 6` FIRST.** It replaces R5-4 (the copy) and R5-5 (the gate)
wholesale, which is what R5-12's meta-finding asked for.
Previously: ⛔ **PLAN-REVIEWED 2026-09-05 — NOT APPROVED. Revision 6 owed.**
**8 blockers / 13 should-fix / 10 nits — see `R5-12`.** 🔴 **Its meta-finding is
the one that matters: R5-10 and R5-11's resolutions landed in the W-table and the
red-first table but NOT in R5-4 (the copy) or R5-5 (the gate)** — the two
sections an implementer builds from. *Resolved only where it was raised, for the
sixth time, committed by the fold-in meant to end it.* **Revision 6 moves every
resolution into the spec body and re-runs the traceability audit against R5-4 and
R5-5 specifically, not against the W-table.**
✅ **NOTHING IS BLOCKED ON THE USER.** The cancellation-notice question was put
to them and delegated back; **decision 27** records the call and its reasoning —
**no stated notice expectation**, with the question routed to the client at the
end of September so it is not dropped a fifth time.
⚠️ **BL-2 may need a decision-26 amendment** — the coupling pin is red on arrival
against two approved constants, and one of its two fixes changes approved copy.
Previously: **REVISION 5 WRITTEN 2026-09-05 — RESCOPED TO THE WEB HALF —
AND VALIDATED THE SAME DAY.** ✅ traceability audit (**R5-10**) and ✅ adversarial
pre-read (**R5-11**) both run as fresh agents; both sets folded in **and applied
in place**. 🔴 **NOT APPROVED. Owed: PLAN REVIEW**, one fresh agent briefed per
R5-8. 🙋 **One item is blocked on the user** — with the refund block deleted,
does the client want a stated cancellation-notice expectation? (R5-10, last row.)
⚠️ **The validators found that revision 5 itself dropped six revision-4 rows and
got one gate row factually wrong** — both corrected above. **Fifth consecutive
revision to drop rows; first to be caught the day it was written.**
**READ `# 🟦 REVISION 5` FIRST. It is the spec**; revisions 2–4 are superseded
as scope and survive only so a finding can be recovered by its ID.
Revision 5 folds in R4-2, R4-4, R4-5 and R4-6, drops R4-1 and R4-3 to **BK-55**,
and **corrects nine line/path citations that were wrong** (R5-0) — including one
supporting quotation, restated across three revisions, that **does not exist in
the file** (C-e).
🔴 **Two NEW blockers, in no review:** **R5-1** — decision 24's split has a seam
running through the terms box, because `booking-copy.ts`'s constants render into
every customer email as well as the web; and **R5-2** — no gate script emits a
count of assertions RUN, so the ticket's own pre-registration rule is
unfalsifiable. Both have resolutions in the revision.
⚠️ **The split is NOT copy-ticket / gate-ticket** — that would break the
one-commit rule. See decision 24. Copy **APPROVED** (decision 26); **no number
appears on any web surface** (decision 25).
Previously: ✂️ SPLIT 2026-09-05 (decision 24) — the email surfaces, the resend
defect, the charged-path terms and the adversarial pass moved to **BK-55**.
Revision 3 was validated by two more fresh agents (traceability + adversarial),
which converged on **three blockers** and found revision 3 had **silently
dropped 21 items of its own**. **Tier: Reviewed** — see R5-8 for why, and for
the condition under which it goes back to Reviewed + adversarial.
Previously: revision 3 written, awaiting validation. Answers all **28 blockers** from four fresh reviewers on three lanes,
and re-derives every enumerated case against **decisions 17–23**.
Previously: revised, re-reviewed — NOT APPROVED.
Revision 2 went to two more fresh agents (copy lane, pins lane): **12 further
blockers.** ⚠ **Decision 19's claim to resolve R2-B2 is WITHDRAWN — verified
false in code.**
Previously: **plan-reviewed — NEEDS REVISION, NOT APPROVED.**
Round 1 (code completeness / pins): **11 blockers / 8 should-fix / 4 nits.**
Round 2 (customer experience, irreversibility, policy): **5 blockers /
5 should-fix / 5 nits, plus 7 questions that must go to the user.**
See "PLAN REVIEW ROUND 1" below. Opened 2026-09-02.
**No longer gated by `MEASURING-THE-FREE-CHANGEOVER.md`** — moves 2 and 3 were
withdrawn by the user 2026-09-05 and move 1 was already discharged.

**Phase:** free-booking changeover · **Tier:** **Reviewed** — plan review +
implementation review. *(C8 discharged 2026-09-05: the header's original "plain
Reviewed, not adversarial, copy-only" line was struck in round 1 and then
survived as the first thing a cold reader met; round 1's correction to "Reviewed
+ FULL adversarial pass" was right for the UNSPLIT ticket. Decision 24 then moved
the money, the write path and the adversarial pass to **BK-55**.)*
🔴 **The adversarial pass has NOT gone away — it moved.** It runs **once, at the
TIP of `bk51-gated`, over the union of BK-51 + BK-55 + BK-53** (R5-1, seam rule
4), because this ticket edits `booking-copy.ts` constants that render into every
customer email. **If seam rule 4 is not adopted, this ticket returns to
Reviewed + adversarial.**
**Target branch:** `bk51-gated` — **never pushed to any remote.**
**Blocks:** **the push of BK-51**, jointly with BK-55. This is GATE 1.

## Reading order — **and read the `Status:` line above before any of it**

| Read | Section |
| --- | --- |
| **1. `# 🟥 REVISION 7`** | **THE SPEC.** R7-2 first (the gate has no refund or credit ban), then R7-1 (the radio prices, dropped by R6) and R7-3 (the coupling pin, now per-SENTENCE). **R7-0 is the absorption pass.** It amends R6-2 and R6-3 **in place** |
| **2. `# 🟪 REVISION 6`** | current for **R6-0** (baseline + citations) and **R6-1** (the gate cannot see its own copy — measured). **R6-2 and R6-3 are current ONLY as amended by R7** |
| **3. `# 🟦 REVISION 5`** | current for **R5-0** (nine corrected citations), **R5-1** (the seam, as amended by R7-6b), **R5-2** (the assertion count), **R5-3** (scope). 🔴 **R5-4 and R5-5 are DEAD — do not build from them** |
| **4. THE DECISIONS — all of them, in full. Load-bearing** | `# ✅ DECISIONS 17–19` · `# ✅ DECISIONS 20–22` · `# ✅ DECISIONS 24–26` · `# ✅ DECISIONS 27–29`. 🔴 **DECISIONS 20–23 SIT PHYSICALLY BELOW REVISION 3's HEADER — inside the range row 6 calls "superseded". They are CURRENT and row 6 does NOT apply to them.** **Decision 20 — *nothing is handed to the customer in writing at the visit* — is stated once and is what falsifies three live pages** |
| 5. R5-10 / R5-11 / R5-12 | the validation record: traceability audit · adversarial pre-read · plan review |
| 6. `# 🟩 REVISION 4` and everything below revision 3's header | **superseded history — EXCEPT the decisions in row 4.** Read only to recover a finding by its ID |

⚠️ **15 findings from rounds 1–3B exist only as digits in review headers and are
unrecoverable from this document.** If a reviewer finds something "new", assume
it may be one of those.

## Why this exists

BK-51 makes the default outcome of every approval a **free confirmed booking**.
The site and every confirmation email still price and refund an assessment
nobody is charged for. **Until this ships, BK-51 cannot go live.**

| Surface | What it still says |
| --- | --- |
| `booking-copy.ts:401-405` `FEE_TERMS_ITEMS` → `ContactSection.astro:349`, `BookingForm.svelte:1115`, **and every confirmation email** (`booking-email.ts:525`) | `$399 + GST` / `$699 + GST` / `$1,199 + GST` |
| `booking-copy.ts:462` `FEE_TERMS_PAYMENT[2]` → `book.astro:143`, `ContactSection.astro:351`, `BookingForm.svelte:1119` | *"we email you a secure payment link, stating the amount and when we need it paid"* |
| `booking-copy.ts:504-510` `FEE_TERMS_REFUND` | four refund promises, in the same email as `NO_CHARGE_LINE` — *"There is no charge for this visit"* |
| `booking-copy.ts:541` `FEE_TERMS_ACK_LABEL` | asks the customer to tick *"including the amount I will be asked to pay"* |
| `/llms.txt:54` | the credited-in-full sentence — a price claim in words, no dollar sign |
| `ContactSection.astro:287` | *"Credited in full when you go ahead"* |

⚠️ **THIS TABLE IS INCOMPLETE — third instance of the copy-inventory trap.** It
names `FEE_TERMS_PAYMENT[2]` and misses at least two bigger payment-link claims:

| Missed | Renders on |
| --- | --- |
| **`booking-copy.ts:367-368` `FEE_TERMS_INTRO`** — *"we email you a secure payment link — and your appointment is confirmed as soon as the payment goes through"* | **five** surfaces: `ContactSection.astro:347`, `BookingForm.svelte:1113`, `book.astro:11`, and **both request-email arms** |
| **`booking-copy.ts:639-640` `RECEIVED_NEXT_STEPS[1]` and `[2]`** — *"we email you to approve it, with a secure payment link"* / *"Your appointment is confirmed once that payment goes through"* | `/book/received/` via `BookingConfirmation.svelte:149` **and both request-email arms** — **the first thing every web customer reads** |

Also `booking-copy.ts:613` `QUOTE_TIMING_NOTE`.

---

# ✅ DECISIONS 17–19 — user, 2026-09-05. These answer both reviews' policy blockers.

## Decision 17 — WHAT IS FREE. *(answers R2 Gap 1, the sharpest lane-D finding)*

**User, verbatim:** *"visit is free except for travel fee if client decides when
confirming the booking, report, actual work etc is quoted at the end of the
visit or after the visit. so the idea is free assessment."*

**Therefore:**

| | |
| --- | --- |
| **Free** | the on-site assessment visit itself |
| **May be charged** | a travel fee, **decided by the client at confirmation**, handled on the call and **outside the system** (decision 8) |
| **Quoted separately** | the report, the estimate, the sketch, lab work, and the restoration work — at the end of the visit or after it |

⚠️ **CONSEQUENCE THE PICKER MUST REFLECT.** Decision 15 keeps the assessment-type
picker, and `ASSESSMENT_TIER_DESCRIPTIONS` (`booking-copy.ts:580-587`) describes
a cause-of-loss report, a repair estimate, a measured sketch and lab samples.
**Under decision 17 those deliverables are NOT free.** The picker is now *"what
do you think you need"* — an indication that helps the office prepare — **not a
menu of things included with the free visit.** Copy that leaves the old reading
intact is the fee-terms trap in a new place: the prices come off and the
implicature that the deliverables are included survives.

## 🆕 Decision 17a — W22 FOLDS INTO THIS TICKET. *(found while recording 17; in no review)*

**"Free except a travel fee" means the assessment is NOT unconditionally free.**
The site would say *free* while a real charge can follow. `CONVERSION-2026-08-31.md`
§7 W22 already owns this and has been **untickteted since it was written**:

> **W22 · Disclose that a travel fee may apply beyond 30 km, before payment.**
> `#38` proves it is real (57750 → 65888 = CA$50 + GST); the terms copy's own
> comment concedes the form price is *"false for a job beyond 30 km."*
> **Disclosing that a fee may apply needs no client decision; naming the amount
> does.** **Reviewed.**

**W22 is now owned by BK-53** — it is the same sentence, on the same surface, in
the same deploy. Disclosing *existence* needs no further client input per W22's
own text; **naming the amount still does and is NOT assumed here.**

## Decision 18 — THE TERMS BOX SURVIVES, REWRITTEN, CHECKBOX STILL REQUIRED. *(answers R2-B3)*

**User delegated the call; recorded with reasoning so it can be overturned on
its merits.** The box keeps `FEE_TERMS_HEADING`, the `<ul>`, and the required
acknowledgment; **its contents are replaced, not emptied.** Under decision 17
there are three true, non-price terms, and they are exactly the three things a
customer could otherwise dispute:

1. the assessment visit is free;
2. **a travel fee may apply further out, and we tell you when we confirm** (17a);
3. the report and the restoration work are quoted separately.

**Why this over deleting the box:**
- It closes R2's **Gap 3** — with `NO_CHARGE_LINE` as the only surviving money
  sentence, *"There is no charge for this visit"* reads as *"this is free"* full
  stop. These three lines are what stop that reading.
- **The office can still charge by hand** (decision 19), so a consent record
  retains value; deleting the box leaves a charged customer having agreed to
  nothing.
- **Keeping the checkbox required is the NO-CHANGE option for the funnel.**
  Removing a required field would itself be a funnel change, in the same window
  as the pricing flip — against the one-funnel-change-per-week rule.
- `terms_acked_at` keeps meaning rather than becoming decorative.

⚠️ **This does NOT resolve R2-B2.** Post-flip acks attest to different text
under an identical schema, and only `created_at` and git history separate them.
**The terms snapshot is still required** — see decision 19's note and R2-B2.

## Decision 19 — THE CHARGED PATH GETS ITS TERMS IN THE APPROVAL EMAIL. *(answers R2 Gap 2 / R1-D4)*

**Manual charging is KEPT.** BK-51 deliberately preserved it; the travel fee
makes charging an **expected** path rather than a rare one; and decision 16
says do not remove capability from a client still experimenting.
**Accepting the gap was rejected outright** — with ~20 Interac payments across
two months, a live money path with no written terms is a real exposure.

**The fix:** `APPROVED_*` (`booking-email.ts:738-771`) carries the refund and
deadline terms **itself**. That puts the disclosure at the moment money is first
mentioned, and keeps price copy off the public pages this ticket is stripping.
It also repairs `APPROVED_DEADLINE_NOTE` disclosing the slot-release
consequence for the first time in the email asking for money.

~~**Consequence for R2-B2, and it resolves the polarity question:** the terms a
charged customer receives now live on the approval email … which is resolution
(a), the snapshot, arriving by a different door. A gate over today's constants
is no longer being asked to restore anything.~~

🔴 **WITHDRAWN 2026-09-05 — THIS PARAGRAPH WAS WRONG, AND R2-B2 IS STILL OPEN.**
Verified in code: `resend.ts:94` refuses anything but `status === 'confirmed'`,
then `:106` calls `planForAppointment(...)`, which builds the **confirmed**
message. **`APPROVED_*` is never resent by that route.** R2-B2 is about the
**Resend button**, so putting terms into the approval email does not touch it.

**And the ticket as written would make that defect STRICTLY WORSE.** After
BK-53, the office clicks Resend on a charged `confirmed` row and that customer
receives today's terms (*"the visit is free … a travel fee may apply … quoted
separately"*) beside their real charge, **with no refund policy at all.** The
pre-ticket defect at least shipped a refund policy of the wrong era.

🔴 **THE WORKED EXAMPLE THIS PARAGRAPH USED IS FALSE, AND IT WAS INHERITED BY
FOUR DOCUMENTS.** Every version said *"the office clicks Resend on `#36` —
`confirmed`, `cs_live_`, **$628.43**."* **`ROADMAP.md:387` and `:418`, both
checked 2026-08-26, record #36 as `cancelled`** — and `resend.ts:94` refuses
anything but `confirmed`. **The Resend button is not offered on #36 at all.**
⚠️ **The defect is real; the example is not.** Any genuinely `confirmed` row
charged under prepay reaches the same code. **A spec that argues from a false
example invites a reviewer to refute the example and close the finding.**
See `BK-55.md` §A1.

**Decision 19 remains correct and stays — as a fix for Gap 2, the approval
path.** It simply does not close R2-B2. **R2-B2 still requires an explicit
choice between (a) snapshot the terms at insert and render `resend.ts` from the
snapshot, or (b) suppress the terms block for pre-flip rows and state that their
terms record is git-history-only.** This is the one item the ticket scopes its
adversarial pass to, so it is the one spec that must not be ambiguous.

**Still open and NOT assumed:** the exact wording of all of the above, and the
travel-fee **amount** (W22: existence needs no decision, the number does).

---

# ✅ DECISIONS 24–26 — user, 2026-09-05. **DECISION 24 SPLITS THIS TICKET.**

## Decision 24 — SPLIT BK-53. And the obvious split axis is WRONG.

**User: *"ok, lets go with the split."***

⚠️ **DO NOT split copy-ticket / gate-ticket.** That is the intuitive cut and it
violates the project's own rule, recorded in `CONVERSION` §7 constraint 5:
**within a claim, the copy edit and its pin are ONE commit** — *"pin first and
the build goes red; copy first and there is an unpinned window."* A gate ticket
separated from its copy is both failure modes at once.

**Split by SURFACE GROUP instead — each half carries its own copy AND its own
pins, so no claim is ever unpinned:**

| | Scope | Tier |
| --- | --- | --- |
| **BK-53** *(rescoped)* — **the WEB surfaces** | the terms box, the tier picker, `book.astro`, `ContactSection.astro`, `llms.txt`, the eyebrows, meta + JSON-LD, `[service].astro`'s qualifier — **plus the coupling pin and the `verify-cutover.ts` arms that govern exactly those** | **Reviewed** |
| 🆕 **BK-55** — **the EMAIL surfaces** | `assessmentSummary`/`tierLine` (R4-1), `FEE_TERMS_ITEMS` as it reaches email, the `resend.ts` suppression (R4-3), `CANCEL_LINE`'s surviving render, `APPROVED_*`'s terms — **plus every `verify-booking-email.ts` arm** | **Reviewed + FULL adversarial pass** |

**Why this axis and not the other:**
- **It puts the money and the write path in ONE ticket.** BK-55 owns the resend
  defect, the charged-path terms and every arm that touches a real customer's
  record — so the adversarial pass lands where blast radius actually is, instead
  of being spread thin across both halves.
- **It lets BK-53 drop to plain Reviewed**, which is what its content honestly
  is once the email half leaves.
- **`verify-booking-pricing.ts` goes to BK-53** (it zips `FEE_TERMS_ITEMS`
  against the tier table — a web-copy dependency), **with R3B-B2's correction
  intact: keep the `forEach` and the absolute `tierDefaultCents` pin, delete only
  the two `copy.includes` checks, 42 → 35.**

**Both still ship in ONE push with BK-51.** The split is about **reviewable
blast radius, not deploy separation** — decision 24 does not repeal §4.

## Decision 25 — THE $150 NEVER APPEARS ON THE WEBSITE

**User, verbatim:** *"only spoken via call or the confirmation email from the
owner triggered from the admin panel."*

- **Website copy:** *"a travel fee may apply"* — **and never a number.** This
  settles W22's open half: existence is disclosed, the amount is not.
- **The amount reaches the customer only** by the confirmation call, or by an
  email the owner triggers from the admin panel.

🔴 **AND THAT SECOND CHANNEL DOES NOT EXIST TODAY. Verified:** `admin_notes`
(`db.ts:108`) is **office-only** — it is written by `booking-admin-entry.ts`,
labelled *"Office notes"*, and **renders in no customer-facing message**
(`grep admin_notes src/` outside `db.ts` returns only commit/entry/admin files).
There is **no free-text field on any customer email**, and BK-51 refuses a
non-zero travel fee outright (`review.ts:246`), so the amount cannot travel as
money either.

**→ 🆕 BK-56 owed: let the owner add a line to the customer's confirmation email
from the admin panel.** Small, but it is a **customer-facing write path** and it
is the only mechanism decision 25 names that the software does not have.
**Not a gate on BK-53/BK-55** — the phone channel works today.

## Decision 26 — THE DRAFTED COPY IS APPROVED

**User: *"ok."*** Revision 3 §1 + §5a, **as amended by revision 4**, are approved
for implementation — subject to one change from decision 25: **no number appears
in any web surface**, and to revision 4's corrections (R4-4's re-derived claim
shapes, C1's *"confirmed before the visit"* → *"…when we confirm your
appointment"*).

---

# ✅ DECISIONS 27–29 — 2026-09-05. **The user delegated all three; recorded with reasoning so each can be overturned on its merits** (the decision-18 pattern).

**User, verbatim, to all three questions: *"whats your recommendation?"*** These
are therefore **recommendations adopted as decisions**, not client answers. Each
states what would change if it is reversed.

## Decision 27 — NO STATED CANCELLATION-NOTICE EXPECTATION. *(answers R5-10's last row)*

**The terms box says how to cancel and nothing about how much notice.**

**Why:**
- **The old line existed because money was at stake.** `FEE_TERMS_REFUND` carried
  a notice window because a late cancellation cost the customer a refund. Under
  decision 17 nothing is forfeited, so **the rule loses its consequence, not just
  its home.** A notice rule with no consequence is either ignored or reads as an
  implied threat the software cannot carry out.
- **It would be a NEW customer-facing rule shipped in the same deploy as the
  pricing flip** — a second funnel change in one window, which is exactly what
  decision 18 refused when it kept the required checkbox.
- **The channel already exists.** The office asks for notice on the confirmation
  call — the same channel decision 25 gives the travel fee.
- **Adding it later is one commit** into an array this ticket is already
  rewriting.

⚠️ **NOT the same as deciding the client does not want one.** → **Ask the client
at the end of September**, alongside the qualitative check-in ROADMAP index item
10 already schedules. **Recorded there so it is not lost a third time** — it was
raised in plan review round 3 and dropped by four consecutive revisions.

**If reversed:** one terms item, its claim shape added to the order pin, and a
number from the client. No structural change.

## Decision 28 — THE OFFICE'S MESSAGE LOSES THE FIGURE TOO. *(answers BK-55's S3; reverses assumption A3)*

**`booking-email.ts:1214` names the tier, with no price — the same as the
customer's. `assessmentSummary` becomes dead and is DELETED with its branch.**

🔴 **A3's stated basis was wrong and the reviewer was right to reject it.** It
cited decision 8 (*"the office decides the travel fee on the confirmation call
and needs the number"*) — but **decision 8 says the $150 stays out of the
codebase entirely**, and the figure at `:1214` is **not a travel fee**:
`booking-email.ts:219-221` records that `assessmentSummary` calls
`assessmentQuote` with **no `travelFeeCents`**. It is the **assessment** price —
the thing decision 17 makes free.

**And the deciding evidence is on the branch, not in the argument.** Verified at
`bk51-gated:[id].astro:1818-1836`: the approval screen's **`assessment_amount`
field is pre-filled `value="0.00"`**, and the hint beneath it shows the **tier
NAME only**. **BK-51 already decided that the office's own working surface does
not quote the retired price.** An internal email still saying *"$699.00 + GST
($733.95 total)"* contradicts the screen the office actually acts on — and it is
the one message that would have survived the flip untouched.

**Why this over keeping it:**
- **The office is not deprived.** The screen they approve on carries the tier and
  an amount field; they type a figure when they mean to charge one.
- **G2 would have been actively harmful.** Pinning *"the office keeps the
  figure"* pins a **retired price list** into place and turns the suite red on
  its removal — a pin defending a defect.
- **It simplifies the fix.** Both callers go to `tierNameOf`; `assessmentSummary`
  and its dead comment at `:450-459` go with them.

⚠️ **This partially vindicates R4-1's instinct and does not vindicate its edit.**
R4-1 was right that the figure should go from the customer's message; it was
wrong that the way to do that is `return name;` inside `assessmentSummary`, which
would have duplicated `tierNameOf` and changed `:1214` **silently**. **The
destination was right, the mechanism was not, and the scope question BK-55 §1
answered with A3 was answered wrongly.** All three statements are now on the
record.

**If reversed** (the office keeps it): `:1214` stays as-is, G2 inverts to *"the
office arm still carries a figure"*, red-first row 3 inverts with it, and
`assessmentSummary` survives — but then **`[id].astro`'s `0.00` default and the
email disagree**, and that disagreement needs its own answer.

## Decision 29 — THE RESEND PATH IS THREE-STATE, AND THE ROWS THAT ACCEPTED NOTHING GET THEIR OWN SENTENCE. *(answers BK-55's S8 and B4)*

**Three branches, three sentences, all three pinned:**

| Row | What it gets |
| --- | --- |
| **Was actually charged** (paid-era terms) | the pointer: *"The terms you accepted are in your original confirmation email."* |
| **Free-era, terms accepted** | today's terms block, rendered normally |
| **Accepted no terms** — `terms_acked_at` NULL, admin-created by design | its own line: *"This booking was taken over the phone. Our current terms are on the website."* |

**Why this over the alternatives:**
- **It is the only option where nothing false reaches anyone.** The pointer
  sentence is false for a customer who accepted nothing, and false again for a
  pre-BK-27 row whose original email had no terms block at all.
- ***"Say nothing"* contradicts `booking-email.ts:501-511`**, which treats the
  block as structurally always present, and ships a message with a hole in it.
- ***"Route to office"* removes a capability**, on the one button that exists to
  recover an email a customer never got — and **decision 16 says do not remove
  capability from a client still experimenting.**
- 🔴 **The branch has to exist anyway.** B4 established that `terms_acked_at` is
  nullable by design (`verify-booking-commit.ts:806`) and that `null < cutoff`
  coerces to `true`, routing every phone booking into the wrong arm. **Once the
  three-state gate is mandatory, giving the third state a sentence is nearly
  free.**

⚠️ ~~Adopt B5's fix and key the first branch on `total_amount_cents > 0`.~~
🔴 **AMENDED 2026-09-05, SAME DAY — that discriminator misclassifies a
population.** Re-derived against the enumerated cases: a **post-flip
hand-charged** booking has `total_amount_cents > 0` and accepted the **current**
terms, so money is a proxy that was exact only while every booking was charged —
i.e. only before the flip this ticket ships. *(CLAUDE.md's rule-rewrite trap,
caught rather than repeated: the predicate changed, so every case under it was
re-derived.)*
**→ The discriminator is `terms_acked_at` against a named `TERMS_ERA_CUTOFF`
constant, with NULL as its own branch tested FIRST** — in JS `null < someDate`
coerces to `0 < someDate` → **true**, which routes every phone booking into the
paid-era arm. B5's objection is still answered: a **named constant set at
implementation and pinned**, never a clock read at send time. **The three states
are unchanged. See `BK-55.md` §C-1 for the full derivation.**

**If reversed:** the third sentence is dropped, but **the third BRANCH stays** —
B4 is a defect regardless of what the branch renders.

---

# 🟥 REVISION 7 — 2026-09-06. **THE SPEC. R6-2 AND R6-3 ARE AMENDED IN PLACE BELOW.**

Written against the traceability audit (**R7-A**) and adversarial pre-read
(**R7-B**) of revision 6. **5 adversarial blockers + 8 audit blockers**, nine of
them **executed** rather than argued.

🔴 **The audit's diagnosis, adopted as a standing rule:**

> **When a section is replaced wholesale, enumerate the OLD section's rows
> against the new one and state which verdicts changed.**

R6 did that for R5-12's *findings* and not for R5-4/R5-5's own *content* — so six
rows fell through a table that never claimed to cover them. **R7-0 is that
enumeration**, and it is the deliverable that stops this recurring. *(It is
CLAUDE.md's rule-rewrite trap, one level up: applied to sections, not rules.)*

## R7-0 · THE ABSORPTION PASS — R5-4 / R5-5 rows against R6-2 / R6-3

| Old row | Verdict in R6 | Now |
| --- | --- | --- |
| **R5-5c** — delete `BookingForm.svelte:1171-1175`; re-point `verify-cutover.ts:594-598` to assert the amounts are ABSENT while the LABELS remain; keep `:600-603` | 🔴 **DROPPED.** In neither R6-2 nor R6-3; survived only as red-first row 4's *expectation* | **R7-1 — restored to BOTH the copy and the gate** |
| **R5-5d Class 2** — `verify-booking-email.ts:525-546` `carries` | 🔴 **DROPPED.** BK-55 §E-2 hands it to BK-53; R6-3e never took it | **R7-6a — classified** |
| R5-5d Class 3 — `verify-booking-email.ts:424` (`AFTER_HOURS_NOTE`) | 🟠 dropped | **R7-6a — Class 3, restored** |
| R5-5a A5 — the **`dist/` positive twin** | 🟠 R6-3b specifies only the SOURCE twin, while red-first row 3 predicts the dist twin's behaviour | **R7-3d — both twins specified** |
| R5-5d D1 — `:571-579` pins `QUOTE_HEADING` + `AFTER_HOURS_NOTE` **present**, both deleted | 🟠 kept as "Class 2 re-point" with no note that both subjects are gone | **R7-6a — Class 3 for both names; the loop keeps only the surviving three** |
| R5-4 — `FEE_TERMS_INTRO`'s branch-agnostic redraft (R3-BL-1) | 🟠 R6 deletes the constant; **BK-55 §B-2 still renders `INTRO(request)`** | **R7-6b — the request arm's intro is `ASSESSMENT_TERMS[0..1]`; R3-BL-1 is discharged, not dropped** |
| R5-5a A1 — coupling covers "every file in R5-4's table" **at source** | 🟠 R6-3a covers six `booking-copy.ts` constants only | **R7-3c — the template surfaces get their own arm** |
| Class reassignments with no reason (`:590-600`→`:592`, `:536-545`→`:530-549`) | 🟠 | **R7-6a — reasons stated** |

---

## R7-1 · 🔴 THE RADIO PRICES — restored to the copy AND the gate

**Dropped by revision 6 after being a blocker in three rounds (R1-A1 → R4-2 →
R5-5c). It is the most visible price surface on the site.**

**Verified:** `BookingForm.svelte:1173` renders
`{formatCents(tierQuotes[tier].baseCents)} + GST` inside its own `{#if tierQuotes}`
(`:1171-1175`) within `{#each ASSESSMENT_TIERS}` (`:1154-1182`) — **not** inside
the `{#if quote}` block at `:1198-1217`. And `verify-cutover.ts:597`
**positively requires** the string `island.includes('formatCents(tierQuotes[tier].baseCents)')`,
as the third clause of a triple-AND at `:594-598`.

**→ COPY: delete `BookingForm.svelte:1171-1175`.** The tier LABEL stays.
**→ GATE: re-point `:594-598`** to assert `formatCents(tierQuotes` is **ABSENT**
from the island while `ASSESSMENT_TIER_NAMES[tier]` is still present — a negative
**with** its positive twin.
**→ KEEP `:600-603`** (*"the island contains no price arithmetic of its own"*) —
**more** valuable once no price renders (R3B-D3).
⚠️ **`tierQuotes` then has no reader in the template.** `noUnusedLocals` is not
set, so pin its absence explicitly.

## R7-2 · 🔴 THE GATE HAS NO REFUND OR CREDIT BAN — and the ticket's title is *"stop promising a refund"*

**Executed.** All four money-promise claims match **zero** of the ten
`FREE_CLAIM_SHAPES` and zero of the four `WEB_AMOUNT_SHAPES`:

| Restored claim | coupling | amount |
| --- | --- | --- |
| *"Cancel 24 hours or more before your appointment and we refund you in full."* | GREEN | GREEN |
| *"Inside 24 hours, the assessment is not refunded."* | GREEN | GREEN |
| *"the full amount comes off your final invoice"* | GREEN | GREEN |
| `llms.txt`'s *"The assessment fee is credited in full against the final invoice"* | GREEN | GREEN |

**And `verify-cutover.ts` contains no refund pin to inherit** — grepped: every
occurrence of `refund`/`credit` is a **comment** or the ordering message at
`:369`, and R6-3e deletes or re-points all three constant names that carry them.
**After BK-53 the file has nothing that can see a refund promise.**

🔴 **This is CLAUDE.md's negative-assertion trap in its exact BK-48 shape** — the
pins are *named* for free claims and every reviewer reads them as covering the
money-promise category. **And the adversary is not hypothetical: all four
sentences are the deleted `FEE_TERMS_REFUND`/`FEE_TERMS_CREDIT` text verbatim, so
the break is one `git revert` of a constant.**

**→ W29 · `MONEY_PROMISE_SHAPES`, banned outright on every web surface** (there
is no qualifier that makes them true — the assessment is free, so there is
nothing to refund or credit):

```ts
const MONEY_PROMISE_SHAPES = [
  /\brefunds?(?:ed|able)?\b/i,
  /\bcredit(?:ed|s)?\b[^.<>]{0,40}?\b(?:invoice|final bill|against|back)\b/i,
  /\bcomes? off\b[^.<>]{0,30}?\b(?:invoice|bill|total)\b/i,
  /\bmoney[\s-]?back\b/i,
  /\bdeposit\b/i,
];
```
⚠️ **Scope: the web surfaces of R7-3c, NOT `booking-copy.ts` wholesale** — the
twelve email lifecycle constants (`REFUND_TIMING_LINE`, `REFUNDED_LEAD`,
`refundedAmountLine()`, …) legitimately say *refund* about **real refunds of real
charges** and are KEEP-UNCHANGED (BK-55 §B-5). **Per-constant, per
`verify-booking-email.ts:368-372`'s existing pattern (R3B-C2).**
**→ Red-first row 15: restore `FEE_TERMS_REFUND[1]` → `MONEY_PROMISE` red.**

## R7-3 · 🔴 THE COUPLING PIN — per SENTENCE, because decision 17a says so

### 3a · Red-first rows 1 and 2 are GREEN. **Executed, twice, independently.**

Deleting `ASSESSMENT_TERMS[1]` and changing nothing else leaves the pin **green**,
because `FEE_TERMS_ACK_LABEL` still says *"a travel fee may apply"* and R6-3a
joins it into `TERMS_TEXT`. **The row that validates the whole coupling design is
the one break it cannot see.**

🔴 **And a blob is the wrong unit on the merits, not just for the break row.**
**Decision 17a requires the qualifier IN THE SAME SENTENCE as the free claim.**
Executed: moving the travel clause out of `ASSESSMENT_TERMS` and into
`ASSESSMENT_TIER_DESCRIPTIONS.sketch` also stays **green** — the customer reads
*"The on-site assessment visit is free."* at the top of the box while the
qualifier sits inside one of three radio descriptions.

**→ The unit is the SENTENCE.** Split each constant on `/(?<=[.!?])\s+/`, and for
every sentence matching a `FREE_CLAIM_SHAPE`, require `TRAVEL_QUALIFIER` **in
that sentence or the one immediately following it within the same constant.**
✅ Assert at least one sentence matches a free shape — **a positive twin**, so the
pin cannot pass by matching nothing (R7-B nit 5: *"assert both sides non-empty"*
was ambiguous and vacuous on its natural reading).

### 3b · Two drafted sentences must change — decision 17a requires it

Under a per-sentence rule these are non-compliant **as approved**, which is a
decision-17a correction rather than a taste change:

- **`ASSESSMENT_TIER_NOTE`** → *"The visit is free whichever you pick — a travel
  fee may apply further out. Picking one just tells us what to prepare; anything
  written comes later, with your quote."*
- **`FEE_TERMS_ITEMS[0]`** → drop the bare *"**Free.**"*. `ASSESSMENT_TERMS[0]`
  already states it **with** its qualifier two lines above, and a bare *"Free."*
  cannot carry one without absurdity. ⚠️ **This also retires
  `FREE_CLAIM_SHAPES[2]`, which R7-B SF-8 measured DEAD inside the join anyway**
  (`join(' ')` puts a letter after the space so neither `$` nor the closing-quote
  class can match). **Keep the shape for the `dist/` arm, where `</li>` supplies
  the `<`; drop it from the source arm and say why.**
- ⚠️ **Join with `'\n'`, not `' '`** — R7-B nit 4 measured the blob manufacturing
  a claim across a boundary (`"Assessment terms The on-site assessment visit is
  free"` spanning `FEE_TERMS_HEADING` + `ASSESSMENT_TERMS[0]`). Sentence-splitting
  makes this moot; the newline makes it impossible.

### 3c · 🔴 The scan misses the surfaces the ticket actually edits

R6-3a covers six `booking-copy.ts` constants. **R6-2 puts drafted copy on three
surfaces that are not constants at all:** `book.astro:36-37`/`:94`,
`ContactSection.astro:287`, `llms.txt.ts:54`, `[service].astro:79`.

**→ `WEB_CLAIM_SURFACES` — the source arm scans, per rendered template:**
`book.astro`, `src/sections/ContactSection.astro`, `BookingForm.svelte`,
`llms.txt.ts`, `[service].astro` — through `stripForUse` + `normalise` +
`templateOf` where applicable.
⚠️ **R7-B SF-2:** red-first row 8 (*"drop the travel clause from
`[service].astro:79`"*) was attributed to the coupling pin, **which structurally
cannot see a template**. With this arm it can. **Verified: all four built service
pages carry `no obligation` today with no qualifier — they are RED ON ARRIVAL
until `[service].astro:79` lands.** That is the correct order and must be stated.

### 3d · Both twins, not one

R6-3b specified only the **source** twin while red-first row 3 predicted the
**`dist/`** twin's behaviour (*"the dist twin alone stays green"*).
**→ Specify both:** `dist/client/index.html` and `dist/client/book/index.html`
each carry a free claim **and** a qualifier (R5-5a A5, restored), **and** the
source twin of R6-3b. **Neither alone is sufficient** — R7-B measured that the
`dist/` twin passes when the eyebrow is deleted, because the terms box on the same
built page carries the claim.

## R7-4 · 🔴 `WEB_AMOUNT_BAN` — scope and shapes, both too narrow

**Enumerated against the real `dist/`:** `bookingSurfaces`
(`verify-cutover.ts:1128-1135`) is `dist/client/book/**.html` +
`dist/client/index.html` + the terms `.js` chunks. **Out of scope and edited by
this ticket:** `dist/client/llms.txt` *(never in `bundles` — it is neither `.html`
nor `.js`)*, the **four** `[service]` pages, and `contact/index.html`
*(`ContactSection.astro:287` renders on `/contact/` too — `contact.astro:82`)*.

🔴 **`llms.txt` is the sharpest of the three**, and `verify-cutover.ts:1188-1191`'s
own comment says why: it is *"the file an AI assistant quotes with no page around
it to qualify the claim."*

**→ Scope = `bookingSurfaces` ∪ `dist/client/llms.txt` ∪ the four
`dist/client/<service>/index.html` ∪ `dist/client/contact/index.html`.**
**Enumerate what the scope EXCLUDES, in the pin's comment** — `insurance-claims`
(correct `$500`–`$2,500`), `about`, `blog/**`, `404` — so the next reader sees the
boundary rather than re-deriving it.

**And the shapes miss ordinary spellings (R7-B SF-4, executed):**
`"a 150 dollar travel fee"` (singular), `"a travel fee of CAD 150"` (number
second), `"Normally 399, free now"` (no `$`, no `+ GST`) all pass.
**→** add `/\b\d[\d,]*\s*dollars?\b/i`, `/\bCAD\s*\d/i`, and — inside
`WEB_CLAIM_SURFACES` only — a bare `/\b\d{3,}\b/` with the four-digit year and the
phone number excluded by name.
⚠️ *"Shaped for the three prices being removed rather than for the category"* is
the A14 criticism R6-3e levels at `!figures.includes('399')`, **reproduced one
section later in `WEB_AMOUNT_SHAPES` itself.**

## R7-5 · The weekend claim loses its ONLY web pin and gains nothing

**Executed: restoring *"Saturday and Sunday appointments are charged at 1.5 times
the weekday price."* to `ASSESSMENT_TERMS` is GREEN on both the coupling pin and
the amount ban.** R6-3e **deletes** `verify-cutover.ts:605-619` (the AC11 pair)
with `AFTER_HOURS_NOTE`, and BK-55's G3 is scoped to the **email**. **Net: BK-53
removes the only web-side weekend assertion and adds none** — while R6-2's own
note about `1.5×` (U+00D7) vs `1.5x` (ASCII) reads as though a web ban exists to
be evaded.
**→ W30: G3's claim-shape × multiplier-shape cross-product runs on
`WEB_CLAIM_SURFACES` too.** ⚠️ **Both spellings**, verified live:
`booking-copy.ts:597` uses `1.5×` (U+00D7), `booking-email.ts:1167` uses `1.5x`.

## R7-6 · The absorbed class assignments, and the rows that lived only in a table

### 6a · Classes — completing R6-3e

- 🔴 **`verify-booking-email.ts:525-546` `carries` — Class 2, BK-53's.** Its
  arrays are exactly the constants this ticket deletes; **BK-55 §E-2 hands it
  here and R6-3e never took it.** Four assertions redden with no anticipated
  disposition — **BK-44's trap, where an implementer meeting an unpredicted red
  reaches for the assertion instead of the fixture.**
- **`verify-booking-email.ts:424`** (`statesMultiplier(AFTER_HOURS_NOTE)`) —
  **Class 3**, restored; the constant is deleted.
- **`verify-cutover.ts:571-579`** — 🔴 **Class 3 for `QUOTE_HEADING` and
  `AFTER_HOURS_NOTE`** (both deleted), **Class 2 for the surviving three**. R6
  kept the whole loop as "re-point" without noting two subjects are gone (D1).
- **`:590-600` → `:592` Class 1** *(reason: the triple-AND at `:594-598` becomes a
  negative-plus-twin — R7-1)*; **`:536-545` → `:530-549` Class 2** *(reason: the
  presence loop's membership changes with `TERMS_CONSTANTS`; the order claim
  survives)*.
- 🔴 **`verify-booking-email.ts:742-745` is BK-53's, Class 1 — ONE owner.**
  R6-3e claimed it Class 1 and BK-55 §D claimed it Class 2. **"Owned by both"
  fails exactly the way "owned by nobody" did at `:554-563`** (R5-11 A5), which is
  what seam clause 5 exists to prevent. **Clause 5 decides it: it reads
  `booking-copy.ts` symbols and asserts their composition → BK-53.**

### 6b · Rows that were table-only, now in the body

- **W10's `[id].astro:825-828` / `:1835` deletions are UNPINNED.**
  `verify-cutover.ts` does not scan `src/pages/admin/**` and admin pages never
  reach `dist/client` (`prerender = false`). **Recorded in the copy row as
  *verified by eye, not by gate*** — an unpinned deletion recorded as such is
  honest; one that looks covered is not. **Not doing it ships green.**
- 🔴 **`FEE_TERMS_INTRO`'s replacement — R7-0 row 6, WHICH R7-0 ITSELF DROPPED.**
  *Recorded plainly: the absorption pass written to stop rows vanishing lost one
  of its own eight rows, one level down. R7-0 routed R3-BL-1 here and R7-6b did
  not carry it. That is the sixth instance of this failure and the first inside
  the fix for it.*
  **The substance, and it is R5-12's BL-1 still open:** `booking-email.ts:524`
  (HTML) and `:605` (text) render `FEE_TERMS_INTRO` on the **request** arm only.
  BK-53 deletes that export, so those two lines need replacement text or BK-53's
  commit does not typecheck. **→ The request arm renders `ASSESSMENT_TERMS[0]`
  and `[1]` — the free claim and its qualifier, which is what R3-BL-1's
  "branch-agnostic" requirement asked for.** The confirmed arm renders neither,
  as today. **R3-BL-1 is DISCHARGED, not dropped.**
- **`verify-cutover.ts:466-468`'s scoping comment is STALE (R5-11 A25).** It
  justifies the two-file `no obligation` ban with *"~50 other unqualified 'free
  assessment' claims are a recorded Known trap"* — **those claims are gone**
  (`grep -c` returns 0 on both built booking pages). R7-3c re-scopes that very
  ban; **rewrite the comment in the same commit**, or the next editor widens or
  narrows it from a false premise. *A25 was the 11th item in a list its own header
  called "ten nits", which is how it escaped four passes.*
- **`verify-booking-admin.ts:1161-1167` — KEEP, Class 0** (T7). It pins the
  `afterHours` **flag**, a fact about the slot, not the surcharge copy.
- **`ASSESSMENT_TIER_NOTE`'s position pin is written FRESH** (T8) — the AC11 pair
  is deleted with `AFTER_HOURS_NOTE` and **must not be re-pointed**; its reason
  (*"a weekend price must be EXPLAINED where it is shown"*) does not transfer to a
  note that explains no number.
- **`FEE_TERMS_REFUND` / `FEE_TERMS_CREDIT` web render sites** (T4) —
  `book.astro:146-148`/`:149`, `ContactSection.astro:354-356`/`:357`,
  `BookingForm.svelte:1122-1124`/`:1125`. **In the copy section now, not only in
  W4/W5/W6.**
- 🔴 **`#36` is struck from R5-7's drain probe.** BK-55 §A1 declared it struck
  *"here and in `BK-53.md`"* and R5-7 still carried it. **#36 is `cancelled`, so
  it fails R5-7's own `status = 'confirmed'` arm** — a drain probe argued from it
  is a reviewer's free refutation of a real hazard.
- **R5-1 rule 3 and R5-7 still carry the VOID ordering rationale** and still order
  a `slice(0,2)` → named-constants conversion that BK-55 §B-2 replaced with
  outright deletion. **BL-1 said "wrong in all three places"; one was fixed.**
  → **Both struck; the order stands for BK-55 §E-1's reason.**
- **The withdrawn exempt list is still cited as load-bearing** by BK-55 §B-5 and
  by R5-3's W24. → Both corrected: `NO_CHARGE_LINE` is out of scope because it is
  **not in `TERMS_TEXT`**, not because it is exempted.
- **R6-2a should name BK-52.** It says *"a separate ticket if the client ever
  wants it"* — **BK-52 exists**, drafted 2026-09-02, and is decision 15's home.
- **`RECEIVED_NEXT_STEPS`'s "NO" verdict keeps its answer and LOSES its reason.**
  R6-2 said *"the terms box renders on that page too."* **False, verified:**
  `book/received.astro:37` renders `BookingConfirmation.svelte`, which imports
  `RECEIVED_NEXT_STEPS` and **no `FEE_TERMS_*` symbol at all**. The verdict stands
  on a different footing — *the page is a receipt, not an offer* — and the false
  reason is struck, because R6-2 recorded it *"so it is not re-raised"*, which
  means the false reason is what the next reader inherits.

## R7-7 · RED-FIRST — corrected

| # | Break | Expect | Status |
| --- | --- | --- | --- |
| 1 | Delete `ASSESSMENT_TERMS[1]` **only** | coupling red | ⇄ **was GREEN; fixed by the per-sentence rule (R7-3a)** |
| 2 | *"no obligations"* **and** delete the travel clause from that sentence | coupling red; then restore the clause → green | ⇄ **corrected** |
| 7 | Restore *"credited in full"* to `llms.txt.ts:54` | **`MONEY_PROMISE` red** | ⇄ **was unachievable — the claim matches zero free shapes and is the LIVE state today (R7-B SF-1)** |
| 8 | Drop the travel clause from `[service].astro:79` | **`WEB_CLAIM_SURFACES` arm red** | ⇄ **was attributed to a pin that cannot see a template** |
| 15 🆕 | Restore `FEE_TERMS_REFUND[1]` to any web surface | `MONEY_PROMISE` red | ☐ |
| 16 🆕 | Move the travel clause into `ASSESSMENT_TIER_DESCRIPTIONS.sketch` | coupling red **(per-sentence)** — **executed GREEN under R6's blob** | ☐ |
| 17 🆕 | Add `"a 150 dollar travel fee"` | `WEB_AMOUNT_BAN` red — **executed GREEN under R6's shapes** | ☐ |
| 18 🆕 | Restore the weekend sentence to `ASSESSMENT_TERMS` | web weekend ban red — **executed GREEN under R6** | ☐ |
| 19 🆕 | Delete the `WEB_AMOUNT_BAN` block **and** append a name to `TERMS_CONSTANTS` | the count must still catch it — **R7-B SF-9 shows the count is restored and row 12's `check(true,…)` tell is absent** | ☐ |

⚠️ **R7-B SF-9 also shows the count is BUILD-DEPENDENT** — `verify-cutover.ts`
loops per dist chunk, so a Vite chunking change or one new blog post moves it, and
it gets *"corrected"* rather than investigated. **→ Pre-register a RANGE with a
stated cause for each bound, and re-register after any dependency bump.**

## R7-9 · Four bookkeeping defects the audit found, fixed here

- 🔴 **`verify-booking-ics.ts` has no counter and no owner.** R5-2a assigns the
  counter to `verify-cutover.ts` + `verify-booking-pricing.ts`; BK-55 G0 takes
  `verify-booking-email.ts`. **`verify-booking-ics.ts` gets neither — and R6-1
  MEASURED it exiting 1 with zero `✗` and no summary line**, the BK-33 trap live.
  This ticket edits it (R6-3e Class 1, `:751`), and **red-first row 6 orders
  scoring on "(exit code, summary line)" for a run that produces no summary
  line.** → **It is BK-53's, and it gets the same three-line counter.**
- 🔴 **W-NUMBER COLLISION. `W20`, `W21`, `W22`, `W25` mean two different things.**
  Decisions 17a and 23 use `CONVERSION` §7's numbering (W20 = strike *"no
  obligation"* from `[service].astro`, W21 = the Ads assets, W22 = travel-fee
  disclosure, W25 = the Interac receipt); R5-3's scope table reuses all four for
  `PAYMENT_CANCELLED_LEAD`, the ROADMAP defects, `verify-booking-admin.ts` and
  `WEB_AMOUNT_BAN`. **→ This ticket's own items are `W53-1…W53-30` from here;
  a bare `W2x` means CONVERSION's.** ⚠️ **And decision 23's live sentence
  *"W21 is NOT struck — it changes shape"* is about CONVERSION's W21 — the ad
  copy still saying *"free, no-obligation QUOTE"*, which decision 20 falsifies.
  It reads, against the scope table, as being about the ROADMAP defects.**
- **`verify-booking-email.ts:374` has no class.** R5-5d had it Class 3; R6-3e's
  Class 1 list has `:373`, the same assertion under a different citation. → **`:373`
  is the call site; `:374` is its message argument. One arm, Class 1.**
  ⚠️ **It is also the THIRD of the "three arms moved Class 1 → Class 3 with no
  per-arm justification". Two are now named (`:339-349` by A4, this one). The
  obligation to find the third has been restated for three revisions — DISCHARGE
  IT OR STRIKE IT.**
- **BK-53 is TWO commits and never said so** — the counter first (R5-2a), then
  the copy and pins. BK-55's review (N11) forced that statement for the identical
  shape; **against the one-ticket-one-commit rule, say it: (a) counters for
  `verify-cutover.ts`, `verify-booking-pricing.ts` and `verify-booking-ics.ts`;
  (b) everything else.**

## R7-8 · Assumptions added

| # | Assumption | Basis |
| --- | --- | --- |
| **A-R7-1** *(renumbered — `A9`–`A12` collide with R5-11's adversarial findings A9–A14, which R6-3a/R6-3e/R6-4 cite by those bare IDs)* | The coupling unit is the **sentence**, not the constant, file or blob | Decision 17a says *"same sentence"*; all three coarser units were measured green on a real break |
| **A-R7-2** | `MONEY_PROMISE_SHAPES` is an outright ban on web surfaces, with **no** qualifier form | Under decision 17 there is nothing to refund or credit, so no qualified form of the claim is true |
| **A-R7-3** | `ASSESSMENT_TIER_NOTE` and `FEE_TERMS_ITEMS[0]` are amended | Decision 17a compliance — a **correction**, not a taste change to decision 26's approval |
| **A-R7-4** | The pre-registered assertion count is a **range** with stated bounds | It is build-dependent; a scalar invites correction over investigation |


---

# 🟪 REVISION 6 — 2026-09-05. **THE SPEC. R5-4 AND R5-5 ARE SUPERSEDED WHOLESALE.**

**Written against the plan review (R5-12) and its meta-finding**, which is the
reason this is a rewrite of two sections rather than another layer of deltas:

> *"R5-10 and R5-11's findings were folded into the W-table and the red-first
> table, but largely not into R5-4 (the copy) or R5-5 (the gate) — the two
> sections an implementer actually builds from."*

**So: `R6-2` REPLACES `R5-4`, and `R6-3` REPLACES `R5-5`.** Read neither of the
originals; they are kept only to recover a finding by its ID. R5-0 (citations),
R5-1 (the seam), R5-2 (the count), R5-3 (scope) stand **as amended below**.
R5-10, R5-11 and R5-12 are the validation record and are not re-litigated here.

## R6-0 · Baseline, measured — and four more citations corrected

**Gate baseline, RUN 2026-09-05 on this tree** *(identical to `bk51-gated` for
every file either ticket touches except `[id].astro`)*:

| Command | Exit | Summary line |
| --- | --- | --- |
| `npm run typecheck` | 0 | `0 errors, 4 warnings` — all four in `BrandLogo.svelte`, pre-existing, unrelated |
| `npm run verify:booking:pricing` | 0 | `✓ booking pricing checks passed` |
| `npm run verify:booking:email` | 0 | `✓ booking notification checks passed` |

🔴 **Neither summary line carries a number, and that is R5-2 demonstrated on the
artifact rather than argued from the source** — the form CLAUDE.md's BK-48 rule
asks for. **Both scripts are green today**, so every red in this ticket is
attributable to this ticket.
⚠️ `verify:cutover` and `verify:booking:admin:db` were **not** run — the first
runs its own `astro build` and rewrites `dist/` with a sentinel tree, the second
needs the database. **Not run is not green; do not log them as a baseline.**

**Call sites, counted by regex excluding each file's `function check(` definition
(R5-12 SF-9 / A16 — three different numbers had been in circulation):**
`verify-cutover.ts` **91** · `verify-booking-pricing.ts` **41** ·
`verify-booking-email.ts` **229**. ⚠️ **These are CALL SITES. Assertions RUN is a
different and larger number** — `verify-booking-pricing.ts:73-88` executes three
times, once per tier. **R6-4 pre-registers the second, never the first.**

### Four more citations were wrong. R5-0's table is extended.

| # | Inherited claim | Verified truth |
| --- | --- | --- |
| **C-j** | 🔴 **C-b itself was incomplete, and this is the third error in that neighbourhood.** C-b corrected the tier requirement to `booking-form.ts:273-275` | **`booking-form.ts` is the CLIENT validator only** — imported by `BookingForm.svelte`, not by the API route. **The server's 422 comes from `src/lib/booking-payload.ts:277-286`**, reached through `parseBookingPayload()` at `src/pages/api/booking/create.ts:73`. Two independent checks in two files, deliberately: `booking-payload.ts:284-286` adds the error **only when `options.entry === 'public'`**, which is why an admin-created row may carry no tier at all. **W17 therefore touches the public WRITE PATH in two files — see R6-2's verdict** |
| **C-k** | R5-4/T10: the new meta description is *"151 characters"* | **148.** Measured on the exact string, em dash U+2014. A length pin written to the ticket's number would fail |
| **C-l** | W6: *"`:780-782`'s inverted comment"* | The comment is **`:772-779`**; the sentence it governs is **`:776-778`**. R5-11 A2/N-2 had it as `:780-782` |
| **C-m** | W21/T6 cite `ROADMAP:881-890`; R5-12 N-5 corrected it to `:886-895` | **Both wrong. `:886-897`** |

⚠️ **C-j is the fourth citation in this ticket to be corrected twice.** The
pattern is not carelessness about digits — **it is describing a mechanism from
one of its two implementations.** C-b found the wrong field in the right file;
C-j finds the right field in only one of two files. **When a rule is enforced in
more than one place, name every place or the spec is a half-truth.**

## R6-1 · 🔴 THE FINDING THAT CHANGES THE GATE: `verify-cutover.ts` CANNOT SEE ITS OWN COPY

**Measured, not reasoned.** A verification agent replaced `FEE_TERMS_ITEMS` with
`[]` in a sandboxed copy of the tree, ran every booking script against baseline
and broken, and deleted the sandbox. **The repo was never touched.**

| Script | Baseline | With `FEE_TERMS_ITEMS = []` | `✗` printed | Summary line |
| --- | --- | --- | --- | --- |
| `verify-booking-pricing` | exit 0 | **exit 1** | 1 | **none — crashed at `:76`** |
| `verify-booking-email` | exit 0 | **exit 1** | 5 | **none — crashed at `:592`** |
| `verify-booking-ics` | exit 0 | **exit 1** | **0** | **none — crashed at `:791`** |
| **`verify-cutover`** | exit 0 | ✅ **exit 0** | 0 | ✅ **`✓ cutover checks passed`** |
| `tsc --noEmit --strict` | — | **exit 0** | — | typecheck stays **green** |

🔴 **`verify-cutover.ts` — the WEB gate, this ticket's own gate — stays FULLY
GREEN while `$399`, `$699` and `$1,199` vanish from `dist/client/book/index.html`
and `dist/client/index.html`.**

**Why:** every terms pin in that file matches the **identifier name**, not the
value. `TERMS_CONSTANTS` (`:350-357`) is six **strings**: `'FEE_TERMS_HEADING'`,
`'FEE_TERMS_INTRO'`, … `checkTermsOrder` (`:363-372`), the presence loop
(`:406-407`), the CTA/form-arm loop (`:445-453`) and the island loop (`:536-537`)
all run `code.includes('<NAME>')`. **The templates still name the constant, so
everything passes.** There is no `399`, `699` or `1,199` anywhere in
`verify-cutover.ts`.

⚠️ **So revision 5's premise — that BK-53's copy edits are pinned by the web
gate — is false in the direction that matters.** The web gate pins that a
constant is *referenced*, in the right *order*, in the right *arm*. It pins
nothing about what the constant *says*.

**→ R6-3 adds VALUE-level pins to `verify-cutover.ts`, and that is the single
biggest change in this revision.**

### And three scripts crash rather than fail

- **`verify-booking-ics.ts` exits 1 with ZERO `✗` and no summary line.**
  **CLAUDE.md's BK-33 trap, live, in this repo, on this ticket's own edit.** A
  harness measuring `grep -c "✗"` reads it as a clean pass.
- `verify-booking-pricing.ts` prints one `✗` then throws at `:76`, so
  **`:83-87`'s `formatCents` and `+ GST` pins never run.**
- `tsconfig.json` extends `astro/tsconfigs/strict`, which sets only
  `"strict": true` — **no `noUncheckedIndexedAccess`** — so `FEE_TERMS_ITEMS[0]`
  types as `string` on an empty array and **every one of those crashes is
  invisible to `npm run typecheck`.**

⚠️ **This corrects R5-12's BL-7 in both directions.** BL-7 said the length zip is
*"the only assertion in the repo that reddens when `FEE_TERMS_ITEMS` is
emptied"*. **It is not** — six named `✗` fire across two scripts. **But BL-7's
instruction stands and is now better founded: keep the zip**, because it is the
one assertion that fails *cleanly* before the crashes start. **And every
red-first row touching this array must score on `(exit code, summary line)`, never
on a `✗` count** — one of the three scripts prints no marker at all.

---

## R6-2 · THE COPY — **replaces R5-4 entirely**

⚠️ **Decision 25 is absolute: no number on any web surface.** Every draft is
checked against it, and R6-3's `WEB_AMOUNT_BAN` enforces it.

### The terms block — **and it gets a NAME (BL-3)**

🔴 **The constant is `ASSESSMENT_TERMS`, a new export in `booking-copy.ts`.**
It **replaces** `FEE_TERMS_INTRO`, `FEE_TERMS_PAYMENT`, `FEE_TERMS_REFUND` and
`FEE_TERMS_CREDIT`. `FEE_TERMS_HEADING`, `FEE_TERMS_ITEMS` and
`FEE_TERMS_ACK_LABEL` survive under their own names.

*(R5-12 BL-3: revision 5 never named it, so `TERMS_CONSTANTS` could not be
re-pointed and the coupling pin's colour was the implementer's coin-flip. Naming
it is what makes R6-3 writable. And per R5-11 A17: **do not** reuse
`FEE_TERMS_REFUND` for five non-refund sentences — a constant whose name lies
about its contents is how the next reader gets it wrong.)*

```ts
export const ASSESSMENT_TERMS: readonly string[] = [
  'The on-site assessment visit is free.',
  'If your address is outside our service area, a travel fee may apply. We tell you the amount when we confirm your appointment.',
  'You get our findings in person at the visit. If you want to go ahead, we follow up with a quote — that is where any report options and prices are.',
  'There is no obligation to go ahead.',
  'We never take money without telling you the amount first — any charge is a link you choose to pay.',
  'If we ever ask you to pay for something before the visit, we tell you the amount and the date. If it is not paid by then we may release the time — and you are welcome to book again.',
  CANCEL_LINE,
];
```

**Seven items. Item 6 is R5-10 T2's slot-release pre-consent** — the only
statement, anywhere the customer sees before consenting, that a slot can be
released; R3-SF12 showed `APPROVED_*` cannot carry it because that email *is* the
one asking for money. **Item 7 is `CANCEL_LINE` itself, not a copy** — the same
identity `FEE_TERMS_REFUND[4]` had (`booking-copy.ts:509`), so its two email
renders and its web render stay one string.

⚠️ **Item 5 is TRUE TODAY:** `booking-payment.ts:374` uses `mode: 'payment'` with
no `setup_future_usage` and no stored customer (R3-BL-6).
⚠️ **Item 5 deliberately avoids the words `nothing is charged` / `no charge` /
`not charged` / `nothing to pay`** — all four are members of R6-3's
`FREE_CLAIM_SHAPES`, and `verify-booking-email.ts:377` positively *requires* one
of them in `FEE_TERMS_PAYMENT` today. **Deleting that array dissolves the
collision; wording item 5 with those words would rebuild it** (R5-11 A6).

### `FEE_TERMS_ITEMS` — figures stripped, array **KEPT**, name kept

🔴 **DO NOT DELETE (R3-BL-2), and DO NOT EMPTY (R6-1).**

1. *"The assessment visit — a technician walks the affected areas and talks you through what they find. **Free.**"*
2. *"A written cause-of-loss report **and a repair estimate**: the documentation your adjuster works from if you are filing a claim. **Quoted with your job.**"*
3. *"A measured sketch **and diagram** of the affected areas, alongside the report **and estimate**. **Quoted with your job.** Where lab samples are taken, **results take 3–5 business days**."*

⚠️ *"and estimate"* / *"and diagram"* are restored deliberately — dropping them
is the exact edit BK-36 made and plan review reverted (R3-SF3).
⚠️ **It is the ONLY path by which the 3–5 day lab turnaround reaches any email**
(BK-55 G9 pins that).
⚠️ **The length zip in `verify-booking-pricing.ts:68-71` stays** and keeps this
array at one bullet per tier.

### The tier picker

- **`ASSESSMENT_TIER_LEGEND`** (`:589`) ⇄ *"What do you think you need?"*
  🔴 **NOT *"(optional)"* — verdict REVERSED, see R6-2a.**
- 🆕 **`ASSESSMENT_TIER_NOTE`** *(confirmed absent from `booking-copy.ts`)* —
  *"The visit is free whichever you pick. Picking one just tells us what to
  prepare; anything written comes later, with your quote."*
- **`ASSESSMENT_TIER_NAMES`** (`:565-569`) — **UNCHANGED.**
- **`ASSESSMENT_TIER_DESCRIPTIONS`** (`:580-586`) ⇄ rewritten; `standard`'s
  current *"writes up the scope of the damage"* is **FALSE** under decision 20:
  - `standard`: *"A technician walks the affected areas and talks you through what they find."*
  - `report`: *"You also want a written cause-of-loss report and a repair estimate — what your adjuster works from. Quoted with your job."*
  - `sketch`: *"You also want a measured sketch and diagram, alongside the report and estimate. Quoted with your job. Where lab samples are taken, results take 3–5 business days."*

### 🔴 R6-2a · W17 REVERSED — the tier field stays REQUIRED

**Revision 5 said make it optional. Three findings say do not.**

1. **C-j: it is enforced in TWO files, and only one was named.**
   `booking-form.ts:273-275` is the **client** validator, imported by
   `BookingForm.svelte`. The server's 422 comes from
   **`booking-payload.ts:277-286`** via `create.ts:73`, and it fires **only when
   `options.entry === 'public'`** — which is why admin rows may carry no tier.
2. **It is a change to the public WRITE PATH**, not copy. This ticket is
   **Reviewed**, and its whole justification for that tier is that the email/money
   half left.
3. **Decision 18's own reasoning forbids it** (R5-12 SF-11): *"Removing a
   required field would itself be a funnel change, in the same window as the
   pricing flip — against the one-funnel-change-per-week rule."* Revision 5 kept
   the checkbox required on exactly that argument and then removed a different
   required field in the same commit.

**→ The legend does NOT say "(optional)". The field stays required. R3-SF9's
contradiction is resolved by fixing the COPY, which is what this ticket owns.**
Making it genuinely optional is a separate ticket if the client ever wants it.

### The remaining web surfaces

| Surface | Draft |
| --- | --- |
| `book.astro:94` + `ContactSection.astro:287` eyebrow | *"Free on-site assessment — travel fee may apply further out"* — ⚠️ **the eyebrow carries its OWN qualifier** (R5-10 T11), because R5-11 A2 verified there is **no literal `travel fee` string on either page** today and A6's source twin cannot otherwise be satisfied without `code.includes('FEE_TERMS_INTRO')`, the import-satisfies-the-pin trap `verify-cutover.ts:328-332` documents |
| `book.astro:36-37` meta description **+ JSON-LD `:49`** | *"Free on-site restoration assessment in Edmonton — about 30 minutes, no obligation. A travel fee may apply further out; we tell you the amount first."* — **148 characters (C-k; the ticket said 151 twice), pinned by LENGTH as well as content** |
| `book.astro` `<title>` + WebPage `name` | **KEEP** — `book.astro:20-33`'s recorded rationale leaves the SEARCH surface to BK-29's site-wide sweep. Stated, not inverted in silence (R3-SF4) |
| `RECEIVED_NEXT_STEPS[1]` (`:639`) | *"If we can take it, we get in touch to confirm — and tell you about a travel fee if one applies to your address."* |
| `RECEIVED_NEXT_STEPS[2]` (`:640`) | *"Once it is confirmed, we email you the details and a calendar invite."* |
| 🆕 `RECEIVED_NEXT_STEPS` — **the free claim** | ⚠️ **R5-11 A19/N5, verdict owed and given: NO.** `/book/received/` is the first page every web customer sees and after this rewrite no step says the visit is free. **It does not need to** — the terms box renders on that page too, and adding an eighth free claim means an eighth qualifier. **Recorded so it is not re-raised as an omission.** ⚠️ `BookingConfirmation.svelte:149` is `{#each … (step)}`, **keyed on the string**: two identical steps throw at runtime (A20/N6). Nothing pins it; do not make two steps identical |
| `llms.txt.ts:54` ⚠ **claim + qualifier in ONE sentence** | *"Pick a time for an on-site assessment — about 30 minutes, and the visit itself is free, though a travel fee may apply outside the usual service area and we tell you the amount when we confirm your appointment. Booking online sends a request: the office reviews it and gets in touch to confirm. Nothing is handed over in writing at the visit; if the customer wants to go ahead, a quote follows, and that is where report options appear. This is the way to request a quote."* |
| `[service].astro:79` — **ONE template → 4 pages (C-g)** | *"About 30 minutes, no obligation. A travel fee may apply outside our service area."* |
| `FEE_TERMS_ACK_LABEL` (`:541`) | *"I understand the assessment terms above, including that a travel fee may apply and that I will be told the amount when we confirm my appointment."* ⚠️ **Renders on ONE surface only** — `BookingForm.svelte:1223`, and it is **outside** the terms-box div (`:1220-1228` is a sibling). `book.astro:127` is a comment mention |
| `QUOTE_TIMING_NOTE` (`:612-613`) + `QUOTE_HEADING` (`:600`) | ⇄ **DELETED** — both render only inside `BookingForm.svelte:1198-1217` (R3-SF1) |
| `AFTER_HOURS_NOTE` (`:597`) | ⇄ **DELETED** — decision 21. ⚠️ It spells the multiplier **`1.5×` (U+00D7)** while `booking-email.ts:1167` spells it **`1.5x` (ASCII)** — a ban on one spelling misses the other |
| `FEE_TERMS_PAYMENT` (`:459-464`) | 🔴 **DELETED — the WHOLE array.** `[0]`, `[1]`, `[2]` go; `[3]` is replaced by `ASSESSMENT_TERMS[5]`. **BK-55 removes the email's dependence FIRST** (its §E-1); five other files name it — `book.astro:13`/`:143`, `ContactSection.astro:9`/`:351`, `BookingForm.svelte:37`/`:1119`, `verify-booking-ics.ts:45`/`:752`, and **`verify-cutover.ts:354`, which pins the STRING and so reddens on a rename too** |
| `FEE_TERMS_INTRO`, `FEE_TERMS_REFUND`, `FEE_TERMS_CREDIT` | 🔴 **DELETED — replaced by `ASSESSMENT_TERMS`.** ⚠️ Their **email** renders (`booking-email.ts:522-533`, `:604-611`) are **BK-53's**, not BK-55's — R5-1 rule 1. This ticket edits them |
| `PAYMENT_CANCELLED_LEAD` (`:777-778`) | 🔴 **FIXED, not kept (R5-12 BL-4; the old table said UNCHANGED).** → *"If we sent you a payment link, it works until the date in that email — open it again whenever you are ready. If you were not asked to pay for anything, nothing is outstanding and your request is still with us."* The **route** KEEP verdict is unchanged and separate |
| `PAYMENT_CANCELLED_HELP_LINE` (`:780`), `PAID_*` | **UNCHANGED** |
| `[id].astro:825-828`, `:1835` — **`bk51-gated`** | **DELETED** — decision 21. ⚠️ `:1781` is the **payment-deadline** hint and must survive (C-h) |
| `booking-payment.ts:288` | **Verdict, which R5-12 SF-5 said was left undecided: RESTATE, do not delete.** The branch is unreachable today (`review.ts:246`) but the line is a `price_data` **description**; BK-55 G8 pins it. → *"Travel to your address"* |
| `TRAVEL_FEE_CENTS_PER_KM` / `_FREE_RADIUS_KM` (`booking-pricing.ts:123-124`) | **KEEP the exports and BK-51's pin that nothing in `src/` reads them.** The pin is worth more than the tidiness; add a comment saying the per-km rule is **retired**, not paused |
| `booking-copy.ts:437` | **Rewrite the comment** — its *"a travel-fee field at approval"* clause is false since BK-51 (`review.ts:246`) |
| `ContactSection.astro:56-82` and `BookingForm.svelte:772-779` | 🔴 **Rewrite BOTH comments.** They record *"`no obligation` is actively false"* and *"a bare 'free' … is the contradiction the ticket exists to remove"* — **rationales decisions 17 and 23 invert.** C-l: the second is `:772-779` governing `:776-778`, **not** `:780-782`. R5-11 A19 / R5-12 SF-10 |
| **The `<ul>` markup** | 🔴 **A markup change on FOUR surfaces that no W-row listed (R5-11 A18).** Verified: the `<ul>` maps `FEE_TERMS_ITEMS` **alone** at `book.astro:140-142`, `ContactSection.astro:348-350`, `BookingForm.svelte:1114-1118` and `booking-email.ts:525-527`, while PAYMENT/REFUND render as sibling `<p>`s. `ASSESSMENT_TERMS` must render as its own list **before** `FEE_TERMS_ITEMS` |

---

## R6-3 · THE GATE — **replaces R5-5 entirely**

### 3a · The coupling pin — **over VALUES, and the pattern already exists**

> **A surface that claims the assessment is free, OR releases obligation, must
> carry the travel-fee qualifier. Claim and qualifier travel together, or red.**

🔴 **The unit is the JOINED TERMS TEXT, not the file and not the constant.**
R5-12 BL-2 proved the per-constant form is red on arrival —
`ASSESSMENT_TIER_NOTE` and `FEE_TERMS_ITEMS[0]` each carry a free claim and
neither can carry a qualifier without absurdity. BL-6 proved the whole-file form
is red on arrival too, for a different reason: **8 of 12 matches in
`booking-copy.ts` are in comments.**

**The joined-values unit is what a customer actually reads, and there is a
working precedent — `verify-booking-email.ts:283-289` already does exactly
this:**

```ts
const terms = [FEE_TERMS_INTRO, ...FEE_TERMS_ITEMS, ...FEE_TERMS_PAYMENT,
               ...FEE_TERMS_REFUND, FEE_TERMS_CREDIT].join(' ');
```

**→ `verify-cutover.ts` imports the values and builds the same shape:**

```ts
// The SECOND import from `src/` in this file. The first (:43) earned its place
// by finding the built chunk; this one earns its place because every terms pin
// below it matched an identifier NAME, and a sandboxed break proved the whole
// file stays green while the prices vanish from dist/. Names tell you a constant
// is referenced. Only the value tells you what it says.
import {
  ASSESSMENT_TERMS, ASSESSMENT_TIER_NOTE, ASSESSMENT_TIER_DESCRIPTIONS,
  FEE_TERMS_ACK_LABEL, FEE_TERMS_HEADING, FEE_TERMS_ITEMS,
} from '../src/lib/booking-copy';

const TERMS_TEXT = [
  FEE_TERMS_HEADING, ...ASSESSMENT_TERMS, ...FEE_TERMS_ITEMS,
  ASSESSMENT_TIER_NOTE, ...Object.values(ASSESSMENT_TIER_DESCRIPTIONS),
  FEE_TERMS_ACK_LABEL,
].join(' ');
```

```ts
const FREE_CLAIM_SHAPES = [
  /\bfree[^.<>]{0,40}?\b(?:visits?|assessments?)\b/i,
  /\b(?:visits?|assessments?)\b[^.<>]{0,40}?\b(?:is|are|'s|itself is)\s+free\b/i,
  /(?:^|[.>])\s*free\.\s*(?:$|[<'"`,])/im,        // R5-11 A7 — the trailing quote
  /\bno[\s-]+obligations?\b/i,                     // plural + reflow, BK-48
  /\bwithout\s+obligation\b/i,
  /\bno\s+(?:commitment|strings|cost|charge)\b/i,
  /\b(?:nothing is charged|not charged|nothing to pay|at no cost)\b/i,
  /\b(?:don'?t|do not|won'?t)\s+charge\b/i,
  /\b(?:pay nothing|costs? you nothing|complimentary|waived?)\b/i,
  /\$\s*0(?:\.00)?\b/,
];
const TRAVEL_QUALIFIER = /\btravel[\s-]+fees?\b/i;   // A10 + N-1: hyphen IS native — booking-copy.ts:447
```

**Assert: if any shape matches `TERMS_TEXT`, `TRAVEL_QUALIFIER` matches it too —
and assert BOTH sides are non-empty**, so the pin cannot pass by matching nothing.

⚠️ **The exempt question dissolves.** `NO_CHARGE_LINE`, `DECLINED_LEAD`,
`EXPIRED_REQUEST_LEAD` and `PAYMENT_EXPIRED_LEAD` are **not in `TERMS_TEXT`** —
they are email lifecycle copy, not terms. **R5-10 T3's exempt list is no longer
needed and is withdrawn**, which is strictly better: *an exemption is how a ban
dies quietly*, and this design has none.

### 3b · The rendered-surface twin — **a real block slice, not a whole file**

R5-11 A2 showed A6's source twin is unsatisfiable on `ContactSection.astro` and
satisfied by a `<meta>` tag on `book.astro`. **The eyebrow now carries its own
qualifier (R6-2), so the twin is writable — and it uses machinery that exists.**

**`verify-cutover.ts:784-791` is the pattern to copy** — a start+end slice that
**asserts the slice is locatable and non-empty before using it**:

```ts
const cardAt = islandRaw.indexOf('{#if result}');
const formAt = islandRaw.indexOf('{:else}');
check(cardAt !== -1 && formAt !== -1 && cardAt < formAt, '… the slice below is real');
const card = /* slice */; check(card.length > 0, 'and the card slice is non-empty');
```

**Assert, per surface:** the eyebrow's own line contains a free claim **and**
`TRAVEL_QUALIFIER` — as **literal text in the rendered template**, never a
constant name. ⚠️ **State in the pin's comment that
`code.includes('FEE_TERMS_INTRO')` does NOT satisfy it** (the trap
`verify-cutover.ts:328-332` already documents).

⚠️ **Do not slice the island's terms-box div for this.** Verified: `:1111-1218`
contains the tier fieldset, `AFTER_HOURS_NOTE`, three radio labels with
`formatCents(...) + GST` at `:1173`, `QUOTE_HEADING` and the Assessment/GST/Total
`<dl>`. **It is not a pure terms region** and a regex over it sees all of that.

### 3c · The four ban sites — corrected

| Site | Today | After |
| --- | --- | --- |
| **`:471-477`** `!/no obligation/i` over `book.astro` + `ContactSection.astro`, **whole file incl. frontmatter** | 🔴 **RED** — the new meta description says *"no obligation"* in frontmatter | Re-scoped to the coupling form |
| **`:512-517`** `!/free\s+assessment/i`, **`templateOf` only** | ⚠️ **GREEN — and R5-5b said RED, wrongly (A8).** `\s+` does not span `on-site `. **Re-scope it because it is VACUOUS**, not because it fails | Coupling form |
| **`:1055-1071`** `BANNED_SHAPES` over `dist/client/**/*.{js,html}` | 🔴 **RED** — `/\bfree[^.<>]{0,40}?assessments?\b/i` matches *"Free on-site assessment"* in the two pages A5's twin **requires** to carry it | Coupling form; **`.js` excluded** (A3 — chunk co-location is a Vite decision) |
| **`:1188-1193`** over `dist/client/llms.txt` | ⚠️ **GREEN, and that is the defect.** The draft's gap between *assessment* and *is free* is far past `{0,24}` | Re-point anyway — the negative-assertion trap in its BK-48 shape |

### 3d · 🔴 `WEB_AMOUNT_BAN` — decision 25 enforced, and the scope is **already computed**

R5-12 BL-5 showed the naive scope is red on arrival. **Verified empirically
against the real `dist/`:**

```
dist/client/book/index.html            $399, $699, $1,199   ← FEE_TERMS_ITEMS
dist/client/index.html                 $399, $699, $1,199   ← FEE_TERMS_ITEMS, CTA arm
dist/client/insurance-claims/index.html  $500, $2,500       ← unrelated, CORRECT copy
dist/client/_astro/booking-handoff.*.js  $399               ← the terms chunk
```

🔴 **And the decisive fact: `$150` appears in exactly TWO places in all of `src/`
on `bk51-gated`** — `[id].astro:621` (a comment) and `:1861` (rendered **admin**
markup) — **and reaches NO file in `dist/client`**, because every
`src/pages/admin/**` page carries `export const prerender = false;` and lands in
`dist/server`. **`dist/client` has no `admin` directory at all.**

**→ Scope it to `bookingSurfaces`, which `verify-cutover.ts:1130-1135` ALREADY
BUILDS** — `dist/client/book/**.html`, `dist/client/index.html`, plus
`termsBundles` (the `.js` chunks containing `TERMS_MARKER`). **That excludes
`insurance-claims` structurally rather than by a carve-out list**, which answers
R5-12's N-3: the carve-outs matched none of their own shapes because there was
nothing legitimate to carve out once the scope was right.

```ts
const WEB_AMOUNT_SHAPES = [
  /\$\s?[\d,]/,
  /\b\d[\d,]*\s*(?:dollars|CAD)\b/i,
  /\b(?:one|two|three|four|five|six|seven|eight|nine)\s+hundred\b/i,
  /\b\d[\d,]*\s*(?:\+\s*GST|plus GST)\b/i,
];
```

**After this ticket the tier prices are gone, so this ban is GREEN on arrival and
stays green.** ⚠️ **It is red TODAY, deliberately** — that is red-first row 9, and
it must be observed red before the copy lands.
⚠️ **`SUPPORT_PHONE`, *"about 30 minutes"* and *"3–5 business days"* match none of
the four shapes** — verified — so **no carve-out list exists at all.** If one ever
becomes necessary, it needs a comment per entry and a non-growing assertion.

### 3e · The reddened assertions — classified

🔴 **`verify-booking-pricing.ts:68-71` — the length zip — is KEPT** (R5-12 BL-7,
re-founded by R6-1: it is the one assertion that fails *cleanly* before three
scripts start crashing). **Delete only `:76` and `:87`'s `copy.includes(...)`
checks and the `expected` figures they read.** Keep the `forEach`, keep the
absolute `formatCents(tierDefaultCents(tier))` pin at `:84` — **verified as the
only absolute-value pin on `tierDefaultCents` in the repo.**
⚠️ **A16/SF-9, stated once and correctly:** `verify-booking-pricing.ts` has **41
call sites**; this removes **2**; the *runtime* count is different again because
`:73-88` executes three times. **R6-4 pre-registers assertions RUN.**

⇄ **Class 2 (re-point):** `verify-cutover.ts:347-357` → `TERMS_CONSTANTS` becomes
`['FEE_TERMS_HEADING', 'ASSESSMENT_TERMS', 'FEE_TERMS_ITEMS']` · `:395-416` ·
`:437-454` · `:530-549` · `:571-579` · `:584-588` **and `:546-549`** — 🔴 **both,
because `outroAt` is `indexOf('FEE_TERMS_CREDIT')` (`:542`) and that constant is
deleted.** With `outroAt = -1`, `:586`'s `tierAt > outroAt` is **vacuously true**
and the tier picker could move above the whole box, green (A11). **Re-anchor
`outroAt` on `'ASSESSMENT_TERMS'`** — named here so the implementer does not pick
(R5-12 SF-2). ⚠️ `:546-549` reddens and `:584-588` does not; **the edit that makes
the red one green is deleting it, which leaves the silent one broken.**
⇄ **Class 3 (subject gone):** `verify-cutover.ts:605-619` (the AC11 pair — 🔴
**delete it WITH `AFTER_HOURS_NOTE`; do NOT re-point `noteAt` to
`ASSESSMENT_TIER_NOTE`.** AC11's reason is *"a weekend price must be EXPLAINED
where it is shown"* and the new note explains no number — R3B-D4/T8).
⇄ **Class 1 (rewrite to assert the replacement):** `verify-cutover.ts:364-372` ·
`verify-booking-email.ts:246-254` — 🔴 **the replacement is the CATEGORY pin
`!/\$\s?[\d,]/.test(figures)`, not `!figures.includes('399')`**, which is shaped
for three numbers and passes `$150`, `$385` and a re-added `$399.00` (A14). **It
is the same shape as `WEB_AMOUNT_SHAPES[0]`; write it once and use it twice.**
· `:291-296` · `:303`, `:310`, `:320` **(A15/N-1: the call sites are `:303/:310/
:320`, not `:304/:311/:321`)** · `:373`, `:377`, `:384`, `:396`, `:419`, `:427`
— 🔴 **all six positively REQUIRE the deleted payment copy, and `:384` asserts
*"the appointment is confirmed BY the payment"*, which decision 17 reverses** ·
`:447` (`TURNAROUND.test(FEE_TERMS_ITEMS[2])`) · `:554-563` `mustNotCarry` —
**rewritten, never re-indexed** · `:592` · `:638` · `:742-745` ·
`verify-booking-ics.ts:751` (`FEE_TERMS_ITEMS[0]` in `BANNED`).
🔴 **`verify-booking-email.ts:339-349` is CLASS 2, not Class 3** — `:346-349` is
the sole assertion that `CANCEL_LINE` closes the block, and `CANCEL_LINE`
survives as `ASSESSMENT_TERMS[6]` (A4). ⚠️ **One of the three unjustified
Class 1 → Class 3 moves, now named. Find the other two.**

**Binding rules:**
- ⚠️ **DELETE constants; never EMPTY them** — R6-1 measured what emptying does.
- ⚠️ **Pin the orphaned readers' ABSENCE.** `noUnusedLocals` is not set
  (`verify-cutover.ts:558-559` records an orphaned import raising no error).
  Once the quote block goes, `quote`, `tierQuotes`, `assessmentQuote` and
  `formatCents` are dead in the island.
- ⚠️ **Scope the payment-link ban PER CONSTANT** (`verify-booking-email.ts:368-372`
  is the pattern) — a whole-file ban on `booking-copy.ts` cannot exclude
  `PAYMENT_CANCELLED_LEAD`, which lives in it (R3B-C2).
- ⚠️ **`stripForUse`'s `//` rule is `/(^|[^:])\/\/[^\n]*/g`**, deliberately, so it
  does not eat `https://`. Any new source-level scan must use it and not a naive
  stripper — and note it does **not** run on `dist/`, where every amount is real.
- ⇄ **§5a's `/\bEverything above\b/i` → a positive twin:** each non-`standard`
  description matches `/quoted with your job/i` (R3B-D5).

---

## R6-4 · PRE-REGISTRATION AND RED-FIRST

**The counter (R5-2a) lands FIRST**, and prints on **both** branches (A9c):

```ts
if (failures > 0) { console.error(`\n✗ ${passes} passed, ${failures} failed\n`); process.exit(1); }
console.log(`\n✓ cutover checks passed — ${passes} passed, 0 failed\n`);
```

⚠️ **N-9: `verify-cutover.ts` prints a SECOND line after the summary** (`:1235`,
the SENTINEL-build note). **The summary line is the one beginning `✓` or `✗`.**
⚠️ **A9(b): a scalar count cannot see a collapsed scan set.** `verify-cutover.ts`'s
sweeps are **one** `check(offenders.length === 0)` over a filtered array — the
count is 1 whether the filter read 40 files or 0. **→ Every sweep also asserts its
SCANNED-FILE COUNT is non-zero and within a pre-registered range.**
✅ `:978`'s `check(bundles.length > 0, …)` is the existing precedent.

| Script | Assertions RUN, before | Expected after | Notes |
| --- | --- | --- | --- |
| `verify-cutover.ts` | ☐ | ☐ | 91 call sites; **not run in the baseline** — it rewrites `dist/` |
| `verify-booking-pricing.ts` | ☐ | ☐ | 41 call sites, −2; `:73-88` runs ×3 |
| `verify-booking-email.ts` | ☐ | ☐ | 229 call sites; `:565-576` loops 2×2, `:2009-2013` loops ×3 |

🔴 **Fill these from the counter run BEFORE any copy moves, and paste the summary
lines here.**

| # | Break the PRODUCTION target | Expect | red-observed |
| --- | --- | --- | --- |
| 1 | Remove the travel clause from `ASSESSMENT_TERMS[1]` | coupling pin red | ☐ |
| 2 | ⇄ **CORRECTED (BL-8): change to *"no obligations"* AND delete the travel clause**, then restore only the travel clause | red, then **green** — proving the plural is still SEEN. *The old row could not go red: `s?` matches the plural and the qualifier is in the same unit* | ☐ |
| 3 | Delete the eyebrow from `ContactSection.astro:287` | **the source twin** red — the `dist/` twin alone stays green | ☐ |
| 4 | Re-add `{formatCents(tierQuotes[tier].baseCents)}` to the island | re-pointed `:594-598` red | ☐ |
| 5 | Drift `TIER_PRICE_CENTS` `39900 → 3990` | `verify-booking-pricing.ts:84` red | ☐ |
| 6 | Set `FEE_TERMS_ITEMS = []` | **exit 1 on pricing, email AND ics.** ⚠️ **Score on (exit code, summary line): `ics` prints ZERO `✗` and no summary — R6-1 measured it** | ☐ |
| 7 | Restore *"credited in full"* to `llms.txt.ts:54` | `:1188-1193` re-pointed, red | ☐ |
| 8 | Drop the travel clause from `[service].astro:79` | coupling pin red on all 4 built pages | ☐ |
| 9 | 🔴 **Add `$150` to `ASSESSMENT_TERMS[1]`** | **`WEB_AMOUNT_BAN` red.** Nothing catches this today, and the coupling pin reads it as *more* compliant | ☐ |
| 10 | Move the tier picker above the whole terms box | re-anchored `:584-588` red — with `outroAt = -1` it is vacuously true | ☐ |
| 11 | Point a sweep's file list at a directory that does not exist | **scanned-file-count** red — against `offenders.length === 0` alone it passes having read nothing | ☐ |
| 12 | Delete one coupling arm and add a `check(true, …)` | the pre-registered count must still catch it | ☐ |
| 13 | Reflow `[service].astro:79` so `travel` ends a line | **stays GREEN** — `\s+`. Under `/\btravel fee\b/` the pin goes blind while the customer still reads it | ☐ |
| 14 | Delete `CANCEL_LINE` from `ASSESSMENT_TERMS` | re-pointed `verify-booking-email.ts:339-349` red **(Class 2)** — as Class 3 it was deleted and this ships green | ☐ |

⚠️ **Assert the break LANDED. Score on (exit code, summary line). Restore from a
FILE BACKUP** — `git checkout --` reverts to HEAD and eats uncommitted work, and
on `bk51-gated` that work is BK-51's. **Clear stranded probe rows first:**
`npm run verify:booking:admin:db -- --reset`, which **exits 3** by design.

---

## R6-5 · DISPOSITION — every R5-12 finding, and where it landed

| ID | Landed in |
| --- | --- |
| BL-1 email renders unowned | **R6-2's table** (`FEE_TERMS_INTRO/REFUND/CREDIT` row) + **BK-55 §E-1** re-derives the order |
| BL-2 pin red on arrival | **R6-3a** — the unit is joined VALUES; the exempt list is withdrawn |
| BL-3 constant unnamed | **R6-2** — `ASSESSMENT_TERMS`, with the new `TERMS_CONSTANTS` in R6-3e |
| BL-4 T5 not in the copy table | **R6-2's `PAYMENT_CANCELLED_LEAD` row** |
| BL-5 `WEB_AMOUNT_BAN` unscoped | **R6-3d** — reuses `bookingSurfaces` |
| BL-6 `stripForUse` missing | **R6-3a** (values, so comments are structurally excluded) + R6-3e's binding rule |
| BL-7 length zip | **R6-3e** — KEPT, re-founded by R6-1 |
| BL-8 red-first row 2 | **R6-4 row 2**, corrected |
| SF-1 class lists | **R6-3e** — all nine arms folded in |
| SF-2 `outroAt` anchor | **R6-3e** — `'ASSESSMENT_TERMS'`, named |
| SF-3 literal-not-name | **R6-3b** |
| SF-4 length pin + 151→148 | **R6-2's meta row** + **C-k** |
| SF-5 `booking-payment.ts:288` verdict | **R6-2's row** — restate to *"Travel to your address"* |
| SF-6 W10 unpinned | **R6-2's `[id].astro` row**; ⚠️ **recorded as unpinned and verified by eye** — `verify-cutover.ts` does not scan `src/pages/admin/**` and admin pages never reach `dist/client` |
| SF-7 `<ul>` markup | **R6-2's last row** |
| SF-8 G0 both branches | **R6-4** + **BK-55 §D G0** |
| SF-9 count units | **R6-0** and **R6-3e** |
| SF-10 `ContactSection` comment | **R6-2's comment row** |
| SF-11 W17 funnel | **R6-2a — REVERSED** |
| SF-12 `RECEIVED_NEXT_STEPS` free claim | **R6-2's row — verdict NO, recorded** |
| SF-13 "five items" vs six | **R6-2 — seven, stated** |
| N-1 `[\s-]+` | **R6-3a** |
| N-2 `:780-782` | **C-l** |
| N-3 carve-outs | **R6-3d** — none exist |
| N-4 assumption order | fixed in R6-6 |
| N-5 ROADMAP lines | **C-m — `:886-897`** |
| N-6 A24 weekend multiplier | **R6-6 A8** |
| N-7 "13 → 15" | fixed in all three places |
| N-8 keyed `{#each}` | **R6-2's `RECEIVED_NEXT_STEPS` row** |
| N-9 second line after summary | **R6-4** |
| N-10 `verify-booking-pricing` rationale | **R6-3e** — it keeps the `FEE_TERMS_ITEMS` import via the length zip, so decision 24's rationale survives |

## R6-6 · ASSUMPTIONS

| # | Assumption | Basis |
| --- | --- | --- |
| A1 | The seam rule + BK-55's clause 5 | R5-1, extended by that ticket's review B7 |
| A2 | One adversarial pass at the branch tip over the union | Testing a mid-branch commit tests code that never ships |
| A3 | `ASSESSMENT_TERMS` is one array holding claim and qualifier | It is what makes the coupling pin satisfiable at all (BL-2) |
| ~~A4~~ | ~~The coupling unit is joined VALUES~~ | 🔴 **STRUCK by R7-3a — the unit is the SENTENCE.** The joined-values form was measured green on a real break (deleting the travel clause left the ack label holding the qualifier). *Struck here rather than only asserted in R7, because R7-8 is titled "Assumptions ADDED" and never claimed to remove one* |
| ~~A5~~ | ~~`WEB_AMOUNT_BAN` scopes to `bookingSurfaces`~~ | 🔴 **STRUCK by R7-4** — that scope excludes `llms.txt`, `/contact/` and the four `[service]` pages, all of which this ticket edits |
| A6 | W17 reversed — the tier stays required | Decision 18's own funnel reasoning + it is a two-file public write-path change |
| A7 | `book.astro`'s `<title>` / WebPage `name` stay with BK-29 | `book.astro:20-33`'s recorded rationale |
| A8 | 🆕 The weekend multiplier stays in `booking-pricing.ts` while W10 removes the office's view of it | **Recorded, not resolved (N-6).** `AFTER_HOURS_NUMERATOR/DENOMINATOR` still multiply a hand-charged assessment 1.5× on weekends. **A review question for the adversarial pass, and a candidate for the client** |


---

# 🟦 REVISION 5 — THE WEB HALF. 2026-09-05. **THIS IS THE SPEC.**

**Written against decision 24's split.** This section is the implementable spec
for BK-53 as rescoped. It supersedes revisions 2, 3 and 4 **as scope**; those
sections stay only so a finding can be recovered by its ID.

**Target branch: `bk51-gated`.** Ships in ONE push with **BK-51 and BK-55**,
never separately. Decision 24 split the *review*, not the *deploy*.

## R5-0 · What was re-verified before a word of this was written

Every line number below was re-read against the tree this session, and the
branch premise was established first rather than assumed. **`bk51-gated`
differs from `main` in exactly six files** — `scripts/verify-booking-admin-db.ts`,
`scripts/verify-booking-review.ts`, `src/lib/booking-payment.ts`,
`src/lib/booking-review.ts`, `src/pages/admin/appointments/[id].astro`,
`src/pages/api/admin/appointments/review.ts`. **Of everything this ticket
touches, only `[id].astro` is among them.** So for the copy files and the two
verify scripts, `main` and `bk51-gated` are byte-identical and a line number
read on either is valid; for `[id].astro` it is not, and every `[id].astro`
citation below was read from `git show bk51-gated:`.

**Nine citations inherited from earlier revisions are WRONG. Corrected here:**

| # | Inherited claim | Verified truth |
| --- | --- | --- |
| C-a | `src/components/ContactSection.astro` — used throughout revisions 2–4 | 🔴 **That path does not exist.** The file is **`src/sections/ContactSection.astro`**. `verify-cutover.ts:471`/`:512` already use the correct path; the ticket does not |
| C-b | `booking-form.ts:276` hard-requires the assessment tier (R3-SF9) | 🔴 **Wrong field.** `:276` is the **`termsAck`** check. The tier requirement is **`:273-275`**. The *substance* of R3-SF9 holds — the server does 422 without a tier — but the citation points at the checkbox |
| C-c | `ASSESSMENT_TIER_*` at `:580-587` | Only `ASSESSMENT_TIER_DESCRIPTIONS` (**`:580-586`**) is in that range. `ASSESSMENT_TIER_NAMES` is **`:565-569`**, `ASSESSMENT_TIER_LEGEND` is **`:589`** |
| C-d | `FEE_TERMS_PAYMENT[2]` at `:462`; `[1]` also cited as `:462` | `[1]` is **`:461`**, `[2]` is **`:462`** |
| C-e | Revision 3 §2a A2: `verify-cutover.ts:440-453`'s *"own comment records SHIPPED GREEN ONCE"* | 🔴 **No such comment exists.** Grepped the whole file: zero hits for that phrase in any casing. **The assertion at `:440-453` is real and the reasoning for keeping it is sound; the supporting quotation was invented.** Struck. *(This is the copy-inventory trap's cousin: a citation restated across three revisions without anyone opening the file.)* |
| C-f | `verify-booking-pricing.ts` has **42** assertions, going to 35 (R3B-B2) | **41 call sites.** The 42nd `check(` is the **function definition** at `:26`. See R5-4 — the count is also not observable at runtime, which is a bigger problem than the off-by-one |
| C-g | R4-6: *"add the travel-fee clause to those four pages' CTA blocks — **one sentence each**"* | The four pages are generated from **one** template, `src/pages/[service].astro`, via `getStaticPaths()` over the 4 `pagedServices`. `:79`'s *"About 30 minutes, no obligation."* is **hardcoded in the template, in one place**. It is **one edit in one file**, not four |
| C-h | `[id].astro:1781` is the weekend hint (revision 3) | R4's correction **CONFIRMED on `bk51-gated`**: `:1781` is not it. The weekend claims are **`:825-828`** (`money.afterHours ? ' · weekend slot' : ' · weekend rate, 1.5×'`) and **`:1835`** (`suggestedQuote.afterHours ? ' · weekend rate, 1.5× applied' : ''`) |
| C-i | `QUOTE_TIMING_NOTE` at `:613` | Declaration `:612`, string `:613`. `QUOTE_HEADING` is `:600` |

---

## R5-1 · 🔴 THE SPLIT HAS A SEAM, AND IT RUNS STRAIGHT THROUGH THE TERMS BOX

**This is new. It is in no review, and it blocks both halves until it is settled.**

Decision 24's table gives **the terms box** to BK-53 and **`FEE_TERMS_ITEMS` as
it reaches email** to BK-55. **Those are the same constants.** Verified —
`booking-email.ts:37-75` imports `CANCEL_LINE`, `FEE_TERMS_CREDIT`,
`FEE_TERMS_HEADING`, `FEE_TERMS_INTRO`, `FEE_TERMS_ITEMS`, `FEE_TERMS_PAYMENT`,
`FEE_TERMS_REFUND`, `RECEIVED_NEXT_STEPS` and `ASSESSMENT_TIER_NAMES`, and
renders them at:

| Constant | Web renders (BK-53's group) | Email renders (BK-55's group) |
| --- | --- | --- |
| `FEE_TERMS_HEADING` | `book.astro`, `ContactSection.astro`, island | `booking-email.ts:522` (HTML), `:604` (text) |
| `FEE_TERMS_INTRO` | `book.astro:11`, `ContactSection.astro:347`, `BookingForm.svelte:1113` | `:524`, `:605` — **request arm only** |
| `FEE_TERMS_ITEMS` | `ContactSection.astro:349`, `BookingForm.svelte:1115` | `:526`, `:606` |
| `FEE_TERMS_PAYMENT` | `book.astro:143`, `ContactSection.astro:351`, `BookingForm.svelte:1119` | `:1151` via `slice(0, 2)` |
| `FEE_TERMS_REFUND` | terms box | `:532`, `:609` |
| `FEE_TERMS_CREDIT` | terms box | `:533`, `:611` |
| `CANCEL_LINE` | terms box, as `FEE_TERMS_REFUND[4]` (`booking-copy.ts:509`) | `:574`, `:627`, `:771`, `:804` |
| `RECEIVED_NEXT_STEPS` | `BookingConfirmation.svelte:149` | `:491`, `:595` |

**So "the terms box" is not a web surface. It is a shared surface**, and the
edit that rewrites it lands on every customer email in the same keystroke.

🔴 **Why this is a blocker and not a note.** `CONVERSION` §7 constraint 5 —
the rule decision 24 invoked to choose this axis — requires the copy edit and
its pin to be **one commit**. Under a literal reading of decision 24's table:

- BK-53 deletes `FEE_TERMS_REFUND`. `FEE_TERMS_REFUND[4]` **is** `CANCEL_LINE`,
  so that commit silently removes one of `CANCEL_LINE`'s two renders in every
  customer email.
- The pin that would catch it — *"positively pin the survivor at
  `booking-email.ts:574`/`:627`"* — is **BK-55's**, by BK-55 §4.
- Between the two commits the claim is live and unpinned. **That is precisely
  the failure decision 24 chose this axis to avoid**, reproduced inside it.

### ✅ RESOLUTION — the seam rule. Both tickets adopt it verbatim.

1. **`src/lib/booking-copy.ts` belongs to BK-53, whole.** Every constant in it,
   and **every assertion in any verify script that reads a `booking-copy.ts`
   symbol** — `verify-cutover.ts` and `verify-booking-email.ts` alike — is
   BK-53's to edit, in BK-53's commit. Constant and pin stay in one commit,
   which is the whole of constraint 5.
2. **BK-55 owns claims that ORIGINATE in `booking-email.ts` / `resend.ts`** —
   the computed `assessmentSummary`/`tierLine` line (R4-1), the `APPROVED_*`
   terms wording (decision 19), the `resend.ts` suppression (R4-3), the
   `slice(0, 2)` index addressing, and the `verify-booking-email.ts` arms over
   **those**. BK-55 does **not** edit `booking-copy.ts`.
3. **The `slice(0, 2)` interaction is BK-55's and it is a real dependency, not a
   courtesy.** `booking-email.ts:1151` returns `FEE_TERMS_PAYMENT.slice(0, 2)`
   for the non-request arm. BK-53 changes that array's length. **BK-53 must not
   land before BK-55 has converted the index addressing to named constants** —
   otherwise shrinking the array silently changes what a confirmed customer
   reads and no arm fails. **Therefore: BK-55's commit precedes BK-53's on the
   branch.** Recorded in R5-5.
4. 🔴 **The adversarial pass runs ONCE, at the TIP of `bk51-gated`, after
   BK-51 + BK-55 + BK-53 have all landed — never against BK-55's commit alone.**
   An adversarial pass against a mid-branch commit breaks code that will never
   ship in that state. Its scope is the **union** of both halves. This is what
   keeps BK-53 honestly at **Reviewed** while its email renders still get
   adversarial coverage.

⚠️ **Decision 24's stated reasons survive this rule intact:** the money and the
write path are still in one ticket (R4-1, R4-3, decision 19 are all BK-55's),
and BK-53 is still the smaller, plainer half. What changes is only *where the
constant file lives* — and it has to live somewhere whole, or constraint 5 is
violated by construction.

---

## R5-2 · 🔴 THE GATE CANNOT EMIT THE NUMBER THE TICKET ORDERS IT TO LOG

**Also new, also in no review, and it voids R4's own remedy.**

Revision 3 binds the implementer to *"**Log the summary line's ASSERTION
COUNT**, not just the exit code"*, and R4 sharpens it to *"pre-register the
expected count per script IN THIS TICKET before touching code, and assert
`(exit code, summary line)`"* — CLAUDE.md's BK-33 rule.

**Verified in all three scripts: there is no count to log.**

```ts
let failures = 0;
function check(condition: boolean, message: string) {
  if (!condition) { console.error(`  ✗ ${message}`); failures++; }
}
...
if (failures > 0) { console.error(`\n✗ ${failures} check${...} failed\n`); process.exit(1); }
console.log('\n✓ cutover checks passed\n');          // verify-cutover.ts:1231-1235
```

`check()` counts **only failures**. The success summary line is a bare
`✓ … checks passed` with **no number** — identical text whether 91 assertions
ran or 4 did. Same in `verify-booking-pricing.ts` (`✓ booking pricing checks
passed`) and `verify-booking-email.ts` (`✓ booking notification checks passed`).

🔴 **So the only number any of them ever prints is the `✗` count — the exact
quantity CLAUDE.md's BK-33 trap forbids scoring on.** Pre-registering an
expected assertion count against these scripts is unfalsifiable: nothing
observable can contradict it. A whole `describe`-equivalent block could be
deleted and the summary line would not change by one character.

### ✅ RESOLUTION — R5-2a, and it is three lines per script

Add a pass counter and print it. **BK-53 does this to the two scripts it owns;
BK-55 does the same to `verify-booking-email.ts`. No seam.**

```ts
let failures = 0;
let passes = 0;
function check(condition: boolean, message: string) {
  if (!condition) { console.error(`  ✗ ${message}`); failures++; return; }
  passes++;
}
...
// BOTH branches carry the number — R5-11 A9. A red run is exactly when
// "score on (exit code, summary line)" needs a count, and the original
// version of this snippet printed one only on success.
if (failures > 0) {
  console.error(`\n✗ ${passes} passed, ${failures} failed\n`);
  process.exit(1);
}
console.log(`\n✓ cutover checks passed — ${passes} passed, 0 failed\n`);
```

🔴 **A scalar count is not enough on its own (R5-11 A9).** `verify-cutover.ts`'s
sweeps are **one** `check(offenders.length === 0)` over a filtered array — the
count is **1 whether the filter read 40 files or 0**. And a scalar detects only
**net** deletion: drop an arm, add any `check(true, …)`, and count, exit code and
summary line are byte-identical. **→ Every sweep additionally asserts its
SCANNED-FILE COUNT is non-zero and within a pre-registered range.** A file-count
assertion is the only thing that distinguishes *"nothing offended"* from
*"nothing was read"*.

**Do this FIRST, as the first commit of the ticket, before any copy moves** —
the pre-registered numbers in R5-6 are meaningless until the scripts can report
against them. It is additive, it cannot redden anything, and it is the only way
the red-first evidence in this ticket is worth reading.

⚠️ **Count assertions RUN, not `check(` call sites.** Several calls sit inside
`forEach`/`for` loops — `verify-booking-pricing.ts:73-88` runs its body three
times, once per tier — so the call-site count (**41**, per C-f) is *not* the
runtime count, and the two must not be conflated in the pre-registration table.

---

## R5-3 · SCOPE — what is in, what left, and where it went

~~**Nothing below is dropped. Every item of revision 4 has a row.**~~
🔴 **THAT CLAIM WAS FALSE WHEN WRITTEN — see R5-10.** The traceability audit of
this revision found **six** revision-4 items with no row, plus seven older ones.
All are dispositioned in **R5-10**, which is part of this spec, not a note on it.
*(Per the 2026-09-05 trap: a row that disappears between two versions of a plan
is a decision nobody made — and a revision that asserts it dropped nothing is
how the fourth one did it anyway.)*

### IN — BK-53, the web half

| # | Item | From |
| --- | --- | --- |
| W1 | The terms box rewrite — `FEE_TERMS_*` constants, whole (**seam rule 1**) | rev 3 §1, decision 18 |
| W2 | `FEE_TERMS_ACK_LABEL`, with R4's *"before anyone comes out"* correction | rev 3 §1, R4 C1 |
| W3 | The tier picker — `LEGEND`, new `NOTE`, `DESCRIPTIONS`; `NAMES` unchanged | rev 3 §1, decisions 15/17/20 |
| W4 | `book.astro` — eyebrow `:94`, meta description `:36-37`, JSON-LD `:49`, `FEE_TERMS_PAYMENT` `:143`, **`FEE_TERMS_REFUND` `:146-148`, `FEE_TERMS_CREDIT` `:149`**, `<title>` + WebPage `name` (R3-SF4) | rev 3 §1, R4 restored, **R5-10 T4** |
| W5 | `ContactSection.astro` — eyebrow `:287`, `:347`, `:349`, `:351`, **`:354-356`, `:357`** **(at `src/sections/`, C-a)** | rev 3 §1, **R5-10 T4** |
| W6 | `BookingForm.svelte` — the `{#if quote}` block `:1198-1217`, the tier-price span `:1171-1175`, **`:1122-1124`, `:1125`, and `:780-782`'s inverted comment** | rev 3 §1, **R4-2**, **R5-10 T4/T9** |
| W7 | `llms.txt.ts:54`, with R4's C1 correction | rev 3 §1, decision 22 |
| W8 | `RECEIVED_NEXT_STEPS[1]`/`[2]` → `BookingConfirmation.svelte:149` | rev 3 §1 |
| W9 | `[service].astro:79` — the travel-fee clause **(one template, C-g)** | **R4-6** |
| W10 | `[id].astro:825-828` + `:1835` weekend hints — **on `bk51-gated`** | R4, decision 21, C-h |
| W11 | `AFTER_HOURS_NOTE` `:597`, **`FEE_TERMS_PAYMENT` `:459-464` — the WHOLE array, `[0]`/`[1]`/`[2]` deleted and `[3]` replaced by terms item 6**, `QUOTE_HEADING` `:600`, `QUOTE_TIMING_NOTE` `:612-613`, `FEE_TERMS_CREDIT` `:525` — deletions | rev 3 §1, decision 21, **R5-10 T1/T2** |
| W12 | The coupling pin + positive twin, re-derived | **R4-4**, **R4-5**, rev 3 §2a |
| W13 | `verify-cutover.ts` reddened arms — Classes 1/2/3 | rev 3 §2b, R4 restored |
| W14 | `verify-booking-pricing.ts:73-88` — R3B-B2's surgical edit | rev 3 §2b, C-f |
| W15 | **Every `verify-booking-email.ts` arm that reads a `booking-copy.ts` symbol** | **seam rule 1 — NEW** |
| W16 | The assertion-count fix, two scripts | **R5-2a — NEW** |
| W17 | The tier field's optional/required contradiction — `booking-form.ts:273-275` **(C-b)** | R3-SF9 |
| W18 | `/book/payment-cancelled/` + `/book/confirmed/` — **KEEP**, reasoning per rev 3 §3 | rev 3 §3, R3B-C1 |
| W19 | The drain probe, widened | R4 restored |
| W20 | **`PAYMENT_CANCELLED_LEAD` `:777-778` — the last unqualified payment-link claim, on a fully static public route.** FIXED, not merely kept | R2-SF2, **R5-10 T5** |
| W21 | **The three defects ROADMAP assigns to *"the prices-off copy ticket"*** — `booking-payment.ts:288`, `TRAVEL_FEE_*` `:123-124`, `booking-copy.ts:437` | `ROADMAP:881-890`, **R5-10 T6** |
| W22 | **`verify-booking-admin.ts:1161-1167` — classified KEEP/Class 0**, stated so it is not swept | R4 decision-21 list, **R5-10 T7** |
| W23 | **`ASSESSMENT_TIER_NOTE`'s position pin written FRESH**; AC11's pair deleted with `AFTER_HOURS_NOTE`. **Re-pointing forbidden** | R3B-D4, **R5-10 T8** |
| W24 | **The coupling pin's per-constant scoping and its named exempt list** | **R5-10 T3** |
| W25 | 🔴 **`WEB_AMOUNT_BAN` — decision 25 enforced. It had NO pin at all, and the coupling pin made adding `$150` read as MORE compliant** | **R5-11 A1** |
| W26 | **Six live positive pins that require the deleted payment copy** — `verify-booking-email.ts:373`, `:377`, `:384`, `:396`, `:419`, `:427`, all Class 1 | **R5-11 A6** |
| W27 | **`verify-booking-email.ts:554-563`'s index-addressed `mustNotCarry`** — REWRITTEN, never re-indexed; nobody owned it under the specs as written | **R5-11 A5** |
| W28 | **`verify-cutover.ts:584-588` and `:546-549`** — `outroAt` re-anchored off the deleted `FEE_TERMS_CREDIT` | **R5-11 A11** |

### OUT — moved, with its destination named

| Item | Went to | Why |
| --- | --- | --- |
| **R4-1** — the request email's computed price (`booking-email.ts:1159-1168`), and `verify-booking-email.ts:2013-2045`'s fixtures | **BK-55 §1** | Computed in `booking-email.ts`, from `assessmentQuote` — no `booking-copy.ts` symbol involved. Seam rule 2 |
| **R4-3** — the `resend.ts` suppression | **BK-55 §3** | Write path, `resend.ts`. Seam rule 2 |
| **R4-3(a)** — terms snapshot at insert | **BK-54** | Insert path. Not a gate on either half |
| `APPROVED_*` terms wording (decision 19) | **BK-55 §4** | Constants live in `booking-email.ts:738-771`, not `booking-copy.ts` |
| `FEE_TERMS_PAYMENT`'s `slice(0, 2)` index addressing (R1-D5) | **BK-55 §4** | `booking-email.ts:1151`. **And it must land before W1** — seam rule 3 |
| `booking-email.ts:1167` (customer) + `:1214` (office) weekend claims | **BK-55** | Decision 21's surface list, email half |
| The owner-authored line on the confirmation email | **BK-56** | Decision 25's second channel; does not exist. Not a gate |
| `insurance-claims.astro:70`, `services.ts:389`, `services.ts:291` | **ROADMAP Known traps, HIGH, unowned** | Decision 20 falsifies them; deliverable claims on unrelated pages. **Do not fix inline** |
| `new.astro:192` *"Payment comes before dispatch"* | **ROADMAP Known traps, unowned** | Decision 17 reverses it |
| 🆕 **The refund / no-charge constant family** — `REFUND_TIMING_LINE` `:166-167`, `REFUND_CHASE_LINE` `:192-193`, `REFUNDED_HEADING` `:138`, `REFUNDED_LEAD` `:140-141`, `CANCELLED_REFUNDED_LEAD` `:134-135`, `refundedAmountLine()` `:153-155`, `PAID_IN_FULL_LINE` `:256-257`, `NO_CHARGE_LINE` `:270`, `PAYMENT_ATTENTION_RULE` `:823-824`, `DECLINED_LEAD` `:704-705`, `EXPIRED_REQUEST_LEAD` `:784-785`, `PAYMENT_EXPIRED_LEAD` `:828-829` | **BK-55, needs a per-constant verdict** | 🔴 **NEW — no revision gave these a verdict.** They are in `booking-copy.ts`, so seam rule 1 puts the *edit* here; but every one of them renders **only** into email and every one is about a **real** refund of a **real** charge, which still happens on the hand-charged path. **BK-53's verdict is KEEP-UNCHANGED for all twelve**, and BK-55 must confirm that per constant rather than inherit it. `PAYMENT_EXPIRED_LEAD` is the one to look at hardest: *"was not paid for by the deadline, so we have released it"* — about a visit the site now calls free |

### Still open — one, not blocking

Whether the four `[service].astro` pages carry the **free** claim. **Answered
enough to act:** GA4 measured 2026-09-05 that the homepage takes ~92% of paid
sessions and the four service pages take 23 of ~1,204 between them, so the
premise that made this urgent is false. **Default stands: the free claim is NOT
added to them in this ticket.** W9 adds only the travel-fee qualifier, because
R4-6 requires it for a phrase already live there.

---

## R5-4 · THE COPY — approved (decision 26), corrected (decisions 25, R4-C1)

⚠️ **Decision 25 is absolute: no number appears on any web surface.** Every
draft below is checked against that. The *existence* of the fee is disclosed;
the **$150 is not**, anywhere.

### The terms box · `FEE_TERMS_*`

`FEE_TERMS_HEADING` (*"Assessment terms"*) **stays** — the checkbox makes a
claim about it. Five items, then `CANCEL_LINE` as the closing line.

1. *"The on-site assessment visit is free."*
2. *"If your address is outside our service area, a travel fee may apply. We
   tell you the amount when we confirm your appointment."*
3. *"You get our findings in person at the visit. If you want to go ahead, we
   follow up with a quote — that is where any report options and prices are."*
4. *"There is no obligation to go ahead."*
5. *"We never take money without telling you the amount first — any charge is a
   link you choose to pay."*
6. 🆕 *"If we ever ask you to pay for something before the visit, we tell you
   the amount and the date. If it is not paid by then we may release the time —
   and you are welcome to book again."*
   ⚠ **ADDED BY R5-10 (T2).** This **replaces `FEE_TERMS_PAYMENT[3]`**; it is
   the **only pre-consent statement that a slot can be released**, and R3-SF12
   showed that `APPROVED_*` cannot carry it because that email is itself the one
   asking for money. **Do not drop it as redundant with decision 19.**
7. **`CANCEL_LINE`** — *"To cancel or reschedule, call or text …"*
   ⚠ **Not optional.** `FEE_TERMS_REFUND[4]` **is** `CANCEL_LINE`
   (`booking-copy.ts:509`); on the homepage card, `/book/` and the island the
   terms box is its **only** web render. **(R3-BL-3)**

🔴 **`FEE_TERMS_PAYMENT` IS DELETED — all four members, each with its own
verdict (R5-10 T1).** Revision 5 originally named only `[1]`, and `[0]`/`[1]` are
what `feeTermsPaymentFor` hands to the **confirmed** arm — *the one message the
free customer keeps*. ⚠️ **The array is deleted, not shortened, so BK-55's
`slice(0, 2)` conversion must land replacement lines rather than re-address the
old ones — removing the export breaks `booking-email.ts`'s import.**

⚠️ **Item 5 is true today**: `booking-payment.ts:374` uses `mode: 'payment'`
with no `setup_future_usage` and no stored customer. **(R3-BL-6)**

**`FEE_TERMS_ACK_LABEL`** ⇄ *"I understand the assessment terms above, including
that a travel fee may apply and that I will be told the amount when we confirm
my appointment."*
⚠️ **R4-C1 applied** — revision 3's *"before anyone comes out"* is decision 22's
struck *"never after the visit"* in other words. Keeps the *"above"* reference
form, so that markup claim and its existing pin survive.

### `FEE_TERMS_ITEMS` — figures stripped, array **KEPT**

🔴 **DO NOT DELETE. (R3-BL-2.)** `booking-email.ts` renders
`ASSESSMENT_TIER_NAMES` only; `ASSESSMENT_TIER_DESCRIPTIONS` renders on **one**
surface, `BookingForm.svelte:1178`. **This array is the ONLY path by which the
3–5 day lab turnaround — a client requirement of 2026-08-18 — reaches any
email.** ⇄ **R4-C4:** it is not *replaced* by the six numbered items; it renders
**after** them in the same `<ul>` (`booking-email.ts:526`, `:606`).

1. *"The assessment visit — a technician walks the affected areas and talks you
   through what they find. **Free.**"*
2. *"A written cause-of-loss report **and a repair estimate**: the documentation
   your adjuster works from if you are filing a claim. **Quoted with your job.**"*
3. *"A measured sketch **and diagram** of the affected areas, alongside the
   report **and estimate**. **Quoted with your job.** Where lab samples are
   taken, **results take 3–5 business days**."*

⚠️ *"and estimate"* / *"and diagram"* restored deliberately — **dropping them is
the exact edit BK-36 made and plan review reverted (R3-SF3).**

### The tier picker

- **`ASSESSMENT_TIER_LEGEND`** (`:589`, **C-c**) ⇄ *"What do you think you need?
  (optional)"* — ⚠ **and W17 must make it actually optional.**
  `booking-form.ts:273-275` 422s without a tier. **If optional is out of scope,
  the legend does NOT get the word** — copy and validation must not disagree.
- 🆕 **`ASSESSMENT_TIER_NOTE`** *(confirmed absent from `booking-copy.ts`)* —
  *"The visit is free whichever you pick. Picking one just tells us what to
  prepare; anything written comes later, with your quote."*
- **`ASSESSMENT_TIER_NAMES`** (`:565-569`, **C-c**) — **UNCHANGED.**
- **`ASSESSMENT_TIER_DESCRIPTIONS`** (`:580-586`, **C-c**) ⇄ rewritten;
  🔴 `standard`'s current *"writes up the scope of the damage"* is **FALSE**
  under decision 20:
  - `standard`: *"A technician walks the affected areas and talks you through
    what they find."*
  - `report`: *"You also want a written cause-of-loss report and a repair
    estimate — what your adjuster works from. Quoted with your job."*
  - `sketch`: *"You also want a measured sketch and diagram, alongside the
    report and estimate. Quoted with your job. Where lab samples are taken,
    results take 3–5 business days."*

### The remaining web surfaces

| Surface | Draft |
| --- | --- |
| `book.astro:94` + `ContactSection.astro:287` eyebrow | ⇄ **R5-10 T11:** *"Free on-site assessment — travel fee may apply further out"*. **The eyebrow carries its OWN qualifier or does not carry the claim.** R3-SF6 asked for same-**sentence**; "same card" is not that, and A2's per-block test is a backstop, not the guard |
| `book.astro:36-37` meta description **+ JSON-LD `:49`** | ⇄ **R5-10 T10:** *"Free on-site restoration assessment in Edmonton — about 30 minutes, no obligation. A travel fee may apply further out; we tell you the amount first."* — **151 characters.** ⚠️ The earlier draft measured **171**, and a ~155-char SERP cut landed on *"…we tell you the amount \|"*, putting the operative half of the qualifier outside the snippet. **PIN THE LENGTH, not just the presence** |
| `book.astro` `<title>` + WebPage schema `name` | **R3-SF4, restored.** `book.astro:20-33` carries a recorded rationale for leaving the SEARCH surface to BK-29's site-wide sweep. **That rationale still holds and the verdict is KEEP** — but it is now stated rather than inverted in silence |
| `FEE_TERMS_INTRO` (`:367-368`) ⇄ **branch-agnostic (R3-BL-1)** | *"We review every request before confirming it. If we can take it, we get in touch to confirm your appointment — and if a travel fee applies to your address, we tell you the amount then."* |
| `RECEIVED_NEXT_STEPS[1]` (`:639`) | *"If we can take it, we get in touch to confirm — and tell you about a travel fee if one applies to your address."* |
| `RECEIVED_NEXT_STEPS[2]` (`:640`) | *"Once it is confirmed, we email you the details and a calendar invite."* |
| `llms.txt.ts:54` ⚠ **claim + qualifier in ONE sentence** | *"Pick a time for an on-site assessment — about 30 minutes, and the visit itself is free, though a travel fee may apply outside the usual service area and we tell you the amount when we confirm your appointment. Booking online sends a request: the office reviews it and gets in touch to confirm. Nothing is handed over in writing at the visit; if the customer wants to go ahead, a quote follows, and that is where report options appear. This is the way to request a quote."* ⚠️ **R4-C1 applied** — revision 3's *"confirmed before the visit"* re-printed what decision 22 struck, on the surface an LLM quotes bare |
| `[service].astro:79` **(W9, one template → 4 pages)** | *"About 30 minutes, no obligation. A travel fee may apply outside our service area."* — **the minimum that makes an existing live phrase honest.** Not the deferred question |
| `QUOTE_TIMING_NOTE` (`:612-613`) + `QUOTE_HEADING` (`:600`) | ⇄ **DELETED, not redrafted** — both render **only** inside `BookingForm.svelte:1198-1217`, which goes **(R3-SF1)** |
| `AFTER_HOURS_NOTE` (`:597`) + `FEE_TERMS_PAYMENT[1]` (`:461`) | ⇄ **DELETED** — decision 21 |
| `FEE_TERMS_CREDIT` (`:525`) | **DELETED** — there is no credit |
| `[id].astro:825-828`, `:1835` — **`bk51-gated`** | **DELETED** — decision 21. ⚠️ `:1781` is the **payment-deadline** hint and must survive **(C-h)** |
| `PAYMENT_CANCELLED_*`, `PAID_*` | ⇄ **UNCHANGED — W18** |

---

## R5-5 · THE GATE

### 5a · The coupling pin, re-derived around the noun the copy standard mandates

> **A surface that claims the assessment is free, OR releases obligation, must
> carry the travel-fee qualifier. Claim and qualifier travel together, or red.**

🔴 **R4-4 was right and revision 3's shapes stay wrong until this lands.**
R3-BL-4 mandated *"ONE canonical phrase, and the noun is **visit**"*, and §2a
then anchored every regex on **"assessment"**. Three of the approved drafts
match nothing: `ASSESSMENT_TIER_NOTE` (*"The visit is free whichever you pick"*),
`llms.txt.ts:54` (*"the visit itself is free"* — **and it is the surface most
likely to be quoted out of context**), and `FEE_TERMS_ITEMS[0]` (*"…what they
find. **Free.**"* — shape 1 needs `free` **before** `assessment`, and the `.` is
excluded by `[^.<>]`).

```ts
const FREE_CLAIM_SHAPES = [
  // the noun the copy standard mandates, both grammars, both orders
  /\bfree[^.<>]{0,40}?\b(?:visits?|assessments?)\b/i,
  /\b(?:visits?|assessments?)\b[^.<>]{0,40}?\b(?:is|are|'s|itself is)\s+free\b/i,
  // the standalone predicate — FEE_TERMS_ITEMS[0]'s "…what they find. Free."
  /(?:^|[.>])\s*free\.\s*(?:$|[<'"`,])/im,   // the trailing quote — R5-11 A7
  // obligation-release, decision 23 — PLURAL AND HYPHEN VARIANTS ARE THE POINT
  /\bno[\s-]+obligations?\b/i,
  /\bwithout\s+obligation\b/i,
  /\bno\s+(?:commitment|strings|cost|charge)\b/i,
  /\b(?:nothing is charged|not charged|nothing to pay|at no cost)\b/i,
  /\b(?:don'?t|do not|won'?t)\s+charge\b/i,
  /\b(?:pay nothing|costs? you nothing|complimentary|waived?)\b/i,
  /\$\s*0(?:\.00)?\b/,
];
const TRAVEL_QUALIFIER = /\btravel\s+fees?\b/i;  // \s+ and s? — R5-11 A10
```

⚠️ **`no[\s-]+obligations?` — the `+` and the `s?` are both load-bearing.**
A one-character edit (*"no obligations"*) turns a `no[\s-]obligation` pin off,
and Prettier reflow inserts a second space. **This is BK-48 verbatim, in the
newest pin in the ticket.**

**Five corrections that make it non-vacuous:**

- **A1 — scope follows where the FINAL copy lands.** §R5-4 puts claims as
  **inline literals** into `book.astro`, `ContactSection.astro` and
  `[service].astro`. **Scan: `booking-copy.ts`, every file in R5-4's table,
  `BookingForm.svelte:780-782` (R5-10 T9), and `dist/`.**
- 🔴 **A1a — `booking-copy.ts` is scanned PER CONSTANT, with a named exempt
  list. (R5-10 T3, and it is not optional.)** Scanned whole, this pin reddens
  `NO_CHARGE_LINE` — which R5-9's A3 promises to leave unchanged. **Two rows of
  this revision contradicted each other**, which is round 1's B2 in its fifth
  incarnation. The per-constant form is the pattern
  `verify-booking-email.ts:368-372` already uses and R3B-C2 already made binding.
  **The principle that decides membership: the qualifier is owed where the
  customer has not yet been told, and not where they have.** Exempt, each with
  its reason in the code: `NO_CHARGE_LINE` (confirmation email — the fee is
  already settled and disclosed), `DECLINED_LEAD`, `EXPIRED_REQUEST_LEAD`,
  `PAYMENT_EXPIRED_LEAD` (facts about a terminated booking, not offers).
  ⚠️ **Assert the exempt list is non-growing.** An exemption is how a ban dies
  quietly.
- **A2 — proximity is tested AT SOURCE, per rendered block**, not whole-file.
  Whole-file on `dist/index.html` passes with the claim in the hero and the
  qualifier in a component hundreds of lines away. `dist/` stays a **presence**
  backstop only, and its comment must say so. ⚠️ **The supporting quotation in
  revision 3 was fabricated — see C-e.** `verify-cutover.ts:440-453` does assert
  the terms constants sit in the CTA arm and **never** the form arm, and it is
  worth keeping for that reason alone; it carries no comment about having
  shipped green.
- **A3 — the `.js` arm is excluded from the coupling check** (kept for the plain
  ban): `bundles` is `dist/client/**` filtered to `.js` and `.html`
  (`verify-cutover.ts:972-977`), and chunk co-location is a Vite decision nobody
  reviews.
- **A5 — the POSITIVE TWIN.** The coupling pin is a *conditional* and passes
  vacuously: **delete the eyebrow entirely and every gate stays green.** Assert
  `dist/client/book/index.html` and `dist/client/index.html` each carry a free
  claim **and** a qualifier.
- 🆕 **A6 — the twin needs a SOURCE sibling.** `dist/` is populated by the
  sentinel build the script runs itself (`verify-cutover.ts:946`), and its own
  closing line warns the tree is a sentinel build. A twin that lives only in the
  `dist/` arm inherits every fragility A2 assigns to it. **Add the same
  present-and-qualified assertion over `src/sections/ContactSection.astro` and
  `src/pages/book.astro` at source.**

### 5b · 🔴 THE BANS — R4-5 WAS RIGHT AND NAMED THREE SITES. THERE ARE **FOUR**.

R4-5 re-scoped `:512-517`, `:1056-1071` and `:1188-1193`. **It omitted
`verify-cutover.ts:471-477`, and that is the one that fails first.**

| Site | Assertion | Scope | What the approved copy does to it |
| --- | --- | --- | --- |
| **`:471-477`** 🆕 | `!/no obligation/i.test(prose)` over `book.astro` and `src/sections/ContactSection.astro` | **whole file** — `stripForUse(readFileSync(...))`, frontmatter included | 🔴 **RED** — off `book.astro:36-37`'s new **frontmatter** meta description, which this arm reads and `:512-517` does not. ⚠️ **R5-11 A8 corrects the reason:** decision 23 puts the terms-box sentence in **`booking-copy.ts`**, which *neither* ban reads. The finding stands; two of its three original reasons did not |
| `:512-517` | `!/free\s+assessment/i.test(template)` over the same two files | **template only** — `templateOf(raw, file)` | ⚠️ **GREEN — and this row said RED, wrongly (corrected by R5-11 A8, by execution).** `\s+` is literal whitespace and *"Free **on-site** assessment"* puts `on-site ` in the gap. **It must be re-scoped BECAUSE IT IS VACUOUS against the copy that ships**, not because it fails. The frontmatter description escapes this arm entirely — the two bans have different scopes |
| `:1055-1071` | `BANNED_SHAPES` over every `dist/client/**/*.{js,html}` | built output | 🔴 **RED** — `/\bfree[^.<>]{0,40}?assessments?\b/i` matches *"Free on-site assessment"* in `dist/client/index.html` and `dist/client/book/index.html`, **the two files A5's twin requires to carry it** |
| `:1188-1193` | `BANNED` over `dist/client/llms.txt` | built output | ⚠️ **GREEN — and that is the problem.** The draft reads *"an on-site assessment — about 30 minutes, and the visit itself is free"*; the gap between *assessment* and *is free* is far past `{0,24}`, so **neither today's ban nor a naive coupling pin sees the surface most likely to be quoted bare.** Fixed by the re-derived shapes in 5a, not by the ban |

**→ All four are re-scoped to the coupling form — claim permitted WITH
qualifier, red without — in the SAME commit as the copy.** ⚠️ **`:1188-1193`
must be re-pointed even though it is green today**; leaving it is the
negative-assertion trap in its BK-48 shape — *the pin is named for a category
and shaped for one claim*.

### 5c · R4-2 — the form prices every radio, and the gate positively requires it

**Verified.** `BookingForm.svelte:1171-1175` sits in its **own** `{#if tierQuotes}`
inside `{#each ASSESSMENT_TIERS}` (`:1154-1182`) — **not** inside the
`{#if quote}` block at `:1198-1217` that R5-4 deletes:

```svelte
1169  <span class="flex flex-wrap items-baseline gap-x-2">
1170    <span class="font-semibold text-yeg-text">{ASSESSMENT_TIER_NAMES[tier]}</span>
1171    {#if tierQuotes}
1172      <span class="text-yeg-text font-semibold whitespace-nowrap">
1173        {formatCents(tierQuotes[tier].baseCents)} + GST
1174      </span>
1175    {/if}
```

🔴 `verify-cutover.ts:594-598` **positively requires it** —
`island.includes('formatCents(tierQuotes[tier].baseCents)')`, as the third
clause of a triple-AND — and revision 3's neighbouring instruction is *"KEEP
`verify-cutover.ts:600-603`"*, so an implementer reads the whole region as a
keep.

**→ Delete `:1171-1175`. Re-point `:594-598` to assert the amounts are ABSENT
while the tier LABELS remain. `:600-603` stays** (*"the island contains no price
arithmetic of its own"* — **more** valuable once no price renders, and its
opening `check(` sits inside a range revision 2 told an implementer to rewrite,
R3B-D3).

### 5d · The reddened assertions — classified before anything is touched

🔴 **`verify-booking-pricing.ts:73-88` is NOT deleted as a block. (R3B-B2.)**
The same `ASSESSMENT_TIERS.forEach` holds `copy.includes(expected[tier])` at
`:76` **and** the absolute pin
`formatCents(tierDefaultCents(tier)) === '${expected[tier]}.00'` at `:84` —
**verified as the only absolute-value pin on `tierDefaultCents` in the repo**
(`:107`, `:119`, `:126` are all relative). Deleting the block lets
`TIER_PRICE_CENTS` drift `39900 → 3990` **fully green**.
**Keep `expected` and the `forEach`; delete only the two `copy.includes(...)`
checks, the length zip, and the import.**

⇄ **Class 2 (structure, re-point only) — six arms:** `verify-cutover.ts:347-357`,
`:395-416`, **`:440-453`**, `:571-579`, `verify-booking-email.ts:525-547`,
`:590-600`.
⇄ **Class 3 (subject genuinely gone):** `verify-booking-email.ts:374`, `:424`,
`verify-cutover.ts:605-619`.
🔴 **`verify-booking-email.ts:339-349` WAS in this list and has been MOVED TO
CLASS 2 (R5-11 A4).** `:346-349` is the **sole** assertion anywhere that
`CANCEL_LINE` closes the block, and R5-4 keeps `CANCEL_LINE` and calls it *"Not
optional"* — **the subject is not gone.** ⚠️ **This is one of the three arms
"moved Class 1 → Class 3 with no per-arm justification" that this revision warns
about without naming. It is now named. FIND THE OTHER TWO before implementation**
— an inherited warning nobody discharged is how this one survived four rounds.
⇄ **Class 1 (rewrite to assert the replacement) — RESTORED**, revision 3 stated
only Classes 2 and 3: `verify-booking-email.ts:246-254`, `:291-296`, `:304`,
`:311`, `:321`, `:638`; `verify-cutover.ts:364-372`, `:536-545`.
⚠ **Three arms were moved Class 1 → Class 3 with no per-arm justification —
re-justify or move them back.** *A deleted assertion is an unpinned claim.*
🔴 **Restored from round 1:** `verify-cutover.ts:571-579` (pins
`QUOTE_HEADING` and `AFTER_HOURS_NOTE` **present**, both deleted here — **D1**),
`verify-booking-email.ts:742-745` (the `$0` message's `!/\$[\d,]+\.\d{2}/`,
whose justifying comment cites the very figures being removed).

⚠️ **Every `verify-booking-email.ts` arm listed above reads a `booking-copy.ts`
symbol, so all of them are BK-53's under seam rule 1.** The arms that read
`assessmentQuote`, `APPROVED_*` or `resend.ts` are BK-55's. **If an arm reads
both, it is BK-53's** — the constant is the thing being changed.

**Rules that bind the implementer:**
- ⚠️ **DELETE constants; never EMPTY them.** An emptied array makes
  `for (const line of carries)` run **zero iterations and stay green having
  asserted nothing** (R1-B3).
- ⚠️ **Pin the orphaned readers' ABSENCE.** `tsconfig.json` extends
  `astro/tsconfigs/strict`, which does **not** set `noUnusedLocals`;
  `verify-cutover.ts:558-559` records orphaned imports raising no error when a
  fieldset was deleted. Once `AFTER_HOURS_NOTE` and the quote block go, `quote`,
  `tierQuotes`, `assessmentQuote` and `formatCents` are dead in the island.
- ⇄ **Scope the payment-link ban PER CONSTANT**, the pattern
  `verify-booking-email.ts:368-372` already uses — a whole-file ban on
  `booking-copy.ts` cannot exclude `PAYMENT_CANCELLED_LEAD`, which lives in it
  (R3B-C2).
- ⇄ **§5a's `/\bEverything above\b/i` is replaced by a positive twin:** each
  non-`standard` description must match `/quoted with your job/i`. The old ban
  was shaped for a sentence being deleted, not for the claim (R3B-D5).

---

## R5-6 · PRE-REGISTERED COUNTS AND RED-FIRST

**R5-2a lands first**, or none of this is measurable.

| Script | Assertions run, before | Expected after | Delta and why |
| --- | --- | --- | --- |
| `verify-cutover.ts` | ☐ record from the R5-2a run | ☐ pre-register **before** editing copy | Class 3 removals + the coupling pin + twin + A6 sibling |
| `verify-booking-pricing.ts` | ☐ record | ☐ pre-register | −2 per tier × 3 tiers (`copy.includes`) − 1 length zip. **41 call sites, C-f — but the runtime count is higher: `:73-88` runs three times** |
| `verify-booking-email.ts` | ☐ record | ☐ pre-register | Class 1/2/3 over `booking-copy.ts` symbols only; BK-55's arms are counted in BK-55 |

🔴 **Fill these in from the R5-2a run BEFORE touching any copy, and paste the
run's summary line here.** A number read off the post-change run cannot
contradict that run — that is the whole of R4's *"not falsifiable as written"*.

**Red-first — one entry per failure mode, per CLAUDE.md:**

| # | Break the PRODUCTION target | Expect | red-observed |
| --- | --- | --- | --- |
| 1 | Remove the travel-fee clause from `ContactSection.astro`'s CTA card, leave the free eyebrow | coupling pin red | ☐ |
| 2 | Change *"no obligation"* → *"no obligations"* in `FEE_TERMS_*` | coupling pin red (`s?` arm) | ☐ |
| 3 | Delete the free eyebrow entirely from both pages | **positive twin** red (the conditional alone passes vacuously) | ☐ |
| 4 | Re-add `{formatCents(tierQuotes[tier].baseCents)}` to the island | re-pointed `:594-598` red | ☐ |
| 5 | Drift `TIER_PRICE_CENTS` `39900 → 3990` | `verify-booking-pricing.ts:84` red | ☐ |
| 6 | Empty `FEE_TERMS_ITEMS` to `[]` rather than editing it | an arm red — **if nothing reddens, the DELETE-never-EMPTY rule has no pin** | ☐ |
| 7 | Restore *"credited in full"* to `llms.txt.ts:54` | `:1188-1193` re-pointed, red | ☐ |
| 8 | Drop the travel clause from `[service].astro:79` | coupling pin red on all 4 built pages | ☐ |
| 9 | 🔴 **Add `$150` to terms item 2** | **`WEB_AMOUNT_BAN` red (W25).** Today NOTHING catches this, and the coupling pin reads it as *more* compliant | ☐ |
| 10 | Delete the eyebrow from `ContactSection.astro:287` only | **A6's source twin red.** The `dist/` twin alone stays GREEN — the terms box on the same page carries the claim | ☐ |
| 11 | Delete the travel sentence from the terms box AND the ack label, leaving `FEE_TERMS_INTRO` | **per-constant coupling pin red.** Under a whole-file scan every arm is green and decision 17a is undone | ☐ |
| 12 | Drop `CANCEL_LINE` from the rewritten terms array | **re-pointed `:339-349` red (Class 2).** As Class 3 it was deleted and this ships green | ☐ |
| 13 | Reflow `[service].astro:79` so `travel` ends a line | **must stay GREEN** — `\s+`. Under `/\btravel fee\b/` the pin goes blind while the customer still reads it | ☐ |
| 14 | Point a sweep's file list at a directory that does not exist | **scanned-file-count assertion red.** Against `offenders.length === 0` alone it passes having read nothing | ☐ |
| 15 | Delete one coupling arm and add a `check(true, …)` | **pre-registered count must still catch it** — if it does not, the count is measuring net change and A9 is unfixed | ☐ |
| 16 | Move the tier picker above the whole terms box | **re-anchored `:584-588` red.** With `outroAt = -1` the middle clause is vacuously true | ☐ |

⚠️ **Assert the break LANDED** before scoring it. **Score on (exit code,
summary line)** — which R5-2a is what makes possible. **Restore from a file
backup**, never `git checkout --`, which reverts to HEAD and eats uncommitted
work. **Clear stranded probe rows first** after any interrupted run.

---

## R5-7 · SEQUENCING, THE DRAIN, AND THE PUSH

**Commit order on `bk51-gated`** — seam rule 3 fixes it:

1. BK-51 (already on the branch)
2. **BK-55** — including the `slice(0, 2)` → named constants conversion
3. **BK-53** — R5-2a first, then the copy + pins
4. **The adversarial pass, at the TIP, over the union** (seam rule 4)
5. One push

**On `main`, BK-53 alone is a priceless site with a live charging screen** —
`[id].astro:505-512` still pre-fills `assessmentQuote` and mints Checkout
Sessions, and the approval field shows **$399.00** at `:1776` while the Session
charges **$418.95**. **Never push this half alone.**

**Drain probe — run it, paste the output here, before the push:**

```sql
SELECT id, status, slot_start, payment_status, paid_amount_cents,
       refunded_amount_cents, payment_due_at, total_amount_cents
FROM appointments
WHERE status IN ('pending_review','approved_awaiting_payment','payment_expired')
   OR (status = 'confirmed' AND slot_start >= now() - interval '7 days')
ORDER BY status, slot_start;
```

⚠️ `approved_awaiting_payment` is the row that hurts — a live Checkout Session
and a deadline, untouched by a copy deploy, paid days later for a visit the site
now calls free. ⚠️ **The `confirmed` arm is the one R4 added and it is the
sharper one:** a `confirmed` row with a future `slot_start` is a customer who
**has already paid** for a visit about to be advertised free — `#36`,
`cs_live_`, **$628.43**. `pending_review` is the benign direction (decision 16
accepts those going free).

---

## R5-8 · TIER, AND THE BRIEF EVERY REVIEWER GETS

**Tier: Reviewed** — plan review + implementation review. Decision 24 drops the
adversarial pass from this half **because BK-55 carries it and seam rule 4 runs
it over the union at the branch tip.** If seam rule 4 is not adopted, **this
ticket goes back to Reviewed + adversarial** — the email renders of the
constants it edits are otherwise uncovered.

🔴 **Every reviewer of this ticket MUST be briefed with all four of these:**

1. **The target branch is `bk51-gated`, not `main`,** and its approval path
   differs: nothing here is a PR, `git push origin main` is the deploy, and this
   branch is never pushed. *(Two of four earlier reviewers reasoned from `main`
   and produced findings false on the branch. A wrong line number fails loudly;
   a wrong premise reads as a confident finding.)*
2. **Of the files this ticket touches, only `[id].astro` differs between the
   branches** — for everything else, `main` and `bk51-gated` are byte-identical.
   `[id].astro` citations must be read via `git show bk51-gated:`.
3. **The copy is APPROVED (decision 26).** Do not re-litigate wording on taste.
   Do challenge it on truth, on decision 25 (**no number on any web surface**),
   and on whether a pin actually matches it.
4. **The reviewer receives the ticket, the diff and the gate output — nothing
   else.** Enumerate every nit; do not count them in a header.

---

## R5-9 · ASSUMPTIONS LOG — settled without the user this session

| # | Assumption | Basis |
| --- | --- | --- |
| A1 | The seam rule (R5-1) — `booking-copy.ts` whole to BK-53, `booking-email.ts`/`resend.ts` to BK-55 | Judgment call on a mechanical consequence of decision 24; preserves both of decision 24's stated reasons. **Reversible** — the alternative is moving the terms box to BK-55, which contradicts the user's own framing of BK-53 as the web half |
| A2 | The adversarial pass runs once at the branch tip over the union (seam rule 4) | Follows from A1; also strictly better than the alternative, since a pass against a mid-branch commit tests code that never ships |
| A3 | The twelve refund/no-charge constants are **KEEP-UNCHANGED** in BK-53, with BK-55 owing a per-constant verdict | They render only into email and describe real refunds of real charges, which the hand-charged path still produces. ⚠️ **Amended by R5-10 T3:** four of them are also named on the coupling pin's exempt list, which is what makes this assumption consistent with R5-5a instead of contradicting it |
| A6 | The coupling pin's exempt list (R5-10 T3) is drawn on *"has the customer been told yet"*, not on *"is this constant convenient to exempt"* | Judgment call. **The most reversible item here and the one most worth a reviewer's attack** — every exemption is a hole with a reason attached |
| A4 | `[service].astro` gets the travel qualifier and **not** the free claim | R4-6 requires the qualifier for a phrase already live; the free claim is the deferred question, and GA4 measured the premise behind its urgency false |
| A5 | `book.astro`'s `<title>` and WebPage `name` stay with BK-29's site-wide sweep | `book.astro:20-33`'s recorded rationale, restated rather than inverted (R3-SF4) |

## Status

**Revision 5 written 2026-09-05.** ✅ **Traceability audit — RUN (R5-10).**
✅ **Adversarial pre-read — RUN (R5-11).** Both were fresh agents; both findings
sets are folded in above **and applied in place**, not appended as notes.
🔴 **STILL NOT APPROVED. Owed: PLAN REVIEW** — a fresh agent, briefed per R5-8,
reading revision 5 including R5-10 and R5-11.
🙋 **And ONE item now needs the user** — R5-10's closing row: with the refund
block deleted, does the client want a stated cancellation-notice expectation?
It is policy, it is the only thing on this ticket that is blocked on a person,
and **the terms box is where the answer would live**, so it blocks shipping the
box rather than writing the spec.

**Four findings in this revision are NEW and in no review** — R5-1 (the split's
seam), R5-2 (no gate script emits an assertion count), and from the validation
passes **R5-11 A1** (decision 25 has no pin at all) and **R5-11 A5** (deleting a
`FEE_TERMS_PAYMENT` member makes an assertion pass on `undefined` and crashes its
sibling). All four were found by opening the file rather than by reading the
ticket, which is also how C-e and A8 were found.

🔴 **Two rows of revision 5 as first written were themselves wrong, and the
validators caught both:** R5-3's *"nothing below is dropped"* headline (six
revision-4 items had no row — R5-10) and R5-5b's claim that `:512-517` goes red
on the approved eyebrow (it does not; `\s+` does not span `on-site ` — R5-11 A8).
**The fifth consecutive revision to drop rows, and the first to be measured
doing it on the day it was written.**

⚠️ **The two validators converged independently on `FEE_TERMS_PAYMENT` and on
the `NO_CHARGE_LINE` contradiction, by different methods, within the hour.
Treat the convergence as the finding, not the redundancy** — it is the second
time on this ticket that it has happened.

---
## R5-10 · TRACEABILITY AUDIT OF THIS REVISION — 2026-09-05, fresh agent

**The audit ran on revision 5 the day it was written, per the standing rule. It
found thirteen dropped rows and one self-contradiction, and it was right about
twelve of them.**

🔴 **R5-3's headline — *"Nothing below is dropped. Every item of revision 4 has a
row"* — WAS FALSE when written.** Six items had no row. **That is the fourth
consecutive revision to drop rows from the round it was written to answer**, and
the first to claim in its own text that it had not. The claim is struck; what
follows is the enumeration that should have been there.

⚠️ **Read the disposition, not the count.** Every row below is now carried,
struck with a reason, or moved to a named destination that has been checked to
contain it.

### 🔴 Blockers this audit reopened

**T1 · `FEE_TERMS_PAYMENT` — three of its four members had no verdict. (R2-B1,
raised in round 2 and dropped by revisions 3, 4 and 5.)** Revision 3's heading
said the terms box *"replaces INTRO / ITEMS / **PAYMENT** / REFUND / CREDIT"*;
revision 5 deleted that phrase and named only `[1]`. Verified verbatim:

| Index | Text | Verdict |
| --- | --- | --- |
| `[0]` `:460` | *"**Those are our standard rates.** What you pay depends on the service you need and the time you pick — **the booking form shows that price** before you send your request, and **the approval email states the amount we will charge**."* | 🔴 **DELETE.** Every clause fails: *"Those"* dangles once `FEE_TERMS_ITEMS`'s figures go; *"the booking form shows that price"* is false the moment R4-2 deletes the radio prices; and *"the approval email states the amount we will charge"* is false on the free path |
| `[1]` `:461` | the weekend 1.5× sentence | **DELETE** — decision 21. Already W11 |
| `[2]` `:462` | *"Nothing is charged when you send your request. Once we approve it we email you **a secure payment link**, stating the amount and when we need it paid."* | **DELETE** — the payment-link claim this ticket opens with |
| `[3]` `:463` | *"If we do not have the payment in time, **we may release the time**, and you are welcome to book again."* | ⇄ **REPLACED, not deleted** — see T2 |

🔴 **`feeTermsPaymentFor` (`booking-email.ts:1150-1152`) hands `[0]` and `[1]` to
the CONFIRMED arm** — *the one message the free customer keeps*. Round 2 raised
this and predicted reviewers would miss it because they work from the table.
Three revisions later, they had.

⚠️ **Consequence for seam rule 3, and it is a real change:** the array is
**deleted**, not shortened. So BK-55's `slice(0, 2)` conversion is not a
refactor — **removing the export breaks `booking-email.ts`'s import**, and the
named-constants conversion must land the replacement lines, not merely re-address
the old ones. **BK-55 §4 is amended accordingly.**

**T2 · The slot-release pre-consent clause. (R3-SF12, explicitly restored by
revision 4, then dropped by revision 5 — and BK-55 cited it while implementing
the thing it rejected.)** R3-SF12's point was that putting terms in `APPROVED_*`
cannot repair a disclosure that first appears **in the email asking for money**;
the remedy is one clause in the terms box, which the customer sees *before*
consenting. Deleting `FEE_TERMS_PAYMENT[3]` removes the only pre-consent
statement that a slot can be released.

**→ Terms box item 7, and it replaces `[3]` rather than dropping it:**
*"If we ever ask you to pay for something before the visit, we tell you the
amount and the date. If it is not paid by then we may release the time — and you
are welcome to book again."*

⚠️ **BK-55 §4 must stop citing R3-SF12 as satisfied by `APPROVED_*`.** The
`APPROVED_*` disclosure is decision 19's fix for **Gap 2** and remains correct;
it is not R3-SF12's fix, and R3-SF12's fix is here, on the web surface.

**T3 · The coupling pin reddens a constant this ticket promises not to touch.**
🔴 **My own R4-5, reproduced in the fix for it.** R5-5a's
`/\bno\s+(?:commitment|strings|cost|charge)\b/i` matches `NO_CHARGE_LINE`
(`booking-copy.ts:270`, *"There is no charge for this visit."*); A1 puts
`booking-copy.ts` in the scan scope; and R5-9's assumption A3 declares
`NO_CHARGE_LINE` **KEEP-UNCHANGED**. **Two rows of the same revision contradict
each other** — *the gate forbids the copy this ticket exists to ship*, round 1's
B2, in its fifth incarnation.

**→ RESOLVED, and not by weakening the regex.** The coupling pin scans
`booking-copy.ts` **PER CONSTANT**, with a named exempt list carrying a reason
each — **the pattern `verify-booking-email.ts:368-372` already uses and which
R3B-C2 already made binding for the payment-link ban.** The principle that
decides membership:

> **The qualifier is owed where the customer has not yet been told. It is not
> owed where they have.**

| Exempt | Reason |
| --- | --- |
| `NO_CHARGE_LINE` | Renders on the **confirmation** email (`booking-email.ts:445`) — *after* the office has decided the travel fee on the confirmation call (decision 8) and told the customer. A qualifier there says *"a fee may apply"* to someone who already knows whether it did |
| `DECLINED_LEAD`, `EXPIRED_REQUEST_LEAD`, `PAYMENT_EXPIRED_LEAD` | *"Nothing has been charged"* — a statement of fact about a **terminated** booking, not an offer |

⚠️ **The exempt list is itself a pin.** Adding a name to it must require a
comment, and the list must be asserted non-growing — an exemption is how a ban
dies quietly.

### Carried in, each with a verdict it did not have

- **T4 · `FEE_TERMS_REFUND` and `FEE_TERMS_CREDIT`'s render sites (R1-A4, R1-A5,
  round-1 blockers raised because an earlier table omitted them).** Verified
  live: **`book.astro:146-148`/`:149`, `ContactSection.astro:354-356`/`:357`,
  `BookingForm.svelte:1122-1124`/`:1125`.** W4/W5/W6 stopped at `:143`/`:351`/
  `:1119`. **Added to all three rows.** Typecheck catches the deletion; reviewers
  work from the table, and the enumeration had regressed to the state round 1
  rejected.
- **T5 · `PAYMENT_CANCELLED_LEAD` is FIXED, not merely kept (R2-SF2).** Revision
  5 said `PAYMENT_CANCELLED_*` UNCHANGED and R3B-C2's per-constant scoping keeps
  the payment-link ban off it, so the site's **last unqualified payment-link
  claim** would ship live and unpinned — on a **fully static route anyone can
  reach by URL** (R3B-C1). `booking-copy.ts:777-778` currently reads *"The
  payment link in your approval email works until the deadline it gives."*
  **→ *"If we sent you a payment link, it works until the date in that email —
  open it again whenever you are ready. If you were not asked to pay for
  anything, nothing is outstanding and your request is still with us."*** The
  route KEEP verdict (W18) is unchanged; only the copy moves.
- **T6 · The three defects ROADMAP assigns to *"the prices-off copy ticket"* —
  which is this ticket.** `ROADMAP.md:881-890` names all three and revision 5
  carried none:
  - **`booking-payment.ts:288`** — the Stripe line item *"Round trip beyond the
    free radius"*, describing the retired per-km rule, now unreachable because
    nothing can set a non-zero travel fee. **Delete the branch or restate it for
    the flat fee; decide explicitly and say which.**
  - **`TRAVEL_FEE_CENTS_PER_KM` / `TRAVEL_FEE_FREE_RADIUS_KM`** (`booking-pricing.ts:123-124`)
    — dead exports naming a **retired** rule, not a paused one. ⚠️ **BK-51 pins
    that nothing in `src/` reads them.** Deleting them removes the pin that
    prevents reintroduction. **Verdict: keep the exports, keep the pin, and add
    a comment saying the rule is retired** — the pin is worth more than the
    tidiness.
  - **`booking-copy.ts:437`** — the comment justifying live customer copy with
    *"an editable amount **and a travel-fee field at approval**"*, whose second
    clause BK-51 made false (`review.ts:246`). **Seam rule 1 puts it squarely
    here.** Rewrite it with the constant it explains.
- **T7 · `verify-booking-admin.ts:1161-1167`, named by revision 4 in decision
  21's expanded surface list and mentioned by neither ticket.** Verified: it
  asserts *"a Saturday settled row still knows it is a weekend booking"* — the
  `afterHours` **flag** on `appointmentMoney`, not the surcharge copy.
  **Verdict: KEEP, Class 0 (untouched).** Decision 21 kills the 1.5× **price**;
  the flag is a fact about the slot, which is exactly the distinction
  `bk51-gated:[id].astro:822` draws in its own comment. **Stated so it is not
  swept.**
- **T8 · `ASSESSMENT_TIER_NOTE` and `AFTER_HOURS_NOTE` compete for one pinned
  position (R3B-D4, restored by revision 4, dropped by revision 5).** Verified —
  `verify-cutover.ts:610` takes `noteAt = island.indexOf('AFTER_HOURS_NOTE')` and
  `:617-619` asserts `radiosAt > noteAt`. Revision 5 deletes that constant and
  adds a new note in roughly the same place. **An implementer who re-points the
  `indexOf` inherits a position assertion written for a different constant for a
  different reason** — AC11's reason is *"a weekend price must be EXPLAINED where
  it is shown"*, and the new note explains no number. **→ Delete the AC11 pair
  with `AFTER_HOURS_NOTE` (Class 3), and write `ASSESSMENT_TIER_NOTE`'s position
  pin fresh, with its own reason in its own message string.** Re-pointing is
  forbidden here and the ticket says so.
- **T9 · `BookingForm.svelte:780-782` (R3B-A1's fifth inline site).** Verified:
  *"On-site assessment, about 30 minutes. All times are Edmonton time."* under a
  comment reading *"a bare 'free' two steps before the terms box is the
  contradiction the ticket exists to remove."* 🔴 **The free model inverts that
  rationale** — the same class as R3-SF4, which was restored for exactly this.
  **→ Added to W6 and to R5-5a's scan scope.** The sentence itself may stand;
  **the comment must not**, or the next editor re-derives a deleted policy from
  it.
- **T10 · The meta description truncates its own qualifier (R3-SF7).** Measured:
  the revision-5 draft is **171 characters**, and a ~155-character SERP cut lands
  on *"…we tell you the amount |"* — the operative half of the qualifier is
  outside the snippet, on the surface where the claim travels furthest. **→
  Rewrite to ≤155 with claim and qualifier both inside**, and **pin the length**,
  not just the presence:
  *"Free on-site restoration assessment in Edmonton — about 30 minutes, no
  obligation. A travel fee may apply further out; we tell you the amount first."*
  (**151 characters.**)
- **T11 · The eyebrow's rule is "same card"; R3-SF6 asked for "same sentence".**
  Revision 5 applied same-sentence to `llms.txt.ts:54` and `[service].astro:79`
  and left the eyebrow at *"only where the travel-fee clause follows in the same
  card"*, while R5-5a's A2 tests per rendered **block**. **A block is not a
  sentence**, and the eyebrow is the shortest, most quotable free claim on the
  site. **→ The eyebrow carries its own qualifier or does not carry the claim:**
  *"Free on-site assessment — travel fee may apply further out"*. Then A2's
  per-block test is a backstop rather than the only guard.

### Struck, with reasons — so they stop coming back

- **T12 · R2 Lane C (c), `ASSESSMENT_PRICES_SHOWN` / `assessmentTermsBlock()`.**
  🔴 **Verified: neither symbol exists anywhere in `src/` or `scripts/`.** Round
  2 proposed *creating* one as a greppable anchor for the "delete to a boundary"
  design, and warned that a dead boolean reads as a switch. **STRUCK: the anchor
  is served by the artifacts revision 5 actually builds** — the coupling pin, its
  positive twin, the named exempt list (T3) and the explicit KEEP lists. **Adding
  a constant whose only job is to be found is the dead weight round 2 objected
  to.** Recorded rather than left absent, because that is how it came back three
  times.
- **T13 · R3B-E3, the admin Travel field's "missing guidance" — RE-RAISED BY THE
  AUDIT AND REFUTED AGAIN.** Verified on the branch:
  `git show bk51-gated:src/pages/admin/appointments/[id].astro` **:1857-1867**
  carries *"Free in Edmonton, Spruce Grove, St. Albert, Beaumont and Leduc.
  Further out, a flat $150 may apply — **decide it on the confirmation call and
  tell the customer then.** … Settled outside this system. There is no field for
  it here, and an amount cannot be approved on this screen."* **That is more
  guidance than `main` carries, not less.** The audit's `:1744` is a different
  thing — the hidden `travel_fee` input on the confirm-amount form, which BK-51
  pins to zero. ⚠️ **`HANDOFF-2026-09-05.md` §5 already struck this finding as
  verified false, and it came back within a day inside a validation pass whose
  own brief said to verify branch claims with `git show`.** **Fifth instance of
  the answered-question-still-open trap** — and the first where the re-raiser was
  an agent explicitly told how to check. **Do not re-open without quoting
  `:1857-1867`.**

### The header-only findings — the count was wrong too

The ticket's header says **13** findings exist only as digits. The audit's
enumeration says **15**: round-1 nits ×4, round-2 nits ×5 **and 3 of its 7 user
decisions**, round-3 nits ×4 **and R3-SF10** *(the round-3 should-fix list jumps
from 9 to 11)*, and round 3B's ninth should-fix *(C2, D3, D4, D5, E2, E3, E4, E5
is eight)*. **Corrected to 15 throughout.** They remain unrecoverable; a reviewer
who finds something "new" still cannot tell it from a re-discovery.

### 🙋 One item goes back to the user — it is the only one

**A client question from round 3, dropped by every revision since and in no
"still open" list: now the refund block is being deleted, does the client want a
stated cancellation-notice expectation?** `CANCEL_LINE` survives and tells the
customer **how** to cancel; `FEE_TERMS_REFUND` was the only place that said
**how much notice** is expected, and it is going. **This is policy, not
judgment** — CLAUDE.md sends it to the user. **Not a blocker on writing the
spec; it is a blocker on shipping the terms box**, because the box is where the
answer would live.

## R5-11 · ADVERSARIAL PRE-READ OF THIS REVISION — 2026-09-05, fresh agent

**Question asked: *"assuming this ships exactly as written, what still ships
GREEN?"*** Six blockers, eight should-fix, ten nits. **It converged
independently with the traceability audit on two of them** — `FEE_TERMS_PAYMENT`
(its blocker 6 = R5-10 T1) and the `NO_CHARGE_LINE` contradiction (its blocker 3
= R5-10 T3), found by different methods within an hour of each other. That is
the second time on this ticket that two validators converged; **treat the
convergence as the finding, not the redundancy.**

**Every claim below was re-verified against the tree before being written down,
including the two that say this revision is wrong.**

### 🔴 A1 · DECISION 25 SHIPS WITH ZERO ENFORCEMENT — and the new pin makes the violation EASIER

**The single most important finding in either pass.** Decision 25 is the one
user instruction in this ticket marked **absolute**: *the $150 never appears on
any web surface.* **Nothing asserts it.**

**The break:** change terms item 2 to *"…a travel fee **of $150** may apply. We
tell you the amount when we confirm your appointment."* Or put `$150` in
`FEE_TERMS_ACK_LABEL` — **the sentence the customer legally ticks** — or in
`llms.txt.ts:54`, or in the meta description.

🔴 **The coupling pin PASSES, and passes harder.** The sentence contains a free
claim and `travel fee`, so **adding the number makes it more compliant.** All
four re-scoped bans pass — `BANNED_SHAPES` is shaped for `free…assessment`, not
for digits. `verify-booking-pricing.ts`'s surviving pin is on `tierDefaultCents`,
not on copy. And `verify-booking-email.ts:742-745`'s `!/\$[\d,]+\.\d{2}/` **does
not see `$150` even on the surface it guards** — no decimals. **BK-48, verbatim,
for the fourth time in this ticket's life.**

**→ W25: a `WEB_AMOUNT_BAN`, and it is a GATE, not a nicety.**

```ts
// Decision 25: no amount reaches a web surface. Currency-shaped, digit-shaped,
// and spelled-out — the number is the thing banned, not one of its spellings.
const WEB_AMOUNT_SHAPES = [
  /\$\s?[\d,]/,                                  // $150, $1,199, $ 150
  /\b\d[\d,]*\s*(?:dollars|CAD)\b/i,             // 150 dollars
  /\b(?:one|two|three|four|five|six|seven|eight|nine)\s+hundred\b/i,
  /\b\d[\d,]*\s*(?:\+\s*GST|plus GST)\b/i,       // 150 + GST
];
```

**Scope: every file in R5-4's table, plus `dist/client/**/*.html`.**
⚠️ **Two carve-outs, each named and commented, never a pattern:** the phone
number (`SUPPORT_PHONE`) and *"about 30 minutes"* / *"3–5 business days"*.
**Anything else matching is a build failure.** ⚠️ **Assert the carve-out list is
non-growing** — same rule as R5-10 T3's exempt list, for the same reason.
**Red-first row 9 (new): add `$150` to terms item 2 and watch it go red.**

### 🔴 A2 · A6's SOURCE TWIN IS UNSATISFIABLE ON ONE FILE AND SATISFIED BY A `<meta>` TAG ON THE OTHER

**Verified: there is no literal string `travel fee` anywhere in `book.astro`,
`ContactSection.astro`, `BookingForm.svelte` or `booking-copy.ts` today** — the
only two hits in `booking-copy.ts` (`:267`, `:447`) are **inside comments.**

- **`ContactSection.astro` — A6 red on arrival.** The CTA card (`:284-357`)
  gets the free eyebrow at `:287` and **no literal qualifier sentence at all**;
  the qualifier reaches that page only through `{FEE_TERMS_INTRO}` at `:347`,
  i.e. as a constant **name**. 🔴 **The repair an implementer reaches for is
  `code.includes('FEE_TERMS_INTRO')` — the import-satisfies-the-pin trap this
  very file documents at `:328-332`.**
- **`book.astro` — passes for the wrong reason.** Its only literal `travel fee`
  after R5-4 is in the **frontmatter `description`**, so a `<meta>` tag would
  satisfy a pin about the visible card.

**→ RESOLVED by R5-10 T11, and this is why T11 is a blocker rather than a
polish:** the eyebrow carries **its own literal qualifier** — *"Free on-site
assessment — travel fee may apply further out"* — on both surfaces. **A6 then
asserts a literal sentence in the rendered template, and the ticket states in
terms that a constant NAME never satisfies it.**

**The break A6 was written to catch, restated so it is testable:** delete
`ContactSection.astro:287`'s eyebrow entirely. A5's `dist/` twin still passes,
because the terms box on the same built page says *"The on-site assessment visit
is free."* **Green.** A6 with a literal source assertion is what catches it.

### 🔴 A3 · THE COUPLING PIN'S GRANULARITY OVER `booking-copy.ts` — both readings fail

A2 tests *"per rendered block"*. **`booking-copy.ts` has no rendered blocks.**
R5-10 T3 already chose **per constant**; this pass proves the other reading is
not merely worse but **permanently vacuous**, and adds the missing rule:

**The break under a whole-file reading:** delete the travel sentence from the
terms box **and** the travel clause from `FEE_TERMS_ACK_LABEL`, leaving
`FEE_TERMS_INTRO` alone. The acknowledged terms now claim a free visit with no
qualifier; `booking-copy.ts` still contains `travel fee`; `dist/index.html` and
`/book/` still contain it because INTRO renders on both. **Every arm green — and
W22 / decision 17a, the entire reason 17a folded into this ticket, is undone
with no red.**

⚠️ **N7, and it is the same hole from the other side: the scan MUST strip
comments.** `booking-copy.ts:221` (*"genuinely free assessment"*) and `:283`
(the site-wide sweep note) are comment-only free claims, and `:267`/`:447` are
comment-only *qualifiers*. Without `stripForUse` the pin is **red on arrival for
reasons unrelated to the copy**, and a qualifier in a comment satisfies a claim
in a string. **Use `stripForUse`, and say so in the pin's own comment.**

### 🔴 A4 · `CANCEL_LINE`'s ONLY WEB PIN IS IN THIS REVISION'S CLASS 3 — one of the three unjustified moves, now identified

**Verified.** `verify-booking-email.ts:346-349` —
`refundOrder.indexOf(CANCEL_LINE) === refundOrder.length - CANCEL_LINE.length` —
is the **sole** assertion anywhere that `CANCEL_LINE` closes the block.
R5-5d put `:339-349` in **Class 3, "subject genuinely gone" → delete**, and
BK-55 G6 pins `CANCEL_LINE` only at `booking-email.ts:574`/`:627`/`:771`/`:804`
— **all email.**

**The break:** drop `CANCEL_LINE` from the rewritten terms array. The homepage
card, `/book/` and the island lose the only cancel-or-reschedule number a
customer has. **BK-55 G6 green** (email renders untouched); **BK-53's Class-3
deletion removed the arm that saw it. Green.**

🔴 **The subject is NOT gone** — R5-4 keeps `CANCEL_LINE` and calls it *"Not
optional"*. **`:339-349` is CLASS 2, re-point only.** ⚠️ **This is one of the
"three arms moved Class 1 → Class 3 with no per-arm justification" that this
revision warns about without naming. It is now named. Find the other two before
implementation** — the warning was inherited, and an inherited warning nobody
discharged is how this one survived four rounds.

### 🔴 A5 · DELETING `FEE_TERMS_PAYMENT` MEMBERS MAKES AN ASSERTION PASS BY TESTING `undefined` — AND CRASHES THE OTHER ARM

**Verified at `verify-booking-email.ts:554-563`:** `mustNotCarry` holds
`FEE_TERMS_PAYMENT[2]` and `FEE_TERMS_PAYMENT[3]` **by index**.

- `tsconfig.json` extends `astro/tsconfigs/strict`, which does **not** set
  `noUncheckedIndexedAccess` — so the type stays `string` and **typecheck is
  green** on an out-of-range index.
- **Text arm:** `body.includes(needle(undefined))` → `includes("undefined")` →
  `false` → negated → **the assertion passes having tested nothing.**
- **HTML arm:** `escapeHtml(undefined)` (`booking-email.ts:280` calls
  `value.replace(...)`) **throws** — a crash producing **no `✗` and no summary
  line**, which is **BK-33's exact misreading** and the reason R5-2a exists.

⚠️ **And there is a seam bug behind it.** BK-55 §4 says convert `:538-540`
**and** `:554-563`. But seam rule 1's tiebreak — *"if an arm reads both, it is
BK-53's"* — takes both back, and **R5-5d's class lists cover `:525-547` and OMIT
`:554-563`.** So under the specs as written, **nobody owns it.**
**→ `:554-563` is BK-53's, Class 1, and it is REWRITTEN — never re-indexed.**
Since W11 deletes the whole array (R5-10 T1), index addressing has no target at
all; the arm must name the replacement sentences directly.

### 🔴 A6 · SIX LIVE PINS POSITIVELY REQUIRE THE COPY THIS TICKET DELETES — five of them unclassified

**Verified, all in `verify-booking-email.ts`, all reading the joined
`FEE_TERMS_PAYMENT`:**

| Line | What it REQUIRES | Under the free model |
| --- | --- | --- |
| `:373` | *"FEE_TERMS_PAYMENT names the payment link"* | 🔴 red — the link claim is deleted |
| `:377` | one of `nothing is charged \| not charged \| no charge \| nothing to pay` | 🔴 red |
| `:384` | *"the appointment is confirmed BY the payment"* | 🔴 red — **decision 17 reverses it** |
| `:396` | `standard rates` **and** `booking form \| before you send` | 🔴 red |
| `:419` | reads the same array | 🔴 red |
| `:427` | reads the same array | 🔴 red |

R5-5d classified `:424` (`AFTER_HOURS_NOTE`) and **none of these six.**
**Per CLAUDE.md's BK-44 trap, unanticipated reds are exactly where rules get
loosened** — an implementer meeting six reds nobody predicted will reach for the
assertion, not the fixture.
**→ All six are Class 1, enumerated here, each rewritten to assert the
REPLACEMENT sentence rather than deleted.** ⚠️ **`:384` is the sharpest: it does
not merely go stale, it asserts the opposite of decision 17.**

🔴 **And a cross-contradiction nobody had noticed:** `:377` **requires**
`FEE_TERMS_PAYMENT` to contain one of `nothing is charged | not charged | no
charge | nothing to pay` — **every one of which is a member of
`FREE_CLAIM_SHAPES`.** Under R5-10 T3's per-constant coupling pin, `:377` and
the coupling pin **cannot both be satisfied** unless that constant also carries
`travel fee`. Since W11 deletes the array outright the collision dissolves, **but
the same collision returns the moment any replacement sentence uses those
words** — which the terms box's item 5 nearly does. **Checked: item 5 says
*"We never take money without telling you the amount first"*, which matches no
shape. Keep it that way, and say why.**

### Should-fix — each verified by execution, not by reading

- **A7 · `FREE_CLAIM_SHAPES[2]` matches nothing where A1 puts it.** Executed:
  `/(?:^|[.>])\s*free\.\s*(?:$|[<])/im` against the real source line
  `  'The assessment visit — … what they find. Free.',` → **no match.** The
  trailing `'` blocks both `$` and `<`. It matches the bare sentence and the
  rendered `…Free.</li>`, but A1 scans **source** and A2 demotes `dist/` to a
  presence-only backstop — so the one shape written explicitly for
  `FEE_TERMS_ITEMS[0]` is **dead code at the only scope that matters.**
  **→ `(?:$|[<'"`,])`.**
- 🔴 **A8 · R5-5b's row 2 IS WRONG, and this revision wrote it.** Executed:
  `/free\s+assessment/i` does **NOT** match *"Free on-site assessment"* — `\s+`
  is literal whitespace and `on-site ` sits in the gap. **`:512-517` is GREEN on
  the approved eyebrow**, not red. What actually reddens is the `dist/` sweep at
  `:1055-1071`, whose `\bfree[^.<>]{0,40}?assessments?\b` **does** match — which
  is precisely why the source ban *looked* covered. **If `:512-517` is left alone
  on the strength of a red that never happens, the source-level ban on the two
  booking surfaces is permanently vacuous against the copy that ships.** It must
  be re-scoped **because it is vacuous**, not because it fails.
  ⚠️ **Same row, second error:** decision 23 does not put *"no obligation"* "on
  exactly these two files" — it puts it in **`booking-copy.ts`**, which neither
  `:471-477` nor `:512-517` reads. `:471-477` **does** go red, but only off
  `book.astro`'s **frontmatter** meta description. *The finding stands; two of
  its three reasons were wrong.*
- **A9 · The pre-registered count is not falsifiable against substitution or a
  collapsed scan set.** (a) A single scalar detects **net** deletion only: drop
  the `[service].astro` arm, add any `check(true, …)`, and count, exit code and
  summary line are byte-identical. (b) `verify-cutover.ts`'s sweeps are **one**
  `check(offenders.length === 0)` over a filtered array — **the count is 1
  whether the filter read 40 files or 0.** If the coupling arm's file list
  resolves to nothing (a wrong `dist/client/<slug>/` path, a glob that drops
  `.html`), `offenders` is `[]`, the check passes, and `passes` does not move.
  (c) **R5-2a prints the number on the SUCCESS line only** — on a red run the
  summary line carries no count, and a red run is exactly when R5-6's *"score on
  (exit code, summary line)"* needs it.
  **→ R5-2a is amended: print `${passes} passed, ${failures} failed` on BOTH
  branches, and every sweep additionally asserts its SCANNED-FILE COUNT is
  non-zero and within a pre-registered range.** A file-count assertion is the
  only thing that distinguishes *"nothing offended"* from *"nothing was read"*.
- **A10 · `TRAVEL_QUALIFIER` has the reflow hazard this revision flags one line
  above it.** R5-5a warns that `no[\s-]+obligations?`'s `+` is load-bearing
  against Prettier, then defines `/\btravel fee\b/i` with a **literal space**.
  `[service].astro:79`'s host sentence already wraps mid-sentence in the real
  file. **Break: reflow so `travel` ends a line — the customer still reads
  "travel fee", the pin goes blind.** This is the **silent** direction.
  **→ `/\btravel\s+fees?\b/i`.**
- **A11 · Deleting `FEE_TERMS_CREDIT` silently voids `verify-cutover.ts:584-588`.**
  Verified: `outroAt = island.indexOf('FEE_TERMS_CREDIT')` becomes `-1`, so in
  `check(tierAt > 0 && tierAt > outroAt && ackAt > tierAt)` the middle clause is
  **vacuously true**. **Break: move the tier picker above the whole terms box.
  Green.** R5-5d classifies `:536-545` and `:571-579`, **not `:584-588`**.
  ⚠️ **And the trap has a second jaw:** `:546-549`'s own `check(outroAt > 0 && …)`
  **does** redden, so the implementer will be standing at that line — and the
  edit that makes it green is deleting it, **leaving `:584-588` broken and
  silent.** → Both are Class 2; re-anchor `outroAt` on the surviving closing
  constant and say which one in the ticket.
- **A12 · W10's `[id].astro` deletions have no pin and no red-first row.**
  `verify-cutover.ts` does not scan `src/pages/admin/**`, and admin pages never
  reach `dist/client`. **Break: don't delete them. Green.** → Either add an
  admin-source arm or record in terms that W10 is **unpinned and verified by
  eye** — an unpinned deletion recorded as such is honest; one that looks
  covered is not.
- **A13 · `booking-payment.ts:288` is a free claim on a MONEY artifact, in the
  crack between both tickets.** `line('Travel', 'Round trip beyond the free
  radius', …)` is a Stripe `price_data` description — **it reaches a real
  Checkout Session and a real receipt** on the hand-charged path BK-51
  preserves. BK-53 owns web; BK-55 owns `booking-email.ts`/`resend.ts`;
  `booking-payment.ts` is in neither, never reaches `dist/`, and no coupling
  scope includes it. **This is BK-48's shape exactly — a claim on an artifact
  nobody has opened, on the surface that charges.** → **W21 already claims it
  via `ROADMAP:881-890`; this upgrades its reasoning from tidiness to a live
  claim, and it gets a pin.**
- **A14 · Class 1's replacement for `verify-booking-email.ts:247-254` is
  unspecified, and the obvious one is BK-48-shaped.** The arms are
  `figures.includes('399')`, `('699')`, `('1,199')`, `('GST')`. *"Rewrite to
  assert the replacement"* names no replacement, and the natural
  `!figures.includes('399')` is **a pin shaped for three specific numbers** —
  `$150`, `$385` or a re-added `$399.00` all pass at least one spelling.
  **→ The category pin: `!/\$\s?[\d,]/.test(figures)` — which is also A1's
  missing half.** Write them as one shape used twice.

### Nits — enumerated, not counted

- **A15 (N1)** · R5-5d cites `verify-booking-email.ts:304`, `:311`, `:321`; the
  `check(` call sites are `:303`, `:310`, `:320`.
- **A16 (N2)** · **"42 → 35" is now wrong twice.** C-f corrected the 42 to 41 and
  left the 35. **41 call sites − 7 deletions = 34.** ⚠️ **And per R5-6 the
  runtime count differs again, because `:73-88` runs three times** — which is the
  reason the pre-registration table asks for assertions RUN and not call sites.
  **Do not restate any of these three numbers as if they were one.**
- **A17 (N3)** · R5-4's terms list **never names which constant holds it.**
  `TERMS_CONSTANTS` (`verify-cutover.ts:350-357`) is the presence-and-order spine
  across three web surfaces and both email arms — **a NEW constant is invisible
  to it**, and reusing `FEE_TERMS_REFUND` for five non-refund sentences leaves the
  constant's name lying about its contents. **Name the constant in the ticket.**
- **A18 (N4)** · R4-C4 says the six items render *"after them in the same `<ul>`"*,
  but the `<ul>` at `book.astro:140-142`, `ContactSection.astro:348-350` and
  `BookingForm.svelte:1114-1118` maps **`FEE_TERMS_ITEMS` alone**. R5-4 therefore
  mandates a **markup change on four surfaces** that W4/W5/W6 do not list.
- **A19 (N5)** · After the rewrite **`RECEIVED_NEXT_STEPS` never says the visit is
  free**, and `/book/received/` is the first page every web customer sees.
- **A20 (N6)** · `BookingConfirmation.svelte:149` is
  `{#each RECEIVED_NEXT_STEPS as step, i (step)}` — **keyed on the string.** Two
  identical steps throw at runtime. The drafts are distinct today; nothing pins
  it, and it constrains every future edit.
- **A21 (N7)** · The comment-stripping requirement — folded into A3 above.
- **A22 (N8)** · After W14, `verify-booking-pricing.ts` reads **no**
  `booking-copy.ts` symbol, so decision 24's stated reason for assigning it to
  BK-53 (*"a web-copy dependency"*) does not survive the edit. Harmless; **do not
  cite the rationale as if it does.**
- **A23 (N9)** · Deleting the length zip removes the only guard that
  `FEE_TERMS_ITEMS` and `ASSESSMENT_TIERS` stay the same length, with nothing
  replacing it.
- **A24 (N10)** · Decision 21 strips the weekend disclosure from the admin panel
  (W10) while `AFTER_HOURS_NUMERATOR`/`DENOMINATOR` still multiply a
  hand-charged assessment 1.5× on weekends — **removing the office's own
  visibility of a multiplier they can still charge.** → Assumptions log.
- **A25** · `verify-cutover.ts:466-468`'s scoping comment cites *"~50 other
  unqualified 'free assessment' claims"* as the reason the ban is two files
  wide. **Those claims are already gone.** The rationale is stale, and it is the
  rationale that keeps the ban narrow.

## R5-12 · PLAN REVIEW OF REVISION 5 — 2026-09-05, fresh agent. **NOT APPROVED.**

**8 blockers · 13 should-fix · 10 nits.** Briefed per R5-8; every regex executed
rather than read; `[id].astro` read via `git show bk51-gated:`.

🔴 **THE META-FINDING, and it is the one that matters:**

> *"R5-10 and R5-11's findings were folded into the W-table and the red-first
> table, but largely not into **R5-4 (the copy)** or **R5-5 (the gate)** — the
> two sections an implementer actually builds from."*

**That is 'resolved only where it was raised' — the exact failure this ticket has
now repeated six times, committed by the fold-in that was supposed to end it.**
Revision 6 moves every resolution into the spec body, and **re-runs the
traceability audit against R5-4 and R5-5 specifically, not against the W-table.**

### The eight blockers

- **BL-1 · Deleting `FEE_TERMS_REFUND`/`_CREDIT`/`_PAYMENT` breaks
  `booking-email.ts`, and no W-row and neither ticket owns the repair.**
  `:522-533` (HTML) and `:604-611` (text) render all three. Revision 5 has **no
  row that edits those renders**, and BK-55 §0.1 forbids itself from touching
  `booking-copy.ts`. **So BK-53's commit does not typecheck.**
  🔴 **And seam rule 3's ordering makes it unfixable in place:** BK-55 lands
  first and is told to "land the replacement lines", but the replacement is
  BK-53's new terms constant, **which does not exist yet at BK-55's commit.**
  → **Re-derive the order. The render sites are BK-53's** (seam rule 1's own
  tiebreak already points there), and the ordering claim is wrong in all three
  places it appears — R5-1.3, R5-7, BK-55 §0.3/§6.
- **BL-2 · The per-constant coupling pin is RED ON ARRIVAL on approved copy.**
  Executed: `ASSESSMENT_TIER_NOTE` (*"The visit is free whichever you pick"*)
  hits shape 2, `FEE_TERMS_ITEMS[0]` (*"…what they find. Free."*) hits shape 3 —
  **neither carries `travel fee`, and neither can join A1a's exempt list under
  A1a's own principle**, since both render on the booking form *before* any
  confirmation call. **R5-5a deliberately re-derived those two shapes to match
  those two drafts.** Round 1's B2, sixth incarnation, authored by the fix for
  the fifth.
- **BL-3 · R5-4 still never names the constant holding the terms lines.**
  A17/N3 raised it and nothing landed. `verify-cutover.ts:350-357`'s
  `TERMS_CONSTANTS` is a hardcoded list of six **names** and `checkTermsOrder`
  asserts their render order on three surfaces; R5-5d says "Class 2, re-point
  only" — **re-point to what?** ⚠️ **This decides BL-2's outcome:** one array
  containing both the claim and the qualifier scans green; the claim and the
  qualifier in different constants scans red. **The implementer currently picks,
  and the gate's colour follows the pick.**
- **BL-4 · T5's resolution did not land in R5-4.** The copy table still reads
  `PAYMENT_CANCELLED_* — UNCHANGED` while T5 and W20 say it is fixed and supply
  the draft. **R5-4 is the section headed THE COPY.**
- **BL-5 · `WEB_AMOUNT_BAN` is red on arrival as scoped, and its carve-outs are
  vacuous.** `insurance-claims.astro:65` (*"commonly **$500 to $2,500** in
  Alberta"* — correct, unrelated copy) reaches `dist/client/`, which
  `verify-cutover.ts:945`'s own build always populates. `ContactSection.astro:65`
  holds `$699` **inside a comment** and W25 never requires `stripForUse`. And
  the three named carve-outs **match none of the four shapes** — a non-growing
  list protecting nothing while the two real offenders are absent.
- **BL-6 · The comment-stripping requirement (A3/N7) never reached R5-5a.**
  Executed over today's `booking-copy.ts`: **12 matches, 8 of them in comments.**
  Red on arrival for reasons unrelated to any customer-visible claim — and in
  the other direction `:447`'s comment-only *"travel-fee field"* would satisfy a
  claim in a string.
- **BL-7 · R5-5d orders deletion of the only pin its own red-first row 6 depends
  on.** `verify-booking-pricing.ts:68-71`'s length zip contains **no figure**,
  stays true under the new copy, and is **the only assertion in the repo that
  reddens when `FEE_TERMS_ITEMS` is emptied** — `TERMS_CONSTANTS` tests the
  constant *name*, and `for (const line of carries)` runs zero iterations, which
  is R1-B3 verbatim. **The spec answers its own red-first row with "delete it".**
  → Keep the zip and the import; delete only `:76` and `:87`.
- **BL-8 · Red-first row 2 cannot go red.** The `s?` makes the pin **match** the
  plural, and the qualifier sits in the same constant, so the coupling pin stays
  green. **A row whose predicted failure mode does not exist gets logged
  green-as-red.** → The break is plural **plus** qualifier removal; then restore
  only the qualifier and confirm it is still red.

### Should-fix (13) and nits (10) — recorded in full

**SF-1** R5-5d's class lists omit all nine arms R5-11 added (W26/W27/W28) ·
**SF-2** A11's re-anchor constant for `outroAt` is still unnamed · **SF-3** A2's
*"a constant NAME never satisfies the source twin"* never landed in A6 ·
**SF-4** T10 mandates *"PIN THE LENGTH"* and no pin exists — **and the draft is
148 characters, not the 151 stated twice** · **SF-5** W21's
`booking-payment.ts:288` verdict is explicitly left undecided · **SF-6** A12's
disposition for W10 never landed · **SF-7** A18/N4's markup change is still
absent from W4/W5/W6 · **SF-8** BK-55 §5 G0 still prints on the success line
only, so BK-53's own pre-registration is unscorable on a red run · **SF-9**
A16's arithmetic mixes call sites and runtime counts — the units it warns
against · **SF-10** `ContactSection.astro:58-74`'s comment is T9's defect in a
file W5 already touches, unclaimed · **SF-11** W17 removes a required field in
the same commit as the pricing flip, which is what decision 18's own reasoning
refuses · **SF-12** A19/N5 (`RECEIVED_NEXT_STEPS` never says the visit is free)
still has no verdict · **SF-13** *"Five items, then `CANCEL_LINE`"* precedes a
list of six plus `CANCEL_LINE` as #7.

**N-1** `TRAVEL_QUALIFIER` should be `[\s-]+` — `booking-copy.ts:447` shows
*"travel-fee"* is native here · **N-2** W6 cites `:780-782` for a comment at
`:772-779` · **N-3** the carve-out list has zero members that could ever match ·
**N-4** R5-9's rows run A1, A2, A3, A6, A4, A5 · **N-5** `ROADMAP:881-890`
should be `:886-895` · **N-6** A24 never reached R5-9 · 🔴 **N-7 the header-only
count says *"corrected to 15 throughout"* and `BK-53.md:70`, `:1498` and
`BK-55.md:427` all still say 13 — resolved only where it was raised, in the
paragraph about resolving things only where they are raised** · **N-8** A20/N6
has no owner or pin · **N-9** `verify-cutover.ts:1235` prints a second line
after the summary; say which line is the summary · **N-10** A22/N8 inverts once
BL-7 is applied.

### ✅ What the review confirmed correct and complete

**R5-0's nine citation corrections, all re-verified independently** — including
C-e's strike (no such comment exists) · **R5-1's seam diagnosis** and its render
table · **R5-2 / R5-2a**, both script tails checked · **R5-5b, all four rows,
including A8's correction** — `/free\s+assessment/i` does not match *"Free
on-site assessment"*, and `normalise` only rewrites nbsp entities so it cannot
rescue the match · **R5-5c** · **A7's regex fix**, executed against the real
source line including its trailing `',` · **R5-10 T13's refutation** — confirmed
at `bk51-gated:[id].astro:1857-1867`; **do not reopen** · **R5-7's drain probe
and the never-push-this-half-alone warning.**

# 🟩 REVISION 4 — 2026-09-05. Revision 3's deltas. **READ THIS BEFORE REVISION 3.**

**Revision 3 went to two more fresh agents — a traceability audit and an
adversarial pre-read. They converged independently on the same three blockers.**

⚠️ **The audit's verdict, and it is fair: *"the same failure mode fired again,
harder, in the revision written to answer it."*** Revision 3 restored three
vanished rows and then **silently dropped 21 items of its own**, including two
round-1 blockers. **15 findings now exist only as digits in four review
headers** and are unrecoverable from this document. Recorded as fact, not
excuse: a revision is not a rewrite, and "supersedes every table above it" is
how rows disappear.

## 🔴 THE THREE THAT BLOCK APPROVAL

### R4-1 · The request email prices the assessment, and the GATE DEMANDS IT · restores R1-A2

**Verified on `bk51-gated`.** `booking-email.ts:1159-1168`:

```ts
const weekend = quote.afterHours ? ' — weekend rate, 1.5x' : '';
return `${name} — ${formatCents(quote.baseCents)} + GST (${formatCents(quote.totalCents)} total)${weekend}`;
```

`:411` forces `settled = null` on a request, so `tierLine` (`:456-462`) takes the
`assessmentSummary` branch for **every request email — the first message every
web customer receives.** It renders *"You chose: Assessment + written report —
$699.00 + GST ($733.95 total) — weekend rate, 1.5x"* **in the same email as
"The on-site assessment visit is free."**

⚠️ **It is computed from `assessmentQuote`, not from any constant** — decision 16
keeps `booking-pricing.ts` intact — so **no deletion in revision 3 §1 touches it,
and no pin in §2 sees it** (email bodies are in neither the scan scope nor
`dist/`). It defeats **decision 17 and decision 21 simultaneously**, on the
highest-volume surface in the ticket.

🔴 **And the fixtures REQUIRE the defect.** `verify-booking-email.ts:2013-2045`,
classified nowhere:
`check(body.includes('$399.00'))`, `check(body.includes('$418.95'))`,
`check(mouldBody.includes('$577.50'))`, `check(mouldBody.includes('$606.38'))`,
`check(mouldBody.includes('weekend rate'))` — and **`mouldBody` is the
CUSTOMER's message.** Per CLAUDE.md's BK-44 trap: establish each arm relied on
the old behaviour, **make the fixture honest, do not loosen the rule.**

**→ `assessmentSummary`/`tierLine` become a first-class §1 surface.** Under
decisions 17/20/21 the request email should name the tier and **no figure**:
`return name;`.

### R4-2 · The public form keeps a price beside every radio, and the GATE DEMANDS THAT TOO · restores R1-A1

**Verified.** `BookingForm.svelte:1169-1174` sits in its **own** `{#if tierQuotes}`
inside the `{#each ASSESSMENT_TIERS}` — **not** inside the `{#if quote}` block
revision 3 deletes. Revision 3 names it **nowhere**, and its only trace is a
subordinate clause in §2b asserting those readers become dead — **which is false
unless the span also goes.**

🔴 `verify-cutover.ts:594-598` **positively requires it**:
`island.includes('formatCents(tierQuotes[tier].baseCents)')` — and revision 3's
neighbouring instruction is *"KEEP `verify-cutover.ts:600-603`"*, so an
implementer reads the whole region as a keep.

**→ Delete the span. Re-point `:594-598` to assert the amounts are ABSENT while
the tier labels remain. `:600-603` stays.**

### R4-3 · ⚖️ R2-B2 IS NOW DECIDED — option (b), plus a new ticket for (a)

Revision 3 contained **zero occurrences** of `resend.ts`, `terms_acked_at`,
`terms_version` or `booking-commit.ts`, while the ticket header still promised
*"the `resend.ts` / terms-snapshot fix below"* and decision 19's withdrawal
recorded that shipping without it makes the defect **strictly worse**.

**DECIDED — (b), scoped to this ticket:** `resend.ts`'s confirmed-message build
**suppresses the terms block for any row whose `terms_acked_at` predates the
deploy**, and renders in its place a pointer: *"The terms you accepted are in
your original confirmation email."* **Honest, and strictly better than the two
alternatives** — sending today's free-mode terms beside a real $628.43 charge is
false; sending nothing at all contradicts `booking-email.ts:501-511`.

**(a) — the real fix — gets its own ticket: 🆕 BK-54, terms snapshot at insert.**
R2's lane C called it *"the single highest-value item"* under decision 16,
because it is **the one structure that survives an arbitrary number of pricing
flips.** It is an **insert-path** change and does not belong smuggled into a
copy ticket. **BK-54 is not a gate on BK-53.**

## 🔴 THE GATE STILL CANNOT GO GREEN — three self-contradictions

### R4-4 · `FREE_CLAIM_SHAPES` does not match revision 3's OWN COPY

R3-BL-4 mandated *"ONE canonical phrase, and the noun is **visit**"* — and §2a
then anchored all four regexes on **"assessment"**. Three of §1's own drafts
match **nothing**:

| §1 draft | Why every shape misses |
| --- | --- |
| `ASSESSMENT_TIER_NOTE` — *"**The visit is free** whichever you pick"* | no *"assessment"* |
| `llms.txt.ts:54` — *"the **visit itself is free**"* | no *"assessment"* — **and this is the surface most likely to be quoted out of context** |
| `FEE_TERMS_ITEMS[0]` — *"…what they find. **Free.**"* | shape 1 needs `free` before `assessment`; the `.` is excluded by `[^.<>]` |

**→ Re-derive the shapes around the noun the copy standard mandates**, and add
the grammars the adversary demonstrated: `no obligations` **(plural — a
one-character edit turns the pin off; BK-48 verbatim, in the newest pin in the
ticket)**, `without obligation`, `no commitment`, `no strings`, `no cost`,
`don't charge`, `pay nothing`, `costs you nothing`, `complimentary`, `waive`,
`$0`, and `[\s-]` → `[\s-]+` (Prettier reflow inserts a second space).

### R4-5 · `BANNED_SHAPES` and the positive twin are MUTUALLY EXCLUSIVE

`verify-cutover.ts:1055-1069` fails the build if **any** built file claims a free
assessment. §2a's A5 twin **requires** two built pages to carry exactly that.
**One assertion demands the string; the other fails the build for it.**
R1-B2 named four bans; revision 3 gave a disposition to **one**.
**→ `:512-517`, `:1056-1071` and `:1188-1193` are re-scoped to the coupling
form — claim permitted WITH qualifier, red without — in the same commit.**

### R4-6 · Decision 23 constraint 2 and §6 give opposite answers

Constraint 2 binds the pin to govern `[service].astro:79`; §6 defers those four
pages; §2a's scope excludes them. **Verified:** `:79` reads *"About 30 minutes,
no obligation."* with no qualifier, and it matches the release shape. So the
`dist/` arm goes **red on day one on four pages the ticket refuses to edit** —
and the implementer's only green paths both silently discharge a constraint.
**→ RESOLVED: add the travel-fee clause to those four pages' CTA blocks — one
sentence each, no other change.** That is not the deferred question (whether
they carry the *free* claim, which still waits on the ad URLs); it is the
minimum that makes an existing live phrase honest.

## Restored, corrected, or newly assigned

- 🔴 **`[id].astro:1781` IS THE WRONG LINE.** On `bk51-gated` it is the
  **payment-deadline** hint. The weekend hints are **`:1835`** (*"· weekend rate,
  1.5× applied"*) and **`:828`**. Revision 3 would have deleted the deadline
  disclosure and left the weekend claim.
- **Decision 21's surface list was 3; it is 7** — add `booking-email.ts:1167`
  (customer), `:1214` (office), `[id].astro:825-828`, `:1835`, and classify
  `verify-booking-email.ts:2037-2043` + `verify-booking-admin.ts:1161-1167`.
- ⇄ **C4 resolved:** `FEE_TERMS_ITEMS` and the terms box render **the same
  `<ul>`** (`booking-email.ts:526`, `:606`). The six numbered items are
  `FEE_TERMS_*`'s replacement; **`FEE_TERMS_ITEMS` is the tier list and renders
  after them.** Revision 3's heading said "replaces … ITEMS" while the next
  block said "DO NOT DELETE".
- ⇄ **C1: the llms.txt draft re-prints what decision 22 struck** — *"is confirmed
  **before the visit**"* is *"never after the visit"* in other words, on the
  surface an LLM quotes bare. **→ *"…and is confirmed when we confirm your
  appointment."*** Same fix to the ack label's *"before anyone comes out."*
- **Class 1 is restored** (revision 3 restated only Classes 2 and 3):
  `verify-booking-email.ts:246-254`, `:291-296`, `:304`/`:311`/`:321`, `:638`;
  `verify-cutover.ts:364-372`, `:536-545`. ⚠ **Three arms moved Class 1 → Class 3
  with no per-arm justification** — re-justify or move them back;
  *"a deleted assertion is an unpinned claim."*
- **Newly assigned, each dropped by revision 3:** `/book/`'s `<title>` + WebPage
  schema `name` (R3-SF4 — *"a recorded rationale that quietly inverts is a
  decision nobody made"*, and `book.astro:20-33` carries that rationale) ·
  the **slot-release pre-consent clause** (R3-SF12, which explicitly amended
  decision 19) · the KEEP list `DECLINED_LEAD` `:704`, `EXPIRED_REQUEST_LEAD`
  `:784`, `PAYMENT_EXPIRED_LEAD` `:828` · `booking-payment.ts:288`'s travel line
  item · `PAYMENT_CANCELLED_HELP_LINE` `:780` · R3B-D4's competing pinned
  position · `ASSESSMENT_PRICES_SHOWN`/`assessmentTermsBlock()` (R2 lane C) ·
  the `APPROVED_*` wording decision 19 leaves open.
- **To ROADMAP Known traps, unowned** (out-of-scope rule): `new.astro:192`
  *"Payment comes before dispatch — client policy"*, which decision 17 reverses;
  and **`bk51-gated` removed BOTH `TRAVEL_FEE_*` refs from `[id].astro`, so the
  Travel field has no guidance at the moment decision 17a makes it expected.**
- **The drain probe is widened** — `confirmed` rows with a future `slot_start`
  are customers **who have already paid** for a visit about to be advertised
  free (`#36`, `cs_live_`, $628.43). Select `slot_start`, `payment_status`,
  `paid_amount_cents`, `refunded_amount_cents`; bound by
  `slot_start >= now() - interval '7 days'`.
- ⇄ **"Log the assertion count" is not falsifiable as written.** No expected
  value is pre-registered for the two files where ten arms move, so the number is
  read off the post-change run. **→ pre-register the expected count per script
  IN THIS TICKET before touching code, and assert `(exit code, summary line)`**
  per CLAUDE.md's BK-33 rule.
- **C8:** the header's *"plain Reviewed, not adversarial"* was struck in round 1
  and is still the first line a cold reader meets. **Fix the header.**

## Status

**NOT APPROVED. Revision 5 owed**, covering R4-1…R4-6 and the restored items.
The gate contradictions (R4-4, R4-5, R4-6) are the same structural blocker
round 1 raised as B2 — *the gate forbids the copy this ticket exists to ship* —
**still open after three revisions.**

---

# 🟢 REVISION 3 — THE IMPLEMENTABLE SPEC, 2026-09-05

**This section supersedes every table and draft above it.** Written against all
**28 blockers** from four fresh reviewers on three lanes, and against
**decisions 17–23**. Per CLAUDE.md's rule-rewrite trap, every enumerated case
has been re-derived against the NEW rules; verdicts that changed are marked ⇄.

**Target branch: `bk51-gated`. Ships with BK-51 in ONE push. Never separately.**

## 1 · The copy — complete, and drafted under decisions 20/22/23

### The terms box · replaces INTRO / ITEMS / PAYMENT / REFUND / CREDIT

`FEE_TERMS_HEADING` (*"Assessment terms"*) **stays** — the checkbox makes a
claim about it. **Five items, then `CANCEL_LINE` as the closing line.**

1. *"The on-site assessment visit is free."*
2. *"If your address is outside our service area, a travel fee may apply. We tell
   you the amount when we confirm your appointment."*  **(decision 22)**
3. *"You get our findings in person at the visit. If you want to go ahead, we
   follow up with a quote — that is where any report options and prices are."*
   **(decision 20)**
4. *"There is no obligation to go ahead."*  **(decision 23)**
5. *"We never take money without telling you the amount first — any charge is a
   link you choose to pay."*
6. **`CANCEL_LINE`** — *"To cancel or reschedule, call or text …"*
   ⚠ **Not optional.** `FEE_TERMS_REFUND[4]` **is** `CANCEL_LINE`
   (`booking-copy.ts:509`), and on the homepage card, `/book/` and the island the
   terms box is its **only** render. Deleting the array without this leaves those
   surfaces explaining how to book and nothing about how to cancel. **(R3-BL-3)**

⚠️ **Item 5 is TRUE TODAY and is what makes the acknowledgment honest:**
`booking-payment.ts:374` uses `mode: 'payment'` with **no `setup_future_usage`
and no stored customer** — nothing can be taken without the customer opening a
link and paying it. **(R3-BL-6)**

**`FEE_TERMS_ACK_LABEL`** ⇄ *"I understand the assessment terms above, including
that a travel fee may apply and that I will be told the amount before anyone
comes out."* Keeps the *"above"* reference form, so that markup claim and its
existing pin survive.

### `FEE_TERMS_ITEMS` — ⇄ FIGURES STRIPPED, ARRAY **KEPT**

🔴 **DO NOT DELETE THIS ARRAY. (R3-BL-2, verified.)** `booking-email.ts` renders
`ASSESSMENT_TIER_NAMES` only — `ASSESSMENT_TIER_DESCRIPTIONS` renders on
**one** surface, `BookingForm.svelte:1178`. **So this array is the ONLY path by
which the 3–5 day lab turnaround — a client requirement of 2026-08-18 — reaches
any email.** Deleting it also drops BK-27 Q4's *"each tier includes the ones
below"*, which BK-36 dropped once and plan review restored.

1. *"The assessment visit — a technician walks the affected areas and talks you
   through what they find. **Free.**"*
2. *"A written cause-of-loss report **and a repair estimate**: the documentation
   your adjuster works from if you are filing a claim. **Quoted with your job.**"*
3. *"A measured sketch **and diagram** of the affected areas, alongside the
   report **and estimate**. **Quoted with your job.** Where lab samples are
   taken, **results take 3–5 business days**."*

⚠️ *"and estimate"* and *"and diagram"* are restored deliberately — **dropping
them is the exact edit BK-36 made and plan review reverted (R3-SF3).**

### The tier picker · decision 15 keeps it, decision 20 redefines it

- **`ASSESSMENT_TIER_LEGEND`** ⇄ *"What do you think you need? (optional)"*
  ⚠ **and the field must ACTUALLY become optional** — `booking-form.ts:276` still
  hard-requires it and the server 422s without it. **Copy and validation must
  not disagree (R3-SF9).** If optional is out of scope, keep a required-reading
  legend instead.
- 🆕 **`ASSESSMENT_TIER_NOTE`** — *"The visit is free whichever you pick. Picking
  one just tells us what to prepare; anything written comes later, with your
  quote."*
- **`ASSESSMENT_TIER_NAMES` — UNCHANGED.** They describe scope, not price.
- **`ASSESSMENT_TIER_DESCRIPTIONS`** ⇄ rewritten, because
  🔴 **`standard`'s current text is FALSE under decision 20** (*"writes up the
  scope of the damage"* — nothing is written):
  - `standard`: *"A technician walks the affected areas and talks you through
    what they find."*
  - `report`: *"You also want a written cause-of-loss report and a repair
    estimate — what your adjuster works from. Quoted with your job."*
  - `sketch`: *"You also want a measured sketch and diagram, alongside the report
    and estimate. Quoted with your job. Where lab samples are taken, results take
    3–5 business days."*

### The remaining surfaces

| Surface | Draft |
| --- | --- |
| `book.astro:94` + `ContactSection.astro:287` eyebrow | *"Free on-site assessment"* — **only where the travel-fee clause follows in the same card** |
| `book.astro:36-37` meta description **+ the JSON-LD at `:49`** | *"Free on-site restoration assessment in Edmonton — about 30 minutes, no obligation. A travel fee may apply outside our service area; we tell you first."* |
| `FEE_TERMS_INTRO` (`:368`) ⇄ **branch-agnostic (R3-BL-1)** | *"We review every request before confirming it. If we can take it, we get in touch to confirm your appointment — and if a travel fee applies to your address, we tell you the amount then."* |
| `RECEIVED_NEXT_STEPS[1]` | *"If we can take it, we get in touch to confirm — and tell you about a travel fee if one applies to your address."* |
| `RECEIVED_NEXT_STEPS[2]` | *"Once it is confirmed, we email you the details and a calendar invite."* |
| `llms.txt.ts:54` ⚠ **claim + qualifier in ONE sentence** | *"Pick a time for an on-site assessment — about 30 minutes, and the visit itself is free, though a travel fee may apply outside the usual service area and is confirmed before the visit. Booking online sends a request: the office reviews it and gets in touch to confirm. Nothing is handed over in writing at the visit; if the customer wants to go ahead, a quote follows, and that is where report options appear. This is the way to request a quote."* |
| `QUOTE_TIMING_NOTE` + `QUOTE_HEADING` | ⇄ **DELETED, not redrafted** — both render **only** inside the `{#if quote}` block being removed **(R3-SF1)** |
| `AFTER_HOURS_NOTE` (`:597`) + `FEE_TERMS_PAYMENT[1]` | ⇄ **DELETED** — decision 21 |
| `FEE_TERMS_CREDIT` (`:525`) | **DELETED** — there is no credit |
| `[id].astro:1781` admin hint | **DELETED** — decision 21; BK-51 deferred it here explicitly |
| `PAYMENT_CANCELLED_*`, `PAID_*` | ⇄ **UNCHANGED — see §3** |

### 🔴 Out of this ticket, but falsified by decision 20 — needs its own ticket

`insurance-claims.astro:70`, `services.ts:389`, `services.ts:291` all attach a
written scope **to the assessment**. **Recorded in ROADMAP Known traps; do not
fix inline** (§Decision 20). ✅ **Confirmed still TRUE and NOT to be swept:**
every *"written scope **before work starts**"* claim — `services.ts:262`,
`:226`, `TestimonialsSection.astro:18-19`, `about.astro:195`, `llms.txt.ts:38`,
`insurance-claims.astro:30`/`:270` (both scoped to *"every **job**"*).

## 2 · The gate

### 2a · The coupling pin — ⇄ RE-SCOPED (R3B-A1…A5)

> **A surface that claims the assessment is free, OR releases obligation, must
> carry the travel-fee qualifier. Claim and qualifier travel together, or red.**

```ts
const FREE_CLAIM_SHAPES = [
  /\bfree[^.<>]{0,40}?assessments?\b/i,
  /\bassessments?\b[^.<>]{0,24}?\b(?:is|are|'s)\s+free\b/i,
  /\b(?:no charge|nothing is charged|not charged|nothing to pay|at no cost)\b/i,  // A4
  /\bno[\s-]obligation\b/i,                                                        // decision 23
];
const TRAVEL_QUALIFIER = /\btravel fee\b/i;
```

⚠️ **Four corrections that make it non-vacuous:**

- **A1 — scope follows where the FINAL copy lands, not where today's claims
  live.** §1 puts the claim as **inline literals** into `book.astro` and
  `ContactSection.astro`. **Scan: `booking-copy.ts`, every file in §1's table,
  and `dist/`.** *(Scanning only `booking-copy.ts` + `dist/` was the previous
  reviewer's criticism reproduced in mirror image.)*
- **A2 — proximity is tested AT SOURCE, per rendered block**, not whole-file.
  Whole-file on `dist/index.html` passes with the claim in the hero and the
  qualifier in a different component hundreds of lines away — **and the enabling
  edit (moving the terms box into the `showForm` arm) is one
  `verify-cutover.ts:440-453` already exists to catch, and which its own comment
  records SHIPPED GREEN ONCE.** `dist/` stays as a **presence** backstop only,
  and its comment must say so.
- **A3 — the `.js` arm is excluded from the coupling check** (kept for the plain
  ban): chunk co-location is a Vite decision nobody reviews.
- **A5 — add the POSITIVE TWIN.** The coupling pin is a *conditional* and passes
  vacuously; **delete the eyebrow entirely and every gate stays green.** Assert
  `dist/client/book/index.html` and `dist/client/index.html` each carry a free
  claim **and** a qualifier.

### 2b · The reddened assertions — ⇄ RECLASSIFIED (R3B-B)

🔴 **`verify-booking-pricing.ts:69-88` is NOT deleted as a block. (R3B-B2.)**
The same `forEach` holds
`formatCents(tierDefaultCents(tier)) === '${expected[tier]}.00'` — **verified as
the ONLY absolute-value pin on `tierDefaultCents` in the repo** (`:107`, `:119`,
`:126` are all relative). Deleting the block lets `TIER_PRICE_CENTS` drift
`39900 → 3990` **fully green**, contradicting this ticket's own promise to keep
the engine pinned — and it is a pin bought with a recorded defect
(`"$399.90".startsWith("$399")`). **Keep `expected` and the `forEach`; delete
only the two `copy.includes(...)` checks, the length zip, and the import.
Assertion count 42 → 35 — log the drop.**

⇄ **Class 2 (structure, re-point only) is SIX arms, not two:**
`verify-cutover.ts:347-357`, `:395-416`, **`:440-453`** (asserts the terms sit in
the CTA arm and **never** the form arm — the invariant holds verbatim; treating
it as a copy pin invites deleting it), `:571-579`, `verify-booking-email.ts:525-547`,
`:590-600`.
⇄ **Class 3 (subject genuinely gone):** `verify-booking-email.ts:339-349`,
`:374`, `:424`, `verify-cutover.ts:605-619`.
🔴 **Three arms named in round 1 that VANISHED from revision 2 — restored:**
`verify-cutover.ts:571-579` (`QUOTE_HEADING` pinned present, only render inside
the deleted block — **D1**), `verify-booking-email.ts:742-745` (the `$0`
message's `!/\$[\d,]+\.\d{2}/`, whose justifying comment cites the very
figures being removed), and **R1-D5** (`mustNotCarry` built from three deleted
symbols; `booking-email.ts:1150`'s `slice(0,2)` index addressing → convert to
named constants).

**Rules that bind the implementer:**
- ⚠️ **DELETE constants; never EMPTY them.** An emptied array makes
  `for (const line of carries)` run **zero iterations and stay green having
  asserted nothing** (R1-B3).
- ⚠️ **Log the summary line's ASSERTION COUNT**, not just the exit code.
- ⚠️ **Pin the orphaned readers' ABSENCE.** `tsconfig.json` extends
  `astro/tsconfigs/strict`, which does **not** set `noUnusedLocals`;
  `verify-cutover.ts:558-559` records orphaned imports raising no error when a
  fieldset was deleted. Once `AFTER_HOURS_NOTE` and the quote block go,
  `quote`, `tierQuotes`, `assessmentQuote` and `formatCents` are dead in the
  island.
- ✅ **KEEP `verify-cutover.ts:600-603`** (*"the island contains no price
  arithmetic of its own"*) — **more** valuable once no price renders. Its
  opening `check(` sits inside the range revision 2 told an implementer to
  rewrite (R3B-D3).
- ⇄ **Scope the payment-link ban PER CONSTANT**, the pattern
  `verify-booking-email.ts:368-372` already uses — a whole-file ban on
  `booking-copy.ts` cannot exclude `PAYMENT_CANCELLED_LEAD`, which lives in it
  (R3B-C2).
- ⇄ **§5a's `/\bEverything above\b/i` is replaced by a positive twin:** each
  non-`standard` description must match `/quoted with your job/i`. The old ban
  was shaped for a sentence being deleted, not for the claim (R3B-D5).

## 3 · ⇄ `/book/payment-cancelled/` and `/book/confirmed/` — KEEP, and the reasoning is corrected

Revision 2 merged them into one sentence **true of one and false of the other.**

- **`/book/confirmed/` IS gated** — `planConfirmationRender`
  (`booking-confirmation.ts:154-173`) redirects to `/book/` without a session id.
- 🔴 **`/book/payment-cancelled/` is FULLY STATIC** — no island, no session read,
  no redirect. Its own header says *"every word of it is true for anyone who
  arrives."* **Anyone who types or shares the URL sees "the payment link in your
  approval email."**

**Both KEPT** (deleting either 404s a live `cancel_url`), both `noindex` and
sitemap-excluded, nothing links to them. **Recorded, not fixed:**
`/book/confirmed/`'s gate is a shape check the module itself calls *"A BAR, NOT
A PROOF"* — `?session_id=cs_test_aaaaaaaa` renders `PAID_*` and fires the Ads
conversion for anyone. Unchanged by BK-53.

## 4 · Sequencing and the drain

**Built on `bk51-gated`; BK-51 + BK-53 ship in ONE push.** On `main`,
`[id].astro:505-512` still pre-fills `assessmentQuote` and mints Checkout
Sessions — BK-53 alone is a priceless site with a live charging screen.

**Drain probe — run it, paste the output here, before the push:**

```sql
SELECT id, status, payment_due_at, total_amount_cents
FROM appointments
WHERE status IN ('pending_review','approved_awaiting_payment','payment_expired')
ORDER BY status, slot_start;
```

⚠️ `approved_awaiting_payment` is the row that hurts — it holds a live Checkout
Session and a deadline, a copy deploy touches none of them, and the customer
pays days later for a visit the site now calls free. `pending_review` is the
**benign** direction (decision 16 accepts those going free).

## 5 · Tier

**Reviewed + FULL adversarial pass.** The copy-only claim is struck: it changes
what the appointment insert's consent attests to, moves money claims on a live
send path, removes the live price display from the public form, and its main
artifact **is** the pin set — the highest-value adversarial target this project
has (BK-51: 11 breaks, 11 green, in a suite nobody was rewriting).

## 6 · Still open

**One, and it is not blocking:** whether the four `[service].astro` pages carry
the free claim — **blocked on reading the ad landing pages** (GA4 `invalid_grant`
+ extension not connected). **Default until answered: they are NOT in this
ticket.** They carry no price claim, so nothing on them becomes false; the
exposure is an omission and adding them later is one commit.

---

# ✅ DECISIONS 20–22 — user, 2026-09-05 (second round). THESE CHANGE THE SCOPE.

## Decision 20 — NOTHING IS HANDED TO THE CUSTOMER IN WRITING AT THE ASSESSMENT

**User, verbatim:** *"client assesses the site/damage, **nothing is handed over
to the customer in writing.** if customer wants to proceed with the client, then
**the quote and the other things follow**."* And: *"of course quote will be
given and the customer can decide which will include reports and different
service tiers."*

| Stage | What the customer gets |
| --- | --- |
| **The free visit** | The technician assesses. **No written document. Nothing.** |
| **After, if they want to proceed** | **A quote**, which is where reports and the service tiers appear as options |
| **The work** | Quoted, as before |

⚠️ **THIS RETIRES A DELIVERABLE WE INVENTED, NOT ONE THE CLIENT SOLD.** The
client's settled tier table (`BK-27.md:213-217`, 2026-08-14) read
**`| Assessment | $399 + GST | The on-site assessment. No documents. |`** — and
BK-36 added *"and a written scope of the damage"* on 2026-08-19 without listing
it among its five recorded departures from the approved draft. **Decision 20
restores the client's original position.** `booking-copy.ts:402` and `:581` are
now describing something that does not happen.

## 🔴 Decision 20's REAL cost: a "written scope" inventory across FIVE surfaces, two outside this ticket

**The seam is clean and must be applied claim-by-claim, not file-by-file:**

| Verdict | Shape | Why |
| --- | --- | --- |
| ✅ **STAYS TRUE** | *"a written scope **before work starts**"* | Under decision 20 the quote precedes the work, so a scope arriving with it is honest |
| 🔴 **NOW FALSE** | *"a written scope **at the assessment**"* | Nothing is handed over at the visit |

**Still true — do NOT sweep these:** `services.ts:262` (*"before any work
starts"*), `:226` (*"with timelines before work starts"*), `:106`, `:346`,
`TestimonialsSection.astro:18-19` (*"A written scope before work starts"*),
`about.astro:195` (*"up front"*), `llms.txt.ts:38`,
`insurance-claims.astro:30`/`:270` (both scoped to *"every **job**"*).

**🔴 NOW FALSE — five surfaces:**

| Surface | The claim |
| --- | --- |
| `booking-copy.ts:402` `FEE_TERMS_ITEMS[0]` | *"the on-site assessment **and a written scope of the damage**"* |
| `booking-copy.ts:581` `ASSESSMENT_TIER_DESCRIPTIONS.standard` | *"**writes up the scope of the damage**"* |
| 🔴 **`insurance-claims.astro:70`** | *"**At the assessment we give you a written scope with real costs**"* — and *"real costs"* is a repair estimate, the **$699** tier |
| 🔴 **`services.ts:389`** (sewage FAQ) | *"**The assessment gives you a written scope**"* |
| 🔴 **`services.ts:291`** (mould FAQ) | *"we start with **an assessment and a written scope**, so you know the cost before any work begins"* |

⚠️ **The last three are on pages BK-53 does not currently touch, and none is
reachable from any constant** — they are prose, in their own words, on
`/insurance-claims/` and inside `services.ts` FAQ answers. **`verify-cutover.ts`'s
five-file `CLAIM_SURFACES` sees none of them.** This is CLAUDE.md's
copy-inventory trap at its purest, and it is the **third instance found in this
ticket alone.**

⚠️ **`insurance-claims.astro:70` was recorded this morning as a moderate
out-of-scope defect on the grounds that it gave away the $699 deliverable.
Decision 20 escalates it: it is now a false statement about what the visit
produces**, on a live page, in the claim-filing context where a customer is most
likely to rely on it. **It needs an owner and it does not wait for BK-53.**

## Decision 21 — THE WEEKEND SURCHARGE IS DEAD. Verbatim, at last.

**User, twice, 2026-09-05:** *"we decided to get rid of the weekend surcharge"*
and *"no weekend surcharge (clarified before, asked again)."*

⚠️ **This supplies what decision 7 never had.** Decision 7 was client-*attributed*
but carried **no verbatim quote**, unlike decisions 11, 12, 13, 15 and 17 — and
the original rule rested on four words (*"Yes weekend is extra"*, 2026-08-18)
whose question was never recorded, with the **1.5× and "ordinary weekend
bookings, not only emergencies" both being OUR inference**, written into the
same sentence as the quote so it read as client-stated. **Both directions rested
on inference until now. Decision 21 settles it.**

**Treatment: delete every customer-facing mention; keep the engine.**
`AFTER_HOURS_NOTE` (`booking-copy.ts:597`), `FEE_TERMS_PAYMENT[1]` (`:461`) and
the admin hint BK-51 deferred here (`[id].astro:1781`) all go. The multiplier
and `verify-booking-pricing.ts`'s engine pins stay — same treatment as the price
ladder, per decision 16's cost-it-for-reversal rule.
⚠️ **Pin the orphaned readers' ABSENCE:** `tsconfig.json` extends
`astro/tsconfigs/strict`, which does **not** enable `noUnusedLocals`, and
`verify-cutover.ts:558-559` records orphaned imports raising no error when a
fieldset was deleted.

## Decision 22 — THE TRAVEL-FEE PROMISE IS SOFTENED

**User, verbatim:** *"no need to promise, if a travel fee is applied, customer
is informed based on their location when client confirms the booking."*

⇄ **The drafted *"never after the visit"* is STRUCK.** It was a commitment about
the client's own conduct that no code enforces. Replacement:

> *"If your address is outside our service area, a travel fee may apply. We tell
> you the amount when we confirm your appointment."*

**Also settled: R3-BL-5 and R3-CQ-2 close.** *"May we print 'never after the
visit'"* — **no.**

## Decision 23 — *"NO OBLIGATION"* GOES BACK ON

**User, 2026-09-05:** *"yes put no obligation back."*

**It is true under decisions 17 and 20:** the visit is free, **nothing is handed
over**, and a customer who declines owes nothing and receives nothing. The one
recorded objection — `PREPAY-PLAN:516-519`, *"a decline fee wearing a travel
fee's name"* — **depended on the travel fee being WAIVED if the customer
proceeds.** Decision 17 charges it either way, so **that objection does not
survive and is marked superseded rather than left standing.**

**Two binding constraints on how it ships:**

1. ⚠️ **It must NEVER sit next to a bare "free" without the travel-fee
   qualifier.** *"Free, no obligation"* alone reads as *"this cannot cost me
   anything under any circumstance"* — false for an out-of-zone customer, and
   the pre-prepay claim's shape re-armed in the opposite direction.
2. ⇄ **`verify-cutover.ts:470-475` is REPLACED, not merely struck.** §1
   previously said *"this ticket removes the prohibition and asserts nothing in
   its place"* — **that is now wrong**, and it would leave the phrase ungoverned
   on the one surface where it is already live (`[service].astro:79`).
   **Extend the coupling pin to treat an obligation-release exactly like a free
   claim: release + qualifier travel together, or red.**

**Draft:** *"About 30 minutes, no obligation. A travel fee may apply outside our
service area; we tell you when we confirm."*

⚠️ **This also discharges W20, which is recorded as BACKWARDS** — W20 exists to
*strike* the phrase, and under decisions 17/20/23 it is true and is the best
claim available. **BK-53 absorbs W20's surface; W20 is struck**, the same move
decision 17a made for W22. **W21 is NOT struck — it changes shape:** the ad says
*"free, no-obligation **quote**"*, and under decision 20 the **quote** is not the
free thing, the **visit** is. W21 becomes *fix the noun*, still after W3.

## Still open after this round

✅ **NONE. The last open item closed 2026-09-05 — MEASURED, not inferred.**

**GA4 (property 543677881), paid sessions 2026-07-01→09-05: the HOMEPAGE takes
1,097 of ~1,204 — about 92%. The four `[service].astro` pages take 23 between
them (1.9%), and `/fire-smoke-damage` has received ZERO paid clicks.**

**→ The four service pages do NOT join BK-53's copy scope.** The premise that
they are the ad entry points was one unsourced sentence in `CONVERSION` §4; it
is now measured false, and it had shaped W20's scope and was raised three times
in this ticket's reviews.

✅ **The real ad entry point is already in scope.** `index.astro:66` renders
`<ContactSection showForm={false} />` — the CTA arm `verify-cutover.ts:440-453`
pins the terms box into — so the free claim and its travel-fee qualifier land on
the page 92% of paid traffic actually sees. `HeroSection.astro` carries no
price, free or obligation claim (checked). **A5's positive twin over
`dist/client/index.html` was therefore right, and is now right for a measured
reason rather than a guessed one.**

⚠️ **R4-6 still stands, and is now cheap.** `[service].astro:79`'s
*"no obligation"* must still gain the travel-fee clause — one sentence on each
of four pages — or decision 23's coupling pin goes red on them at build time.
**That is a gate obligation, not a traffic-driven one**, and at 1.9% of paid
sessions it is the minimum honest fix rather than a copy project.

🚧 **ATTEMPTED 2026-09-05 AND BLOCKED. It needs the user.** Both automated
routes are dead:

| Route | Result |
| --- | --- |
| GA4 MCP | `503 … invalid_grant: Bad Request` — this is **W9**, already on the 🙋 index |
| Chrome extension | *"Browser extension is not connected"* |
| Playwright | **Not a substitute** — it launches a clean browser with no Google session |

⚠️ **GA4 is the better instrument than the asset-group final URL**, and the
distinction matters here: a final URL says where traffic was *configured* to go;
**GA4's landing-page report says where it actually WENT** — and §4 notes the
campaign is **Performance Max**, whose final-URL expansion sends traffic to
pages no asset group names. So re-authing GA4 (W9) answers this question
*better* than the originally planned check, **and simultaneously unblocks W5/W6**.

**Manual path if the user prefers:** Google Ads → the *Water Damage Restoration*
campaign → **Landing pages** — it lists the real URLs with clicks against each.
**Read-only; do not edit any asset** (constraint 3: W3 precedes W21, because
editing an asset can clear its policy state).

**Until this is answered, BK-53 does NOT include the four service pages.** They
carry no price claim, so nothing on them becomes false — the exposure is an
omission, not a contradiction, and it is reversible in a later commit.

---

# 📝 PLAN REVIEW ROUND 3 — copy & truthfulness lane, 2026-09-05

**6 blockers, 12 should-fix, 4 nits, 7 client questions.** *"The shape is right
and §5a's implicature finding is the best thing in the ticket. But six drafted
sentences are false or unbacked on the charged path, three recorded client
requirements are silently dropped, and the ack label is not adequate consent."*

## ⚠️ FIRST — TWO FINDINGS CORRECTED. The reviewer read `main`'s semantics onto `bk51-gated`.

**R3-BL-1 and R3-BL-5 both rest on:** *"Enter a travel fee at approval and
`totalCents > 0`, so the row goes down the paid branch — a payment link, a
deadline, slot release, and no calendar invite."*

**That is false on the branch this ticket is built on.** Verified:

```
bk51-gated:review.ts:246   if (travelCents !== 0) return { error: 'travel' };
bk51-gated:review.ts:212   **A travel fee cannot be approved here at all.**
```

**BK-51 refuses a non-zero travel fee outright.** Decision 8 puts it entirely
outside the software, and BK-51 *enforces* that in code. **A travel fee can
never mint a Checkout Session, a deadline, or a slot release.** It is collected
on the phone / on site, off-system.

**This is the branch-drift hazard BK-52 was annotated for, arriving from the
opposite direction** — not a stale line number, but stale *semantics*: a
reviewer reasoning about `main`'s behaviour while reviewing a plan targeting
`bk51-gated`. **Recorded as a standing hazard for every remaining BK-53 review:
brief reviewers that the target branch is `bk51-gated` and its approval route
differs from `main`'s.**

**What survives the correction:**
- **R3-BL-1 survives in weaker form.** The paid branch is still reachable — by
  the office typing a non-zero **base** amount (decision 19 keeps this). So
  *"we email you to confirm your appointment"* still describes one of two live
  branches. **The finding stands; its mechanism was wrong.** Adopt the proposed
  rewording, which is branch-agnostic and better regardless.
- **R3-BL-5 is REDUCED to one true objection.** *"when we call"* and *"never
  after the visit"* are not falsified by a payment link, because there is no
  payment link for travel. But **"never after the visit" is still an
  unenforceable promise about the client's own conduct that the client has not
  made**, and that alone is enough to strike it. → client question.
- ⇄ **R3-CQ-3 is ALREADY ANSWERED and comes off the client list.** *"Travel
  fee: phone, or payment link?"* — **phone, outside the software, enforced in
  code at `review.ts:246`.** Decision 8 + BK-51 settled it.

## 🔴 CONFIRMED BLOCKERS — verified against the code

### R3-BL-2 · Deleting `FEE_TERMS_ITEMS` drops a CLIENT REQUIREMENT out of every email

**Verified:** `booking-email.ts` imports `ASSESSMENT_TIER_NAMES` and renders
only `tierNameOf` (`:462`, `:1155`, `:1166`, `:1213`). **It never renders
`ASSESSMENT_TIER_DESCRIPTIONS`** — that constant renders on exactly one surface,
`BookingForm.svelte:1178`.

So the **3–5 business-day lab turnaround** — a **client requirement of
2026-08-18**, recorded in two constants' comments as *"one fact, two places it
must appear, and they may not drift into two different promises"* — reaches the
emails **only** through `FEE_TERMS_ITEMS`. Delete the array and the `sketch`
customer gets a tier *name* in the message they keep and **no turnaround
statement anywhere in it.**

⚠️ **§5a's claim that the fact is "PRESERVED VERBATIM in intent" is FALSE as
written** — it preserves the render that survives and deletes the one that
carries it into email. Same deletion also drops BK-27 Q4's *"each tier includes
the ones below"* clause, which BK-36's first draft dropped and plan review
restored.

⇄ **CORRECTION TO THE PLAN: do NOT delete `FEE_TERMS_ITEMS`. Strip the FIGURES
from it.** The sentences minus the dollar amounts stay true, keep both client
requirements, and kill the free-deliverables implicature **better than silence
does** — silence is what created the implicature in the first place.

### R3-BL-3 · Deleting `FEE_TERMS_REFUND` removes the homepage card's ONLY cancellation instruction

**Verified:** `booking-copy.ts:509` — `FEE_TERMS_REFUND[4]` **is** `CANCEL_LINE`
(*"To cancel or reschedule, call or text …"*), and the array's comment records
why: *"the terms block has to be self-contained on the homepage card, where no
closing line exists."* `CANCEL_LINE` renders independently in
`booking-email.ts` (`:574`, `:627`, `:771`, `:804`), `booking-ics.ts:266` and
`booking-admin-notify.ts` — **but on `ContactSection.astro`, `book.astro` and
the island the terms box is its only render.**

R2-SF4 pinned the surviving **email** render and stopped there. **Deleting the
array leaves the homepage card explaining how to book and nothing about how to
cancel.** → `CANCEL_LINE` becomes the closing line of the new terms block on all
three web surfaces.

### R3-BL-4 · Four spellings of one claim, and a pin cannot be written for a claim with no shape

*"The on-site assessment is free"* sits inches above
`ASSESSMENT_TIER_NAMES.report` = *"Assessment + written report"*, so the reader
composes **assessment = free → this option = assessment + report → the report
comes with the free thing.** §5a's note *fights* that sentence instead of
removing it. The ticket correctly leaves the tier NAMES alone, so **the fix must
be in the free claim.**

⇄ **ONE CANONICAL PHRASE, AND THE NOUN IS "VISIT".** The drafts currently carry
four spellings — *"The on-site assessment is free"*, *"The assessment visit is
free, whichever you pick"*, *"about 30 minutes, free"*, *"Free on-site
assessment"*. **§1's coupling pin has to match all four.** Pinning a claim shape
is only possible if the claim has a shape.

### R3-BL-6 · The ack label is thin consent — and the honest fix is a fact that is already true

The *"above"* reference form is kept correctly (pinned, and the box still
renders above the checkbox). But the amount is stated nowhere, the terms are
new, and the office can mint a link for any amount.

**The reviewer found the sentence that makes it honest, and it is TRUE TODAY:**
`booking-payment.ts:374` uses `mode: 'payment'` with **no `setup_future_usage`
and no stored customer** — **nothing can be taken without the customer opening a
link and paying it themselves.** Adopt:

- new terms item: *"We never take money without telling you the amount first —
  any charge is a link you choose to pay."*
- label → *"I understand the assessment terms above, including that a travel fee
  may apply and that I will be told the amount before anyone comes out."*

## Should-fix — the ones that change the plan

1. ⇄ **`QUOTE_TIMING_NOTE` and `QUOTE_HEADING` render ONLY inside the `{#if quote}`
   block §5 deletes** (`BookingForm.svelte:1200`, `:1215`). §4's own rule is
   *delete constants, never empty them* — so both are **DELETED, not redrafted.**
   §2's draft for `QUOTE_TIMING_NOTE` is withdrawn; it also kept *"Nothing is
   charged now"*, the phrase R2 flagged as implying a later charge.
2. **`AFTER_HOURS_NOTE` (`:597`) is in NO draft table** though decision 7 removes
   the multiplier and it renders above the radios. **Name it or it ships
   describing a multiplier that no longer exists.**
3. **The `sketch` draft silently narrows the tier** — drops *"and diagram"* and
   *"and estimate"*. **This is the exact edit BK-36 made and plan review
   reverted.** Restore both.
4. **`/book/`'s `<title>` and the `WebPage` schema `name`** are in no surface
   table, though `book.astro:20-33` records why *"free"* was removed from them:
   *"Claiming a price of zero to hold a keyword is not a trade this site gets to
   make."* **That rationale is now discharged — the price IS zero.** Decide
   explicitly; *a recorded rationale that quietly inverts is a decision nobody
   made.*
5. **The ~~six~~ FOUR `/[service]/` pages (count corrected 2026-09-05) are ASSERTED to be the ad entry points — an UNSOURCED premise, see CONVERSION §4** and would say nothing
   about free while `/book/` does. §1 strikes the ban and asserts nothing.
6. ⚠ **"free" survives unqualified in the two places most likely to be quoted
   OUT of context** — `llms.txt` and the eyebrow. **§1's coupling pin is
   per-surface, so an LLM extract or a SERP snippet passes the pin and drops the
   qualifier.** → **bind claim and qualifier in the SAME SENTENCE**, not merely
   the same file. *This materially strengthens §1's pin design.*
7. **Meta description: the qualifier is the truncated tail** at 153 chars, and
   *"we confirm it"* reads as confirming the appointment.
8. **`VISIT_LENGTH_LINE` already owns the 30-minute fact** — drop it from terms
   item 1.
9. ⚠ **Decision 15 makes the picker OPTIONAL; nothing in §5/§5a does that**, and
   `booking-form.ts:276` still hard-requires it (server 422s on a missing
   `assessment_tier`). **The drafted legend reads optional while validation is
   required.** Either make it optional here or keep a required-reading legend —
   *copy and validation currently disagree.*
11. ⇄ **"Quoted separately" prices, but does not RELEASE.** Add *"You are under
    no obligation to go ahead."* — which is the true claim §1 says W20/W21 got
    backwards, and it makes the four service pages' existing *"no obligation"*
    consistent rather than orphaned.
12. ⚠ **Decision 19 does not repair what it claims to.** Putting more terms into
    `APPROVED_*` does not fix that slot release is *first disclosed in the email
    asking for money* — it is the same email. **The pre-consent fix is one clause
    in the terms box.** Otherwise deleting `FEE_TERMS_PAYMENT[3]` removes the
    only pre-consent disclosure of slot release on a path decision 19 calls
    expected. **Decision 19 is amended accordingly.**

## Client questions from this round — added to §7

**R3-CQ-1 is the sharpest and is a defect in MY plan, not the reviewer's:**
§5a marks `standard` — *"A technician walks the affected areas and **writes up
the scope of the damage**"* — as *"unchanged, already true and already free."*
**Nobody decided that.** Decision 17 says the **visit** is free and the
**report** is quoted; a written scope-up **is a document.** This is the one place
the ticket asserts a deliverable is free **on its own judgment**. → user.

Also: may we print *"never after the visit"* (R3-BL-5); does *"no obligation"*
go back on; should the title / schema `name` / service pages carry the free
claim; and does the client want a stated cancellation-notice expectation now
that the refund block is gone.

---

# ⛔ PLAN REVIEW ROUND 3B — pins & code lane, 2026-09-05. NOT APPROVED.

**6 blockers / 9 should-fix / 3 nits.** *"The revision's shape is right — the
coupling pin is the correct pin family, the one-push sequencing is correct, the
drain SQL is valid. What is wrong is the SCOPE of the pin, the CLASSIFICATION of
six arms, one factual claim about a page, and one decision that records itself
as resolving a blocker it does not touch."*

## 🔴 E1 · DECISION 19 CLOSED A BLOCKER THAT IS STILL OPEN — see the withdrawal above

**Verified and accepted. Decision 19's R2-B2 paragraph is struck.** The
correction is recorded at decision 19 itself rather than only here, so a reader
who meets the decision first meets the withdrawal with it.

## 🔴 B2 · `verify-booking-pricing.ts:69-88` CANNOT BE DELETED AS A BLOCK

**§4 said the `FEE_TERMS_ITEMS` zip could go because its subject is gone. That
is true of the copy half and FALSE of the engine half.** Inside the same
`forEach`:

```ts
check(
  formatCents(tierDefaultCents(tier)) === `${expected[tier]}.00`,
  `the ${tier} default … matches the copy's ${expected[tier]} to the cent`,
);
```

**Verified: `:84` is the ONLY absolute-value assertion on `tierDefaultCents` in
the repo.** The other three uses (`:107`, `:119`, `:126`) are all **relative**.
**Delete the block and `TIER_PRICE_CENTS` can drift `39900 → 3990` with
`verify-booking-pricing.ts` fully green** — directly contradicting this ticket's
own boundary decision to *keep `booking-pricing.ts` intact and keep
`verify-booking-pricing.ts` running against it.*

It also destroys a pin **bought with a recorded defect**: the comment above it
records that `"$399.90".startsWith("$399")` was true, so *"the one assertion
whose entire job is price drift passed while the table charged 90 cents more
than the copy states. The cents are the part a typo lands in."*

⇄ **CORRECTED, per BK-44's make-the-fixture-honest rule:** keep `expected` and
the `forEach`; delete **only** the two `copy.includes(...)` checks and the
length zip, and drop the `FEE_TERMS_ITEMS` import. **Assertion count 42 → 35,
not 32 — log the expected drop**, per §4's own count rule.

## 🔴 A1–A4 · THE COUPLING PIN IS THE RIGHT FAMILY AND THE WRONG SCOPE

The reviewer wrote the working regex pair and confirmed §2's drafts match both
shapes. Four scope defects:

- **A1 — the chosen scope is VACUOUS for the surfaces §2 puts the claim on.**
  §1 scoped to `booking-copy.ts` + `dist/` and excluded render files. **But §2
  writes *"Free on-site assessment"* as an INLINE LITERAL** into `book.astro:94`
  and `ContactSection.astro:287` — both inline today. **Under §2's own copy the
  free claim does not live in `booking-copy.ts` at all** except
  `ASSESSMENT_TIER_NOTE`. *"This is R2's criticism reproduced in mirror image."*
  ⇄ **Scope must be derived from where §2's FINAL copy lands, not from where
  today's claims live.** Inline sites outside scope: `book.astro:94`, `:36-37`
  (+JSON-LD `:49`), `ContactSection.astro:287`, `BookingForm.svelte:780-782`,
  `[service].astro:79`.
- **A2 — whole-file co-occurrence on `dist/` is not proximity.** On
  `dist/client/index.html` the claim is the eyebrow and the qualifier is
  hundreds of lines away in another component. **Move the terms box into the
  `showForm` arm — the edit `verify-cutover.ts:440-453` exists to catch, and
  which its own comment records ALREADY SHIPPED GREEN ONCE — and the coupling
  pin stays green** on a homepage claiming an unqualified free assessment.
  → do the proximity half **at source** on the render files; keep `dist/` as a
  whole-file **presence** backstop, and say so in the comment.
- **A3 — on the `.js` arm the verdict is a Vite artifact.** Whether
  `ASSESSMENT_TIER_NOTE` and its qualifier land in one chunk is a bundler
  decision nobody reviews; `:1108-1118` already records a filename-based pin
  that matched **zero** strings while reading like coverage.
- **A4 — the pin is shaped for the word "free" and the claim has other
  grammars — BK-48 verbatim.** `NO_CHARGE_LINE` (*"There is no charge for this
  visit"*) and `QUOTE_TIMING_NOTE` (*"Nothing is charged now"*) are **both free
  claims matching neither shape**, and `verify-booking-email.ts:377-379`
  **already owns the grammar list** (`/nothing is charged|not charged|no
  charge|nothing to pay/i`). A later editor rewording to *"No charge for your
  visit"* **turns the pin off silently — quiet, not red.**
- **A5 — and it is still not a positive twin.** Nothing asserts the free claim
  is *present*: delete the eyebrow entirely and every gate is green. Add the
  real twin over the built pages.

## 🔴 B1 · THREE ROWS NAMED IN ROUND 1 VANISHED FROM THE REVISION

*"A row that disappears between two versions of a plan is a decision nobody
made."* — `verify-cutover.ts:571-579` (`QUOTE_HEADING` pinned present, its only
render inside the deleted block); `verify-booking-email.ts:742-745` (the `$0`
message's `!/\$[\d,]+\.\d{2}/`, **whose justifying comment cites
`FEE_TERMS_ITEMS` printing "$399 + GST"** — and §2 puts a travel-fee sentence
onto that same $0 message); and **R1-D5 entirely** (`mustNotCarry` built from
three symbols §2 deletes, plus the `slice(0,2)` index addressing).

## 🔴 C1 · THE "KEEP" VERDICT IS RIGHT; THE REASONING IS FALSE FOR ONE OF THE TWO PAGES

§3 merged both pages into one sentence. **`/book/confirmed/` is gated** — by
`planConfirmationRender` (`booking-confirmation.ts:154-173`), which redirects to
`/book/` without a session id. **`/book/payment-cancelled/` is FULLY STATIC** —
no island, no session read, no redirect — and its own header says so:
*"every word of it is true for anyone who arrives."* **Anyone who types or
shares that URL sees "the payment link in your approval email" unconditionally.**
Both stay (deleting 404s a live `cancel_url`), but §3's shared reasoning is
false for the static one, **and the reasoning is what a flip-back reader
re-derives from.**

Also recorded: `/book/confirmed/`'s gate is a **shape check the module itself
calls "A BAR, NOT A PROOF"** — `?session_id=cs_test_aaaaaaaa` renders `PAID_*`
**and fires the Ads conversion** for anyone. Unchanged by BK-53, but §3's *"a
visitor reaches either page only when a real payment genuinely exists"* is not
what the code says.

## 🔴 D1/D2 · THE PICKER — one good verdict, two unaddressed reds

✅ **`assessment_tier` still submits; the 422 incident does not recur.** Verified:
the radios sit **outside** both `{#if}` blocks; only the price `<span>` is
inside. §5's deletion is safe.

- **D1 — `QUOTE_HEADING` is pinned present** (`verify-cutover.ts:571-579`) and
  its only render is inside the block §5 deletes. Red on implementation, in no
  class.
- **D2 — `AFTER_HOURS_NOTE` is incoherent, TRIPLE-pinned and unaddressed.**
  *"1.5× the weekday price"* of a free visit. Pinned at `verify-cutover.ts:576`,
  `:605-619` and `verify-booking-email.ts:423-426`, and **§7's user list never
  asks whether the weekend surcharge survives at all.**
  ⚠ **And if it goes, `tierQuotes` loses its last reader: `tsconfig.json`
  extends `astro/tsconfigs/strict`, which does NOT enable `noUnusedLocals`** —
  `verify-cutover.ts:558-559` records orphaned imports raising no error when the
  fieldset was once deleted. **Pin their absence; do not rely on svelte-check.**

## Should-fix from this lane

**C2** the `PAYMENT_CANCELLED_LEAD` exclusion is **unimplementable at whole-file
granularity** — scope the payment-link ban **per constant**, the pattern
`verify-booking-email.ts:368-372` already uses · **D3** `verify-cutover.ts:600-603`
(*"the island contains no price arithmetic of its own"*) is a **KEEP** that
becomes *more* valuable once no price renders, and §4's stated range straddles
it · **D4** `ASSESSMENT_TIER_NOTE` and `AFTER_HOURS_NOTE` compete for one pinned
position (`:1150`) · **D5** §5a's `/\bEverything above\b/i` is **shaped for the
sentence being deleted, not the claim** — the replacement drafts carry the same
implicature in different words; require a quoted-separately clause instead ·
**E2** the ~~six~~ **FOUR** `[service].astro` pages (asserted to be the ad entry points — **unsourced**) say *"no
obligation"* and no section names them — R1-A7 raised it and **the revision
dropped it** · **E3** ⚠ **`bk51-gated` removed BOTH `TRAVEL_FEE_*` references
from `[id].astro`, so on the target branch the Travel field carries NO guidance
at all** — at the moment decision 17a makes it a customer-facing expected
charge; and the "dead exports" claim is **true on the branch, false on `main`**,
a third independent reason §6's one-push rule is load-bearing · **E4**
`admin/appointments/new.astro:192` states *"Payment comes before dispatch —
client policy"*, which decision 17 reverses; owned by nobody · **E5** §7 omits
the `APPROVED_*` wording decision 19 itself says is open.

## Nits

**N1 — §4/§5's line ranges are 4–6 lines stale throughout** and the ticket is
implemented from them; re-derive · **N2** Class 2 has two entries and should
have six · **N3** §3's `PAYMENT_CANCELLED_LEAD` caveat is a spec item, not a
footnote.

## ✅ Confirmed clean by independent grep

`about.astro`, `insurance-claims.astro`, `index.astro`, all blog content,
`public/robots.txt`, `Navbar.svelte` (BK-29's sweep held), and every
`src/pages/api/**` response message. `llms.txt.ts:54` is correctly caught.
The drain SQL's statuses and columns are all real.

---

# 🔁 PLAN REVISION 2 — 2026-09-05. Written against all 16 blockers and decisions 17–19.

**Every case enumerated under the old plan has been re-derived against decisions
17–19**, per CLAUDE.md's rule-rewrite trap. Verdicts that CHANGED are marked ⇄.

## 1 · The pin polarity — resolving R1-B2, the structural blocker

BK-29's four bans exist because *"free assessment"* was **false under the credit
model**. Decision 17 makes it **true for the visit** — and decision 17a makes it
**conditional**, because a travel fee may follow. So neither keeping the ban nor
simply deleting it is right:

- **Keep the ban** → the ticket cannot ship its own copy. Build stays red.
- **Delete the ban** → nothing then stops a surface claiming an *unconditional*
  free assessment, which is false the moment a travel fee applies. That is the
  pre-prepay claim's exact shape, re-armed in the opposite direction.

### ⇄ THE REPLACEMENT IS A **COUPLING PIN**, NOT A BAN AND NOT A PERMISSION

> **On any surface that claims the assessment is free, the travel-fee qualifier
> must also be present.** The claim and its qualifier travel together or the
> gate is red.

This is the **positive twin** CLAUDE.md requires, and it is the only pin shape
that survives a flip back: under a future paid model the free claim simply
stops appearing and the pin goes quiet, rather than needing to be re-inverted.
It also directly answers R1-D8 — the twins **can** now be written, because the
coupling defines them.

**Scope, merging both reviewers' conflicting fixes.** R2 is right that widening
`CLAIM_SURFACES` to render files is near-vacuous: `verify-cutover.ts:735-746`
reads **each file's own source**, and the claims live in `booking-copy.ts`.
R1 is right that the ban list is incomplete. **Therefore both, in two places:**

| Scan | Why |
| --- | --- |
| **`src/lib/booking-copy.ts`** | where the claims actually live — catches them before a render site is even chosen |
| **the built `dist/` output** | how `BANNED_SHAPES` (`:1056-1071`) already works, and how **BK-29 found the claim surviving in `dist/` after a green source sweep** |

Adding the five render files is **not** part of the fix. Recorded so a later
reader does not re-add them as an oversight.

**⇄ `verify-cutover.ts:470-475` (`!/no obligation/i`) is STRUCK, not inverted.**
It was true under the credit model and is false now. Whether *"no obligation"*
goes back **on** is W20/W21's call and a user decision — this ticket removes the
prohibition and asserts nothing in its place.

## 2 · The replacement copy — DRAFTED FOR APPROVAL, not adopted

⚠️ **NOTHING BELOW SHIPS WITHOUT THE USER'S SIGN-OFF.** Client-facing wording is
a user decision per CLAUDE.md; these are drafts to react to, so the ask is a
yes/no rather than a blank page. **The travel-fee AMOUNT is deliberately absent**
— W22: existence needs no decision, the number does.

### The terms box — replaces INTRO / ITEMS / PAYMENT / REFUND / CREDIT

`FEE_TERMS_HEADING` (*"Assessment terms"*) **stays** — the checkbox makes a
claim about it. Three items, one per true term under decision 17:

1. *"The on-site assessment is free. It takes about 30 minutes."*
2. *"If you are outside our usual service area, a travel fee may apply. We tell
   you the amount when we call to confirm — never after the visit."*
3. *"Any report, estimate, or restoration work is quoted separately — at the end
   of your visit or after it."*

**`FEE_TERMS_ACK_LABEL`** ⇄ *"I understand the assessment terms above, including
that a travel fee may apply."* Keeps the reference form (so the "above" markup
claim and its pin survive) and surfaces the one thing that can cost money.

### The other surfaces

| Surface | Draft |
| --- | --- |
| `book.astro:94` + `ContactSection.astro:287` eyebrow | *"Free on-site assessment"* |
| `book.astro:36-37` meta description **and JSON-LD** (R1-A3/R2-SF1) | *"Book a free on-site restoration assessment in Edmonton. About 30 minutes. A travel fee may apply outside our service area — we confirm it before we come."* |
| `FEE_TERMS_INTRO` (`:368`) | *"We review every request before confirming it. Once we approve yours, we email you to confirm your appointment and send the calendar invite."* |
| `RECEIVED_NEXT_STEPS[1]` | *"If we can take it, we email you to confirm your appointment."* |
| `RECEIVED_NEXT_STEPS[2]` | *"Your calendar invite comes with that email."* |
| `QUOTE_TIMING_NOTE` (`:613`) | *"Nothing is charged now. We review your request and email you to confirm."* |
| `llms.txt.ts:54` | *"Pick a time for an on-site assessment — about 30 minutes, free. Booking online sends a request: the office reviews it, and if approved the customer gets a confirmation email and a calendar invite. A travel fee may apply outside the usual service area, confirmed before the visit. Any report, estimate or restoration work is quoted separately. This is the way to request a quote."* |
| `FEE_TERMS_CREDIT` (`:525`) | **DELETED** — there is no credit; the work is quoted separately |

## 3 · ⇄ TWO "OPEN QUESTIONS" NOW CLOSE THEMSELVES — and both close as KEEP AS-IS

**R2-SF2 `/book/payment-cancelled/` and R2-SF3 `/book/confirmed/`.** Both were
flagged as carrying payment copy that survives the flip. **Re-derived: their
copy is TRUE where it renders.**

`cancel_url` is set **only** by `createCheckoutSession` (`booking-payment.ts:384`),
which is reached **only** when `totalCents > 0` (`review.ts:329` routes `=== 0`
to `approveFree`). Likewise `/book/confirmed/` is the Stripe **success** redirect.
**So a visitor reaches either page only when a real payment genuinely exists** —
`PAYMENT_CANCELLED_LEAD`'s *"the payment link in your email"* and `PAID_LEAD`'s
*"what you paid"* are accurate on arrival, not stale.

**Both routes KEPT, both strings KEPT.** Deleting the route would 404 the
`cancel_url` of any session open at deploy time (R1-D3). ⚠ **`PAYMENT_CANCELLED_LEAD`
must still come OFF the payment-link ban list's scope**, or a true sentence
turns the gate red — an instance of the derived-pin trap, caught before it was
written.

## 4 · The reddened assertions — R1-B1, classified before anything is touched

Per CLAUDE.md (BK-44): **establish whether each arm relied on the old behaviour
BEFORE touching the rule. Make the FIXTURE honest; never loosen the rule to
quiet the suite.** Three classes, and they get different treatment:

| Class | Arms | Treatment |
| --- | --- | --- |
| **Asserted a claim this ticket legitimately removes** | `verify-booking-email.ts:246-254`, `:291-296`, `:304`/`:311`/`:321`, `:339-349`, `:374`, `:424`; `verify-cutover.ts:350-357`, `:364-372`, `:396-416`, `:441-456`, `:536-545`, `:610-620` | **Rewrite to assert the REPLACEMENT.** Never delete — a deleted assertion is an unpinned claim |
| **Asserted structure that still holds** | `:525-547` (`carries`), `:590-600` (the three `formatCents` renders) | Re-point at the new constants. ⚠ **`:590-600` depends on the picker decision below** |
| **Cannot survive as written** | `verify-booking-pricing.ts:10, 69-88` — imports `FEE_TERMS_ITEMS` and zips it to `ASSESSMENT_TIERS` | ⇄ **The "keep it running against `booking-pricing.ts`" row is CORRECTED:** the *engine* pins stay; the **`FEE_TERMS_ITEMS` zip is deleted**, because the copy it checked no longer exists. Deleting a check whose subject is gone is not loosening |

**R2-SF4 — `FEE_TERMS_REFUND[4] === CANCEL_LINE`.** Deleting the array silently
removes **one of the two `CANCEL_LINE` renders** in every customer email.
**Positively pin the survivor** at `booking-email.ts:574`/`:627` so it stops
being an accidental one.

**R1-B3 — the emptied-array hazard.** If a constant is **emptied** rather than
deleted, `for (const line of carries)` runs **zero iterations and the suite
stays green having asserted nothing** — the crashed-script trap in a new body.
⚠ **Rule for this ticket: delete constants, never empty them. And the red-first
log records the summary line's ASSERTION COUNT, not just the exit code**
(the count moved 1509 → 1513 → 1540 across BK-51; a drop is the signal).

## 5 · ⇄ THE TIER PICKER — decision 17's consequence, and it is not copy

`BookingForm.svelte:1171-1213` renders `{formatCents(...)} + GST` beside every
radio plus Base/GST/Total (R1-A1). Under decision 17 **those prices are gone**,
and the deliverables they described are **quoted separately, not included**.

**The picker itself SURVIVES** — decision 15, deliberately, and reversing that
here would be a rule rewrite of its own. What changes:

- **the three `formatCents` renders and the whole quote block are deleted**, and
  `verify-cutover.ts:590-600`, which positively *requires* them, is rewritten to
  assert their **absence** plus the presence of the tier labels;
- ⚠ **`ASSESSMENT_TIER_DESCRIPTIONS` (`:580-587`) must stop implying the report,
  the sketch and the lab work come with the free visit.** Prices off + these
  descriptions unchanged = the implicature survives the deletion. **This is the
  fee-terms trap relocated, and it is the single subtlest item in the ticket.**
  Drafts needed; user sign-off required.
- ⚠ **`verify-cutover.ts:556-565` records the picker being deleted with four
  scripts green and the funnel 100% dead on a 422 for `assessment_tier`.**
  Any edit here keeps a **positive** pin that the picker still submits.

## 5a · ⇄ THE TIER PICKER COPY — DRAFTED. *(the implicature fix, §5's subtlest item)*

**The problem in one line:** delete the prices and leave
`ASSESSMENT_TIER_DESCRIPTIONS` alone, and *"Everything above, plus a written
cause-of-loss report…"* now reads as **three things included with a free
visit.** Nothing false was written; the false reading is created by removing the
prices next to it. **That is the fee-terms trap relocated** — and it is exactly
the shape CLAUDE.md's copy-inventory trap describes, a claim carried by a
sentence rather than by the constant anyone would grep for.

**Fix at two levels — a group note that states the rule once, and per-tier lines
that stop implying inclusion.** The group note is preferred over repeating the
caveat three times: one sentence, one pin, one place to change on a flip back.

**🆕 `ASSESSMENT_TIER_NOTE`** *(new constant, sits under the legend)*
> *"The assessment visit is free, whichever you pick. Anything beyond it — a
> report, an estimate, a sketch — we quote after we have seen the damage."*

**`ASSESSMENT_TIER_LEGEND`** ⇄ *"Choose your assessment"* → **"What do you think
you need?"** — the picker is now an indication that helps the office prepare,
not a purchase. Decision 15 kept the control; decision 17 changed what choosing
means.

| Tier | Draft description |
| --- | --- |
| `standard` | *"A technician walks the affected areas and writes up the scope of the damage."* **(unchanged — already true and already free)** |
| `report` | *"The visit, plus a written cause-of-loss report and a repair estimate — the documentation your adjuster works from if you are filing a claim. We quote the report after the visit."* |
| `sketch` | *"The visit, plus a report and a measured sketch of the affected areas — quoted after the visit. Where lab samples are taken, results take 3–5 business days."* |

⚠️ **The lab-turnaround sentence is PRESERVED VERBATIM in intent.** It is a
**client requirement from 2026-08-18**, not drafting, and its rationale still
holds word for word: *"documentation they expected on the day is a complaint
rather than a deliverable."* Under decision 17 it matters **more**, not less —
the customer is now also waiting on a quote. **Do not drop it while editing
around it.**

⚠️ **`ASSESSMENT_TIER_NAMES` is left ALONE** — *"Assessment + written report"*
describes scope, not price, and is true under decision 17. Recorded so a later
sweep does not rewrite it for symmetry.

**Pins:** `ASSESSMENT_TIER_NOTE` gets a positive pin (present, and rendered
above the radios); each description gets a negative pin on
`/\bEverything above\b/i`, the exact phrase that carries the inclusion
implicature — **pinning the claim shape, not the constant name.**

## 6 · Sequencing — R2-B4, and it inverts the ticket's own headline

**BK-53 is built ON `bk51-gated`. BK-51 + BK-53 ship in ONE push.**
*"GATE 1 blocking BK-51"* reads as *BK-53 first*; deployed alone on `main` it
gives a priceless site with an Approve screen still pre-filling $418.95
(`[id].astro:505-512`, verified) and minting Checkout Sessions. **Never
separately.**

**The drain step — R2-B5.** ⇄ The step named only `pending_review`, the **benign**
direction (decision 16 accepts those becoming free). Corrected to three statuses,
with the probe written out per BK-48 rather than an instruction to check:

```sql
SELECT id, status, payment_due_at, total_amount_cents
FROM appointments
WHERE status IN ('pending_review','approved_awaiting_payment','payment_expired')
ORDER BY status, slot_start;
```

`approved_awaiting_payment` is the one that hurts: those rows hold a live
Checkout Session and a deadline, a copy deploy touches none of them, and the
customer pays days later for a visit the site now calls free.
**Its output is pasted into this ticket before the push.**

## 7 · What still needs the user

1. **Sign-off on §2's drafted copy** (or edits).
2. **`ASSESSMENT_TIER_DESCRIPTIONS` rewrite** — §5, drafts to follow.
3. **The travel-fee amount**, or confirmation we disclose existence without a
   number (W22 says existence needs no decision).
4. **Does *"no obligation"* go back on?** — §1, W20/W21's call.

---

# ⛔ PLAN REVIEW ROUND 1 — 2026-09-05. NOT APPROVED.

**11 blockers / 8 should-fix / 4 nits.** The three most consequential are
recorded here in full; the reviewer's numbering is preserved. **The surface
table below is superseded in the places these findings name — read this section
first.** Every finding quoted here was re-verified against the code before
being written down.

## 🔴 B2 — THE GATE FORBIDS THE COPY THIS TICKET EXISTS TO SHIP

**This is the structural finding and it reshapes the ticket.** Four live
assertions ban the claim "the assessment is free":

| Pin | Scope |
| --- | --- |
| `verify-cutover.ts:512-517` | `!/free\s+assessment/i` on the rendered templates of `book.astro` and `ContactSection.astro` |
| `verify-cutover.ts:1056-1071` | `BANNED_SHAPES` — `/\bfree[^.<>]{0,40}?assessments?\b/i` **and** the predicative `/\bassessments?\b[^.<>]{0,24}?\b(?:is\|are\|'s)\s+free\b/i` — over **every built `.html` and `.js` in `dist/`** |
| `verify-cutover.ts:1188-1193` | *"llms.txt does not tell an assistant the assessment is free"* |
| `verify-cutover.ts:470-475` | `!/no obligation/i` on the same two files — **true** under the free model, so the ban is now wrong |

**Verified by direct read.** `npm run verify:cutover` fails the build on any
page or bundle that says the assessment is free. **That is the entire purpose
of this ticket.** BK-29 ("remove the free-assessment claim site-wide") wrote
those pins under the paid model; the model has flipped and every case
enumerated under the old rule must be re-derived — **CLAUDE.md's rule-rewrite
trap, at repository scale.** The ticket's "Do NOT touch" list names three pins
to preserve and is silent on all four of these.

**A plan that cannot produce a green gate for its own replacement copy is not a
plan.** Inverting these pins is now a first-class part of BK-53, not cleanup.

## 🔴 B1 — ~20 EXISTING ASSERTIONS GO RED, AND THE PLAN NAMED NONE

CLAUDE.md: *"A fix that turns EXISTING assertions red is a question about the
assertions, not about the fix."* The plan's only pin work was "add banned
regexes, widen the file list."

`verify-booking-email.ts` positively pins the content being deleted — `:246-254`
(`figures.includes('399'/'699'/'1,199'/'GST')`, its own comment calling itself
*"the reason a silent price deletion cannot ship"*), `:291-296` (the credit),
`:304`/`:311`/`:321` (the three refund windows), `:339-349` (refund block
ORDER), `:374` (*"`FEE_TERMS_PAYMENT` names the payment link"*), `:424`
(`AFTER_HOURS_NOTE` states the multiplier), `:525-547` (`carries` over
{request, confirmed} × {html, text}), `:638`, `:740-750`.
`verify-cutover.ts` — `:350-357`, `:364-372`, `:396-416`, `:441-456`,
`:536-545`, `:575-581`, `:590-600`, `:610-620`.

⚠️ **And `verify-booking-pricing.ts:10, 69-88` imports `FEE_TERMS_ITEMS` and
zips it to `ASSESSMENT_TIERS`.** The "delete to a boundary" table says *"keep
`verify-booking-pricing.ts` running against it"* — **it will not typecheck** if
`FEE_TERMS_ITEMS` is deleted. That row is not achievable as written.

**Per CLAUDE.md (BK-44):** establish for each reddened arm whether it was
relying on the old behaviour BEFORE touching it. Do not loosen a rule to
quiet a suite.

## 🔴 D1 — THE TICKET CONTRADICTS ITSELF ON `FEE_TERMS_REFUND`

- Line ~260: **"Delete `FEE_TERMS_REFUND` outright"** — *"a refund policy a
  config flip can switch on is a policy nobody reviewed."*
- Lines ~219-221: **row-gate the terms block on `settled.totalCents > 0`** so a
  paid-era resend receives the terms it was actually charged under.

**Both cannot hold.** Delete the constants and the `> 0` branch has nothing to
render, so a Resend on a paid-era row (`#36`, `cs_live_`, $628.43) silently
drops the refund policy from the record of a completed purchase — which
`booking-email.ts:499-510` says in as many words must not happen. Two decisions
made under different predicates, neither re-derived against the other.
**Rule-rewrite trap. Policy question — escalate, do not let an implementer
pick.**

## The other eight blockers, in brief

- **A1 — the surface table's single worst error.** `BookingForm.svelte:1171-1213`
  renders `{formatCents(tierQuotes[tier].baseCents)} + GST` beside **every
  radio**, plus Base/GST/Total at `:1196-1213`, fed by `assessmentQuote`.
  **The table below asserts `TIER_PRICE_CENTS` is "kept intact and
  unreferenced." It is referenced, live, on the public form every web booking
  passes through** — the largest price surface on the site. Verified by direct
  read. `verify-cutover.ts:590-600` positively *requires* those three renders.
- **A2 — every REQUEST email prices the assessment.** `booking-email.ts:459-463`
  → `assessmentSummary` `:441-450`: `settled` is null on a request, so it
  renders *"You chose: Standard assessment — $399.00 + GST ($418.95 total)"*
  plus *"— weekend rate, 1.5x"*. Computed from `assessmentQuote`, **not** from
  `FEE_TERMS_ITEMS`, so the table's `booking-email.ts:525` row does not cover it.
- **A3 — `/book/`'s meta description, OG tags and JSON-LD carry the credit
  claim.** `book.astro:36-37` — *"the fee is credited back in full when you go
  ahead with the work"* — reaching `<meta name="description">`, `og:description`,
  `twitter:description` and the `WebPage` schema at `:49`. **This falsifies the
  ticket's own "structured data is fine, there is no price anywhere in it"**,
  which was reached by grepping `priceRange|priceCurrency|lowPrice|highPrice` —
  a constant-shaped grep for a claim stated in words. The trap, again.
- **A4 — `FEE_TERMS_CREDIT` (`booking-copy.ts:525`) is absent from claim 4**,
  though it renders on four surfaces: `ContactSection.astro:357`,
  `BookingForm.svelte:1125`, `book.astro:149`, `booking-email.ts:533`/`:610`.
- **A5 — `book.astro:146` renders `FEE_TERMS_REFUND`**; claim 3 names only
  ContactSection, the island and the email. `/book/` is the indexed page.
- **D2 — `settled.totalCents > 0` is a two-state gate on a three-state value.**
  `settledAmountsOf` returns **null** for the thirteen migration-008 pre-prepay
  `confirmed` rows. Written literally it does not typecheck; written
  `settled?.totalCents > 0` it routes those rows into the FREE branch. Must be
  three-state (`null` / `0` / `>0`).
- **D3 — `/book/payment-cancelled/` IS still reachable, and my premise was
  wrong.** Verified: `review.ts:329` routes `if (totalCents === 0)` to
  `approveFree`, **else** `createCheckoutSession` (`:487`), whose `cancel_url`
  is `BOOKING_PAYMENT_CANCELLED_PATH` (`booking-config.ts:307`, `:348`).
  **Free is the DEFAULT, not the only path.** The open question below —
  *"whether the page should keep existing"* — **has an answer: it must**, or
  the live paid path gets a 404 as its Stripe cancel URL. Its copy needs
  correcting, not deleting.
- **D4 — nothing in the plan says what a CHARGED customer is told.** Same root
  as D3. Because the office can still enter an amount, deleting the price, the
  refund promise and the payment-link sentence does not retire those claims —
  it leaves a **live money path the site now says nothing about.** A customer
  approved at $399 gets a link and a deadline having ticked a box whose terms
  no longer mention refunds, the deadline or the credit. **This is the
  strongest argument against the copy-only classification.**

## Should-fix, condensed

**A6** `AFTER_HOURS_NOTE` (`booking-copy.ts:597`) missing from claim 6, pinned
in three places · **A7** the four `src/pages/[service].astro` landing pages say
*"no obligation"* at `:79`, and the table never names that file · **A8**
`PAYMENT_CANCELLED_HELP_LINE` (`:780`) is a second payment claim on that route ·
**B3** emptying an array rather than deleting it makes `verify-booking-email.ts:525-547`
loop zero times and **stay green having asserted nothing** — twin of the
crashed-script trap; **the red-first log must assert the summary line's
assertion COUNT, not only the exit code** · **D5** `FEE_TERMS_PAYMENT` is
index-addressed in three files (`booking-email.ts:1150` `slice(0,2)`,
`verify-booking-email.ts:538-540`, `:556-560`) — shrinking it silently changes
what `slice` returns · **D6** `terms_acked_at` snapshot named as a risk then
not scheduled; either do it here or record it in ROADMAP Known traps with an
owner, per the out-of-scope rule · **D7** the drain step states the answer, not
the probe — write the literal SQL · **D8** the positive twins cannot be written
until B2 is decided.

## ✅ What the review CONFIRMED

- **The `booking-copy.ts:134-192` boundary is correct.** All six symbols are
  consumed only by `booking-admin-notify.ts` (`:489-491`, `:517`, `:546-547`),
  every call gated on a real refund outcome on the row. None renders on a
  forward-looking surface. **Keep them.**
- **The pin-gap claims (i) and (ii) are both confirmed by inspection** —
  `CLAIM_SURFACES` is five files, and no existing shape matches the payment-link,
  price or credit claim. They are simply not the whole gap.
- **D9 — `TIER_PRICE_CENTS` poses no arithmetic hazard.** No division; integer
  arithmetic throughout. Keeping `booking-pricing.ts` intact is safe.

## ⚖️ TIER — "copy-only, plain Reviewed" DOES NOT HOLD. Adversarial pass required.

The trigger is a property of the change, not of the ticket's description of
itself. Four independent clears: **(1)** it touches what the appointment
insert's consent attests to — `FEE_TERMS_ACK_LABEL` feeds the box whose tick is
stamped at `booking-commit.ts:91-99`, and `verify-cutover.ts:520-545` exists
because a review once deleted that box with every gate green and `terms_acked_at`
stamped against terms never shown; **(2)** it moves money claims on a live send
path (`resend.ts`, and D1/D2 show the conceded scope was drawn before the
contradiction was found); **(3)** it removes the live price display from the
public form (A1) — `verify-cutover.ts:556-565` records the picker being deleted
with four scripts green and the funnel 100% dead on a 422; **(4)** its main
artifact IS the pin set, and BK-51 measured 11/11 adversarial breaks green in a
suite nobody was rewriting.

**Recorded decision: BK-53 takes a full adversarial pass.** The ticket's
earlier "plain Reviewed" line is struck.

---

# ⛔ PLAN REVIEW ROUND 2 — 2026-09-05. Customer / irreversibility / policy lane.

**5 blockers, 5 should-fix, 5 nits, 7 user decisions.** Verdict: *"The plan's
shape — delete to a boundary, keep the pricing engine, snapshot nothing new in
the DB — is right; its enumeration and its one write-path fix are not."*

## 🔴 R2-B4 — BK-53 ALONE ON `main` MANUFACTURES THE DRAIN STEP'S OWN WORST CASE

**Verified by direct read.** `main`'s `[id].astro:505-512` still computes
`suggestedQuote` from `assessmentQuote(tier, service, slot)`. **BK-51's $0
default exists only on `bk51-gated`.**

So a BK-53 deployed by itself yields a live site with **no price, no refund
policy and no payment-link claim anywhere a customer can read**, while the
Approve screen still pre-fills $418.95, mints a Checkout Session
(`booking-payment.ts:383-384`) and sends `APPROVED_*` with a link and a
deadline. That is *"a customer who read free gets a payment link"* — the drain
step's own worst case — applied to **every new booking** rather than to a small
backlog.

**The ticket calls itself "GATE 1 blocking BK-51's push", which reads as
BK-53 FIRST. That reading is dangerous.** The ticket names no branch at all
while `ROADMAP.md:92` says `git checkout bk51-gated  # to work on BK-51 / BK-53`.
**Corrected: BK-53 is built ON `bk51-gated`, and BK-51 + BK-53 ship in ONE
push, never separately.**

## 🔴 R2-B1 — `FEE_TERMS_PAYMENT[0]` SHIPS ON THE ONLY MESSAGE THE FREE CUSTOMER KEEPS

`booking-copy.ts:460` — *"**Those are our standard rates.** What you pay depends
on the service you need and the time you pick — the booking form shows that
price before you send your request, and the approval email states the amount we
will charge."*

`feeTermsPaymentFor` hands `[0]` **and** `[1]` to the **confirmed** arm. Delete
`FEE_TERMS_ITEMS` and the free customer's confirmation reads: *Assessment
terms* → *"Those are our standard rates."* → *"There is no charge for this
visit."* **"Those" refers to nothing that was printed** — and three further
clauses are independently false (no price on the form, no approval email on the
free path, no "amount we will charge").

`[3]` (`:463`) meanwhile threatens, on `/book/`, the island and both request
emails, to **release the slot over a payment nobody will ask for.**

⚠️ **The ticket's own §"Re-derive from `feeTermsPaymentFor`" says in as many
words that fixing `[2]` alone ships `[0]` on the kept message — and the table
below it still omits `[0]` and `[3]`.** Reviewers work from the table.

## 🔴 R2-B2 — CLAIM 7's FIX HAS AN UNSTATED POLARITY, AND NEITHER POLARITY WORKS

Gate the terms block **on** `totalCents > 0` and it renders **exactly for the
paid-era row** — `#36` receives free-mode terms, which is the defect verbatim.
**Invert** it and the paid-era row gets **no terms at all**, contradicting
`booking-email.ts:501-511` (*"terms that vanish from the record of a completed
purchase are terms nobody can point at afterwards"*).

**Root cause: once `FEE_TERMS_REFUND` is deleted outright — this ticket's own
boundary decision — no text remains in the tree that is true for a paid-era
row. A gate SELECTS; it cannot RESTORE.** Only two coherent resolutions, and
the ticket must pick one:

- **(a)** snapshot the rendered terms (or a `terms_version`) at insert
  (`booking-commit.ts:91-99`) and render `resend.ts:106` from the snapshot; or
- **(b)** suppress the terms block for pre-flip rows and state plainly that the
  terms record for paid-era rows is now git-history-only.

⚠️ **`ROADMAP.md`'s entry for this same defect specifies the gate AND the
snapshot together; this ticket kept the gate and demoted the snapshot to a
parenthetical.** A row that disappeared between two versions of a plan is a
decision nobody made — **rule-rewrite trap.** And this is the one item the
ticket scopes its adversarial pass to, so it is the one spec that must not be
ambiguous.

## 🔴 R2-B3 — THE REQUIRED CHECKBOX SURVIVES OVER AN EMPTY BOX

`FEE_TERMS_ACK_LABEL` → `BookingForm.svelte:1222`, required at
`booking-form.ts:276`, mirrored at `booking-payload.ts:316`, stamped
`terms_acked_at` (`booking-commit.ts:99`), shown to the office
(`[id].astro:1018`).

Remove INTRO/ITEMS/PAYMENT/REFUND/CREDIT and all three terms boxes render
`FEE_TERMS_HEADING` (*"Assessment terms"*) over an **empty `<ul>` and zero
paragraphs**. The label's *"the assessment terms **above**"* then points at a
bordered box containing a heading and white space — **and the checkbox is still
REQUIRED.** A customer at step 3 is stopped by a mandatory tick accepting terms
that are not there, on a funnel already at ~1 booking/fortnight, in the month
the free offer is being judged. **User decision — do not resolve in code.**

## 🔴 R2-B5 — THE DRAIN STEP NAMES THE BENIGN STATUS AND MISSES THE HARMFUL ONE

- `pending_review` → approved post-flip becomes a **free** booking. Decision 16
  explicitly accepts that cost. **This is the benign direction, and it is the
  only one the step names.**
- **`approved_awaiting_payment` is the one that hurts:** those rows already
  hold an emailed payment link, a `payment_due_at` and a **live Checkout
  Session**. A copy deploy touches none of them. The customer pays days later
  for a visit the site now calls free, and `markPaid` confirms it.
- `payment_expired` is a third: post-flip that customer receives
  `PAYMENT_EXPIRED_LEAD` — *"was not paid for by the deadline, so we have
  released it"* — **about a free assessment.**

The step also names no query, no owner and no evidence. Per BK-48: **write the
probe, and require its output pasted into the ticket before the push.**

## ⚠️ R2 REFUTES ROUND 1's PIN FIX — it would be near-vacuous

`verify-cutover.ts:735-746` reads **each surface file's own source text**. The
claims live in `booking-copy.ts`. **None of the five files round 1 proposes
adding contains any claim text — they render imported constants.** Widening
`CLAIM_SURFACES` to them therefore catches almost nothing.

**The fix must instead scan `booking-copy.ts` itself and/or the built `dist/`
output** — which is how `BANNED_SHAPES` at `:1056-1071` already works, and how
BK-29 found the claim surviving in `dist/` after a green source sweep. **Both
reviewers' pin sections must be merged before implementation; neither alone is
correct.**

## Should-fix (round 2)

**SF1** `book.astro:37`'s meta description carries the credit claim **and is
interpolated into the WebPage JSON-LD at `:49`** — independently found by both
reviewers, and it **falsifies this ticket's own one-way-door certification**
(*"Structured data. There is no price anywhere in it"*, reached by grepping
`priceRange|priceCurrency|lowPrice|highPrice` — asking what the pin is NAMED,
not what it MATCHES; BK-48's lesson firing inside the paragraph that certifies
reversibility). **The SERP snippet is what a searcher reads before the page
loads** · **SF2** close the open question as **KEEP `/book/payment-cancelled/`**
— fix `booking-copy.ts:778`, keep the route · **SF3** `/book/confirmed/`'s
`PAID_HEADING`/`PAID_LEAD` (*"what you paid"*) are unexamined and the page is
**not** dead on the manual path — decide explicitly, even if the answer is
leave-as-is · **SF4** `FEE_TERMS_REFUND[4] === CANCEL_LINE`, so deleting the
array silently removes **one of the two `CANCEL_LINE` renders** in every
customer email — positively pin the survivor at `booking-email.ts:574`/`:627`
rather than leaving it an accidental one · **SF5** add an explicit **KEEP** list
for the still-true "nothing has been charged" family — `DECLINED_LEAD` (`:704`),
`EXPIRED_REQUEST_LEAD` (`:784`), `PAYMENT_EXPIRED_LEAD` (`:828`).

## Lane C — the boundary, judged

- **Keep `booking-pricing.ts` intact and unreferenced — agreed without
  reservation.** Pure computation, no customer-facing text, 42 pins free to
  keep green, and it is exactly what a fourth pricing model needs.
- **Delete the forward-looking refund promise — agreed. Scope — disagreed.**
  The ticket then leans on a `resend.ts` gate to cover the historical case, and
  after deletion there is nothing left to gate to (R2-B2). Separate them, and
  **take the terms snapshot: under decision 16 it is the single
  highest-value item in this ticket, because it is the one structure that
  survives an arbitrary number of flips** — and it is currently a parenthetical.
- 🔻 **`ASSESSMENT_PRICES_SHOWN = false` — DEAD WEIGHT AS SPECIFIED.** A boolean
  never read in a conditional is a comment with type-checking. It rots, or
  worse someone reads it as a switch, flips it, and gets a half-restored paid
  mode with prices back and **no refund policy and no payment terms** — exactly
  the un-reviewed policy this ticket refuses to let a config flip create.
  **Better: make the terms function itself the anchor** — `assessmentTermsBlock()`
  is greppable, is *called*, and cannot rot into an unused export. If the
  constant stays, **assert it** (exists, is `false`, referenced at N sites).
  *"An unasserted grep anchor is the thing this repo's trap log keeps paying
  for."*

## Lane D — what the site now promises, and where it misleads

Reduces to: a ~30-minute on-site visit, a written scope, no charge for the
visit, a reviewed request, a held slot. **Nothing about the report, lab work,
travel, or what happens if money is ever asked for.**

- **Gap 1 — free VISIT vs free REPORT, the sharpest.** Decision 15 keeps the
  tier picker, and `ASSESSMENT_TIER_DESCRIPTIONS` (`:580-587`) promises a
  cause-of-loss report, a repair estimate, a measured sketch and **lab samples
  at 3–5 business days**. Prices deleted, those become three *free* options,
  the top one carrying a **real per-sample cost**. Decision 16's accepted "one
  or two bad free bookings" was priced against a technician's hour, **not
  against a lab invoice.**
- **Gap 2 — the manual non-zero path has no terms behind it at all.**
  `APPROVED_*` (`booking-email.ts:738-771`) carries no fee terms and no refund
  policy — it never had to, because the terms lived on the booking surfaces.
  After BK-53 they live nowhere. A customer charged $250 by hand has no
  published refund policy, an acknowledged box that said nothing about money,
  and `APPROVED_DEADLINE_NOTE` disclosing the slot-release consequence **for
  the first time in the email asking for money.**
- **Gap 3 — "no charge" reads wider than intended.** `NO_CHARGE_LINE` becomes
  the only money sentence in the confirmed email, and deleting
  `FEE_TERMS_CREDIT` removes the last statement that the **restoration work**
  is billed at all.
- **Gap 4 — copy that implies an amount without naming one:**
  `FEE_TERMS_PAYMENT[0]`, `book.astro:37`, `QUOTE_TIMING_NOTE` (*"Nothing is
  charged **now**"*), `PAID_LEAD` (*"what you paid"*), `PAYMENT_CANCELLED_LEAD`,
  `booking-payment.ts:288`, and `DECLINED_LEAD`'s *"Nothing has been charged"*
  — which implies a world in which something could have been.

---

## ✅ THE SURFACE TABLE, RE-DERIVED FROM THE CLAIMS — 2026-09-05

⚠️ **SUPERSEDED IN PART by BOTH PLAN REVIEWS above** — round 1's A1–A8 and
round 2's B1/SF1/SF3 add surfaces this table misses, and A1 corrects its
`TIER_PRICE_CENTS` row outright. Read them together; do not implement from this
table alone.

⚠️ **SUPERSEDED IN PART by PLAN REVIEW ROUND 1 above** — findings A1–A8 add
surfaces this table misses, and A1 corrects its `TIER_PRICE_CENTS` row outright.
Read them together; do not implement from this table alone.

**Both tables above are superseded by this one.** Built the way the ticket
itself prescribes and CLAUDE.md's copy-inventory trap demands: **grep the
claim across `src/`, in every grammar, then find what renders it** — never
start from the constant. Every row below was produced by a grep, not by
reading the previous table. **Two live customer-facing surfaces appear here
that neither table above names**, which makes this the *fourth* instance of the
trap inside a ticket whose own header warns about the third.

### Claim 1 — the literal tier prices

| Where the claim LIVES | What renders it |
| --- | --- |
| `booking-copy.ts:402-404` `FEE_TERMS_ITEMS` — `$399 + GST` / `$699 + GST` / `$1,199 + GST` | `ContactSection.astro`, `BookingForm.svelte`, `book.astro`, **and `booking-email.ts:525`** |
| `booking-pricing.ts:55-57` `TIER_PRICE_CENTS` (`39900`/`69900`/`119900`) | the engine. **Kept intact and unreferenced** — "delete to a boundary" |

### Claim 2 — a payment link exists · `/payment link|payment goes through|secure payment/i`

| Where the claim LIVES | What renders it |
| --- | --- |
| `booking-copy.ts:368` `FEE_TERMS_INTRO` | **five** — `ContactSection.astro:347`, `BookingForm.svelte:1113`, `book.astro:11`, **both request-email arms** |
| `booking-copy.ts:462` `FEE_TERMS_PAYMENT[2]` | `book.astro:143`, `ContactSection.astro:351`, `BookingForm.svelte:1119`. ⚠ **NOT the confirmed email** — `booking-email.ts:1150` slices `[0,2]` |
| `booking-copy.ts:613` `QUOTE_TIMING_NOTE` | quote path |
| `booking-copy.ts:639-640` `RECEIVED_NEXT_STEPS[1]`/`[2]` | `/book/received/` via `BookingConfirmation.svelte:149` **and both request-email arms** — the first thing every web customer reads |
| 🆕 **`booking-copy.ts:778` `PAYMENT_CANCELLED_LEAD`** — *"The payment link in your approval email works until the deadline it gives"* | **`/book/payment-cancelled/`** — a whole live route, named by NEITHER table above. Under the free model the default path mints no Stripe session, so this page is unreachable for a free booking **and still asserts a payment link exists for anyone who lands on it** |
| `llms.txt.ts:54` | the file an AI assistant quotes with no page around it |

### Claim 3 — refund promises

| Where the claim LIVES | What renders it |
| --- | --- |
| `booking-copy.ts:504-510` `FEE_TERMS_REFUND` — four promises | `ContactSection.astro:354`, `BookingForm.svelte:1122`, and the same email as `NO_CHARGE_LINE`. **Deleted outright**, not gated |

⚠️ **BOUNDARY — do not over-delete.** `booking-copy.ts:134-192`
(`CANCELLED_REFUNDED_LEAD`, `REFUNDED_HEADING`, `REFUNDED_LEAD`,
`refundedAmountLine`, `REFUND_TIMING_LINE`, `REFUND_CHASE_LINE`) is **not this
ticket's**. That family serves **real historical paid rows** — production
carries a live `cs_live_` charge (`#36`, $628.43, refunded 2026-08-20) — and it
is BK-33's closing artifact. **Deleting the forward-looking refund PROMISE is
this ticket. Deleting the machinery that tells a past customer their refund is
moving would destroy evidence and break BK-33.** Keep them.

### Claim 4 — credited in full

| Where the claim LIVES | What renders it |
| --- | --- |
| `ContactSection.astro:287` — *"Credited in full when you go ahead"* | the eyebrow |
| 🆕 **`book.astro:94`** — the **same sentence**, second instance | `/book/`. The table above attributes this claim to `ContactSection.astro:287` **alone**. Deleting only that one leaves the claim live on the booking page itself |
| `llms.txt.ts:54` | a price claim in words, no dollar sign — walks past every `\$`-anchored pin |

### Claim 5 — the acknowledgment asks about an amount

`booking-copy.ts:541` `FEE_TERMS_ACK_LABEL` — asks the customer to tick
*"including the amount I will be asked to pay"*. There is no amount.

### Claim 6 — the weekend 1.5×

`booking-copy.ts:461` `FEE_TERMS_PAYMENT[1]`; engine at `booking-pricing.ts:87-99`.

### Claim 7 — the resend/terms-snapshot defect · Known trap, HIGH, owned here

`resend.ts:106` re-renders **today's** constants against a **stored** row.
Row-gate the terms block on `settled.totalCents > 0`, the pattern already used
eight lines above.

---

## ⚠️ THE PIN GAP — `verify-cutover.ts`'s `CLAIM_SURFACES` IS FIVE FILES

```
BookingForm.svelte · ContactSection.astro · book.astro · contact.astro · llms.txt.ts
```

**Every one of these carries a claim above and is scanned by NOTHING:**

- `src/components/BookingConfirmation.svelte` — renders `RECEIVED_NEXT_STEPS`
- `src/pages/book/received.astro` — `<BookingConfirmation variant="received">`
- `src/pages/book/confirmed.astro` — same component
- 🆕 `src/pages/book/payment-cancelled.astro` — `PAYMENT_CANCELLED_LEAD`
- `src/lib/booking-email.ts` — **23 references to the fee-terms family**, i.e.
  every confirmation and request email, pinned by no claim-shape check at all

And the existing shapes match **none** of claims 2, 3 or 4:
`BOOKED_CLAIM_SHAPES` is about *instant confirmation*; `ONSITE_PAYMENT_SHAPE`
is about *paying on site*. **A payment link existing is a third claim, and it
is unpinned on every surface.** This is BK-48's lesson exactly — *ask what a
pin matches, not what it is named.*

**Therefore the gate is:** add the payment-link, price and credit shapes to
`CLAIM_SURFACES` as new banned regexes, **and widen `CLAIM_SURFACES` to the
five unscanned files above** — the ban list and the file list are two separate
holes and fixing one alone fixes nothing. Per CLAUDE.md, every negative pin
gets a **positive twin**: assert the replacement sentence is present, not only
that the old one is absent.

**Still to derive before implementation:** the replacement copy for each claim
(client-facing wording — a user question, not a code one), and whether
`/book/payment-cancelled/` should keep existing at all once the default path
mints no session.

**Fix by grepping the CLAIM** — `/payment link|payment goes through|secure payment/i`
across `src/` — and add that shape to `verify-cutover.ts`'s `CLAIM_SURFACES` as a
new banned regex. `BOOKED_CLAIM_SHAPES` and `ONSITE_PAYMENT_SHAPE` match
**neither**: they are about instant confirmation and on-site payment, not about a
payment link existing.

⚠ **Re-derive from `feeTermsPaymentFor`, NOT from the constant.**
`booking-email.ts:1150-1151` slices `FEE_TERMS_PAYMENT.slice(0,2)` for a
*confirmed* message — so `[2]` reaches the **request** email and the pages, while
the **confirmed** email carries `[0]` and `[1]` beside `NO_CHARGE_LINE`. Fixing
`[2]` alone ships `[0]` on the message the customer keeps.

> ## ⚠️ THE ADS QUESTION — CORRECTED 2026-09-02, AND THE FIRST VERSION OF THIS
> ## BOX WAS WRONG IN A WAY THAT WOULD HAVE COST MONEY
>
> **What this box used to say:** that a free approval mints no Stripe session, so
> the booking conversion can never fire, leaving a ~CA$59/day campaign with
> *"ZERO conversion input"* — and it recommended **pausing the campaign** as
> *"free, instant"*.
>
> **The mechanism is right. The conclusion was wrong, twice.**
>
> **1 · The account is NOT going dark.** `CONVERSION-2026-08-31.md` §3A's own
> conversion table: **`Website call` (30) and `Calls from ads` (15) are Active and
> last fired Aug 31** — together **45 of August's 66 conversions**. Both live in
> `Analytics.astro:29-35` and are untouched by BK-51/52/53. Meanwhile
> `Assessment booked` has been **Misconfigured since Aug 20** and `Request quote`
> since **Aug 11**. **So this ticket does not kill a live signal; it makes an
> already-dead one structurally dead.**
>
> **2 · Pausing is NOT free.** Google documents *"campaign status"* as a
> learning-disrupting change and *"Reactivated"* as a trigger for re-entering
> Learning. Whether the accumulated model survives a pause is **not stated in
> Google's docs** — do not assume either way. And nothing removes a dark
> conversion action before **13 months**; archiving is reversible.
>
> **The corrected position:**
>
> | Option | Verdict |
> | --- | --- |
> | **Leave it running** on the phone conversions that are actually firing | **Default.** Costs nothing, loses nothing |
> | **Pause it** | A legitimate **budget** decision, not a tracking one. Costs relearning on restart |
> | **Re-point the conversion to `/book/received/`** | ⛔ **The one to avoid.** You pay 1–2 conversion cycles now and again on any flip back — on the primary bidding signal, in the month the offer is being judged, with Google documenting nothing about historical comparability |
>
> **This is T6, orphaned when T2 was struck.** It is a decision for the client,
> taken separately from this code batch. **It does not block BK-53.**

## Folded in

- **The weekend 1.5× removal** (decision 7) — the multiplier plus **six surfaces
  that state it in words**. Grep the *claim*, not `AFTER_HOURS_NOTE`. Includes
  `[id].astro:1801-1802`, which annotates a field now reading `0.00`.
- **W20/W21 are BACKWARDS and must be re-derived here, not shipped as written** —
  they strike *"no obligation"*, which is the **true and best** claim for the
  in-zone majority under decision 1.

## ⚠️ A LIVE CROSS-MODE DEFECT THIS TICKET MUST FIX, NOT INHERIT

**Emails already sent are frozen. The Resend button is not.**

`resend.ts:106` calls `planForAppointment(appointment, …, now)` — it re-renders
**today's constants** against a **stored row**. The numeric half is safe:
`paymentClaim` and `paidAmounts` (`booking-email.ts:412-413`, `:441`) derive from
`settled`, so a paid row still says "paid in full" and a $0 row still says
`NO_CHARGE_LINE`.

**The prose half is not gated at all.** `booking-email.ts:522-533` reads
`FEE_TERMS_ITEMS`, `feeTermsPaymentFor()`, `FEE_TERMS_REFUND` and
`FEE_TERMS_CREDIT` as **unconditional constants**.

**So the moment this ticket ships:** the office clicks Resend on a paid-era
confirmed booking — production has real ones, `#36` was a live `cs_live_` charge
of $628.43 — and that customer receives **free-mode terms, including a refund
policy that contradicts what they were actually charged.** Reachable today:
`resend.ts` is gated only on `status === 'confirmed'` plus a real email address.

**The fix is not a flag.** Row-gate the terms block on `settled.totalCents > 0`,
exactly the way `paymentClaim` at `:441` already is — the correct pattern is
eight lines above the defect. **Red-first it with a paid-era row.**

*(`terms_acked_at` records THAT the box was ticked, never WHAT it said. Once the
text changes there is no reconstruction but git history plus `created_at`. Not
made worse by this ticket, and a second reason the terms must be row-derived.)*

## Inherited defects, owned by this ticket

1. `booking-payment.ts:288` — Stripe line *"Round trip beyond the free radius"*,
   now unreachable, describes the retired per-km rule.
2. `TRAVEL_FEE_CENTS_PER_KM` / `TRAVEL_FEE_FREE_RADIUS_KM` — dead exports naming
   a **retired** rule. BK-51 pins nothing in `src/` reads them.
3. `booking-copy.ts:437` — justifies live customer copy with *"an editable amount
   **and a travel-fee field at approval**"*. False since BK-51.
4. **`appointmentMoney`'s `estimate` arm renders the phantom ladder on the
   office's detail page** — `$399.00 + GST · Estimate, nothing approved yet`.
   Owned jointly with BK-52; decide which ticket removes it.

## DELETE TO A BOUNDARY, NOT TO NOTHING

**Established 2026-09-02 by two design passes, against decision 16 (the client is
experimenting and may flip again).** This ticket as first drafted deletes the
pricing machinery permanently. That is the wrong shape for a model on its third
revision.

**The finding that decides it:** the money engine is **already mode-free.**
`approve()` computes a total and routes on `totalCents === 0`; BK-51 reduced the
entire free-vs-paid difference on that path to **one expression** — what the
amount field defaults to. The DB stores *amounts*, never a mode. A mixed
population of free and paid rows is already the design. **The only real
difference between the two models is static prose.**

**So: do the deletion, but delete to a boundary.**

| Do | Instead of |
| --- | --- |
| Keep `booking-pricing.ts` **intact and unreferenced**, and keep `verify-booking-pricing.ts` running against it | Deleting the tier ladder, the GST helper and 42 pinned checks |
| Route the terms prose through **one function with exactly one implementation** | Eight loose `FEE_TERMS_*` constants read directly by five surfaces and four email renders |
| Use **one named constant** (`ASSESSMENT_PRICES_SHOWN = false`) at the suppression sites, as a grep anchor | Deleting the sites and leaving nothing to grep for |
| **Delete `FEE_TERMS_REFUND` outright** | Gating it — a refund policy a config flip can switch on is a policy nobody reviewed |

**Do NOT write the paid-mode prose you don't need.** Sentences are what rot and
what a reviewer must weigh. **Do NOT add a mode column to the DB** — rows are
self-describing today (`payment_method='none'` + `paid_amount_cents=0`
discriminates cleanly, forever), and a mode column would make the one currently
reversible-by-construction thing permanent.

**The honest counter-argument, recorded because it nearly won:** a flag does not
reduce the work, it relocates it and doubles the state space of every assertion —
in a suite that BK-51 proved cannot catch a re-introduced defect under a
different spelling. **The resolution: the reversible thing is the STRUCTURE, not
the sentences.** What this buys is the elimination of archaeology — *"where did
this render, what did it say, what pinned it"* — which is the cost this repo's
own trap log says it keeps paying.

## What a flip back CANNOT undo

Classified 2026-09-02. Most fears here are unfounded; two are real.

**Fine — verified, stop worrying about these:**
- **DB rows.** Nothing stores a mode. A flip-back reads free rows correctly
  forever.
- **Structured data.** There is **no price anywhere in it** — `seo.ts`'s
  `hasOfferCatalog` carries `itemOffered` only; a grep for `priceRange`,
  `priceCurrency`, `lowPrice`, `highPrice` across `src/` returns **zero**.
  Nothing to remove, nothing to restore, no rich-result loss.
- **Stripe sitting unused 6–12 weeks.** Keys do not expire at that horizon (the
  only documented inactivity rule is 180 days and is scoped to payouts). No
  Products/Prices exist to archive — `booking-payment.ts` builds `price_data`
  inline. `verify-stripe-webhook.ts` stays green at 91/91 throughout.
- **The Google index.** `/book/` is in the sitemap; copy changes re-crawl
  normally.

**Real, and the second one is the sharp one:**
- **`/llms.txt` distribution.** `robots.txt` allows GPTBot, ClaudeBot,
  Google-Extended and CCBot. Training corpora do not retract. *Mild in practice*
  — the paid version is already in those corpora, so a flip-back is the
  better-supported direction.
- **⚠️ NOTHING SNAPSHOTS WHICH TERMS EACH BOOKING ACCEPTED.** `terms_acked_at`
  is a bare timestamp. Combined with the Resend defect above, **a flip in either
  direction rewrites the evidence of what past customers agreed to.** This is the
  single most expensive thing to get wrong here, it cannot be fixed
  retroactively, and it is cheap now: store a `terms_version` (or the rendered
  block) at insert in `booking-commit.ts`, and make `resend.ts` render from the
  snapshot.

## The drain step — required at ANY flip, in either direction

Before deploying, `pending_review` rows submitted under the **old** copy must be
resolved under the **old** rules. Otherwise a customer who read *"free"* gets a
payment link, or a customer holding a live Checkout Session with a
`payment_due_at` pays for what the site now calls free. Production population is
currently tiny — but **the check must be MADE at deploy time, not assumed.**

## Do NOT touch

`verify-cutover.ts`'s `BOOKED_CLAIM_SHAPES` and `ONSITE_PAYMENT_SHAPE`, and
`verify-booking-email.ts:365`'s ban on *"paid at the visit"*. **All are still
TRUE under a free assessment** and the temptation to invert them is the
documented wrong edit.
