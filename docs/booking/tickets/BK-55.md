# BK-55 — The email half of the free changeover: prices, terms and the resend defect

**Status:** 📝 **REVISION 8 + `§N` + `§Q`, 2026-09-09. `§P` IS THE SPEC.**
⛔ **NOT APPROVED — PLAN REVIEW RAN AND RETURNED *NOT APPROVED*, BOTH LANES.
FOURTEEN BLOCKERS — read `§Q`** *(`grep -n '^## §Q-0' BK-55.md`)*. **`§Q-6` is
the owed list.**

🔴 **THE TWO HEADLINES.** **(1) `A-R8-5` IS FALSE** — the paid-era population is
not empty; **`#37` is a named, real, paying customer in it**, and `O-1` is
**RE-OPENED** and blocked on the user (`§Q-1`). **(2) `§N-3`'s nineteen owed
assertions are mostly NOT SPECIFIED** — **8 inadequate, 4 still evadable, 4
missing a control**, five of them stating a diagnosis under a column headed
*"assertion owed"* and stopping (`§Q-3`).

🔴 **DECISION 30, 2026-09-09: THE PAID-ERA BRANCH COLLAPSES.** The user: *"we
dont have to worry about existing bookings made on current terms which we are
about to change."* Three readings were put to them; they chose **collapse**.
⇄ **`TERMS_PRIOR_LINE`, `G4a/G4b/G4c`, red-first rows 18–20 and
`TERMS_ERA_CUTOFF` are all DELETED. `G4` goes to THREE arms on a field renamed
`termsAck`.** ✅ 🔴 **THIS TICKET NOW ADDS NO CUSTOMER-FACING COPY, so the client
is NOT blocked** — `A-R7-3` discharged. **Every case decided under the old rule
is re-derived in `§P-1`.**

🔴 ⛔ **DECISION 31'S PREMISE IS FALSE — SEE `§Q-1`. `#37` IS A NAMED PAID-ERA ROW.** *The decision is the user's and stands until they revisit it; the premise it rests on does not.* **`O-1` is RE-OPENED and blocked on the user.**

⛔ **SUPERSEDED — kept as written on 2026-09-09; its premise is FALSE (`§Q-1`):** ✅ *"DECISION 31 — `§P-4`'s COST IS NIL."* The user: *"ignore refund
entitlement question as no such bookings exist."* **The paid-era population is
empty, so decision 30 enlarges §H-4 by zero.** ⚠️ **Asserted, not measured —
`A-R8-5`.** ⚠️ **§H-4's ORIGINAL population is untouched and STILL OPEN.**
*What follows is the concern as raised, kept because it is only closed by a fact
that could change:*

🙋 ~~**ONE THING FOR THE USER — `§P-4`:**~~ the collapse means a customer who
accepted the **paid** terms is shown the **free** terms block on a resend, and
their **refund entitlement is stated only in that block.** ⚠️ **This is not a new
defect class — it ENLARGES `§H-4`'s**, from post-flip hand-charged rows to those
plus every paid-era row. *Recorded as a consequence of the decision, not an
objection to it.* ⇄ **THE VALIDATION ROUND IS DONE** *(two fresh agents, 2026-09-09)* —
read **`§N`** — find it with `grep -n '^# 🟦 §N' BK-55.md`; it sits immediately
above revision 6. *(⚠️ **Not a line number** — it would be stale by the next edit.)*

🔴 **THIRTY BREAKS RUN; NINETEEN STAYED GREEN, FIFTEEN OF THEM PUTTING A REAL
PRICE, A FALSE PROMISE OR A FALSE STATUS IN A CUSTOMER'S INBOX.**

🔴 **AND THE `397 / 417` FIGURES ARE RETIRED, NOT RE-MEASURED — THEY CANNOT
EXIST.** The script prints **no number at all** at this commit: `check()`
`:86-91` increments only `failures` and the success path is a bare
`console.log`. Measured baseline is `EXIT=0`,
`✓ booking notification checks passed`. **This closes `O15` and `§L-8`.**

🔴 **`§L-4` IS UNDERSTATED, AND THE UNDERSTATEMENT CHANGES THE FIX.** `$399.00 +
GST ($418.95 total)` — **the exact string `!/\$[\d,]+\.\d{2}/` was written to
catch** — ships GREEN on the request email. **It is not a shape problem: no money
regex is EVER evaluated against a request body.** Widening the shapes without
also instantiating the pins fixes none of it. → `§N-3`.

⚠️ **Three blockers were landed at their destinations 2026-09-09:** `§C-1`'s
table still ordered the **retired** pointer as the paid-era copy; `§D`'s `G4` row
still specced the withdrawn `confirmationAlreadySent` split **inside an unclosed
`~~` marker — the sole unmatched `~~` in 2,243 lines — that rendered the dead
text as LIVE**; and `§M-3`'s `G4a/G4b/G4c` and
red-first rows **18–20 were not in `§D` at all**, while `§L-9`'s `O3` asserted
they were.

✅ **THE GUARD IS GONE. The user chose route (e) on 2026-09-07: the sentence
changes, so there is nothing to guard.** The paid-era rendering stops pointing at
an artifact. ⛔ 🔴 **SUPERSEDED BY REVISION 8 (decision 30, 2026-09-09): the
paid-era branch COLLAPSES ENTIRELY. `TERMS_PRIOR_LINE` is deleted, this ticket
adds NO customer-facing copy, and the client is NOT blocked.** → `§P`.
**That deletes:** the second required field, G4's fifth arm (⇄ **four arms,
final**), red-first rows 17/17b, the amendment to §A3's nine literals, and the
production-`SELECT` dependency. **`termsEra` stays four-valued and keeps meaning
a terms epoch.** → §M.

🔴 **STILL TRUE FROM REVISION 6 — the live gap: AT COMMIT (b) NOTHING PINS THE
REQUEST EMAIL.** All three money regexes are `!/\$[\d,]+\.\d{2}/` on
**confirmed**-arm fixtures. **Six planted defects shipped GREEN**, including
**§B-1's opening sentence restored character for character**, and **red-first
rows 3 and 4 — this ticket's own — are not observable.** G1's row ordered *"BOTH
message types"* and nothing performed it.

🔴 **AND THE MEASUREMENT: commit (b) reaches `✓ 397 checks, 0 failed`** ⚠️ *(ONE agent's run — §L-8: this session reproduced the `✗ 10` and the baseline, NOT the 397/417)* from a
417-check baseline — §K-1's `✗ 10` is real but covers only THREE of commit (b)'s
items. ⚠️ **§L-8: this session reproduced the `✗ 10` and the baseline, NOT the
397/417. Re-measure before registering.**

🔴 **READ §L-9's OWED LIST, NOT §L-6's ✅ MARKS.** An independent audit found ten
of fourteen claimed in-place corrections untouched, and three named destinations
that do not exist.

⚠️ **Sequencing unchanged: TWO commits — (a) G0's counter, then (b) everything
else.** ⚠️ **`git push origin main` IS the deploy; `bk51-gated` is never pushed
anywhere, including as a backup — use `git bundle`.**

## Why this ticket exists separately

BK-53 was one ticket doing six jobs and did not close in four review rounds.
**Decision 24 split it by SURFACE GROUP, not copy-vs-gate** — because within a
claim the copy edit and its pin must be **one commit** (`CONVERSION` §7
constraint 5: *"pin first and the build goes red; copy first and there is an
unpinned window"*). So each half carries its own copy **and** its own pins.

**This half is where the blast radius is.** It is the reason the adversarial
pass exists, and separating it means that pass lands on the money path.

---

# 🟩 REVISION 2 — 2026-09-05. **THE SPEC. §0–§8 above are REPLACED by what follows.**

Written against the plan review at the end of this file (7 blockers / 9
should-fix / 15 nits) and against a fresh mechanics verification of every file it
names. **This is a rewrite of the body, not another layer of deltas** — the
sibling ticket's review found that folding findings into summary tables instead
of the sections an implementer builds from is how six rounds of findings got
lost.

## §A · 🔴 THREE THINGS THE OLD BODY ASSERTED THAT ARE NOT TRUE

**All three were verified this session by opening the file. Each had been
restated across multiple documents without being checked.**

### A1 · **Booking #36 cannot be resent. It is `cancelled`, not `confirmed`.**

Every version of this defect — decision 19's withdrawal, R4-3, BK-53 R5-11, and
§3 of this ticket — has used the same worked example: *"the office clicks Resend
on `#36` — `confirmed`, `cs_live_`, **$628.43**."*

🔴 **`ROADMAP.md:387` and `:418`, both checked 2026-08-26, record #36 as
`cancelled`.** And `resend.ts:94-96` is
`if (appointment.status !== 'confirmed' || !hasEmail) return redirect(...)`.
**The Resend button is not offered on #36 at all. The scenario the argument rests
on cannot happen.**

⚠️ **THE DEFECT IS STILL REAL — the example is not.** Any genuinely `confirmed`
row that was charged under prepay reaches the same code, and those exist. **But a
spec that argues from a false example invites a reviewer to refute the example
and close the finding.** → **Every occurrence of #36-as-resend-example is struck,
here and in `BK-53.md`. The population is stated instead: a `confirmed` row whose
`terms_acked_at` predates the flip.**

*(Sibling fact worth keeping: `reconcileRefund` flags only `confirmed`,
`completed` or `no_show`, which is why #36 raised no `needs_attention` — the
ROADMAP already recorded that and nobody connected it to the resend claim.)*

### A2 · **`resend.ts` builds nothing, and there are TWO producers, not one.**

`resend.ts:106` calls `planForAppointment` (`booking-admin-notify.ts:164-198`),
which calls `planBookingNotifications` (`booking-email.ts:1348-1355`, a four-line
delegator), and **the terms block renders inside `customerConfirmation`
(`booking-email.ts:374`) at `:522-533` (HTML) and `:604-611` (text).**

🔴 **The second producer, named in no revision:
`src/lib/booking-payment.ts:1597` on `bk51-gated`** —
`const customer = email ? planForAppointment(row, serviceLabel, now).customer : null;`
inside the private `sendConfirmation` at `:1562`. **This is the path a customer
takes when they actually pay.**
⚠️ **The plan review cited `:1567`; that is the `main` line.** The branch adds 43
lines to this file, 30 of them above the call. **On `bk51-gated` `:1567` is
`): Promise<void> {`.** Read this file with `git show bk51-gated:` — it is one of
the six divergent files.
✅ `verify-booking-ics.ts:1278-1288` already pins that **both** files name
`planForAppointment(` by string, so the two-producer fact is itself pinned.

**→ The suppression goes in `customerConfirmation`, once. Both producers inherit
it. A fix written inside `resend.ts` covers one of the two.**

### A3 · **Terms state does not reach the message layer at all.**

`BookingNotificationInput` (`booking-email.ts:169-238`) has **21 fields and none
of them is about terms.** `grep -n "terms" booking-email.ts` returns **seven
hits, all comments** — and `:501-504` says why in terms:

> *"It is static copy — **no field on `BookingNotificationInput`** — because it
> documents what the form said at submission, not a per-booking value; the
> acknowledgment itself is the `terms_acked_at` stamp on the row."*

**And `terms_version` does not exist anywhere in the repo** — migration 005 added
`terms_acked_at` and nothing else. *(That is BK-54's premise, confirmed.)*

🔴 **So this ticket must ADD a field to that type, and the type's own convention
makes it REQUIRED.** `booking-email.ts:231-236` *(the review cited `:551-555`;
that is the Amount table)*:

> *"── REQUIRED, NOT OPTIONAL, AND THAT IS THE POINT ──. An optional field is how
> the next construction site silently omits the amounts and ships a confirmation
> with none. Required means `astro check` names **all nine sites**, and each one
> has to state an answer."*

**Verified: there are exactly nine full object literals** —
`booking-admin-notify.ts:121` and `:172`, `api/booking/create.ts:216`,
`api/cron/expire-payments.ts:351`, `api/admin/appointments/review.ts:124`
*(a `bk51-gated` file; the line is 124 on both)*, `verify-booking-email.ts:108`,
`verify-booking-admin-entry.ts:692` and `:717`, `verify-env.ts:141`.

⚠️ **And a trap the convention does NOT catch:** three further typed literals are
spread-derived (`verify-booking-email.ts:139`, `:149`, `:1156`) and **~20 inline
`{ ...INSURANCE, … }` call sites** inherit rather than declare. **Nine
compiler-forced decisions, and one fixture default that silently propagates to
everything else** — the same shape as BK-45's `settled` rollout, whose trap
`INSURANCE`'s own comment at `:130-135` records.

---

## §B · THE SCOPE — five items

### B-1 · The customer's message names the tier and no COMPUTED figure

⚠️ **Retitled 2026-09-06 (PR4-S2).** The old title said *"no figure"* and that is FALSE at this ticket's commit: `FEE_TERMS_ITEMS` still renders `$399 / $699 / $1,199` (`booking-email.ts:526` HTML / `:606` text), and **G1's `!/\$[\d,]+\.\d{2}/` is deliberately blind to them** — they carry no decimals (`verify-booking-email.ts:735-741`). **CLAUDE.md's negative-assertion trap by name: ask what the pin MATCHES, not what it is named.** The menu leaves with **BK-53's** commit (G9); *"no figure"* becomes true there, not here.

**The defect.** `booking-email.ts:411` forces `settled = null` on a request, so
`tierLine` (**`:460-464`**) takes the `assessmentSummary` branch and renders
*"You chose: Assessment + written report — $699.00 + GST ($733.95 total) —
weekend rate, 1.5x"* — computed from `assessmentQuote`, so **no constant deletion
in BK-53 touches it and no claim-shape pin sees it.**

⚠️ **The defect is WIDER than "every request email" (review S2).** The
`assessmentSummary` branch is taken by **any message with `settled === null`** —
including **confirmed** ones. That is the thirteen pre-prepay rows and every
admin-created row, reachable through **exactly the Resend button §C is about.**

**The fix — one line, and it is not R4-1's.** Change `tierLine` at `:460-464` to
call `tierNameOf` unconditionally:

```ts
const tierLine = input.assessmentTier
  ? `You chose: ${tierNameOf(input.assessmentTier)}`
  : null;
```

🔴 **Then `assessmentSummary` is DEAD and is deleted with it — decision 28.**
Its only other caller was `:1214`, the office's message, and decision 28 removes
the figure there too. **Delete `assessmentSummary` (`:1159-1169`) and the
now-orphaned comment at `:450-459`** *(the review's `:450-458` stops one line
short and leaves an orphan `*/`)*.

⚠️ **`tierNameOf` (`:1155-1157`) already exists and already returns the bare
name.** R4-1's `return name;` would have duplicated it three lines below itself
**and** changed `:1214` silently.

### B-2 · `FEE_TERMS_PAYMENT` leaves the email entirely

BK-53 deletes the array (its R5-10 T1 gives all four members a verdict).
**Verified blast radius inside this file: exactly two lines fail to compile** —
`:44` (the named import) and `:1151` (both references inside
`feeTermsPaymentFor`). **`:528-530` and `:607` call the function and never name the
constant, so they compile unchanged.**

**→ Delete `feeTermsPaymentFor` (`:1150-1152`) and both call sites (`:528-530` HTML,
`:607` text), and drop the `:44` import.**

🔴 **THE POST-STATE BLOCK ORDER WAS WRONG AND §H-5 CORRECTED ONLY ONE THIRD OF
IT.** It read *HEADING → INTRO(request) → ITEMS → [paymentClaim] → REFUND →
CREDIT* — and **BK-53 deletes `FEE_TERMS_INTRO`, `FEE_TERMS_REFUND` and
`FEE_TERMS_CREDIT`.** §H-5 audited this very line and fixed `[paymentClaim]`
only. **Each ticket believed the other handled it; it is a compile error.**

**The true post-state, and BK-53 R7-6b now specifies the replacement text:**

> **HEADING → `ASSESSMENT_TERMS[0..1]` (request arm only) → `FEE_TERMS_ITEMS` →
> `[paymentClaim]` → `ASSESSMENT_TERMS[2..6]`**

`paymentClaim` (`:441-448`) is `NO_CHARGE_LINE` / `PAID_IN_FULL_LINE` / `null` —
a different thing from `feeTermsPaymentFor`, and it **stays**.

🔴 **What this costs, stated rather than discovered later.** `:1141-1145` records
that `[0]` and `[1]` *"are not optional politeness"*: `FEE_TERMS_ITEMS` prints
standard $399/$699/$1,199 figures, and a mould customer ($385) or a weekend
customer (1.5×) matches none of them, so those two sentences are what reconcile
the two numbers. **Under decision 17 no figure is printed at all, so there is
nothing left to reconcile — the sentences lose their subject, not merely their
accuracy.** Record that as the reason, or the next reader restores them.

🔴 **AMENDED 2026-09-06 — §I-1 · THE ARM EDIT THIS DELETION REQUIRES, AND THE
RED COUNT IT PRODUCES WITHOUT IT.** *(Written here, in §B-2 itself, because
§I-1's instruction lived one section away and revision 4's header said
"everything else stands" — R8-10 T5. An implementer executing §B-2 as it stood
got the deletion, no arm edit, and no stated expectation.)*

**The deletion above, ALONE, is `exit 1` / `✗ 20 checks failed` — sandboxed
twice.** 12 from `verify-booking-email.ts:525-546` `carries`, 8 from the
render-order chain at `:578-603`. **Both arms are BK-53's under seam clause 5,
so SEAM CLAUSE 6 applies (§I-1, `BK-53.md` R8-6d) and this commit makes the
MINIMUM edit that restores green:**

1. **`:525-546`** — remove the four `FEE_TERMS_PAYMENT` members from `carries`.
   **Nothing else.** ⚠️ `FEE_TERMS_HEADING`, `FEE_TERMS_ITEMS`,
   `FEE_TERMS_REFUND` and `FEE_TERMS_CREDIT` **stay** — this ticket deletes only
   the PAYMENT render, so their rows are still true at this commit.
2. **`:587-596`** — remove `['PAYMENT', at(FEE_TERMS_PAYMENT[0])]` from `chain`.
   `REFUND`'s link re-anchors to `ITEMS`, which is the correct claim here.
   `INTRO` stays: it is `request`-only and still rendered.
3. 🔴 **`:554-563` `mustNotCarry` — the four confirmed-arm assertions over
   `FEE_TERMS_PAYMENT[2]`/`[3]` go VACUOUS at this commit, NOT "unaffected"**
   (§J; ⚠️ *the first draft said "verified unaffected", and §J replaced it in
   PROSE ONLY — R9-10 U4*). They pass because **nothing payment-related renders
   at all**, not because the arm drops the right two of four. **2 lines × 2 parts
   = 4 checks passing for the wrong reason.** → **Leave them in place, RECORD
   them as vacuous in the commit message, and name `BK-53.md` R8-6a as the owner
   that re-derives them.** **Do not delete them here** — deleting is B8's
   silent-narrowing defect and this ticket does not own the rewrite.

⛔ ~~**Expected after the minimum edit: exit 0.**~~ 🔴 **STRUCK 2026-09-06 — MEASURED `exit 1, ✗ 10 checks failed` for the FULL commit (b). This line was written from a sandbox of §B-2 alone; §B-1 is in the same commit and was never executed. See §K-1, which enumerates and attributes all ten.** ⚠️ **BK-53 then re-derives both arms
against `ASSESSMENT_TERMS` in its own commit** (`BK-53.md` R8-6a). **Two edits to
one arm across two commits is the intended outcome, not drift.**

### B-3 · `APPROVED_*` carries the charged path's terms — **both arms**

Decision 19. `APPROVED_*` is the only message a hand-charged customer receives and
today it carries **no terms at all**. ⚠️ **It renders TWICE — `:738-771` (HTML)
and `:777-814` (text).** The inherited citation named only the HTML arm.
It also repairs `APPROVED_DEADLINE_NOTE` disclosing the slot-release consequence
in the email asking for money.
⚠️ **This is decision 19's fix for R2's Gap 2 and it is NOT R3-SF12's fix.**
R3-SF12's remedy is a terms-box item and it is **BK-53's** (its R5-10 T2).
**Stop citing R3-SF12 here.**

### B-4 · `CANCEL_LINE`'s email renders — and the inherited claim about them was wrong

**Verified render map:** `:574` (HTML) and `:627` (text) in `customerConfirmation`
— **unconditional, no `isRequest` gate**; `:771`/`:804` in `approvalMessage`.
`declineMessage`, `expiredRequestMessage`, the payment-expired message and
`internalNotification` render it **not at all**.

🔴 **`CANCEL_LINE` renders TWICE in each arm of the confirmed message today**,
because `FEE_TERMS_REFUND[4]` **is** `CANCEL_LINE` (`booking-copy.ts:509`) and
renders at `:532`/`:609`. `booking-copy.ts:496-502` records the duplication as
deliberate. **After BK-53 deletes the array: once.**

🔴 **CORRECTION to the plan review's B6.** It states *"there is currently **no**
pin anywhere that `CANCEL_LINE` reaches an email body."* **False.**
`verify-booking-email.ts:525-546`'s `carries` includes `...FEE_TERMS_REFUND` on
**both** arms (`:532`, `:544`), so `CANCEL_LINE`'s presence **is** pinned —
indirectly, through a constant whose name says *refund*. **B6's conclusion
survives its wrong premise and gets sharper:** those four assertions redden when
BK-53 lands, their messages read *"does NOT carry the fee terms"*, and **they say
nothing about the cancellation instruction they also just stopped covering.**

⚠️ **And a presence pin cannot see the change.** `carries` is `includes()`-based,
so it is green whether `CANCEL_LINE` appears once, twice or five times.
**→ Pin POSITION, not presence** (review B6, and the mechanism is now verified):
`CANCEL_LINE` is **the last content line before the signature block**, and that
footer is invariant — `:575-576` is
`<p …>YEG Restoration · ${SUPPORT_PHONE}</p>` then `WRAP_CLOSE`, two
unconditional array elements with no spread and no ternary; `:628-629` is the
text twin. **`approvalMessage` ends identically (`:772-773`, `:805-806`).**
That invariant survives BK-53's deletion, which is exactly what a presence count
of 2 does not.

### B-5 · The constants that keep their verdicts — **a table, not a sentence**

Review N13/N14: §8's old A5 claimed these were *"confirmed per constant here"*
and there was nothing to confirm them in.

| Constant | `booking-copy.ts` | Verdict |
| --- | --- | --- |
| `REFUND_TIMING_LINE` | `:166-167` | **KEEP** — a real refund of a real charge |
| `REFUND_CHASE_LINE` | `:192-193` | **KEEP** — same |
| `REFUNDED_HEADING` | `:138` | **KEEP** |
| `REFUNDED_LEAD` | `:140-141` | **KEEP** |
| `CANCELLED_REFUNDED_LEAD` | `:134-135` | **KEEP** |
| `refundedAmountLine()` | `:153-155` | **KEEP** — a function over a real refunded amount |
| `PAID_IN_FULL_LINE` | `:256-257` | **KEEP** — true on the hand-charged path |
| `NO_CHARGE_LINE` | `:270` | **KEEP** — ⛔ ~~*and it is on BK-53's coupling-pin exempt list, which is what stops the two rules contradicting*~~ **STRUCK IN PLACE 2026-09-06 (§I-6 T6).** 🔴 **THERE IS NO EXEMPT LIST** — BK-53 R6-3a withdrew it. The verdict is unchanged; its real reason is that `NO_CHARGE_LINE` is **not in `TERMS_TEXT`**. 🔴 **§H-5 ordered this correction and performed it in PROSE ONLY, and §I-0 then marked §H-5 "STANDS"** |
| `PAYMENT_ATTENTION_RULE` | `:823-824` | **KEEP** — internal, about issuing refunds by hand |
| `DECLINED_LEAD` | `:704-705` | **KEEP** — *"Nothing has been charged"*, true |
| `EXPIRED_REQUEST_LEAD` | `:784-785` | **KEEP** — same |
| `PAYMENT_EXPIRED_LEAD` | `:828-829` | 🔴 **KEEP, and here is the verdict the old body owed (N14).** *"was not paid for by the deadline, so we have released it"* reads as being about a free assessment. **It is not reachable for one.** Under BK-51 the free path never mints a `payment_due_at`, so this message can only reach a **hand-charged** row, where every word of it is true. **Stated, so it is not swept as leftover pricing copy.** |

⚠️ **N3: `DECLINED_LEAD`, `EXPIRED_REQUEST_LEAD` and `PAYMENT_EXPIRED_LEAD` each
render TWICE** (`booking-email.ts:1036`/`:1050`, `:867`/`:883`, `:925`/`:942`).
The two-renders-one-claim rule applies if any is ever edited. **None is, here.**

---

## §C · THE RESEND SUPPRESSION — this ticket's adversarial target

**The defect, stated without the false example (§A1).** `resend.ts:94-96` admits
only `confirmed` rows; `:106` builds from **today's** constants. So a customer who
agreed to paid-era terms and was charged can be sent **today's free-mode terms
beside their real charge, with no refund policy at all** — strictly worse than
the pre-ticket defect, which at least shipped a refund policy of the wrong era.

### C-1 · 🔴 Decision 29's THREE STATES stand; its DISCRIMINATOR was wrong and is corrected here

**Decision 29 adopted the plan review's B5 suggestion — key the paid-era branch
on `total_amount_cents > 0` rather than a deploy clock. Re-derived against the
enumerated cases, that misclassifies a population, so it is amended.**

*(This is CLAUDE.md's rule-rewrite trap caught rather than repeated: the predicate
changed, so every case under the old predicate was re-derived.)*

| Population | `total_amount_cents > 0`? | Terms they actually accepted |
| --- | --- | --- |
| Pre-flip web booking, charged | **yes** | paid-era ✅ classified right |
| **Post-flip hand-charged booking** | **yes** | **CURRENT terms** 🔴 **misclassified** |
| Post-flip free booking | no (`0`) | current ✅ |
| The 13 pre-prepay rows | **NULL** | none — predate migration 005 |
| Admin/phone booking | NULL until approved | **none — exempt by design** |

**The question is *which terms did this customer agree to*, and that is temporal,
not monetary.** Money is a proxy that was exact only while every booking was
charged — i.e. only before the flip this ticket ships.

**→ The discriminator is `terms_acked_at` against a named constant:**

```ts
// booking-config.ts — set to the push date when BK-51/53/55 go out, then pinned.
export const TERMS_ERA_CUTOFF = new Date('2026-__-__T00:00:00Z');
```

| `terms_acked_at` | Branch | What renders |
| --- | --- | --- |
| **NULL** | accepted nothing — admin/phone entry (exempt by design) or pre-migration-005 | *"This booking was taken over the phone. Our current terms are on the website."* |
| ⛔ **`< TERMS_ERA_CUTOFF`** | ⛔ **paid-era** | ⛔ **ROW DELETED — REVISION 8, DECISION 30.** The era is not consulted: a row that acknowledged terms renders the terms block, whenever it acknowledged them. **→ `§P-5`'s three arms replace this whole table, and `TERMS_ERA_CUTOFF` itself is deleted.** *Kept, struck, because a row that disappears between two versions of a plan is a decision nobody made.* ⚠️ **Its two superseded contents, for provenance:** revision 7's `TERMS_PRIOR_LINE` (*"You accepted our terms when you booked. Ask us and we'll send you a copy."*), and before that the retired pointer (*"The terms you accepted are in your original confirmation email."*). 🔴 **Neither ever existed in `src/`. Both are deleted; nothing renders here.** |
| **≥ `TERMS_ERA_CUTOFF`** | current | the terms block, rendered normally |

🔴 **NULL MUST BE ITS OWN BRANCH, TESTED FIRST.** In JS `null < someDate`
coerces to `0 < someDate` → **true**, so a two-branch gate routes **every phone
booking** into the paid-era arm and tells a customer who accepted nothing that
their acceptance is on file. **`verify-booking-commit.ts:806` pins that admin rows
leave `terms_acked_at` NULL — *"and must leave `terms_acked_at` NULL — exempt"* —
and migration 005's header says the NULL is permanent and nothing is backfilled.**
This is R1-D2's shape one field over.

⚠️ **B5's objection is answered, not ignored:** the cutoff is a **named constant
set at implementation time and pinned**, never `Date.now()` evaluated at send
time. A clock read at send time reclassifies every row the moment it passes.

### C-2 · Where the branch lives, and the field it needs

**In `customerConfirmation`, gated on a new REQUIRED field on
`BookingNotificationInput`** — §A3. ⚠️ **AMENDED BY §H-1: the type is
`termsEra: 'none-phone' | 'none-legacy' | 'prior' | 'current'`** — four values,
because NULL `terms_acked_at` covers two populations. Resolved **once**, by the
producers:

- `planForAppointment` (`booking-admin-notify.ts:164-198`) — computes it from
  `row.terms_acked_at`. **This is the resend path AND the payment path**
  (`booking-payment.ts:1597`), so both are covered by one change.
- `planForPayload` (`:108-152`) — an office entry has accepted nothing:
  ⚠️ **`'none-phone'`** (was `'none'`; §H-1), stated explicitly beside its
  existing `settled: null`.
- 🆕 **`create.ts:216`** — the public write path, at insert: **`'current'`**
  (§H-3). **A third producer, and §C-2 originally said "the two producers".**

⚠️ **Do NOT put a raw `Date | null` on the type and compare it in the renderer.**
Resolving it at the **three** producers *(⛔ ~~two~~ — corrected revision 7, T5-15; `create.ts:216` is the third and §C-2's own list at `:385` already said so)* means the comparison happens once, and the nine
construction sites are each forced to state a word rather than to re-derive a
clock comparison. **`settled` is the precedent and its docblock is the argument.**

⚠️ **The nine sites will be named by `astro check`; the ~20 `{ ...INSURANCE }`
spreads will NOT.** They inherit whatever `INSURANCE` declares. **Set
`INSURANCE`'s value deliberately and say why in its comment** — it is
`messageType: 'confirmed'` with `settled: null`, which models the
**snapshot-absent** confirmed row. 🔴 **Its value is `'current'` — A7, REVERSED by
§H-2 and restated here by §I-2 because this paragraph argued for the old value
for a full revision after the reversal.** `'current'` is the value that leaves
every inherited arm asserting what it asserts today. **The interesting branches
get their own named fixtures** — `LEGACY_WEB`, `PHONE_ENTRY`, `PRIOR_ERA` —
which is what `INSURANCE`'s own comment at `:130-135` says the settled cases did.
⚠️ **A fixture default chosen to make a NEW branch interesting, at the cost of
~22 assertions about OLD behaviour nobody re-derived, is CLAUDE.md's BK-44 trap
running forward.**

---

## §D · THE GATE

**Every `verify-booking-email.ts` arm that does NOT read a `booking-copy.ts`
symbol is this ticket's** — and see §E for the clause that decides the ones that
do. **Classify before touching**, per BK-44: Class 1 rewrite to assert the
replacement, Class 2 re-point, Class 3 delete only where the subject is gone.

### G0 · The count the gate cannot report — **land this FIRST**

Verified by running it: `npm run verify:booking:email` exits **0** and prints
`✓ booking notification checks passed` — **no number.** `check()` (`:86-91`)
increments only `failures`. **So the only number this script ever prints is the
`✗` count, which is what CLAUDE.md's BK-33 rule forbids scoring on.**

⛔ 🔴 **THIS BLOCK IS DELETED — revision 5, §K-6 / PR4-B7. PERFORMED HERE
2026-09-06 after the traceability audit found the order unexecuted (T5-2).**
It was **two-state** (`passes`; `✗ ${passes} passed, ${failures} failed`) while
**§I-4's is three-state** (`checksRun`; `✗ ${failures} check(s) failed of
${checksRun} run`), and §I-4 never said this one was replaced — §I-0's row said
*"EXTENDED"*. **Both stood, and this gate is scored on (exit code, SUMMARY
LINE).**
🔴 **→ USE §I-4's THREE-STATE BLOCK. It is the ONLY G0 specification.**
⚠️ *The §K-6 note that was supposed to do this sat seventy lines below, at the end
of the class-assignment table, where an implementer executing this step would
never reach it. The pointer is now where the block was.*

⚠️ **Count assertions RUN, not call sites.** Call sites: **229**, counted by
regex excluding the definition. The runtime number is larger — `:565-576` loops
two message types × two arms, `:2009-2013` loops three bodies.
⚠️ **N4: the "expected after" number is a function of arrays BK-53 changes,
after this commit.** → **Register it for THIS commit, and re-register at the
branch tip.** Two numbers, both written down.

| | Assertions run | Summary line |
| --- | --- | --- |
| Baseline, today | ☐ record from the G0 run | `✓ booking notification checks passed` *(no number — that is the defect)* |
| After this ticket | ☐ pre-register **before** editing | |
| At the branch tip, after BK-53 | ☐ re-register | |

### The pins

| # | Pin | Why a re-point is not enough |
| --- | --- | --- |
| **G1** | The customer's message carries **no computed figure** — `!/\$[\d,]+\.\d{2}/` over both arms — **plus a positive twin** asserting `You chose: <tier name>` is present | The negative alone passes if the line disappears. ⚠️ **Scope it to `settled === null` fixtures of BOTH message types** (review S2), not "the request email" |
| **G2** | 🔴 **INVERTED by decision 28.** The **office** arm carries **no** figure either — and a positive twin that it still names the tier | The old G2 pinned the office *keeping* `$699`. That would have **pinned a retired price list in place and reddened the suite on its removal** |
| **G3** | No weekend-surcharge claim in any customer arm — **pinned as a CLAIM, not a spelling**: `/(weekend\|saturday\|sunday\|after[- ]hours)/i` × `/(1\.5\s*[x×]\|1\.5 times)/i` | 🔴 **Verified: `FEE_TERMS_PAYMENT[1]` says *"Saturday and Sunday appointments are charged at 1.5 times the weekday price"* — neither "weekend" nor "1.5x".** And `AFTER_HOURS_NOTE` (`booking-copy.ts:597`) spells it **`1.5×` (U+00D7)** while `booking-email.ts:1167` spells it **`1.5x` (ASCII)**. A ban on either spelling misses the other. **BK-48, three ways** |
| **G4** | ✅ **FOUR ARMS, FINAL (revision 7, §M-2): the fifth arm is DELETED, not deferred** — route (e) removes the pointer it would have rendered. The four `termsEra` branches, each asserted separately. ⛔ **SUPERSEDED BY REVISION 8 (`§P-1`): THREE ARMS, NOT FOUR** — `'none-phone'`, `'none-legacy'`, `'acked'`, on the renamed `termsAck`. **The `'prior'` arm is deleted with decision 30.** → `§P-5`. *The four-arm form is kept below, because a row that disappears between two versions of a plan is a decision nobody made:*

🔴 **THE FOUR ARMS, each asserted separately — RESTATED 2026-09-09, validation round.** *(This row's headline said "four arms, final" while its only enumeration still described the withdrawn FIVE-arm split and named the retired pointer. The strikethrough that was supposed to kill that enumeration had **an unmatched `~~` — the sole unmatched `~~` in 2,243 lines — so the dead text rendered as LIVE. That is `HANDOFF-2026-09-05.md` §12's own paid-for defect, in the row it was paid for.)*

- `'none-phone'` → its own phone sentence; **NOT** the terms block, **NOT** `TERMS_PRIOR_LINE`
- `'none-legacy'` → its own sentence; **NOT** the terms block, **NOT** `TERMS_PRIOR_LINE`
- 🔴 `'prior'` → **`TERMS_PRIOR_LINE`**; **NOT** the terms block and **NOT** the retired pointer
- `'current'` → the terms block; **NOT** `TERMS_PRIOR_LINE`

🔴 **Plus G4a/G4b/G4c on the NEW sentence (§M-3), with red-first rows 18–20 — now landed in the red-first table below, and see §N-2 for the three measured holes in G4b.** **G4a/G4b/G4c now have their own rows below (landed 2026-09-09); they previously existed only inside §M-3.** ⛔ ~~FIVE ARMS as of revision 5 (§K-6) — §I-3 wrote four and §I-6 ordered a fifth in prose without rewriting this row. 'none-phone' and 'none-legacy' render their own sentence and NOT the pointer; 'prior' SPLITS ON confirmationAlreadySent (§K-5's route (b)): TRUE → the pointer and NOT the terms block; FALSE → 'none-legacy''s line; 'current' the terms block and NOT either sentence~~ | Two branches pass by suppressing everything. ⚠️ **These are message-builder assertions with fixtures, so they live in `verify-booking-email.ts`** — **not** `verify-booking-admin-db.ts`. **§F's branch note therefore stands** (review S7 assumed an end-to-end DB pin; it is not needed, because the discriminator is resolved at the producer, not in SQL) |
| ⛔ ~~**G4a**~~ **DELETED — revision 8 (`§P-2`)**: both the subject and the object of this pin are gone. *Struck, not removed:*  *(positive)* | A **paid-era** fixture's customer body **contains `TERMS_PRIOR_LINE`** | The positive TWIN. A negative-only pin passes on an empty body — **measured, row 20** |
| ⛔ ~~**G4b**~~ **DELETED — revision 8 (`§P-2`)**: both the subject and the object of this pin are gone. *Struck, not removed:*  *(negative)* | The retired pointer is **banned by SHAPE across EVERY customer body** — request, `'none-phone'`, `'none-legacy'`, `'prior'`, `'current'`, `$0`, no-snapshot and `APPROVED_*` — using `TERMS_POINTER_SHAPES`, **plus a positive CONTROL asserting the shapes DO match the retired literal held in the test file** | 🔴 **WIDENED AND RE-SCOPED 2026-09-09 (§N-2).** §M-3 specced it as one literal, `/original confirmation email/i`, on the paid-era body alone. **Measured: it is vacuous, it misses two near-miss spellings of the identical dangling pointer, and it is blind to the pointer one branch over.** A-R7-2 says "by SHAPE, never by constant name" — one literal phrase IS a constant name spelled out |
| ⛔ ~~**G4c**~~ **DELETED — revision 8 (`§P-2`)**: both the subject and the object of this pin are gone. *Struck, not removed:*  *(twin)* | A **current-era** fixture's body contains the terms block and **NOT** `TERMS_PRIOR_LINE` | The two eras must not both render. ✅ Sound as specced; observed red, row 19 |
| **G5** | `APPROVED_*` carries the refund and deadline terms in **both** the HTML and text arms | Two renders, one claim |
| **G6** | 🔴 **POSITION, not presence:** `CANCEL_LINE` is the **last content line before the signature block** in the confirmed and approved messages, both arms. 🔴 **SPECCED AS `lastIndexOf` (revision 7, O2/PR4-S3)** — §I-1 clause 1 keeps `FEE_TERMS_REFUND` rendering at commit (b) and `FEE_TERMS_REFUND[4] === CANCEL_LINE` (`booking-copy.ts:509`), **so `CANCEL_LINE` renders TWICE. An `indexOf` pin finds the first occurrence and is RED at the commit that introduces it** | A presence pin is satisfied by the `FEE_TERMS_REFUND` render today and by the `:574` render after — **green either way, and green if it renders five times** |
| **G7** | ⇄ **Restated (review S9).** The **existing** three-state handling of `settled` at `:411-413` is protected as-is | The old G7 pinned a construct this plan never asks anyone to write. It is inherited behaviour and worth a pin; it is not new work |
| **G8** | `booking-payment.ts:288`'s line-item text — **BK-53 W21 owns the edit, this ticket owns the pin** | ⚠️ **Harm restated honestly (review S5).** The file's own BK-48 comment at `:293-298` records that #36's receipt rendered *"names only, no descriptions beside them"* and that Stripe documents `description` as *"for your own rendering purposes, which is not a promise it appears anywhere."* **So this is NOT a claim on a receipt.** It is our own line-item data on the Checkout page, and the branch is **currently unreachable** (`review.ts:246`). **Keep the pin as insurance against re-reachability; do not overstate it** |
| 🆕 **G12** | **N9's one-time gate assertion (O9/T5-17, revision 7): every `status = 'confirmed'` row with NULL `terms_acked_at` has a NON-NULL `source`.** ⚠️ **§H-1's four-valued split rests on `source` being right for the thirteen historical rows, and that rests on a schema DEFAULT rather than on an assertion** | `db.ts:94` |
| **G9** | 🆕 The lab-turnaround survives into email (review N12 — §2's obligation had no G-number) | `FEE_TERMS_ITEMS` is the only path by which the 3–5 day client requirement reaches any email, and BK-53 rewrites it |

### Class assignments — recorded, per review N15

| Arm | Class | Note |
| --- | --- | --- |
| `:2014-2024` (the three-body loop) | 🔴 **1, and SPLIT** | See below |
| `:525-546` `carries`, `:554-563` `mustNotCarry` | **BK-53's** — see §E | Review S6: the index reads are **`:542-543`**, not `:538-540`; `:538-541` is a comment |
| `:680-686` | **2** | Re-point; ⚠️ **N8: `:683` is one half of a `&&` inside ONE `check()`** sharing a single failure message that names neither string. **Split it into two `check()` calls** while re-pointing, or the twin's failure is indistinguishable from the negative's |
| `:742-745` | 🔴 **BK-53's, Class 1 — NOT this ticket's** | **Corrected: this table said Class 2 and BK-53 R7-6a says Class 1 and takes it.** Seam clause 5 decides it — ⛔ ~~the arm reads `booking-copy.ts` symbols and asserts their composition~~ 🔴 **CORRECTED (PR4-S1): the ARM is `check(!/\$[\d,]+\.\d{2}/.test(body), …)` and reads NO constant. It is assigned by the SURROUNDING BLOCK's reads (`NO_CHARGE_LINE` `:727`, `PAID_IN_FULL_LINE` `:747`) and by its shape being justified at `:735-741` from `FEE_TERMS_ITEMS`, which BK-53 rewrites. The verdict stands; the basis was false about the cited lines.** *"Owned by both" fails the way "owned by nobody" did at `:554-563`, which is what clause 5 exists to prevent.* ⚠️ **N7 still applies to whoever writes it:** it is inside a `for (const part of …)` over html/text — say which iterations the twin attaches to |
| `:2037`, `:2038` | **Class 1 SPLIT** | ⛔ ~~N6: it sits INSIDE `:2013-2045`. Not a separate arm~~ 🔴 **N6 IS FALSE — `:2013-2045` spans no construct in the file; the loop is `:2009-2024` and `:2029-2046` is a separate `weekendMould` fixture (§K-4).** **DELETE both figures** — recomputed amounts decision 28 removes. Registered red in §K-1 |
| `:2039-2042` (`'weekend rate'`) | 🔴 **Class 2 REWRITE** | ⛔ ~~INVERT to `!mouldBody.includes('weekend rate')` — it is G3's ONLY positive control (§K-4)~~ 🔴 **WITHDRAWN revision 6 (§L-3): that arm is a SECOND BAN called a control, it is CASE-SENSITIVE against a codebase that spells it `Weekend rate` (measured GREEN on `AFTER_HOURS_NOTE` verbatim), and it is VACUOUS at its own commit.** → **REWRITE to §D's G3 CLAIM shape — both conjuncts, case-insensitive — AND add a genuine POSITIVE control on the `weekendMould` fixture (`mouldBody.includes('On-site assessment')`). The fixture survives either way** |

🔴 **The three-body loop, and it is the sharpest thing in this review (S1).**
`:2009-2013` is:

```ts
for (const [who, body] of [
  ['customer', plan.customer?.html ?? ''],
  ['customer text', plan.customer?.text ?? ''],
  ['office', plan.internal.html],
] as const) {
```

**One third of its iterations is `plan.internal.html` — the office's message.**
Deleting `:2018`/`:2019` wholesale as *"arms that require the defect"* deletes
**G2's evidence in the same keystroke** — BK-44's trap with the classification
varying **within a line** rather than between lines.
⚠️ **And under decision 28 the office loses the figure too, so all three
iterations now want the same negative** — which makes the split *simpler* than it
looked, not harder: **all three lose `$399.00`/`$418.95` and all three gain the
`You chose:` twin.** ⚠️ Note the asymmetry the loop already has: the customer
contributes **both** arms, the office contributes **only html** —
`plan.internal.text` is not in the loop, and G2's twin must not assume it is.

⚠️ **`INSURANCE` is `messageType: 'confirmed'` with `settled: null`
(`:108-137`), not a request fixture** — its comment at `:130-135` marks that
load-bearing. **So `:2018`/`:2019` were never "the request email's figures"**;
they are the snapshot-absent **confirmed** message's figures, which is precisely
the population §C is about.

⛔ **§D's OWN G0 CODE BLOCK IS DELETED (revision 5, §K-6 / PR4-B7).** It was
two-state (`passes`, `✗ ${passes} passed, ${failures} failed`); **§I-4's is
three-state** (`checksRun`, `✗ ${failures} check(s) failed of ${checksRun} run`)
and **§I-4 never said this one was replaced** — §I-0's row said *"EXTENDED"*.
**Both stood, and this gate is scored on (exit code, SUMMARY LINE).**
🔴 **§I-4's three-state block is now the ONLY G0 specification. Use it.**

### Red-first — one entry per failure mode

| # | Break the PRODUCTION target | Expect | red-observed |
| --- | --- | --- | --- |
| 1 | Restore `assessmentSummary(input)` to `tierLine` | **G1** red | ☐ |
| 2 | Delete the `You chose:` line entirely | **G1's positive twin** red | ☐ |
| 3 | ⇄ **INVERTED by decision 28.** Re-add a figure to the office arm at `:1214` | **G2** red | ☐ |
| 4 | Change a customer arm to say *"Saturday and Sunday … 1.5 times"* | **G3** red — **the spelling that has no "weekend" and no "1.5x" in it** | ☐ |
| 5 | Change `1.5×` (U+00D7) to `1.5x` (ASCII) in a banned position | **G3** red on both spellings | ☐ |
| 6 | Render the pointer for a `termsEra: 'current'` row | **G4** red | ☐ |
| ~~6b~~ | ⛔ **WITHDRAWN revision 6 (T5-13 / §L-2)** — it duplicated §I-6's **row 17** under a second number *(one entry per failure mode)*, and its mechanism is withdrawn with §K-5 | — | — |
| 7 | Render the terms block for a `'prior'` row | **G4** red | ☐ |
| 8 | Write the gate as `terms_acked_at < CUTOFF` with **no NULL branch** | 🔴 **G4's TWO `'none-*'` arms red** *(⛔ ~~`'none'` arm~~ — corrected revision 6, T5-13: §I-3 replaced this row and §D was never updated; the three-valued `termsEra` was retired by §H-1)*. This one **typechecks**; if the suite stays green the pin is decorative | ☐ |
| 9 | Remove the terms from `APPROVED_*`'s **text** arm only | **G5** red | ☐ |
| 10 | Move `CANCEL_LINE` above the calendar line at `:574` | **G6** red — **a presence pin stays green** | ☐ |
| 11 | ⛔ ~~Delete `feeTermsPaymentFor`'s replacement and leave the block short~~ 🔴 **NOT EXECUTABLE AT THIS COMMIT (PR4-S6): §I-1 clause 2 REMOVES the PAYMENT link and installs no replacement, so there is no PAYMENT element left to break. The replacement (`ASSESSMENT_TERMS[0..1]`) is BK-53's per §B-2's attribution to R7-6b.** → **RESTATED: break the `ITEMS→REFUND` link, which is what this commit actually re-anchors** | the order pin red | ☐ |
| **8b** | 🆕 *(absorbed from §I-3, revision 7 / O4)* Collapse `'none-phone'` and `'none-legacy'` into one branch — ignore `row.source` | 🔴 **G4 red on a `source: 'web'`, NULL-`terms_acked_at` fixture** — *§H-1's finding: without it, thirteen real web customers are told their booking was taken over the phone* | ☐ |
| **13** | 🆕 *(absorbed from §I-3)* Set `TERMS_ERA_CUTOFF` more than 24h from the deploy timestamp | **§H-3's cutoff assertion red** | ☐ |
| **13b** | 🆕 *(O5 — §H-5's row 13 RENUMBERED, revision 7: `13` was double-assigned to §H-3's cutoff AND §H-5's G8)* G8 is pinned, or recorded as unpinnable-by-artifact | **G8** red | ☐ |
| **14** | 🆕 *(absorbed from §I-3)* Give a `'current'` row `settled.totalCents > 0` and render the free-mode block | **G11 red** (§H-4) | ☐ |
| **15** | 🆕 *(absorbed from §I-3)* Set a **request** fixture to `termsEra: 'prior'` | **G10 red** (§H-5) | ☐ |
| **16** | 🆕 *(absorbed from §I-3)* Delete the `You chose:` twin from the **office** arm only | **G2's twin red** — ⚠️ the loop contributes `plan.internal.html` ONLY | ☐ |
| ~~17~~ ~~17b~~ | ⛔ **WITHDRAWN by revision 7 (§M-2)** — route (e) removes the pointer these tested. **Replaced by rows 18–20, LANDED BELOW 2026-09-09** *(⚠️ §M-3 said these were "added to §D's table" and §L-9's O3 asserted they "ARE in §D". **They were not.** All three existed only inside §M-3 — the phantom-destination trap, inside the list §L-9 was built to cure, covering the ticket's one new customer-facing sentence)* | — | — |
| ⛔ ~~18~~ **WITHDRAWN — revision 8, `§P-2`** | 🆕 Restore *"The terms you accepted are in your original confirmation email."* to the paid-era rendering | 🔴 **G4b red** — *the shape ban, not a constant-name check* | ✅ **OBSERVED 2026-09-09** — adversarial round, throwaway route-(e) implementation: `EXIT=1`, `✗ 4 checks failed` (`✗ G4a` ×2, `✗ G4b` ×2). Isolated as **18b** (pointer ADDED beside `TERMS_PRIOR_LINE` rather than replacing it): `EXIT=1`, `✗ 2 checks failed`, both `G4b` |
| ⛔ ~~19~~ **WITHDRAWN — revision 8, `§P-2`** | 🆕 Render `TERMS_PRIOR_LINE` on a **current-era** fixture | 🔴 **G4c red** | ✅ **OBSERVED 2026-09-09** — `EXIT=1`, `✗ 73 checks failed`, `✗ G4c` ×2 among them |
| ⛔ ~~20~~ **WITHDRAWN — revision 8, `§P-2`** | 🆕 Delete `TERMS_PRIOR_LINE` from the paid-era rendering entirely | 🔴 **G4a red** — *the positive twin; a negative-only pin passes on an empty body* | ✅ **OBSERVED 2026-09-09** — `EXIT=1`, `✗ 2 checks failed`, `✗ G4a` ×2. 🔴 **AND G4b STAYED SILENT ON AN EMPTY BODY — see §N-2, that is the vacuity measured** |
| ⛔ ~~18c~~ **WITHDRAWN — revision 8, `§P-2`** | 🆕 *(§N-2)* Paid-era rendering says *"…are in your original **booking confirmation**."* | 🔴 **G4b must go red** | ⛔ **GREEN 2026-09-09 — `EXIT=0`, `✓ booking notification checks passed`.** The identical dangling pointer ships |
| ⛔ ~~18d~~ **WITHDRAWN — revision 8, `§P-2`** | 🆕 *(§N-2)* Paid-era rendering says *"Your accepted terms are in **the confirmation email we sent you**."* | 🔴 **G4b must go red** | ⛔ **GREEN 2026-09-09 — `EXIT=0`.** Identical dangling pointer |
| ⛔ ~~18e~~ **WITHDRAWN — revision 8, `§P-2`** | 🆕 *(§N-2)* Retired pointer restored to the **current-era** rendering instead of the paid-era one | 🔴 **the pointer ban must go red on ANY customer body** | ⛔ **GREEN 2026-09-09 — `EXIT=0`.** G4b is scoped to the paid-era body only |
| 12 | Point a fixture at a non-existent field so the script throws part-way | 🔴 **The run must not be scored.** Assert **(exit code, summary line)** — a crash prints no `✗` | ☐ |

**Rules, all bought by earlier tickets:**
- ⚠️ **DELETE constants; never EMPTY them** — an emptied array runs zero
  iterations and stays green having asserted nothing.
- ⚠️ **Assert the break LANDED** before scoring it; score on **(exit code,
  summary line)**, which G0 is what makes possible.
- ⚠️ **Restore from a FILE BACKUP.** `git checkout --` reverts to HEAD and eats
  uncommitted work — and on `bk51-gated` the uncommitted work is BK-51's.
- ⚠️ **Clear stranded probe rows first:** `npm run verify:booking:admin:db -- --reset`
  — verified on `bk51-gated` at `:582`, and it **exits 3**, deliberately, because
  a reset runs zero checks and `0` would read as a green suite.

---

## §E · THE SEAM — corrected, with the clause the review found missing

**BK-53 R5-1 rules 1, 2 and 4 stand.** Two changes:

🔴 **E-1 · Rule 3's ordering rationale was VOID and is re-derived (review B1).**
It said BK-55 must precede BK-53 because *"**shrinking** `FEE_TERMS_PAYMENT`
**silently** changes what a confirmed customer reads."* Under R5-10 T1 the array
is **deleted**, which makes `booking-email.ts:44` and `:1151` **compile errors —
loud, not silent.** *The predicate changed and the case justifying the order was
carried forward unexamined: CLAUDE.md's rule-rewrite trap, inside one document.*

**The order is still BK-55 → BK-53, for the opposite reason:** BK-53 cannot
delete an export while `booking-email.ts` still imports it, **so this ticket must
remove the email's dependence first** (§B-2). ⚠️ **Verified: five other files also
name `FEE_TERMS_PAYMENT`** — `book.astro:13`/`:143`, `ContactSection.astro:9`/
`:351`, `BookingForm.svelte:37`/`:1119`, `verify-booking-ics.ts:45`/`:752`, and
`verify-cutover.ts:354` **which pins the STRING `'FEE_TERMS_PAYMENT'`, so it
reddens on a rename as well as a deletion.** All of those are BK-53's, and they
must land in BK-53's commit.

🆕 **E-2 · Seam clause 5, which the review found neither ticket had (B7).**

> **A pin over an EMAIL RENDER is BK-55's, even when the rendered symbol is
> BK-53's. Only pins over a constant's own text, ordering or composition are
> BK-53's.**

Without it, G6 is claimed by rule 1 (it reads `CANCEL_LINE`) **and** by §B-4 —
and *"owned by both"* fails exactly the way *"owned by nobody"* did at
`verify-booking-email.ts:554-563` (BK-53 R5-11 A5).

**Re-derived against clause 5, and the verdicts that changed:**

| Arm | Was | Now | Why |
| --- | --- | --- | --- |
| `:525-546` `carries` / `:554-563` `mustNotCarry` | BK-53 (rule 1) | **BK-53** — unchanged | They assert a constant's *composition* — which constants appear in which arm |
| `:346-349` (`CANCEL_LINE` closes `FEE_TERMS_REFUND`) | BK-53 (rule 1) | **BK-53** — unchanged | It is an *ordering* claim about the constant |
| **G6** (`CANCEL_LINE` is last before the signature) | contested | **BK-55** | It is a claim about the **rendered email**, not about the constant |
| **G9** (lab turnaround reaches email) | unstated | **BK-55** | Same reason |

---

## §F · SEQUENCING AND THE REVIEWER BRIEF

1. BK-51 — already on `bk51-gated`
2. **BK-55 — TWO commits** *(review N11: say it rather than let the
   one-ticket-one-commit rule be broken silently)*: **(a) G0, the assertion
   counter — additive, reddens nothing;** **(b) everything else.**
3. **BK-53** — its own counter fix first, then the copy and pins
4. **The adversarial pass, at the TIP, over the union** (R5-1 rule 4)
5. One push. ⚠️ **`git push origin main` IS the deploy. `bk51-gated` is never
   pushed anywhere, including as a backup — use `git bundle`.**

**Alone on `main` this half is incoherent:** the emails would stop pricing an
assessment the site still prices.

### The brief every reviewer of this ticket gets

1. **The target branch is `bk51-gated`, not `main`.** No PR;
   `git push origin main` is the deploy; the branch is never pushed. *(Two of
   four earlier reviewers on the sibling ticket reasoned from `main`. A wrong
   line number fails loudly; a wrong premise reads as a confident finding.)*
1b. 🔴 **AND SAY WHICH `review.ts`** *(added 2026-09-06 — `BK-53.md` R9-10 nit 11,
   a defect in the BRIEF ITSELF).* **This ticket's bare `review.ts:246` — §G8 —
   means `src/pages/api/admin/appointments/review.ts`.** There is a second
   divergent file, **`src/lib/booking-review.ts`, 171 lines on `main` and 209 on
   `bk51-gated`, which HAS NO LINE 246.** ⚠️ **Four consecutive agent briefings
   in one session made that wrong expansion.** BK-53 R5-8's brief carries the
   same correction.
2. ⚠️ **CORRECTED (review S7):** the old §7.2 said *"of the files this ticket
   touches, NONE differ between the branches."* **That was wrong.**
   **`src/lib/booking-payment.ts` IS one of the six divergent files** and this
   ticket names it twice (§A2's second producer at `:1597`, G8's line item at
   `:288`). **Read it with `git show bk51-gated:`; its `main` line numbers are
   30 lines off.** `booking-email.ts`, `resend.ts` and `verify-booking-email.ts`
   are not divergent, so their line numbers are valid on either branch.
3. **The copy is APPROVED (decision 26)**, subject to decision 25 (no number on
   any **web** surface — this ticket's surfaces are emails). Challenge on truth
   and on whether a pin matches, not on taste.
4. **Read §A first.** Three things every earlier version asserted are not true,
   including the worked example the whole resend argument rested on.
5. **Enumerate every nit.** Do not count them in a header.
5b. ⚠️ **AND TRANSCRIBE THEM.** *(2026-09-06, T5-1 — the 2026-09-06 review's 8
   should-fix and 10 nits reached this ticket only as revision 5's own paraphrase
   in §K-8's disposition column.* **A disposition that is the only surviving
   record of a finding cannot be audited against the finding.** *Now transcribed
   below the blockers.)*
6. 🔴 **SANDBOX THE COMMIT, NOT THE FINDING** *(added 2026-09-06 — §K-1's rule;
   ORDERED in §K-7 and PERFORMED here after T5-3 found it prose-only).* §F names
   each commit's contents; **a sandbox is evidence only about what was in it.**
   **Diff the sandbox against §F's contents before reporting a result, and say
   which items were in it.** *Three rounds and two runs called "end-to-end"
   reported green on a commit whose §B-1 half had never been executed.*

1c. 🔴 **AND SAY WHICH `resend.ts`** *(added 2026-09-06; ORDERED in §K-7 and
   PERFORMED here after T5-3).* **This ticket's bare `resend.ts` means
   `src/pages/api/admin/appointments/resend.ts`.** There is only one in `src/`,
   so the bare form is unambiguous *in the repo* — **but a briefing for this
   ticket expanded it to `src/lib/resend.ts`, which does not exist**, on the same
   day 1b was written about the same class of error. **Expand every bare filename
   in the brief.**

## §G · ASSUMPTIONS — and what is genuinely still unknown

| # | Assumption | Basis |
| --- | --- | --- |
| A1 | The seam rule + clause 5 | BK-53 R5-1, extended by review B7 |
| A2 | One adversarial pass at the branch tip over the union | Testing a mid-branch commit tests code that never ships |
| A3 | ⇄ **WITHDRAWN.** The old A3 ("the office needs the figure") is **reversed by decision 28** | Its stated basis contradicted decision 8, and `bk51-gated:[id].astro:1818-1836` pre-fills `0.00` and shows the tier name only |
| A4 | `tierLine` is edited and `assessmentSummary` deleted | `tierNameOf` already exists; decision 28 removes the only other caller |
| A5 | ⇄ **REPLACED by §B-5's table** | The old A5 claimed a per-constant confirmation that had nowhere to live |
| A6 | `termsEra` is resolved at the **three** producers, not in the renderer *(⛔ ~~two~~ — corrected revision 7, O7/T5-15: `create.ts:216` is the third, as §C-2's own list already said)* | `settled`'s docblock at `:231-236` is the precedent and the argument |
| A7 | `INSURANCE` declares `termsEra: 'prior'` | It is `confirmed` + `settled: null` — the snapshot-absent row — so `'prior'` makes ~20 inherited call sites exercise the interesting branch |

🔴 **NOT KNOWN, and must be queried against production before implementation —
do not infer these:**
1. **#36's `total_amount_cents` and `terms_acked_at`.** The ROADMAP records
   `payment_status = 'paid'` and `paid_amount_cents = 62843` and **never the
   snapshot column.** *(§A1: #36 is `cancelled` anyway, so it is not a resend
   example — but it is still the row the refund work cares about.)*
2. **Whether the "13 rows" are one set or two.** `008-review-lifecycle.ts:162`
   says *"13 historical appointments"*; `ROADMAP:3215` says the table held *"13
   rows"* on 2026-08-22, **after #36/#37/#38 existed.** Two counts taken at
   different times, possibly of different sets. **The `termsEra: 'none'` branch's
   real population is a `SELECT`, not a memory.**
3. **The `TERMS_ERA_CUTOFF` value** — knowable only at the push, by definition.
   Pre-register the **constant**, fill the literal at implementation.

---
## Provenance — nothing in the scope is new; two of the corrections are

| Item | Found by |
| --- | --- |
| §1 (the defect) | **R1-A2**, dropped by revisions 2 and 3, re-found independently by the traceability audit **and** the adversarial pre-read (revision 4, R4-1) |
| §1 (the fix is wrong) | 🆕 **2026-09-05, this plan** — by opening `booking-email.ts` rather than re-reading the ticket |
| §2 | **R3-BL-2**, now BK-53's under the seam rule |
| §3 | **R2-B2**, open across four revisions; decision 19's claim to close it **withdrawn** after a code check (R3B-E1); resolved in revision 4 |
| §4 | **R3-BL-3**, **R2-SF4**, **R2-SF5**, **R1-D5**, **R3B-E5** |
| §4 (the twelve constants) | 🆕 **2026-09-05, this plan** |
| §5 G0 | 🆕 **2026-09-05** — BK-53 R5-2 |
| §0 | 🆕 **2026-09-05** — BK-53 R5-1 |

⚠️ **15 findings from rounds 1–3B exist only as digits in review headers and are
unrecoverable.** If this ticket's reviewers find something "new" in the email
surfaces, assume it may be one of those rather than a fresh discovery.
---

# ⛔ PLAN REVIEW — 2026-09-05, fresh agent. **NOT APPROVED.**

**7 blockers · 9 should-fix · 15 nits.** Briefed on the target branch; every
cited file opened, `booking-payment.ts` and `review.ts` via `git show
bk51-gated:`.

## The seven blockers

- **B1 · §0.3's ordering rationale was invalidated by §4's own amendment and
  never re-derived.** §0.3 justifies BK-55-first because *"**shrinking** that
  array **silently** changes what a confirmed customer reads"*. §4's amendment,
  three sections later, says the array is **DELETED** — which makes
  `booking-email.ts:1151` a **compile error. Loud, not silent.** The failure mode
  named as the reason for the order cannot occur under the rule the same document
  adopts. 🔴 **CLAUDE.md's rule-rewrite trap, inside one document, in the section
  written to answer it.** → The real dependency runs the other way: BK-53 cannot
  delete the export while `booking-email.ts` still imports it, **so BK-55 must
  first remove the email's dependence on `FEE_TERMS_PAYMENT` entirely.**
- **B2 · §4 orders "land the replacement lines" and there are none.** BK-53's own
  verdict table deletes `[0]`/`[1]`/`[2]` and replaces `[3]` with a **web**
  terms-box item living in `booking-copy.ts` — which §0.1 forbids this ticket
  from editing. → **The correct instruction is: delete `feeTermsPaymentFor`
  (`:1145-1152`) and both its renders (`:528-530` HTML, `:607` text)**, leaving
  HEADING → INTRO(request) → ITEMS → REFUND → CREDIT. That is also B1's true
  ordering reason.
- **B3 · §3 names a location that cannot hold the fix.** `resend.ts` builds
  nothing — `:106` calls `planForAppointment`, and the block renders at
  `booking-email.ts:520-533`/`:604-611`. **`BookingNotificationInput` has no terms
  field; `terms_acked_at` never reaches the message layer** (it appears in
  `db.ts:102`, `booking-commit.ts:91`, `[id].astro:1018` and the verify scripts,
  and nowhere in `booking-email.ts` outside comments). The type's own convention
  makes such a field **required** so `astro check` names all nine sites.
  🔴 **And a second producer of the confirmed message is never mentioned:
  `booking-payment.ts:1567`.** A suppression written inside `resend.ts` leaves
  that path shipping today's free-mode terms.
- **B4 · The cutoff discriminator is three-state, and §3 three-states the OTHER
  variable.** §3.1 correctly demands three states for `settled` — but the decided
  fix keys on **`terms_acked_at`, which is nullable by design**
  (`verify-booking-commit.ts:806`: *"must leave `terms_acked_at` NULL — exempt"*;
  admin-created rows take the same path to `confirmed` and are resendable).
  **In JS `null < cutoffDate` coerces to `0 < cutoff` → true**, so every
  admin-created and pre-migration row classifies as pre-cutoff and is told
  *"The terms you accepted are in your original confirmation email"* — **to a
  customer who accepted none.** R1-D2's own shape, one field over, in the section
  that quotes R1-D2.
- **B5 · §3.2 orders a pre-registered literal timestamp that cannot exist at plan
  time.** The deploy is a future push. → **Preferred fix: drop the clock.** Key
  on a fact already on the row — a booking that was actually charged
  (`total_amount_cents > 0`) got the paid-era terms. **Stable across any number
  of pricing flips, needs no cutoff, and is natively three-state with the absent
  snapshot — which also collapses B4.**
- **B6 · G6 is vacuous at two of its four sites.** `CANCEL_LINE` renders **twice**
  in every request and confirmed email today — once as `FEE_TERMS_REFUND[4]` at
  `:532`/`:609`, once at `:574`/`:627`. **A presence pin is satisfied by the
  first, so breaking `:627` stays green and red-first row 8 cannot be observed.**
  And an occurrence-count of 2 inverts to 1 the moment BK-53 lands — **the pin is
  written against a state that does not survive the push.** → Pin **position**:
  `CANCEL_LINE` is the last content line before the signature block, which is
  invariant across the deletion.
- **B7 · §0.1 and §4/G6 contradict each other on who owns the `CANCEL_LINE`
  pin.** Rule 1 gives BK-53 every assertion reading a `booking-copy.ts` symbol;
  G6's pin reads exactly one. **The same construction already produced an arm
  owned by NOBODY (R5-11 A5); one owned by BOTH fails the same way.**
  → **Add seam clause 5: a pin over an EMAIL RENDER is BK-55's even when the
  rendered symbol is BK-53's; only pins over the constant's own text, ordering or
  composition are BK-53's** — then re-derive R5-1's table and state which
  verdicts changed.

## Should-fix

- 🔴 **S1 · §1 misreads the fixtures it calls *"the fixtures REQUIRE the
  defect"*.** `:2009-2013` is a loop over **three** bodies — customer html,
  customer text, **and `plan.internal.html`**. **One third of its iterations is
  precisely the arm G2 exists to protect**, so deleting `:2018`/`:2019` wholesale
  deletes G2's evidence in the same keystroke. **BK-44's trap with the
  classification varying WITHIN a line rather than between lines.** → Split the
  loop: customer arms lose the figures and gain the twin; the office iteration
  keeps both. *(Also: `INSURANCE` is `messageType: 'confirmed'` with
  `settled: null`, not a request fixture.)*
- **S2 · The defect is wider than §1 states and G1 is scoped narrower than the
  bug.** The `assessmentSummary` branch is also taken by any **confirmed**
  message with an absent snapshot — the thirteen migration-008 rows, reachable
  through **exactly the Resend button §3 is about**. The one-line fix covers
  both; only the description and the pin are short.
- 🙋 **S3 · A3's stated basis is contradicted by decision 8 — GOES TO THE USER.**
  A3 justified keeping the figure in the office's message by *"the office decides
  the travel fee on the confirmation call and needs the number"*. **Decision 8
  says the $150 stays out of the codebase entirely**, and the figure
  `assessmentSummary` produces is **not a travel fee** — `booking-email.ts:219-221`
  records that it calls `assessmentQuote` with **no `travelFeeCents`**. It is the
  **assessment** price, the exact thing decision 17 makes free. **So the office's
  message would keep a price for a free service, and G2 would make deleting it
  turn the suite red.** → **Ask before implementing.** If the answer is no, §1
  becomes R4-1's edit after all, and G2 and red-first row 3 both invert.
- **S4 · G3 is shaped for one spelling — the BK-48 error its own justification
  cites.** Banning `weekend`/`1.5x` leaves `FEE_TERMS_PAYMENT[1]` (*"Saturday and
  Sunday appointments are charged at 1.5 times the weekday price"*) live in every
  customer email: it says neither word. And `AFTER_HOURS_NOTE` spells it `1.5×`
  (U+00D7), which an ASCII `1.5x` also misses. → Pin the claim shape,
  `/(weekend|saturday|sunday|after[- ]hours)/i` × `/(1\.5\s*[x×]|1\.5 times)/i`.
- **S5 · G8's harm claim is disproved by the file it cites.**
  `booking-payment.ts:288` is a `price_data.product_data.**description**`, and
  the BK-48 comment eleven lines below records the measured artifact: booking
  #36's receipt rendered **names only, no descriptions**, and the Stripe SDK
  documents `description` as *"for your own rendering purposes"*. **The claim
  *"carries a free claim onto a real receipt"* is what BK-48 already paid to
  disprove for this exact field.** The branch is also **unreachable** on
  `bk51-gated`. → Keep the pin as cheap insurance; **restate the harm honestly**,
  and add a red-first row or record it as unpinnable-by-artifact.
- **S6 · §4's `:538-540` citation is wrong** — `:538-541` is a comment; the index
  reads are `:542-543`, plus `:531`'s whole-array spread. Both `:525-546` and
  `:554-563` are **BK-53's**.
- **S7 · §7.2's branch premise is false for the file G4's pin needs.** An
  end-to-end `resend.ts` pin needs a database and belongs in
  `verify-booking-admin-db.ts` — **one of the six divergent files.**
- 🙋 **S8 · The pointer sentence is false for some of the rows it is written for
  — GOES TO THE USER.** *"The terms you accepted are in your original
  confirmation email"* — an admin-created row accepted none (`terms_acked_at`
  NULL, exempt by design), and a pre-BK-27 row's original email **carried no
  terms block at all**. Client-facing copy on the money path.
- **S9 · G7 pins a construct the plan never asks anyone to write.** The decided
  fix keys on `terms_acked_at`, not `settled`, and `:411-413` already
  three-states `settled` correctly. → Restate G7 as protecting inherited
  behaviour, or say where a new `settled` gate appears.

## Nits — all fifteen, enumerated

**N1** the dead comment is `:450-459`, not `:450-458` — a delete stopping at 458
leaves an orphan `*/` · **N2** §4's KEEP bullet cites `:704-705`/`:784-785`/
`:828-829` with **no file named** (they are `booking-copy.ts`; the bare
`:574`/`:627`/`:771`/`:804` three bullets above are `booking-email.ts`) ·
**N3** each of those three constants renders **twice**; apply the ticket's own
two-renders-one-claim rule · **N4** G0's "expected after" is a function of arrays
**BK-53** changes after BK-55 — register for BK-55's commit and re-register at
the tip · **N5** G0's sketch omits the failure branch it exists to enable ·
**N6** `:2037-2043` sits inside `:2013-2045` · **N7** `:742-745` is inside a
`for (const part of …)` over html/text — say which iterations the twin attaches
to · **N8** G1's twin should pin `You chose: On-site assessment`
(`verify-booking-email.ts:683` already does), not the bare name, which is one
rewrite away from being satisfied by the menu · **N9** red-first row 3 says
*"change `tierNameOf` at `:1214`"* — `tierNameOf` is **not** at `:1214`; the
break as written is not executable · **N10** G2 must name the fixture
(`INSURANCE`, `settled: null`), since `:1214` fires only when the snapshot is
absent · **N11** §6 step 2 and §5 together make BK-55 **two commits** — say so
explicitly against the one-ticket-one-commit rule · **N12** §2's lab-turnaround
obligation has no G-number · **N13** §8 A5 says the twelve constants are
*"confirmed per constant here"* and there is no twelve-row table to fill in ·
**N14** §4 says `PAYMENT_EXPIRED_LEAD` *"needs a verdict"*, gives the reasoning,
and never states the verdict · **N15** §5 mandates a Class 1/2/3 classification
with nowhere to record it.

## ✅ What the review confirmed — verified, not assumed

🔴 **§1's correction of R4-1 is right, and the reviewer calls it *"the strongest
thing in this plan"*.** Grepped all of `src/` and `scripts/`: **nothing outside
`booking-email.ts` reads either function**; `assessmentSummary`'s only second
caller is `:1214`; the one-line `tierLine` fix is type-safe and does not touch
the office path; R4-1's `return name;` would indeed have duplicated `tierNameOf`
and stripped `:1214`.
Also confirmed: **§0's factual base** (the imports, every render line,
`FEE_TERMS_REFUND[4] === CANCEL_LINE` at `booking-copy.ts:509`) — the seam hazard
is real, only its stated mechanism (B1) and one ownership rule (B7) are wrong ·
**§3's defect description** exactly, and option (b) as the right choice ·
**G0's premise**, both script tails, and **229 call sites** as stated ·
**§4's two-render claim for `APPROVED_*`** · **all twelve constants' line numbers
and their KEEP-UNCHANGED reasoning** · **`mustNotCarry` at `:554-563`** ·
**§5's red-first rules**, and `--reset` exists on `bk51-gated` at
`verify-booking-admin-db.ts:582` with the exit-3 semantics the rule depends on.

---

# ⛔ PLAN REVIEW OF REVISION 4 — 2026-09-06, fresh agent. **NOT APPROVED.**

**7 blockers · 8 should-fix · 10 nits.** Briefed on `bk51-gated`; branch files
read with `git show bk51-gated:`. The reviewer executed its headline measurement
in a **detached git worktree**, removed afterwards; the main tree was never
modified. `booking-email.ts` and `verify-booking-email.ts` are non-divergent, so
the measurement is valid on the branch.

> **The shape of it, in the reviewer's own words:** *revision 4 is right about
> §B-2 and unmeasured about §B-1, and the two are in one commit.*

⚠️ **IDs are `PR4-*` and are new.** They do not re-use `B*`/`S*`/`N*` from the
2026-09-05 review — the corollary at `HANDOFF §10` ("a finding's ID is its
handle; do not re-use an ID") after BK-55 revision 2 re-used `N8`.

## The seven blockers, as filed

| ID | Finding |
| --- | --- |
| **PR4-B1** | 🔴 **Commit (b) as specced does not pass its own gate.** Every sandbox in this ticket's history measured **§B-2's deletion in isolation**; §B-1 is in the same commit and has never been sandboxed |
| **PR4-B2** | 🔴 **`verify-booking-email.ts:796-801` — the snapshot-absent fallback — appears in NO table in this ticket.** `grep` for `noSnapshot`, `798`, `800` → zero hits |
| **PR4-B3** | 🔴 **§D's class table closes `:2037-2043` on a false claim (N6), and three assertions die there with no owner** |
| **PR4-B4** | 🔴 **§B-1 deletes `assessmentSummary` and never states what replaces its second caller at `:1214`** |
| **PR4-B5** | 🔴 **§I-6's `'prior'` guard cannot be implemented under §C-2's own rules**, and §I-2 restated §C-2 "whole" without it |
| **PR4-B6** | 🔴 **§I-6 says G4 gains a fifth ARM in prose; §D's G4 row still specifies four** |
| **PR4-B7** | 🔴 **§I-4 replaces §D's G0 in prose only**, and the two code blocks disagree on the summary line the whole gate is scored against |

## Should-fix — all eight, transcribed

⚠️ **Transcribed 2026-09-06 after the traceability audit (T5-1) found that only
the seven blockers had been recorded, and that `PR4-S1…S8` and `PR4-N1…N10`
survived ONLY as revision 5's own paraphrase in §K-8's disposition column.**
*A disposition that is the only surviving record of a finding cannot be audited
against the finding — it is the pre-condition for every silent narrowing this
project has measured. And the review counted them in a header, which §F clause 5
forbids; §K-0's absorption pass did not notice.*

| ID | Finding, as filed |
| --- | --- |
| **PR4-S1** | §D hands `:742-745` to BK-53 on a basis that is **false about the cited lines**. Stated: *"the arm reads `booking-copy.ts` symbols and asserts their composition."* Verified — `:742-745` is `check(!/\$[\d,]+\.\d{2}/.test(body), …)`: no constant, no composition. The surrounding *block* reads `NO_CHARGE_LINE` (`:727`) and `PAID_IN_FULL_LINE` (`:747`); the arm assigned does not. The **verdict** may still be right (the pin's shape is justified at `:735-741` by `FEE_TERMS_ITEMS`, which BK-53 rewrites) — but under clause 5 as written this arm is BK-55's, **and a later round re-deriving from the stated basis re-derives wrong** |
| **PR4-S2** | §B-1's title and G1's name both claim *"no figure"* for a state in which **three figures remain**. At commit (b), `FEE_TERMS_ITEMS` still renders `$399 / $699 / $1,199` (`booking-email.ts:526` HTML / `:606` text). **G1's `!/\$[\d,]+\.\d{2}/` is deliberately blind to them** — `verify-booking-email.ts:735-741` records that those figures carry no decimals. **CLAUDE.md's negative-assertion trap by name:** *ask what the pin MATCHES, not what it is named.* Nothing ships wrong (the menu leaves with BK-53, G9), **but G1 green is not evidence of "no figure"** |
| **PR4-S3** | **G6 goes red at its own commit unless specified as `lastIndexOf`.** §I-1 clause 1 keeps `FEE_TERMS_REFUND` rendering at commit (b), and `FEE_TERMS_REFUND[4] === CANCEL_LINE` (`booking-copy.ts:509`) — so `CANCEL_LINE` still renders **twice** (`:532`/`:609` and `:574`/`:627`). An `indexOf`-based *"last content line before the signature"* pin finds the `:532` occurrence and is red at the commit that introduces it. §B-4 argues the whole mechanism against the **post-BK-53** one-render state. *(The signature invariant itself is sound — `:574→:575`, `:627→:629`, `:771→:772`, `:804→:806`, all unconditional.)* |
| **PR4-S4** | §I-1 clause 1's *"Nothing else"* **orphans a comment that argues for the entries it deletes.** `verify-booking-email.ts:538-541` explains why `FEE_TERMS_PAYMENT[0]`/`[1]` **stay**. Clause 1 deletes `:542-543` and says *"Nothing else."* That is N1's defect one file over — **and §B-2 makes precisely this argument about the same two sentences** (*"Record that as the reason, or the next reader restores them"*) |
| **PR4-S5** | **Red-first row 17 has no positive twin, and two branches now render identically.** Under §I-6, guarded-`'prior'` and `'none-legacy'` emit the same sentence. Row 17 is negative-only (*"assert the pointer does NOT render"*). **Per the BK-51 gate lesson — every negative pin needs a positive TWIN** — it needs the assertion that `'none-legacy'`'s line **IS** present on the guarded `'prior'` fixture |
| **PR4-S6** | **Red-first row 11 is not executable at this commit.** *"Delete `feeTermsPaymentFor`'s replacement and leave the block short."* BK-55 installs **no replacement** — §I-1 clause 2 *removes* the PAYMENT link from the chain, so there is no PAYMENT element left to break. The replacement (`ASSESSMENT_TERMS[0..1]`) is **BK-53's** by §B-2's own attribution to R7-6b. Row 11 belongs to BK-53, or must be restated as breaking the `ITEMS→REFUND` link |
| **PR4-S7** | *"The office loses the figure too"* **addresses one of two branches.** `booking-email.ts:1213` (the `input.settled` branch) still prints `${formatCents(input.settled.totalCents)} total incl. GST`; only `:1214` is deleted. G2 is scoped to `INSURANCE`/`settled: null` (§H-5), so **no pin covers `:1213` either way.** The behaviour is probably correct — a real settled amount is a real fact, consistent with §B-5's KEEP verdicts — **but decision 28's sentence is unqualified**, leaving the next reader an unpinned figure and an instruction to remove it |
| **PR4-S8** | **§F, and what changes if a ticket joins the union.** §F's brief and correctness check out; verified: the five other `FEE_TERMS_PAYMENT` namers, `verify-cutover.ts:352-356` pinning the **string**, and commit (a) genuinely additive — **nothing** outside the script itself consumes `✓ booking notification checks passed`, only prose in docs. **What breaks under a new ticket: §I-4's registration table hard-codes three rows, the last being *"At the branch tip, after BK-53."*** If a ticket lands after BK-53, that row names a **mid-branch** commit and the real tip has no registration — while §I-4's own rule is that a script-wide count cannot see a net-zero swap, so the missing row is the one that matters. A third counter fix would also add a third summary-line format to reconcile. **§F step 4 survives unchanged; steps 2–3 and §I-4's table do not** |

## Nits — all ten, transcribed and enumerated individually

| ID | Finding, as filed |
| --- | --- |
| **PR4-N1** | §D's G0 cites `check()` as `:86-90`; the function is **`:86-91`** (`:91` is its closing brace). *The ticket's own N1 made this exact point about `:450-458`* |
| **PR4-N2** | §J-2's citations use **three conventions**: `:373/:377/:396/:419/:427` are `check(` lines, but **`:366` is a message line** (its check opens at `:364`) and **`:290` is a blank line** (its check opens at `:291`) |
| **PR4-N3** | §B-2 cites the HTML call site as `:528`; §I-1 and the previous review cite `:528-530`. **Verified `:528-530`.** Two spellings, one edit |
| **PR4-N4** | §J-2 calls `:373/:377/:396/:419/:427` *"vacuous"* against §J's own definition (*"the fact is gone and the assertion still passes"*). **The fact is not gone** — `FEE_TERMS_PAYMENT` still exists and still contains those strings at commit (b); what is gone is its **reachability**. The disposition is right either way, but **§J says the distinction *"is worth stating once, because it recurs"*** |
| **PR4-N5** | 🔴 §J-2's vacuous set is **measured against §B-2's deletion only.** §B-1, in the same commit, renders `:2043-2046` (`!mouldBody.includes('$399.00')`) and `:671-678` (the recomputed-figure negatives) vacuous by the same definition — **both bodies can no longer carry a recomputed figure at all.** *A-R4-6's "measured in both directions" was applied to one of the commit's two edits* |
| **PR4-N6** | §I-4's block references `didCrash` and `crashed` without saying where they are set. `verify-booking-email.ts` has **no top-level `try`/`catch` and no `main()`** — it is straight-line `{ }` blocks — **so "copied from that file" is a structural change to a 2,066-line script, in the commit §F calls "additive"** |
| **PR4-N7** | §A1 paraphrases `resend.ts:94` as a one-line `if (…) return redirect(…)`; it is a **two-line block (`:94-96`)**. Immaterial to the argument |
| **PR4-N8** | The ticket uses a bare **`resend.ts`** throughout, while §F 1b exists precisely because a bare `review.ts` was mis-expanded four times. There is only one `resend.ts` in `src/`, so it is unambiguous — **but the brief the reviewer was handed named `src/lib/resend.ts`, which does not exist.** *A defect in the reviewer brief, not in the ticket* |
| **PR4-N9** | §B-4's *"`:628-630` is the text twin"* includes **`:630`, which is `].join('\n')`** |
| **PR4-N10** | The status header's *"§I-1's repair has now been RUN end-to-end TWICE"* is **true of §B-2's deletion and false of commit (b)** — and `HANDOFF-2026-09-05.md:432` had already propagated the stronger reading. *See PR4-B1* |

## What the review confirmed as sound — verified by execution, not assumed

**§I-1's twenty reds reproduce exactly**, mechanism and split (12 from `carries`,
8 from the chain — 2 per iteration × 4, `at()` returning `-1` failing both the
`>` at its own link and the `>= 0` guard at the next). Clauses 1 and 2 are
correct and **sufficient for those twenty**. Clause 3's vacuity verdict is right.
**No 21st red exists from §B-2's deletion.** Also confirmed: §A3's nine-literal
enumeration including `expire-payments.ts:351`; all twelve §B-5 line numbers;
§B-4's render map; §A2's two producers on the branch; §I-4's arithmetic (229
`check()` call sites); §H-1's factual base; §C-1's NULL-coercion argument; G8's
restatement and **both halves of §F 1b's `review.ts` correction**.
`npm run typecheck` → **0 errors**.

**Tried to break and could not:** another owner for `:798-801` under clause 5
(it reads no `booking-copy.ts` symbol, so it cannot be BK-53's); reading §I-1's
"exit 0" as scoped to §B-2 alone (§I-4's row is labelled **"After commit (b)"**
and §F defines (b) as "everything else"); a second `resend.ts`.

---

# 🟩 REVISION 7 — 2026-09-07. **THE SPEC. THE GUARD IS GONE: ROUTE (e) IS CHOSEN — THE SENTENCE CHANGES.**

> ✅ **The deferral §L-2 opened is CLOSED by the user, 2026-09-07: route (e).**
> **The `'prior'` sentence stops pointing at an artifact, so there is nothing to
> guard.** Revision 6 stands in full; this revision only fills the hole it left.

## §M-0 · THE DECISION, AND WHY IT WAS THE ONLY ROUTE THAT REMOVED THE PROBLEM

**The defect (SF6):** the paid-era rendering says *"The terms you accepted are in
your original confirmation email."* **For some rows no confirmation email was
ever sent**, so the sentence points at nothing.

**Guarding it requires knowing whether a confirmation was sent — and the database
cannot answer that.** `booking-commit.ts:185` stamps `confirmation_sent_at` when
the **request acknowledgement** goes out; `booking-payment.ts:1550` records that
`markPaid` *"must not start"* stamping it. **The column is true for rows that
never got a confirmation and false for rows that did.** It was already written
down at `[id].astro:1000-1019` and in ROADMAP Known traps.

| Route | Does the unanswerable question go away? |
| --- | --- |
| (a) fifth `termsEra` value | foreclosed by §I-6 |
| (b) a second boolean field | 🔴 **DEAD — §L-2.** It reads the column that cannot answer |
| (c) collapse at the producer | foreclosed by §H-1 |
| (d) assert the population empty | ⚠️ **No — DEFERS it.** Needs a production `SELECT`, and it is a claim about data that changes |
| (f) pass the rendering variant | ⚠️ **No — RELOCATES it.** The producer must still *choose* the variant, and choosing needs the same fact |
| ✅ **(e) change the sentence** | ✅ **YES. You cannot have a dangling pointer if there is no pointer** |

## ⛔ §M-1 · ~~THE COPY — one sentence, and it is the whole change~~ **WITHDRAWN — REVISION 8, DECISION 30**

⛔ 🔴 **`TERMS_PRIOR_LINE` IS DELETED. THIS TICKET ADDS NO CUSTOMER-FACING COPY
AT ALL**, so decision 26's boundary is never crossed and **the client is not
blocked** — `A-R7-3` is discharged. → `§P-2`. *Kept below as provenance.*

**`booking-copy.ts` gains one constant. The paid-era rendering becomes:**

> **`TERMS_PRIOR_LINE`** — *"You accepted our terms when you booked. Ask us and
> we'll send you a copy."*

⚠️ **APPROVAL: this is customer-facing copy and needs the client's sign-off like
any other sentence** (decision 26 covers the copy set approved on 2026-09-05;
**this sentence is NEW and is not in it**).

**Why this wording and not the pointer:**
- **It is true for every row in the paid era, with no lookup.** `terms_acked_at`
  is what records the acceptance, and it is non-NULL for exactly this population.
- **It is actionable.** *"Ask us"* is a real instruction the office can satisfy;
  *"check your original confirmation email"* is not, when there may be no such
  email.
- **It makes no claim about what we sent** — which is the claim we cannot
  substantiate.

⚠️ **It is mildly less convenient than a working pointer would be.** That is the
accepted cost: the alternative is not a working pointer but **a pointer that is
sometimes wrong**, and this project already treats a confidently-wrong statement
to a customer as worse than a slightly less helpful true one.

## §M-2 · WHAT THIS DELETES FROM THE PLAN

| Withdrawn with route (b) | Where it was |
| --- | --- |
| **`confirmationAlreadySent`**, the second required field | §K-5 — already withdrawn in §L-2, now permanently |
| **G4's fifth arm** | §D's G4 row — ⇄ **back to FOUR arms, final** |
| **Red-first rows 17 and 17b** | §I-6 — the pointer they test no longer renders |
| **The amendment to §A3's nine literals** | §A3 stays as written; **its convention was confirmed by execution** (exactly 9 compiler errors at exactly the nine sites) |
| **The production `SELECT` dependency** | §G's "NOT KNOWN" item 2 — 🔴 **STILL OWED for the refund work, but it no longer blocks this ticket** |

✅ **`termsEra` stays FOUR-valued and keeps meaning a terms epoch** — which was
§H-1's whole objection to the alternatives, and it is preserved rather than
argued around.

## ⛔ §M-3 · ~~WHAT MUST STILL BE PINNED~~ **WITHDRAWN — REVISION 8, DECISION 30**

⛔ **`G4a`, `G4b`, `G4c` and rows 18–20 are all deleted**: the sentence they pin
no longer exists and the string they ban never existed in `src/`. → `§P-2`, which
states why this is a legitimate deletion and not the forbidden one.
✅ ⚠️ **Rows 18, 19 and 20 WERE OBSERVED RED on 2026-09-09 — the only red-first
rows this ticket has ever observed — and that evidence is kept in `§N-2`.**
*The table below is provenance.*

🔴 **A new customer-facing sentence with no pin is how this ticket's whole
history started.** Three assertions, all in `verify-booking-email.ts`:

| Pin | Assertion |
| --- | --- |
| ⛔ ~~**G4a**~~ *(WITHDRAWN, `§P-2`; positive)* | a paid-era fixture's customer body **contains `TERMS_PRIOR_LINE`** |
| ⛔ ~~**G4b**~~ *(WITHDRAWN, `§P-2`; negative)* | that same body **matches no `/original confirmation email/i`** — ⚠️ **the retired pointer is BANNED by shape, not by constant name.** *CLAUDE.md's copy-inventory trap: grep the claim, not the constant* |
| ⛔ ~~**G4c**~~ *(WITHDRAWN, `§P-2`; twin)* | a **current-era** fixture's body contains the terms block and **NOT** `TERMS_PRIOR_LINE` — *the two eras must not both render* |

**Red-first, added to §D's table:**

| # | Break the PRODUCTION target | Expect |
| --- | --- | --- |
| ⛔ ~~18~~ **WITHDRAWN — revision 8, `§P-2`** | 🆕 Restore *"The terms you accepted are in your original confirmation email."* to the paid-era rendering | 🔴 **G4b red** — *the shape ban, not a constant-name check* |
| ⛔ ~~19~~ **WITHDRAWN — revision 8, `§P-2`** | 🆕 Render `TERMS_PRIOR_LINE` on a **current-era** fixture | 🔴 **G4c red** |
| ⛔ ~~20~~ **WITHDRAWN — revision 8, `§P-2`** | 🆕 Delete `TERMS_PRIOR_LINE` from the paid-era rendering entirely | 🔴 **G4a red** — *the positive twin; a negative-only pin passes on an empty body* |

⚠️ **Rows 18–20 replace withdrawn rows 17 and 17b.** ⚠️ **All three are ☐ and
UNOBSERVED**, like every other row in this ticket.

## §M-4 · ASSUMPTIONS

| # | Assumption | Basis |
| --- | --- | --- |
| **A-R7-1** | 🆕 **`TERMS_PRIOR_LINE` makes no claim about what was sent**, so it needs no delivery fact | The database cannot supply one; §M-0 |
| **A-R7-2** | 🆕 **The retired pointer is banned by SHAPE (`/original confirmation email/i`), never by constant name** | BK-36's name-trap: `EMAILED_LINE` vs `RECEIVED_EMAILED_LINE` |
| **A-R7-3** | 🆕 **The new sentence is NOT covered by decision 26** and needs the client's sign-off before it ships | Decision 26 approved a copy set written on 2026-09-05; this sentence is new |
| **A-R6-1** | ⇄ **DISCHARGED** — the guard is no longer deferred | The user chose route (e), 2026-09-07 |

---

# ⛔ PLAN REVIEW — 2026-09-09. **TWO FRESH AGENTS, TWO LANES. NOT APPROVED. FOURTEEN BLOCKERS.**

> A **rule-rewrite lane** *(is revision 8's absorption complete?)* and a
> **gate/pins lane** *(would `§N-3`'s nineteen assertions catch anything?)*.
> Neither saw the other or the implementer's reasoning.
> 🔴 **BOTH RETURNED *NOT APPROVED*.**

## §Q-0 · THE TWO HEADLINES

| | |
| --- | --- |
| 🔴 **`A-R8-5` IS FALSE** | The paid-era population is **NOT** empty. **`#37` is a named, real, paying customer in it.** → `§Q-1` |
| 🔴 **`§N-3`'s NINETEEN ARE MOSTLY NOT SPECIFIED** | **3 adequate · 4 need a control · 4 still evadable · 8 inadequate.** Five rows state a diagnosis under a column headed *"assertion owed"* and stop. → `§Q-3` |

## §Q-1 · 🔴 `A-R8-5` IS FALSE — and `O-1` was closed on it

**`A-R8-5` says:** *"no row has `terms_acked_at` non-NULL and before the
cutover."* **The chain that refutes it, every link independently re-verified by
the orchestrator:**

| Fact | Source |
| --- | --- |
| A public booking **cannot be submitted** without ticking terms | `booking-payload.ts:260` — `options.entry === 'public' && termsRaw !== true` **rejects** |
| Ticking it **stamps the column** | `booking-commit.ts:99` — `${p.termsAcked ? now.toISOString() : null}` |
| BK-27 + migration 005 reached **PRODUCTION 2026-08-16** | `ROADMAP.md:2691` — *"DEPLOYED 2026-08-16 (`a1eedc9`; migration 005 applied to production first)"* |
| **`#37` was created 2026-08-23, through the WEB** | `CONVERSION-2026-08-31.md:279`, `:576`, `:946` — *"the one case whose origination is unambiguously post-Aug-16"* |
| **`#37` is a REAL customer who PAID TWICE** | `ROADMAP.md:400-405` — lead Aug 20, booked Aug 23, **job COMPLETED**, paid **Aug 25 and Aug 30** |

🔴 ***`#37` has non-NULL `terms_acked_at`, stamped a week after the paid terms
went live. It is a paid-era row. `A-R8-5` names its own counterexample in the
document `§P-7` cites as its basis.***

⚠️ **`§P-7` also MISREADS that basis.** `CONVERSION-2026-08-31.md` records the
collapse **beginning** 2026-08-16 — the same day terms shipped. **That is
evidence the population is SMALL. It is quoted as evidence it is ZERO.**

**What breaks — and the path is ordinary, not exotic.** `#37` reads `declined`
today, so `resend.ts:94` *(`status !== 'confirmed'` → refused)* will not offer
Resend. 🔴 **But `ROADMAP.md:404-412` records that `declined` IS WRONG — *"the row
does not describe what happened"* — and names W23/W24/W25 as the missing tooling
to correct it.** **The moment the office corrects that row and hits Resend, the
`'acked'` arm renders the free terms block, with NO refund policy, to the one
real customer who paid twice under the paid terms.** *That is `§P-4`'s harm, on a
named row, reached by an office action the ROADMAP is already asking for.*

🔴 **AND ROUTE (d) IS RE-ADOPTED WITHOUT ITS CONDITION.** `§P-1` records
`§M-0`/`§L-2`'s six routes as *"ALL SIX MOOT."* **Route (d) WAS *"assert the
population empty"*, which is exactly what `A-R8-5` + decision 31 do.** Both
sections foreclosed it with a stated reason:

> *"(d) assert the population empty — ⚠️ **No — DEFERS it.** Needs a production
> `SELECT`, and it is **a claim about data that changes**."*

**Revision 8 re-adopts it WITHOUT the `SELECT`, and the foreclosure reason is
live: `#37` is the data that changed.**

🔴 **And the mitigation the ticket itself designed is now foreclosed.** `§L-2`
said route (d)'s risk resolves *"with a one-time gate assertion"* — the `G12`
pattern. **Such an assertion would read *"no row has `terms_acked_at` non-NULL and
`< TERMS_ERA_CUTOFF`"* — which `§P-0` makes INEXPRESSIBLE by deleting the
constant.** *Deleting the cutoff removes a push-day footgun **and** the only way
to assert the claim the ticket now rests on. `§P-1` does not note that trade.*

🙋 **→ `O-1` MUST REOPEN.** *Reported as a consequence of decision 31, not as a
dispute with it.* **Three ways out, for the user:** run the `SELECT` and record
the artifact; keep decision 31 and **name `#37` as the exception**, with a guard
refusing Resend on any row acked before the cutover; or leave it recorded as
asserted with a named counterexample.

## §Q-2 · RULE-REWRITE LANE — the absorption is incomplete in five checkable places

### 🔴 QB2 · `§P-1` omits `A7`, `A9`, `A13`, `A6` and `A-R4-5` — **the exact omission this ticket already paid for**

| Assumption | Decided under the old rule | Status now |
| --- | --- | --- |
| 🔴 **`A7`** | `INSURANCE.termsEra = 'current'` | **`'current'` IS NOT A VALUE IN THE NEW TYPE.** This is the fixture **29 `{ ...INSURANCE }` spreads** inherit *(the ticket says ~20; the reviewer counted 29)*, and `§H-2` reversed it precisely because a wrong default reddens ~22 arms in a block this ticket does not own. **It now has NO VALID VALUE AT ALL**, and `§N-6`'s `N7` is still open on the previous version of the same defect |
| 🔴 **`A9`** | `create.ts:216` → `'current'` | Same dead value, **on a public write path**, one of the nine compiler-forced sites |
| **`A13`** | the cached `/book/` page straddling the cutoff | ⇄ **MOOT** with `TERMS_ERA_CUTOFF` deleted. **Verdict changed and nobody said so** |
| **`A6`** | resolved at the three producers | substance survives; name stale |
| **`A-R4-5`** | `'prior'` guarded on `confirmation_sent_at` | moot, not stated |

🔴 ***`BK-55.md:2233` records this identical failure VERBATIM:*** *"§H-6
assumptions A7, A8, A9, A13 — **ADDED 2026-09-06 — THIS TABLE OMITTED IT (§I-6
T4), which is the exact failure the table exists to catch, in the pass added to
prevent it.**"* **`§P-1` is the SECOND absorption table to drop the same four
rows.**

### 🔴 QB3 · Five red-first rows were decided under the old rule; `§P-1` re-derives NONE, and **two escape `O-2`'s sweep entirely**

| Row | Under the new rule | Reached by `O-2`/`O-3`? |
| --- | --- | --- |
| **6** | unexecutable — no pointer, no `'current'` | yes |
| 🔴 **7** — *render the terms block for a `'prior'` row* | unexecutable. **The surviving invariant — a NULL-acked row must not render the terms block — is UNTESTED** | 🔴 **NO** — the line contains neither `termsEra` nor `TERMS_ERA_CUTOFF` |
| **8** | break no longer expressible; **the NULL-first hazard SURVIVES** (`§P-5` says so) and needs a restated row | yes |
| **13** | unexecutable — constant deleted | yes |
| 🔴 **14** — `G11`'s **ONLY** red-first row | keys on the dead value `'current'` | 🔴 **NO** — says `'current'`, not `termsEra` |
| **15** | reached | yes |

🔴 **`O-2`'s sweep is *"every `termsEra` mention"* — a CONSTANT-NAME sweep.**
`CLAUDE.md`'s copy-inventory trap and **this ticket's own `A-R7-2`** both say to
grep the **claim**, not the constant. **Both lanes independently ran the grep and
confirmed rows 7 and 14 are not among the `termsEra` hits.**

🔴 **AND THE `'acked'` ARM HAS NO RED-FIRST ROW AT ALL.** Rows 6 and 7 were the
two that proved the era split; both die. Rows 8/8b cover the NULL split only.
**Nothing breaks the arm the collapse created.**

### 🔴 QB4 · `§H-4`/`G11` is declared *"SURVIVES"* and is in fact unowned, unrekeyed and unpinnable

✅ **Decision 31's SCOPING IS RIGHT** — it closes only the *enlargement*;
`§H-4`'s original population is empty today and, per decision 19, **grows**.
**The ownership is broken five ways at once:**

1. **Declared open inside a row marked ✅ CLOSED** (`O-1`), with no `O-` item of its own. `O-2`…`O-5` do not cover it.
2. **`§P-1`'s pointer is WRONG** — the `§H-4` row says *"→ §P-5"*; `§P-5` is the three-arm table and says nothing about `G11`. The section is `§P-4`.
3. **`G11` still has no row in `§D`'s pins table** — `§N-6` `N8`, open, unreferenced by `§P-6`.
4. 🔴 **`G11` IS NOT RE-KEYED.** `'acked'` now covers post-flip **and** paid-era rows, so `'acked' + settled > 0` is a **WIDER** predicate than `'current' + settled > 0` was. **Nobody stated that verdict change — and it is the change that would have caught `#37`.**
5. **What `G11` renders is unspecified and may collide with `A-R8-2`.** `§H-4` wants *"the charged-path terms"*; `§B-3` puts those in `APPROVED_*`; `§H-4` itself records that **`resend.ts` NEVER sends `APPROVED_*`**. `A-R8-2` says this revision adds no customer-facing copy. **So `G11` either relocates `APPROVED_*` copy (state it) or is unimplementable here (say so and give it a ticket).**

### 🔴 QB5 · `BK-53.md:595` still specs `TERMS_ERA_CUTOFF` as live

`BK-53.md` is revision 12, **NOT APPROVED, live**, and `:595` reads:
*"**→ The discriminator is `terms_acked_at` against a named `TERMS_ERA_CUTOFF`
constant** … **See `BK-55.md` §C-1 for the full derivation.**"* **Unstruck, and
pointing at the row revision 8 deleted.** `O-3` scopes the deletion to *"anywhere
else it is specced"* and names only this ticket. Also stale: `BK-53.md:2456`
(*"BK-55 G4's four-valued `termsEra` survives every three-valued collapse
attempted"*) — **a finding about a type that no longer exists, sitting under a
heading reading *"Confirmed clean — so it is not re-raised."***

### Rule-rewrite lane should-fix

| # | Finding |
| --- | --- |
| 🔴 **QS1** | **`G12` IS VACUOUS, and `§P-1` marks it "UNCHANGED".** `migrations/002-booking.ts:53` is `source TEXT **NOT NULL** DEFAULT 'web' CHECK (...)`. **A NULL `source` is impossible by schema — `G12` can NEVER fire.** `§I-6`'s `N9` actually worried the **DEFAULT was right at insert**, which `G12` does not test. **Under the collapse this matters MORE: `source` now discriminates two of three arms instead of two of four.** `N3` notes `G12` has no red-first row; **it cannot have one** |
| 🔴 **QS2** | **`termsAck` COLLIDES with an existing identifier in the same pipeline, and `§P-3`'s grep checked the OLD name.** Live today: `booking-form.ts:64` `termsAck: boolean` · `BookingForm.svelte:1222` `bind:checked={values.termsAck}` · `booking-payload.ts:64` `termsAcked` → `booking-commit.ts:99` · `verify-booking-form.ts:66`. **A three-valued `termsAck` string beside a boolean `termsAcked` from the same column is a name that WILL be misread.** *Verifying a rename by grepping the name you are LEAVING is the BK-36 name-trap.* → suggest **`termsAckState`** |
| **QS3** | **`§P-4` MIS-ATTRIBUTES the refund-block loss to `§B-2`.** `§B-2` step 1 says `FEE_TERMS_REFUND` **stays**; **BK-53 deletes it.** Verified: `booking-copy.ts:504-510`, `[1]` is *"Cancel 24 hours or more … we refund you in full."* **It survives BK-55's commit (b).** So `§P-4`'s harm is a **branch-TIP** property and **neither ticket owns a pin for it** |
| **QS4** | **`§G`'s "NOT KNOWN" item 2 is load-bearing on `'none-legacy'` and is not carried.** It is the population of the arm that exists to stop thirteen web customers being told they booked by phone. **Decision 31 does NOT touch it**, and `§P-4` cites it as though it were the paid-era question. **They are different unknowns** |
| **QS5** | `§P-6` does not state its relation to `§N-6`'s twelve open items, **two of which revision 8 ALTERED** (`N7`'s target value no longer exists; `N8`) |
| **QN1** | `§P-1`'s `§I-6` row **overstates**: `§I-6` foreclosed route (a) because it put an **email-delivery fact** in a terms field. **The collapse removes the delivery fact, so `§I-6` is satisfied by the COLLAPSE, not by the rename.** The rename is name hygiene. *And `termsAck` still carries a CHANNEL distinction inside a field named for acknowledgment — the same shape `§I-6` objected to, just benign* |
| **QN2** | `§C-2`'s named fixture **`PRIOR_ERA` is dead** and `§P-6` does not list it |
| **QN3** | `BK-53.md:3996`'s *"a `'web'` row with non-null `terms_acked_at` predating migration 005 is impossible"* is **true and NOT support for `A-R8-5`**, whose population is rows **after** 005. Annotate so it is not cited as such |

## §Q-3 · GATE LANE — the nineteen, adjudicated

### 🔴 QG1 · **FIVE of the nineteen have NO ASSERTION AT ALL**

**`B10`, `B19`, `B17`, `B18`, `B20` state a diagnosis under a column headed
*"Assertion owed"* and stop.** `§N-6`'s `N12` then orders *"every assertion in
`§N-3`'s right-hand column"* — **for these five that column contains nothing.**

**Measured still-green with `§N-3` fully implemented as written:**

```
"We reply within one hour, guaranteed."           GREEN
"You will hear back inside the hour."             GREEN
"Typical response: under an hour."                GREEN
"We are a GST registrant (BN 775654577)."         GREEN
"Weekend rate applied."  (Tuesday fixture)        GREEN
"A calendar invitation is attached to this email." GREEN
"Cancel online at yegrestoration.ca cancel-booking" GREEN
```

⚠️ **`B17` additionally HAS NO OWNER:** `§D`'s class table rewrites `:2039-2042`
and **never touches `:2020-2023`, which is where `B17` landed.**

### 🔴 QG2 · **`B13`'s refund ban CONTRADICTS `§B-3` and `G5` in the same gate**

`§N-3` orders `REFUND_PROMISE_SHAPES` banned on **`approvalMessage`**, with a
mandatory control that the shapes **fire on `FEE_TERMS_REFUND`**. **`§B-3`/`G5`
order the opposite for the same message:** *"`APPROVED_*` carries the refund and
deadline terms in both arms."*

**Measured — a list satisfying the ordered control necessarily fires on the
ordered content:**

```
fires on FEE_TERMS_REFUND[0]                            -> true
fires on "This payment is fully refundable until 24h"   -> true
```

🔴 **The two halves of one gate cannot both be green.** **Fix: remove
`approvalMessage` from the ban; on that surface `G5`'s POSITIVE pin is the
assertion, and the class ban belongs only where no charge exists.**

### 🔴 QG3 · **The widened money shapes CANNOT BE GREEN at BK-55's own commit**

`§L-4` and `§N-3` `B1` order the widening. `§B-1` states — correctly — that
`FEE_TERMS_ITEMS` **still renders the rate menu at this commit** and *"leaves with
BK-53's commit (G9)."* **Nothing joins the two.**

**Measured on a real request message from `planBookingNotifications`:**

```
request.html  decimal-free hits: ["$399","$699","$1,199","$399.00","$418.95"]
request.text  decimal-free hits: ["$399","$699","$1,199","$399.00","$418.95"]
request.internal.html          : ["$399.00","$418.95"]   (no menu — safe)
```

**After `§B-1`'s `tierLine` fix the two computed figures go; `$399 / $699 /
$1,199` STAY.** 🔴 **A widened ban on the customer request body is therefore RED
at BK-55's commit, on legitimate copy.** *This is `§I-1`'s own class — "commit (b)
cannot pass its own gate" — and the ticket paid for it once already.*

**Fix: state the dependency and choose** — sequence the widening behind BK-53's
`FEE_TERMS_ITEMS` rewrite *(the canonical shape IS sufficient for `B1` once
`assessmentSummary` dies)*, **or** run the widened shapes over the body with the
rate-menu lines excised and pin the excision. 🛑 **Do NOT write an exemption keyed
to the three spellings.**

### 🔴 QG4 · `§D`'s `G3` is never corrected, and `§N-3`'s fix for it is **measured inadequate**

`§N-3` says *"THESE DEFEAT `§D`'s `G3` AS SPECCED"* — **`§D`'s `G3` row is
unamended, `§N-6` owes no correction to it, and the prescribed repair KEEPS THE
CONJUNCTION.**

| String | G3 as specced | First conjunct ALONE |
| --- | --- | --- |
| `FEE_TERMS_PAYMENT[1]` (live) | CAUGHT | CAUGHT |
| `AFTER_HOURS_NOTE` (live) | CAUGHT | CAUGHT |
| *"Weekend appointments carry a 50% premium."* | 🔴 **GREEN** | CAUGHT |
| *"Saturday visits are charged at time and a half."* | 🔴 **GREEN** | CAUGHT |
| *"Weekend appointments **cost more** than weekday ones."* | 🔴 **GREEN** | CAUGHT |
| *"Saturday and Sunday bookings are billed at **a higher rate**."* | 🔴 **GREEN** | CAUGHT |
| *"After-hours visits are one and a half times the weekday price."* | 🔴 **GREEN** | CAUGHT |

🔴 **Rows 5 and 6 have NO MAGNITUDE AT ALL, so no magnitude disjunction reaches
them.** **The conjunction is strictly weaker than either half**, and after `§B-1`
and `§B-2` **no customer email legitimately says weekend/saturday/sunday/
after-hours at all** — so the first conjunct alone is both sufficient and
available. ⚠️ *Verify the slot-label case first: `Sat, Aug 15` does not match
`/saturday/i`, but say so and pin it.*

🔴 **AND ITS CONTROL DIES AT THE TIP.** `§N-3` names `FEE_TERMS_PAYMENT[1]` as a
positive control; **`§B-2` records that BK-53 DELETES that array.** *This is
`§K-4`'s own title — "G3's ONLY positive control dies in G3's own commit" —
recommitted by the round that cites `§K-4`.* **Only `AFTER_HOURS_NOTE` survives.**

### 🔴 QG5 · `B14` lands on a line `§D` assigns to **BK-53**, on a reading that contradicts seam clause 5

`§N-3` `B14` orders the widening of `verify-booking-email.ts:743`. **`§D`'s class
table: *"`:742-745` — BK-53's, Class 1 — NOT this ticket's."*** Seam clause 5
(`§E-2`) reads: *"A pin over an EMAIL RENDER is BK-55's, even when the rendered
symbol is BK-53's."* 🔴 **`§K-3` applies exactly that clause at ARM granularity to
`:794-801` — a structurally identical bare-literal money regex whose surrounding
block ALSO reads `PAID_IN_FULL_LINE`/`NO_CHARGE_LINE` — and concludes *"seam
clause 5 makes it unambiguously BK-55's."*** **Two identical arms, one file,
opposite owners, incompatible readings of one clause.**

**Consequence: one of the fifteen inbox defects — a real price on the `$0`
goodwill message, `CLAUDE.md`'s named GST walk-past — is NOT IN THIS GATE'S SCOPE
AT ALL.**

### 🔴 QG6 · **NOT ONE of `§N-3`'s nineteen has a red-first row**

`§D`'s red-first table has rows for `G1`–`G6`, `G8`, `G10`, `G11` and the retired
`G4a/b/c`. **It has NO row for any of the nineteen, and `N12` does not ask for
one.** **Revision 8 deleted rows 18–20, the only rows this ticket ever saw go
red.** 🔴 **At approval the gate would carry nineteen new assertions with an EMPTY
CATCH RECORD, in a ticket whose own record says an assertion with no red-first row
is UNTESTED.**

### 🔴 QG7 · **A NEW break class `§N-3` does not contain: ATTACHMENTS**

Every owed shape list is scoped to `html` / `text` / `subject`. **The only
attachment assertion ordered anywhere is `B12`'s amount ban.** The customer's
confirmation carries its own `.ics` (`:1040-1074`), the office copy another, and
the ICS *"outlives the email"* by `§N-3`'s own words. **A booked claim, a paid
claim or a refund promise in either `DESCRIPTION` is banned NOWHERE.**

✅ **The fix is cheap and already in the file:** **`allText()` (`:156-176`) already
joins subject, bodies, and both folded and unfolded attachment content.** **Run
the shape lists over `allText(message)`, not over three named parts.**

### 🔴 THE TALLY — `§N-3`'s nineteen

| Verdict | Count | Which |
| --- | --- | --- |
| ✅ **adequate** | **3** | `B4`, `B5` *(the entity/tag normaliser — **the only row with a MECHANISM control**)*, `B6`, `B16` — *(reviewer's table lists 4 as adequate across B4/B5/B6/B16; the tally line says 3. **RE-COUNT AT IMPLEMENTATION** — this is the ticket's own "read the list beside the number")* |
| ⚠️ **needs a control** | **4** | `B2`, `B3`, `B23`, `B12`, `B9` |
| ⚠️ **still evadable** | **4** | `B7`, `B8`, and the shapes above |
| 🔴 **inadequate** | **8** | `B1`, `B10`, `B13`, `B14`, `B17`, `B18`, `B19`, `B20` |

⚠️ 🔴 **The reviewer's own tally line and its table do not reconcile exactly.
RE-COUNT FROM THE TABLE, NOT FROM THE LINE.** *That is this project's rule, and
it applies to a review as much as to a ticket.*

### Gate lane should-fix — eleven, transcribed

| # | Finding |
| --- | --- |
| 🔴 **GS1** | **Four of seven new shape families get NO positive control** — `MONEY_SHAPES`, the ICS ban, hoisted `BOOKED_CLAIM_SHAPES`, the GST ban. They get a **body-non-empty twin**, which proves the fixture rendered and **not** that the shape list still matches anything. ***That is `§N-2`'s measurement 2 — `G4b` silent on an empty body — RECOMMITTED IN THE SECTION THAT RECORDS IT.*** Live controls exist: `$654.68` on the paid confirmation, `GST_REGISTRATION_LINE`, a retired literal in the test file |
| 🔴 **GS2** | **`§N-6`'s `N9` is HALF FALSE, and the wrong half matters.** `G1`'s row **does** carry the instantiation order — *"Scope it to `settled === null` fixtures of BOTH message types"* — and `§L-4` says so itself. **Only the widened shapes are missing.** As written **`N9` can be closed by a NO-OP.** Restate: *the instantiation has been ordered since revision 2 and never performed across four revisions* |
| **GS3** | `G7`, `G9`, `G12` have no red-first row *(`G12` acknowledged; `G7`/`G9` not)*. **`G0` has none either**, though `§I-4`'s per-block registration exists to catch a net-zero `check(true, …)` swap |
| **GS4** | `§K-3`'s specced replacement **uses the shape `§N-3` retires** — `!/\$[\d,]+\.\d{2}/` on `noSnapshot.html` |
| **GS5** | **`G5`'s subject is deleted by the sibling ticket** — it pins *"the refund and deadline terms"* and names no constant; the only one is `FEE_TERMS_REFUND`, which BK-53 deletes |
| 🔴 **GS6** | **`§I-4`'s crash contract is FALSE and unowned.** *"exit 2 is CRASHED, exit 1 is FAILED"* — **`§L-5` measured that a MODULE-LOAD throw raises before the in-module `try` and yields exit 1 with an empty summary.** `§L-5` records it *"owed and NOT closed"*; `§N-6` has no item. **Close it (wrapper process, or dynamic `import()` inside the `try`) or write the qualification beside the contract** |
| **GS7** | `§I-4` orders *"register `G1`…`G11` individually"*; **`G12` was added later and is outside the range** |
| **GS8** | **The confirmed OFFICE arm's `text` and `subject` are unpinned for money** — `:2009-2013` loops `internal.html` only. **`B22` is recorded UNTESTED in `§N-8` and nothing owes it a pin** |
| **GS9** | `:803`'s `!noSnapshot.html.includes('Total')` **reads html only**; `§N-3` states it and owes no assertion |
| **GS10** | **`:683`'s negative half goes VACUOUS after `§B-1`** — `!body.includes('On-site assessment — $399.00')` cannot fail once `assessmentSummary` is deleted. `§D` orders a re-point and gives **no vacuity verdict**, though `§J-2` maintains a vacuous set for exactly this |
| 🔴 **GS11** | **`ALL_CUSTOMER_MESSAGES` IS NEVER ENUMERATED.** `declineMessage` and `paymentExpiredMessage` are customer messages with **no money or claim pin ordered anywhere**; `expiredRequestMessage` appears only via `B23`. **Membership must be written down, or the list is whatever the implementer remembers** |

### Gate lane nits

- 🔴 **The divergence count is stale in FOUR places, including `§N-8`** — `:149`, `:682`, `:872` and `§N-8` all say *"the six divergent files."* **Measured: nineteen files, eleven docs, EIGHT non-docs.** ⚠️ **`§N-8` uses the six to certify *"no code finding is affected"*.** *The conclusion happens to be right — `verify-booking-email.ts` imports none of the eight — **but it was computed over an incomplete set.***
- `§N-3` `B9` cites `BOOKED_CLAIM_SHAPES` at `:952-959`; it is at **`:955-962`**.
- `§N-3` `B14` says *"twin already exists at `:741`"*; **`:741` is a comment.** The twin is at **`:726-728`**.
- `§N-3` `B10` quotes `:1459` as one alternative; the live pin has three. *"Abbreviating a pin to one alternative is the habit that produced these nineteen."*
- `§N-6`'s header promises *"checkable in one grep each"*; **`N12`'s grep column reads "implementation."**
- After `§B-1` the `weekendMould` fixture asserts nothing weekend-specific except the claim ban — **near-vacuous**.

## §Q-4 · CUSTOMER-FACING SURFACES — pinned vs not

| Message | Money | Booked/paid claim | Refund promise | Attachment |
| --- | --- | --- | --- | --- |
| **request confirmation** | 🔴 **owed** (B1/B2/B3) | owed (B9) | owed (B6) | n/a |
| confirmed — settled | ✅ `:664-678` | ✅ `:783-788` | 🔴 **none** | 🔴 **unpinned** |
| confirmed — `$0` goodwill | ⚠️ `:743`, **owned by BK-53** (QG5) | ✅ identity `:747` | owed (B6) | 🔴 **unpinned** |
| confirmed — no snapshot | ⚠️ `§K-3`, narrow shape | ✅ `:819-821` | owed (B6) | 🔴 **unpinned** |
| **approval / `APPROVED_*`** | 🔴 **none ordered** | 🔴 **none** | 🔴 **CONTRADICTED** (QG2) | n/a |
| expired request | ⚠️ **subject only** (B23) | ✅ `:1776` | 🔴 **none** | n/a |
| **payment expired** | 🔴 **NONE** | ⚠️ `:1844-1847` | 🔴 **NONE** | n/a |
| **decline** | 🔴 **NONE** | ⚠️ partial `:1899` | 🔴 **NONE** | n/a |
| customer `.ics` | ⚠️ B12, **which ICS unstated** | 🔴 **none** | 🔴 **none** | — |

## §Q-5 · ✅ WHAT THE REVIEWS CONFIRMED

- ✅ **`§N-1` reproduced independently:** the script unmodified is `EXIT=0`,
  `✓ booking notification checks passed`, **no number.** *The `417` is retired.*
- ✅ **`§N-4` reproduced by reading the source:** `check()` `:85-91` increments only
  `failures`, there is **no top-level `try`/`catch` and no `uncaughtException`
  handler**, so an uncaught throw exits 1 with no summary. **The summary line's
  ABSENCE is the only crash signal today.**
- ✅ **`§P-2`'s deletion argument is SOUND and correctly applied.** `termsEra`,
  `TERMS_PRIOR_LINE`, `TERMS_ERA_CUTOFF` and *"original confirmation email"*
  return **zero hits** in `src/` and `scripts/` on `main` **and in all eight
  divergent files on `bk51-gated`.** `A-R8-4` holds.
- ✅ **Decision 31's SCOPING is right** — it closes the enlargement only.
- ✅ **`§H-1`'s premise holds** — migration 005 adds only `terms_acked_at`; NULL is
  permanent for pre-005 rows and admin entries.
- ✅ **`§A3`'s NINE literals confirmed structurally**, and the three spread-derived
  sites correctly excluded. *(⚠️ **29** `...INSURANCE` spreads, not ~20.)*
- ✅ **`B4`/`B5`'s normaliser is the best-constructed row** — a genuine mechanism
  control.

## §Q-6 · 🔴 THE OWED LIST — NOT marked done

| # | Owed | Destination |
| --- | --- | --- |
| 🙋 ☐ **Q1** | **`A-R8-5` is FALSE — REOPEN `O-1`.** Run the `SELECT`, or name `#37` and guard it. ⛔ **BLOCKED ON THE USER** | `§P-6`, `§P-7` |
| ☐ **Q2** | `QB2` — add `A6`, `A7`, `A9`, `A13`, `A-R4-5` to `§P-1` with verdicts. **`A7`/`A9` assigned `'acked'` EXPLICITLY, in the table** | `§P-1` |
| ☐ **Q3** | `QB3` — re-derive rows **6, 7, 8, 8b, 13, 14, 15** against `§P-5`; **name `§D`'s red-first table in `O-2`**; add a row that breaks the **`'acked'`** arm | `§D`, `§P-6` |
| ☐ **Q4** | `QB4` — `G11`: own `O-` item · fix the `§P-5`→`§P-4` pointer · a `§D` pins row · **re-key to `'acked'` and state the widening** · settle the `APPROVED_*` collision | `§D`, `§P-6` |
| ☐ **Q5** | `QB5` — `BK-53.md:595` and `:2456` | `BK-53.md` |
| ☐ **Q6** | `QG1` — write the five missing assertions **and their controls** | `§N-3`, `§D` |
| ☐ **Q7** | `QG2` — remove `approvalMessage` from the refund ban | `§N-3` |
| ☐ **Q8** | `QG3` — **sequence the widening behind BK-53's commit, or excise-and-pin.** State the dependency | `§N-3`, `§D` `G1` |
| ☐ **Q9** | `QG4` — drop `G3`'s conjunction; **re-home its control on `AFTER_HOURS_NOTE`**; verify the slot-label case | `§D` `G3` |
| ☐ **Q10** | `QG5` — reassign `:742-745`, or state `B14` is BK-53's and out of this gate | `§D` class table |
| ☐ **Q11** | `QG6` — **a red-first row per new shape family**, scored on `(exit code, summary line)` | `§D` |
| ☐ **Q12** | `QG7` — run every shape list over **`allText()`** | `§N-3` |
| ☐ **Q13** | `QS1` — `G12` re-spec or record unpinnable | `§D` |
| ☐ **Q14** | `QS2` — rename to **`termsAckState`** | `§P-3` |
| ☐ **Q15** | `QS3`/`GS2`/nits — fix `§P-4`'s attribution, restate `N9`, correct the four stale "six divergent" claims and the three line citations | throughout |
| ☐ **Q16** | `GS1`–`GS11` — disposition **in writing**, not silently | `§N-6` |

## §Q-7 · WHAT THE REVIEWS DID **NOT** COVER

- ⚠️ **No database.** `G12`'s SQL claim and the `termsAck` → column mapping are
  **untested**, and the `SELECT` that would settle `A-R8-5` is **outside a
  reviewer's reach** — *which is itself part of `Q1`.*
- ⚠️ **No break was planted and no email sent.** Every measurement is against
  built message objects, as the verify script itself does.
- ⚠️ **Whether another script in the suite catches any of the nineteen is still
  UNKNOWN** — `§N-8` records it and neither lane closed it.
- ⚠️ Whether the thirteen historical rows genuinely carry `source='web'` in
  production — **unqueryable here, and it is what `G12` fails to test.**
- ⚠️ `§C`'s resend-suppression design *(except its gate)*; `§E`'s seam clauses
  *(except clause 5)*; copy taste; BK-53's own gate.
- ⚠️ Neither modified a file, pushed, checked out `bk51-gated`, or ran
  `git checkout --`.

---

# 🟩 REVISION 8 — 2026-09-09. **DECISION 30: THE PAID-ERA BRANCH COLLAPSES. THE SPEC.**

> ✅ **User, 2026-09-09, verbatim:** ***"we dont have to worry about existing
> bookings made on current terms which we are about to change."***
> The reading was **not** assumed. Three were put to the user — collapse the
> branch, keep the sentence, or render nothing — and the user chose
> **collapse the branch: old-terms bookings just get the normal current terms.**

⚠️ **This is a new revision because it carries a NEW USER DECISION, not another
round of self-correction.** *That is what revision layers are for.*

## §P-0 · THE NEW RULE, STATED ONCE

🔴 **A row that acknowledged terms renders the terms block — whenever it
acknowledged them. The ERA distinction is deleted.**

**Therefore `TERMS_ERA_CUTOFF` has no consumer and is DELETED**, which removes a
**manual push-day step**: nothing has to be set to the deploy date and then
pinned. *(§C-1's `new Date('2026-__-__T00:00:00Z')` placeholder was a live
footgun — a date nobody had filled in, gating a customer-facing branch.)*

## §P-1 · 🔴 EVERY CASE RE-DERIVED AGAINST THE NEW RULE

⚠️ **`CLAUDE.md`: *"A rule rewrite invalidates every case enumerated under the
old rule, including the cases in the same document… re-derive every enumerated
case against the NEW rule and state which verdicts changed and why. A row that
disappears between two versions of a plan is a decision nobody made."*** **BK-44
paid for that rule. This table is it.** Every case decided under the four-valued
`termsEra` appears below, including the ones that are unchanged.

| Case | Verdict under the OLD rule | Verdict under the NEW rule | Changed? |
| --- | --- | --- | --- |
| **§M-0's six routes (a)–(f)** | (e) CHOSEN; (a) (b) (c) (d) (f) foreclosed | ⇄ **ALL SIX MOOT.** The question *"how do we guard a pointer for the paid-era population"* **does not arise** — there is no paid-era rendering to guard | 🔴 **YES** |
| **§M-1 `TERMS_PRIOR_LINE`** | the whole change | ⛔ **DELETED.** No new sentence | 🔴 **YES** |
| **§M-3's `G4a` / `G4b` / `G4c`** | three new pins | ⛔ **DELETED** — §P-3 | 🔴 **YES** |
| **§M-3's red-first rows 18 · 19 · 20** | specced; **observed red 2026-09-09** | ⛔ **DELETED** — §P-3 | 🔴 **YES** |
| **§N-2's rows 18c · 18d · 18e** | measured GREEN, three holes in `G4b` | ⇄ **MOOT as rows; the LESSON survives** — §P-3 | 🔴 **YES** |
| **§M-2's `confirmationAlreadySent`** | withdrawn | ✅ **STILL DELETED**, now for a stronger reason: nothing needs the fact at all | no |
| **§M-2's rows 17 / 17b** | withdrawn | ✅ **STILL WITHDRAWN** | no |
| **§M-2's §A3 amendment** | not needed | ✅ **STILL NOT NEEDED** | no |
| **§M-2's production `SELECT`** | not blocking, owed for refunds | ⇄ **NOT NEEDED BY THIS TICKET AT ALL.** Still owed for the refund work | partly |
| **§D's `G4`** | **FOUR arms** | ⇄ **THREE arms** — `'none-phone'`, `'none-legacy'`, `'acked'` | 🔴 **YES** |
| **§C-1's branch table** | three rows, keyed on `terms_acked_at` vs the cutoff | ⇄ **REKEYED.** NULL splits on `source`; **non-NULL is ONE arm** | 🔴 **YES** |
| **§C-2's field** | four-valued `termsEra` | ⇄ **three-valued, and RENAMED** — §P-4 | 🔴 **YES** |
| 🔴 **§H-1** — `'none'` tells **13 web customers** their booking was taken over the phone | the defect that split `'none'` | ✅ **SURVIVES INTACT AND UNCHANGED.** It is keyed on `source`, **not on the era**, and the collapse does not touch it | **no — and this is the row most at risk of vanishing** |
| 🔴 **§H-4 / `G11`** — a post-flip hand-charged row shown free-mode terms beside a real charge | open defect | ✅ **SURVIVES — and is ENLARGED.** § P-5 | **no, but its POPULATION grows** |
| **§H-5 / `G10`** — a **request** fixture must never render `'prior'` | *"unreachable in production, asserted rather than left true by accident"* | ⇄ **RESTATED, not deleted:** a request fixture must never render **`'none-legacy'`**. A new web booking always stamps the ack, so the historical arm must be unreachable from a request. **Same invariant, new arm** | 🔴 **YES — restated** |
| **§I-6** — *"`termsEra` must keep meaning a terms EPOCH"* | forecloses route (a) | ⇄ **SATISFIED, not overruled** — by renaming the field, §P-4. ⚠️ *Left un-renamed, the collapse would have VIOLATED this and nobody would have logged it* | 🔴 **YES** |
| **§L-2's six routes** | enumerated; (b) dead | ⇄ **MOOT**, with §M-0 | 🔴 **YES** |
| **§A3's nine required literals** | nine compiler-forced decisions | ✅ **UNCHANGED.** A field is still added and still required; only its type narrows | no |
| **§B-1 · §B-2 · §B-3 · §B-4 · §B-5** | the five scope items | ✅ **UNCHANGED.** None keys on the era | no |
| **§D's `G0`–`G3`, `G5`–`G9`, `G12`** | as specced | ✅ **UNCHANGED** | no |
| **§N-1** — the `397/417` retraction | measured | ✅ **UNCHANGED** | no |
| **§N-3** — the request email has no money pin | measured, 19 green breaks | ✅ **UNCHANGED AND STILL THE BIGGEST ITEM IN THIS TICKET** | no |
| **`A-R7-1`** — the new line makes no delivery claim | 🆕 | ⇄ **MOOT.** No such line | 🔴 **YES** |
| **`A-R7-2`** — ban by SHAPE, never by constant name | 🆕 | ✅ **SURVIVES as a general rule** — §P-3 | no |
| **`A-R7-3`** — the new sentence needs client sign-off | 🆕 | ⇄ 🔴 **DISCHARGED. There is NO new customer-facing copy, so decision 26's boundary is never crossed and the client is not blocked** | 🔴 **YES** |
| **`A-R6-1`** | discharged | ✅ still discharged | no |

## §P-2 · WHAT IS DELETED — and why each deletion is legitimate

🔴 **`CLAUDE.md` and `untested-not-worthless`: *"A CONSTANT WITH NO RED-FIRST ROW
IS UNTESTED, NOT WORTHLESS. Before deleting any assertion for an empty catch
record, write the row that would catch its failure and RUN it."***

⚠️ **That rule does not license these deletions, and it does not forbid them
either — it is about a DIFFERENT reason for deleting.** The distinction, stated
so nobody has to re-derive it:

| Reason for deleting an assertion | Verdict |
| --- | --- |
| *"its catch record is empty"* | 🛑 **FORBIDDEN.** Two constants were deleted on this reasoning and each was the only detector of a real failure |
| ✅ *"the thing it asserts about no longer exists"* | **LEGITIMATE.** There is nothing left to catch |

**`G4a`, `G4b`, `G4c` and rows 18–20 fall in the second category, and it is
checkable:** they assert about `TERMS_PRIOR_LINE`, which **is deleted**, and they
ban *"The terms you accepted are in your original confirmation email"*, which
🔴 **has never existed in `src/` or `scripts/` — verified 2026-09-09,
`grep` returns nothing.** *It was proposed copy that never shipped.* **Both the
subject and the object of every one of these pins are gone.**

⚠️ **Rows 18, 19 and 20 were OBSERVED RED on 2026-09-09** — the first observed
rows in this ticket's history. **That evidence is not deleted with them.** It
stays in `§N-2` as the record that `G4a` and `G4c` were sound and `G4b` was not.

✅ **`A-R7-2`'s rule survives the pin that violated it:** *a ban written as one
literal phrase is banned by constant name, whatever the regex looks like, and
needs a positive control or it is unfalsifiable.* **`§N-2` measured that. It
applies to every ban in `§N-3`'s list**, which is where this ticket's real work
now is.

## §P-3 · 🔴 THE FIELD IS RENAMED — `termsEra` → `termsAck`

**Values: `'none-phone' | 'none-legacy' | 'acked'`.**

**Why the rename is not cosmetic.** §I-6 foreclosed route (a) on the ground that
`termsEra` **must keep meaning a terms epoch**. 🔴 **The collapse makes it stop
meaning that** — it now records *whether* terms were acknowledged and *by what
channel*, not *which epoch*. **Left named `termsEra`, this revision would have
violated §I-6 silently.** Renaming makes the field's name match its meaning, so
§I-6 is **satisfied rather than overruled**.

✅ **The rename is free: `termsEra` does not exist in `src/` or `scripts/`.**
Verified 2026-09-09 — the whole field is proposed, so there is no migration, no
call-site churn and no compatibility window.

## §P-4 · 🔴 THE COST OF DECISION 30, STATED PLAINLY — AND IT ENLARGES §H-4

⚠️ **This is the consequence the user may not have had in view. It is recorded
here, not buried, and it is flagged for them.**

**Today's `FEE_TERMS_*` block IS the paid terms** — `$399`/`$699`/`$1,199`, a
payment link, and a five-line refund policy including *"Cancel 24 hours or more
before your appointment and we refund you in full."* **After BK-53 and BK-55
ship, that block becomes the free version, and `FEE_TERMS_PAYMENT` leaves the
email entirely (§B-2).**

🔴 **So under decision 30, a customer who accepted the PAID terms — and who has a
stated refund entitlement — is shown the FREE terms block on any resend.**

⚠️ **This is NOT a new defect class. It is §H-4's, enlarged.** §H-4 already
records that a resend on a post-flip hand-charged `confirmed` row produces
*"the visit is free… a travel fee may apply"* **beside a real charge with zero
refund policy.** **Decision 30 widens that population** from *post-flip
hand-charged rows* to *those PLUS every paid-era row.*

| | |
| --- | --- |
| **Mitigating** | The booking's money facts come from `settled`, **not** from the terms block (§B-1, §B-5). The terms block is not the artifact that states what a customer paid |
| 🔴 **Not mitigating** | **The refund entitlement is stated ONLY in the terms block**, and after §B-2 it is not in the email at all |
| ⚠️ **Unmeasured** | **The size of the paid-era population is still unknown.** §G's *"NOT KNOWN"* item 2 has wanted a production `SELECT` since revision 4 and it has never been run. *Decision 30 removes this ticket's NEED for it and does not answer it* |

**→ `§P-6` `O-1`. This is for the user, and it is not a blocker on the rest of
the ticket.**

## §P-5 · THE THREE ARMS — replaces §C-1's table

| `terms_acked_at` | `source` | Renders |
| --- | --- | --- |
| **NULL** | `'admin'` | *"This booking was taken over the phone. Our current terms are on the website."* — `'none-phone'` |
| **NULL** | `'web'` | *"Our current terms are on the website."* — `'none-legacy'`. 🔴 **No claim about how it was booked** (§H-1's thirteen) |
| **non-NULL** | any | **the terms block, rendered normally** — `'acked'`. ⇄ **The era is not consulted** |

🔴 **NULL MUST STILL BE TESTED FIRST.** In JS `null < someDate` coerces to
`0 < someDate` → **true**. *§C-1's warning survives the collapse even though the
comparison it warned about is gone, because `source` is only reached on the NULL
arm.*

## §P-6 · OPEN, AND OWED

| # | Item | Owner |
| --- | --- | --- |
| 🔴 **O-1 — RE-OPENED 2026-09-09 by PLAN REVIEW (`§Q-1`). `A-R8-5` IS FALSE: `#37` IS A PAID-ERA ROW.** ⛔ **BLOCKED ON THE USER.** ~~CLOSED by decision 31~~ | ~~Decision 30 enlarges §H-4: paid-era customers lose the only statement of their refund entitlement.~~ ⇄ **THE USER STATES THE POPULATION IS EMPTY:** *"ignore refund entitlement question as no such bookings exist."* **With no rows in the paid era, the enlargement of §H-4 is by ZERO and `§P-4`'s cost is nil.** ⚠️ **SEE `A-R8-5` — this is ASSERTED BY THE OWNER, NOT MEASURED**, and §H-4's ORIGINAL population *(post-flip hand-charged rows)* is **untouched and still open** | ✅ closed |
| ☐ **O-2** | §D's `G4` row, `§C-1`, `§C-2`, `§H-5`'s `G10`, and every `termsEra` mention → the three-arm `termsAck` form | this ticket |
| ☐ **O-3** | `TERMS_ERA_CUTOFF` deleted from `§C-1`'s code block and anywhere else it is specced | this ticket |
| ☐ **O-4** | **`§N-3`'s nineteen green breaks are untouched by this revision and are still the largest item in the ticket** | this ticket |
| ☐ **O-5** | The production `SELECT` — no longer needed here, still owed for refunds, **still with no owning ticket and no ROADMAP entry** (`§N-6` `N10`) | unowned |

## §P-7 · ASSUMPTIONS

| # | Assumption | Basis |
| --- | --- | --- |
| **A-R8-1** | 🆕 **A row that accepted the old terms may be shown the current terms block.** ⚠️ **This is the user's decision, not a derived result** — and §P-4 states its cost | Decision 30, chosen from three stated readings |
| **A-R8-2** | 🆕 **This revision adds NO customer-facing copy**, so decision 26's boundary is not crossed and **no client sign-off is needed.** ⇄ **discharges `A-R7-3`** | §P-2 |
| **A-R8-3** | 🆕 **`TERMS_ERA_CUTOFF` is deleted, removing a manual push-day step** — a placeholder date gating a customer-facing branch | §P-0 |
| **A-R8-4** | 🆕 **`termsEra` and `TERMS_PRIOR_LINE` exist nowhere in `src/` or `scripts/`**, so the collapse deletes only unbuilt machinery | Verified by grep, 2026-09-09 |
| 🔴 **A-R8-5** | ⛔ **FALSE — REFUTED 2026-09-09 BY PLAN REVIEW, `§Q-1`, EVERY LINK RE-VERIFIED.** `#37` was created **2026-08-23 through the WEB**, a week after terms reached production on **2026-08-16**; the public door cannot submit without ticking, and ticking stamps the column. **It is a real customer who paid twice and whose job was completed.** ⚠️ **The harm needs one ordinary office action the ROADMAP already asks for (W23–W25): correct `#37`'s wrong `declined` status, hit Resend, and the `'acked'` arm shows free terms with no refund policy to a customer who paid.** 🔴 **AND route (d) — *"assert the population empty"* — is RE-ADOPTED here without the `SELECT` its own foreclosure required, while `§P-0`'s deletion of `TERMS_ERA_CUTOFF` makes the `G12`-shaped assertion that would have guarded it INEXPRESSIBLE.** ⛔ **The assumption as originally written, kept for provenance:** *"🆕 THE PAID-ERA POPULATION IS EMPTY — no row has `terms_acked_at` non-NULL and before the cutover. ⚠️ **ASSERTED BY THE OWNER (2026-09-09), NOT MEASURED.** No production `SELECT` was run and no database was connected to. **Consistent with what the repo already records** — web bookings collapsed on 2026-08-16 *(`CONVERSION-2026-08-31.md`)*, and §H-1's thirteen historical rows are **NULL** `terms_acked_at`, i.e. `'none-legacy'`, **not** paid-era. 🔴 **If this is ever falsified, `§P-4` reopens and `G4`'s `'acked'` arm shows free terms to a customer who paid** | User, 2026-09-09 |

## §P-8 · WHAT THIS REVISION DOES **NOT** CLAIM

- ⚠️ **The paid-era population was NOT measured.** No production `SELECT` was
  run, and **no database was connected to.** *Decision 30 makes the count
  unnecessary for the branch; it does not make it known.*
- ⚠️ **`§P-1`'s table was derived by reading the ticket, not by executing
  anything.** No break was run for this revision. **The three observed rows in
  `§N-2` are the last measurement this ticket has**, and they are of a pin this
  revision deletes.
- ⚠️ **The three-arm form is SPECCED, not implemented.** `termsAck` has no code,
  no fixture and no red-first row. **Every arm needs one, and `G10`'s restated
  form needs one, before any of this counts toward a gate.**
- 🔴 **`§N-3`'s nineteen green breaks are NOT addressed here and are not made
  smaller by this revision.**

---

# 🟦 §N · VALIDATION ROUND — 2026-09-09. **FOUR FRESH AGENTS. THE GATE IS WORSE THAN §L-4 SAYS, AND THE `417` CANNOT EXIST.**

> Two fresh agents on this ticket: a **traceability audit** and an **adversarial
> pre-read** with a worktree and an order to RUN the breaks. Neither saw the
> other's output, the implementer's reasoning, or each other's brief.
> **Every finding below is landed at its destination, not promised here** —
> §N-6 is the checkable list of where.

## §N-0 · THE HEADLINE — three numbers

| | |
| --- | --- |
| **Breaks run** | **30**, every one with the landing greped at its destination first |
| 🔴 **Breaks that stayed GREEN** | **19** — and **15 of them put a real price, a false promise or a false status into a customer's inbox** |
| 🔴 **The `417` baseline** | **DOES NOT EXIST AND CANNOT.** See §N-4 |

## §N-1 · 🔴 THE BASELINE IS NOT A NUMBER — `417` AND `397` ARE UNMEASURABLE AT THIS COMMIT

**Measured, `scripts/verify-booking-email.ts` unmodified at `a01aabe`** *(byte-identical to `bk51-gated` for this file)*:

```
EXIT = 0
SUMMARY = "✓ booking notification checks passed"
```

🔴 **The script prints NO NUMBER AT ALL.** `check()` at `:86-91` increments only
`failures`; the success path is a bare `console.log`. **So the `397 / 417` pair
cannot be a measurement of this commit** — it can only have come from a worktree
with §I-4's three-state counter already installed.

⇄ **This CLOSES `O15` and `§L-8` in the direction neither expected.** §L-8 said
*"re-measure before registering."* Re-measured: **there is nothing to
re-measure.** ✅ **§D's `G0` is correct that the only number this script can emit
is the `✗` count — and G0 is now a PREREQUISITE, not a reporting nicety.** → §N-5.

## §N-2 · 🔴 G4b — THE NEW PIN, MEASURED, FAILS THREE WAYS

The adversary implemented route (e) and G4a/G4b/G4c as throwaway code and ran
rows 18, 19 and 20 as real breaks. ✅ **All three go RED as specced** — those
three rows are now **observed**, and are the first observed rows in this ticket's
history. **G4a and G4c are sound and correctly twinned.** `G4b` is not:

| # | The hole | The measurement |
| --- | --- | --- |
| **1 · SHAPED FOR ONE SPELLING** | `/original confirmation email/i` is **one literal phrase, which is a constant name spelled out.** *"…are in your original **booking confirmation**."* and *"Your accepted terms are in **the confirmation email we sent you**."* are the **identical dangling pointer** — the exact defect §M-0 exists to remove | **Both GREEN, `EXIT=0`.** Rows **18c** and **18d** |
| **2 · VACUOUS** | Row 20 deletes the paid-era rendering entirely: **G4a fires twice, G4b stays SILENT.** A body with nothing in it satisfies G4b. Since no live constant contains the phrase once route (e) lands, **G4b passes at every commit whether its regex is correct, misspelled, or deleted** | `EXIT=1`, `✗ 2 checks failed`, **both `G4a`** |
| **3 · SCOPED TO ONE BODY** | The pointer restored to the **current-era** rendering instead of the paid-era one. §M-3 scopes all three pins to *"that same body"* — **that scoping is the hole** | **GREEN, `EXIT=0`.** Row **18e** |

🔴 ***`A-R7-2` says the pointer is banned "by SHAPE, never by constant name," and
the pin written under it bans one literal phrase. The assumption is right; the
pin does not implement it.*** *This is `CLAUDE.md`'s negative-assertion trap —
"a negative assertion is shaped for the claim it was written against and covers
no other claim of the same class" — committed by the pin that cites its sibling.*

**→ The three fixes, all landed in §D's `G4b` row:**
- `TERMS_POINTER_SHAPES = [/(?:original|previous|earlier|first)\s+(?:booking\s+)?confirmation/i, /confirmation (?:e-?mail|message)\b[^.]{0,40}\b(?:terms|accepted|agreed)/i, /\bterms\b[^.]{0,60}\b(?:in|attached to|included in)\b[^.]{0,20}\b(?:e-?mail|confirmation)\b/i]`, `.some()`-banned.
- 🔴 **A positive CONTROL**: assert the shape list **does** match a retired-copy literal held in the test file. Then a broken regex reddens on its own. *Without it the ban is unfalsifiable — measurement 2.*
- **Run it over EVERY customer body**, not the paid-era one.

## §N-3 · 🔴 §L-4 IS RIGHT AND UNDERSTATED — THE CANONICAL SHAPE ITSELF SHIPS GREEN

§L-4 attributes the six known escapes to **shape** — `$399` has no decimals,
`399.00 CAD` has no sign. **That diagnosis is incomplete, and the incompleteness
changes the fix.**

🔴 **`$399.00 + GST ($418.95 total)` — the exact string `!/\$[\d,]+\.\d{2}/` was
written to catch — ships GREEN on the request email.** `EXIT=0`,
`✓ booking notification checks passed`. **It is not a shape problem. No money
regex is ever evaluated against a `messageType: 'request'` body at all.**

⚠️ **→ Widening the shape list without ALSO instantiating the pins on request
fixtures fixes NONE of it.** Both halves are required, and §D's `G1` row
currently orders neither — §L-4's widening never reached it. → §N-6, item 11.

### The green breaks, ranked, with the assertion owed and its twin

| Break | What shipped green | Assertion owed |
| --- | --- | --- |
| 🔴 **B1** | `$399.00 + GST ($418.95 total)` in the **request** body, html **and** text | `MONEY_SHAPES` over `customer` **and** `internal`, `html`/`text`/`subject`, on request fixtures. **Twin:** the same body still contains `RECEIVED_HOLD_LINE` |
| 🔴 **B2 · B3 · B23** | A price in the request **subject**; in the **text arm only**; and `$399.00 still due` in the **expired-request subject** | 🔴 **`subject` is checked for money NOWHERE in the file, on ANY message type.** Build the surface list explicitly over `ALL_CUSTOMER_MESSAGES` × `html`/`text`/`subject`. **Twin:** the list is non-empty and each message contributes a known heading, so a message dropping out reddens |
| 🔴 **B12** | `Amount due on the day: $418.95` in the **ICS `DESCRIPTION`** | `:1089-1091` bans policy, claim and insurer from the ICS and **nothing bans an amount.** The calendar entry outlives the email. **Twin:** the ICS still carries `Assessment —` and `VISIT_LENGTH_LINE` |
| 🔴 **B14** | `A callout fee of 150 dollars applies…` on the **`$0` goodwill** message | 🔴 **`CLAUDE.md`'s GST walk-past, live, on the pin `CLAUDE.md` names.** `:743`'s `!/\$[\d,]+\.\d{2}/` is documented in-file as *"no computed figure"*; `150 dollars` carries no `$` and no cents. **Twin already exists** at `:741` — only the negative needs widening |
| 🔴 **B4 · B5** | `&#36;399.00` and `$3<span>99.00</span>` | Run the shapes over a **normalised** body as well as the raw one. **Twin:** the normaliser is itself pinned — `stripTags('<p>$1.00</p>') === '$1.00'` |
| 🔴 **B9** | **`Your booking is confirmed.` on the REQUEST email** | `BOOKED_CLAIM_SHAPES` (six regexes, `:952-959`) runs on the **confirmed** message only; the request gets one regex, `/you'?re booked/i`. **Five of six have no request-side instance.** 🔴 *This is BK-36's trap, on the surface BK-36 was filed about.* Hoist the shapes to module scope, run over request html/text/subject |
| 🔴 **B6 · B13** | `…refund you in full, no questions asked.` on the request, and `This payment is fully refundable…` on **`APPROVED_*`, the email that asks for money** | 🔴 **Nothing in the file bans a refund promise on ANY customer message.** `REFUND_PROMISE_SHAPES`, banned on request / `$0` / no-snapshot / `approvalMessage`. **Positive control (mandatory):** the shapes must fire on `FEE_TERMS_REFUND`, which legitimately contains refund language |
| 🔴 **B16** | `Your account is settled in full; nothing further is due.` | `:747`, `:783-784`, `:819-821` all ban `PAID_IN_FULL_LINE` **by identity**. Same claim, walks past all three — **including on the short-payment message, where the two claims contradict.** `PAID_CLAIM_SHAPES`, controlled against `PAID_IN_FULL_LINE` itself |
| 🔴 **B10** | `We reply within one hour, guaranteed.` | `:1459`'s `/within \d+ (minutes\|hours)/i` requires **digits**; `one hour` is prose. The pin's own comment says the client deliberately did not publish this |
| 🔴 **B19** | `We are a GST registrant (BN 775654577).` on request / `$0` / no-snapshot | `:916` bans the literal `775654577RT0001`. **BK-48's own defect class, re-landed in the pin BK-48 wrote** |
| 🔴 **B17** | `Weekend rate applied.` on a **Tuesday** fixture | `:2021` is **case-sensitive** — `includes('weekend rate')` |
| 🔴 **B18 · B20** | `A calendar invitation is attached…` on the request; `Cancel online at yegrestoration.ca cancel-booking` | Both banned by identity / by punctuation. `:199`'s URL check reads **`customer.text` only**, html unchecked |
| 🔴 **B7 · B8** | `…carry a 50% premium.` and `…charged at time and a half.` | ⚠️ **THESE DEFEAT §D's `G3` AS SPECCED, not merely as implemented.** G3 is `/(weekend\|saturday\|sunday\|after[- ]hours)/i` **×** `/(1\.5\s*[x×]\|1\.5 times)/i`. **The CONJUNCTION is what kills it** — G3 is strictly weaker than either half alone, and the multiplier is one number expressible at least six ways. Make the second conjunct a disjunction over **magnitude**, and control it against `FEE_TERMS_PAYMENT[1]` and `AFTER_HOURS_NOTE`, both live copy — **the control §L-3 says §K-4 never installed** |

✅ **The three breaks that went RED** — `B15`/`B15b`/`B15c`, a `Total` line on the
confirmed arm — went red via the **recomputed-total** pin and the `$0`-arm money
regex, **not via any `Total` pin.** `:803`'s *"renders no Amount block"* reads
`noSnapshot.html` **only**, never the text arm.

## §N-4 · 🔴 A CORRECTION TO THIS PROJECT'S OWN SCORING RULE

`CLAUDE.md` and every brief say **"score on (exit code, summary line), never a `✗`
count."** Measured on this script at this commit, with `assessmentSummary` made
to throw (§D row 12):

```
EXIT = 1          ← IDENTICAL to an ordinary failure
✗ count = 0
summary lines = []
```

🔴 **At this commit the EXIT CODE DOES NOT DISTINGUISH A CRASH FROM A FAILURE.**
Both are `1`. §L-8 records `EXIT=2, ✗ CRASHED after 393 checks` — **that is
§I-4's three-state `G0` already installed**, not this commit's behaviour.

**→ Until `G0` lands, "score on (exit code, summary line)" must be read as
"THE ABSENCE OF A SUMMARY LINE IS THE CRASH SIGNAL" — never as "exit 2 means
crash."** ⇄ **That makes `G0` a prerequisite for row 12 being meaningful**, and
it is why §D's sequencing puts the counter in commit (a).

## §N-5 · WHAT THE TRACEABILITY AUDIT FOUND — and where each landed

🔴 **§L-9's fifteen were re-verified at their destinations. "14 done, 1 moot" is
the right COUNT and the wrong VERDICT: six are PARTIAL.**

| # | §L-9 said | Re-verified 2026-09-09 | Disposition |
| --- | --- | --- | --- |
| **O1** | DONE | **4 of 5** — §J-2 `:2085` still cites `:290`/`:366`, and §K-8's PR4-N2 row claims *"all §J-2 citations normalised"* | → §N-6 |
| **O3** | MOOT | **Moot ✓, but its claim was FALSE** — *"replaced by rows 18–20, which ARE in §D"*. **They were not in §D.** | ✅ **LANDED in §D** |
| **O5** | DONE | **PARTIAL** — `13b` landed in §D `:526`; **§H-5, the destination O5 itself names, is untouched** | → §N-6 |
| **O8** | DONE | **PARTIAL** — two combined §L-0 rows, not five. Substance landed | nit |
| **O9** | DONE | **PARTIAL** — `G12` got a §D pin row; **it still has no red-first row**, and nothing states a one-time SQL assertion is exempt | → §N-6 |
| **O11** | DONE | **4 of 5** — `:1868` still `:130-136` | nit |
| **O12** | DONE | **1 of 2** — `BK-53.md:2154` still asserts *"reproduced end-to-end"* **under a heading that tells future adversaries not to re-attack it** | → §N-6 |
| **O13** | DONE | **3 of 4** — `:1744` still carries a bare `A15` **and** an unstruck `confirmationAlreadySent` | → §N-6 |
| **O2 · O4 · O6 · O7 · O10 · O14 · O15** | DONE | ✅ **LANDED, verified at the destination** | — |

**§M-2's five deletions:** rows 17/17b ✅ · §A3 ✅ · G4 four arms **PARTIAL, now
fixed** (§N-6 item 2) · `confirmationAlreadySent` **PARTIAL** (`:454` and `:1744`)
· the `SELECT` dependency **PARTIAL** — retained as owed *"for the refund work"*
with **no owning ticket and no ROADMAP entry**, which `CLAUDE.md` requires, and
attributed to §G's item **1** when §L-2 tied it to item **2**.

**Review counts, counted rather than inherited:** plan review 1 = **7 blockers /
9 should-fix / 15 nits**; plan review 2 = **7 / 8 / 10**. ✅ **Both headers
match their own enumerations.** Every review-1 nit has a landed disposition
except **N5** (*"G0's sketch omits the failure branch"*), answered by §I-4's
three-state block but carrying no explicit disposition — nit, recorded here.

## §N-6 · 🔴 THE OWED LIST — checkable in one grep each, and NOT marked done

⚠️ **Nothing in this table is marked ✅ by the edit that wrote the table.**
*That is the third-time trap; marking is not doing.*

| # | Owed | Destination | Grep that settles it |
| --- | --- | --- | --- |
| ☐ **N1** | §J-2's two citations → `:291` / `:364` | §J-2 `:2085` | `grep -n ':290\|:366' BK-55.md` |
| ☐ **N2** | §H-5's *"G8 gets red-first row 13"* → `13b` | §H-5 | `grep -n 'row 13' BK-55.md` |
| ☐ **N3** | `G12` needs a red-first row, or a written exemption | §D red-first | `grep -n 'G12' BK-55.md` |
| ☐ **N4** | `:1744`'s bare `A15` → `A-R5-3`, and strike `confirmationAlreadySent` | §K-9 `:1744` | `grep -n 'A15' BK-55.md` |
| ☐ **N5** | `BK-53.md:2154`'s *"end-to-end"* → annotate `[RETRACTED — §K-1/§L-1]` | `BK-53.md` | `grep -n 'end-to-end' ../BK-53.md` |
| ☐ **N6** | `:1868`'s `:130-136` → `:130-135` | §I-2 | `grep -n '130-136' BK-55.md` |
| ☐ **N7** | 🔴 **§G's `A7` still asserts `INSURANCE.termsEra: 'prior'`; §H-6 REVERSED it to `'current'`** — blast radius ~22 inherited assertions, and §I-2 exists *because* one paragraph argued the old value for a full revision after the reversal | §G `:659` | `grep -n "A7 " BK-55.md` |
| ☐ **N8** | 🔴 **`G10` and `G11` have no row in §D's pins table**, though §I-4 orders *"register G1…G11 individually"*. Revision 7 added `G12` to that table for exactly this complaint and left these two out of the same edit | §D pins | `grep -n 'G10\|G11' BK-55.md` |
| ☐ **N9** | 🔴 **§D's `G1` carries neither half of §L-4's spec** — not the request-fixture instantiation, not the widened shapes. **§N-3 shows both are required** | §D `G1` | `grep -n 'G1 ' BK-55.md` |
| ☐ **N10** | The production `SELECT`: name an owning ticket **and** a ROADMAP Known-traps entry, and correct §G item **1** → **2** | §M-2 · ROADMAP | `grep -n 'SELECT' ../../ROADMAP.md` |
| ☐ **N11** | §M-1 orders an edit to `booking-copy.ts`; **plan-review B2 reasoned from "§0.1 forbids this ticket from editing" it.** Revision 2 replaced §0–§8 wholesale, so the premise may be dead — **but nothing says so in either direction** | §E | `grep -n 'booking-copy' BK-55.md` |
| ☐ **N12** | Every assertion in §N-3's right-hand column, each with its twin | `verify-booking-email.ts` | implementation |

## §N-7 · ASSUMPTIONS

| # | Assumption | Basis |
| --- | --- | --- |
| **A-R7-4** | 🆕 **The `397 / 417` figures are RETIRED, not re-measured.** The script emits no count at this commit; any future count is a measurement of `G0`, not of the baseline | §N-1, measured |
| **A-R7-5** | 🆕 **A crash and a failure are indistinguishable by exit code until `G0` lands**, so the summary line's ABSENCE is the crash signal | §N-4, measured |
| **A-R7-6** | 🆕 **A pin that bans one literal phrase is banned by constant name, whatever the regex looks like.** `A-R7-2` is upheld; `G4b` as specced violated it | §N-2, measured |
| **A-R7-7** | 🆕 **Widening a money shape and instantiating a money pin are TWO fixes.** §L-4 diagnosed only the first | §N-3, measured |

## §N-8 · WHAT THIS ROUND DOES **NOT** CLAIM

- ⚠️ **No email was sent.** Every assertion is against the **built message
  objects** from `planBookingNotifications` / `approvalMessage` /
  `expiredRequestMessage` — which is what `verify-booking-email.ts` itself does.
- ⚠️ **No database.** The `termsEra` → column mapping (§H-1's thirteen
  `source: 'web'` rows, and `G12`) is **untested** — the adversary's `termsEra`
  was a throwaway input field, not a column read. **No probe rows were created,
  so there is no fixture wreckage to clear.**
- ⚠️ **`B22`** *(a price in the OFFICE subject)* **was written and never landed** —
  the anchor was a self-replacing no-op. **Reported as UNTESTED, not as green**,
  though B2 makes it likely.
- ⚠️ Unicode-digit, fullwidth and spelled-out price spellings were **not**
  measured here. *(They WERE measured on the web side — `BK-53.md` §T-2 — and
  both go green there.)*
- ⚠️ **Only `verify:booking:email` was run.** Whether another script in the suite
  catches any of these nineteen is **unknown**.
- 🔴 **The worktree was 34 commits behind `main` and its `BK-55.md` was a
  135-line REVISION 1 stub.** The agent caught it and read from `main`; the six
  divergent source files and `booking-email.ts` / `verify-booking-email.ts` were
  byte-identical, so **no code finding is affected — but the spec would have
  been.** → a brief must say WHICH working tree. Recorded in the handoff.

---

# 🟫 REVISION 6 — 2026-09-06. **THE SPEC. §K-1's REGISTRATION IS WRONG IN THE OTHER DIRECTION; §K-5 IS WITHDRAWN.**

> 🔴 **Read this before revision 5.** Revision 5 retracted a false GREEN claim and
> replaced it with a false **RED** one. **Commit (b) reaches `✓ booking
> notification checks passed — 397 checks, 0 failed`.** §K-2, §K-3 and §K-6
> survive; **§K-4 and §K-5 do not.**

## §L-0 · ABSORPTION — revision 5's sections against revision 6

| §K section | Verdict |
| --- | --- |
| **§K-1's ten** | ✅ **The ten are REAL and reproduce one for one**, office pair included — independently re-executed. ⚠️ **But they are the count for THREE items of commit (b), not for commit (b)** |
| **§K-1's "Full commit (b)" label** | 🔴 **FALSE — §L-1** |
| **§K-2** (`:1214`'s replacement) | ✅ **STANDS — verified by execution three ways.** ⚠️ one nit, §L-6 |
| **§K-3** (the snapshot-absent rewrite) | ✅ **STANDS, and its POSITIVE TWIN genuinely fires** (measured red in the row-2 control). 🔴 **Its NEGATIVE half leaks — §L-4** |
| **§K-4** (invert `:2039-2042`) | 🔴 **WITHDRAWN — §L-3.** It is a second ban, case-sensitive, and vacuous at its own commit |
| **§K-5** (`confirmationAlreadySent`) | 🔴 **WITHDRAWN ENTIRELY — §L-2.** The column cannot answer the question, and the repo already documented that |
| **§K-6** (G0, G4's fifth arm) | ✅ **G0 STANDS and is measured working** (exit 2, `✗ CRASHED after 393 checks`). ⚠️ **Its registered string is wrong twice — §L-1c.** 🔴 **G4's fifth arm falls with §K-5** |
| **§K-7** (§F) | ✅ **STANDS.** Clauses 1c, 5b and 6 are now **written into §F**, having been prose-only (T5-3) |
| **§I-0, §I-3, §I-5, §J-1** | 🆕 **ADDED revision 7 (O8/T5-17) — §L-0 omitted all four.** §I-0 and §I-5 stand; **§I-3's red-first additions are now absorbed into §D** (O4); §J-1's assumption stands |
| **§I-6's N9 half** | 🆕 **ADDED (O8).** 🔴 **N9's one-time gate assertion is now `G12` in §D** (O9). *It had no G-number, no §D row and no red-first row, having been routed into a section that did not exist* |
| **§K-8, §K-9** | ⚠️ **AMENDED — §L-6.** Five nit dispositions claimed corrections that were never applied; §K-9's bare `A14/A15/A16` reintroduce a collision §I-5 forbade in writing |

## §L-1 · 🔴 THE RETRACTION OVER-CORRECTED — commit (b) is GREEN at 397/417

### a · What §K-1 actually sandboxed

§F defines commit (b) as **"everything else."** §K-1's measured row enumerates
**§B-1 + §K-2's `:1214` + §B-2 + §I-1 clauses 1–2** — and **omits §K-3's rewrite,
§K-4's inversion, §D's Class-1 SPLIT, §C's suppression and G1–G9, all of which
are commit (b).**

| State | Exit | Summary line |
| --- | --- | --- |
| Baseline | 0 | `✓ booking notification checks passed` |
| §K-1's three items | 1 | `✗ 10 checks failed` ✅ **reproduced exactly, ten for ten** |
| **…plus §K-3 + §K-4 + §D's split** | **0** | ✅ **`✓ booking notification checks passed`** |
| Baseline **+ commit (a)'s G0** | 0 | `✓ … — **417 checks**, 0 failed` |
| **Commit (b) + commit (a)'s G0** | **0** | ✅ **`✓ … — 397 checks, 0 failed`** |

**→ The registration is `✓ booking notification checks passed — 397 checks, 0
failed`, from a 417-check baseline.** §K-6's *"expect `✗ 10 check(s) failed of N
run`"* is **struck**.

### b · 🔴 And it contradicts seam clause 6, in the section that quotes it

Clause 6: *"An arm edited twice across two commits is correct; **a red commit is
not.**"* §K-1 registered commit (b) as **red** and did not notice.

> 🔴 **THIS IS THE DEFECT REVISION 5 EXISTS TO RETRACT, COMMITTED ONE LEVEL UP BY
> THE RETRACTION.** §K-1 wrote the rule — *"a commit is measured by sandboxing
> THE COMMIT, not the item a review happened to be about"* — **and then did not
> apply it to the sandbox in the paragraph below it.**

**The rule is unchanged and is right. What failed is that a rule stated in a
revision is not thereby applied by that revision** — the same shape as *an order
issued in prose is not performed*, one level of abstraction up. → **§F clause 6
now requires the sandbox's CONTENTS to be listed and diffed against §F's, which
is the mechanical form of the rule; the prose form was not enough for its own
author.**

### c · The registered string was wrong twice over

§K-6 registered `✗ 10 check(s) failed of N run`. §I-4's block emits
`` `✗ ${failures} check${failures === 1 ? '' : 's'} failed of ${checksRun} run` ``
→ **`✗ 10 checks failed of 397 run`**. **A registration whose entire purpose is
literal matching cannot carry a prose plural** (A5-F7). Moot under §L-1a's green
registration, and recorded because the same error will recur at the branch tip.

## §L-2 · 🔴 §K-5 IS WITHDRAWN — the column cannot answer the question

§K-5 wrote: *"Mechanism verified available: `db.ts:105` is
`confirmation_sent_at: Date | null` ✓."* **It verified the column EXISTS. It did
not verify what it MEANS**, and the repo had already recorded the answer.

**Verified at source:**
- 🔴 **`booking-commit.ts:185` stamps `confirmation_sent_at` when the
  booking-RECEIVED email goes out** — that is the **public request path**, an
  acknowledgement, not a confirmation.
- **`booking-payment.ts:1550`**: *"nothing here stamps `confirmation_sent_at` …
  **and it must not start**"* — so the **post-payment confirmation `markPaid`
  sends** does NOT stamp it.
- **`[id].astro:1000-1019`** carries a reviewed comment saying exactly this, and
  ROADMAP Known traps records: ***"the column cannot answer 'has this customer
  heard from us' without a second one."***

**Both directions are live falsehoods under §K-6's fifth arm:**

| Row | `confirmationAlreadySent` | What renders | Which defect |
| --- | --- | --- | --- |
| request-ack only | **TRUE** | *"The terms you accepted are in your original confirmation email"* — **pointing at an email never sent** | 🔴 **SF6 verbatim — the defect §K-5 exists to fix** |
| confirmed via `markPaid` | **FALSE** | falls to `'none-legacy'`: *"This booking was taken over the phone"* — **to a paying WEB customer** | 🔴 **§H-1 verbatim — the defect §K-5 cited to reject route (c)** |

**§K-5 chose route (b) on §H-1's grounds and reintroduces §H-1's defect through
it.**

### 🔴 And "all three routes enumerated" was not all — there are **SIX**

*(⛔ ~~at least five~~ — corrected revision 7, O14: the table below lists (a) through (f). The section correcting an enumeration miscounted its own.)*

*A refutation is a measurement, and so is an enumeration.* §K-5 cited that rule
and broke it.

| Route | Status |
| --- | --- |
| (a) a fifth `termsEra` value | foreclosed by §I-6 |
| (b) a second boolean field | 🔴 **DEAD — §L-2 above.** *The most expensive of the five, chosen against an enumeration of three* |
| (c) collapse at the producer | foreclosed by §H-1 |
| 🆕 **(d) assert the population EMPTY** | ⚠️ **§I-6's own N9 resolves the structurally identical NULL risk on `row.source` with "a one-time gate assertion."** If the assertion holds there is **no field, no fifth arm, and no amendment to nine literals** |
| 🆕 **(e) change the SENTENCE so no guard is needed** | ⚠️ e.g. *"You accepted our previous terms when you booked; ask us and we'll send you a copy."* **No fifth value, no second field, no producer change — and SF6 is satisfied because nothing points at an artifact** |
| 🆕 **(f) pass the RENDERING VARIANT, not the epoch** | ⚠️ keeps `termsEra` meaning what it is named, which was §H-1's whole objection |

**→ SPEC: routes (d), (e) and (f) are the live candidates and (e) is the
cheapest. THE CHOICE IS DEFERRED to revision 7, and the deferral is deliberate:**
§K-5 chose in one paragraph, against three readings, and the choice was wrong on
a fact the repo had already written down. **Route (d) requires a `SELECT` against
production — §G's "NOT KNOWN" list, item 2, which is still unanswered.**

### 🔴 The knock-on, stated so it is not rediscovered

**§K-5's producer assignment was ALSO wrong, independently (T5-4 / A5-F1):**
`resend.ts:106` and `bk51-gated:booking-payment.ts:1597` are **call sites of
`planForAppointment`**, not builders of a `BookingNotificationInput`. The only
literal is **`booking-admin-notify.ts:172`**, *which both inherit* — exactly what
§A2 concludes and what §I-2's producer table correctly says. **§K-5 contradicted
two sections of its own ticket.** ⚠️ **§I-2's producer table, amended by revision
5 to add `row.confirmation_sent_at`, is REVERTED with §K-5.**
✅ **§A3's convention is confirmed intact by execution:** adding two required
fields to `BookingNotificationInput` produced **exactly 9 errors at exactly the
nine sites §A3 names.** *§A3 was never amended (T5-6), and under the withdrawal it
does not need to be.*

## §L-3 · 🔴 §K-4's INVERSION IS WITHDRAWN — it is a second ban, not a control

§K-4 specced `:2039-2042` → `!mouldBody.includes('weekend rate')` and called the
fixture *"G3's only positive control."* **Three faults, all measured:**

1. 🔴 **It is a NEGATIVE assertion called a positive control.** After commit (b)
   **nothing positively asserts the weekend path reaches an email at all.**
2. 🔴 **It is CASE-SENSITIVE and the codebase spells it differently.** Planting
   `AFTER_HOURS_NOTE` **verbatim** (`booking-copy.ts:597`) — *"**Weekend rate** —
   Saturday and Sunday appointments are 1.5× the weekday price."* → **EXIT=0,
   `✓ booking notification checks passed`.**
3. 🔴 **It is VACUOUS at its own commit** — by §J's own definition, in the ticket
   that made vacuity a rule. After §B-1 deletes `assessmentSummary`, the literal
   lowercase `weekend rate` **exists nowhere reachable by any email body**.
4. 🔴 **It violates §D's own G3 design rule**, which orders the weekend hazard
   *"pinned as a **CLAIM**, not a spelling"* —
   `/(weekend|saturday|sunday|after[- ]hours)/i` × `/(1\.5\s*[x×]|1\.5 times)/i` —
   **and names BK-48 three ways.** §K-4 installed one ASCII spelling.

**→ SPECCED, replacing §K-4:**
- **`:2037`, `:2038`** — DELETE (unchanged from §K-4; recomputed figures decision 28 removes).
- **`:2039-2042`** — **REWRITE to G3's CLAIM shape**, both conjuncts, case-insensitive, per §D's G3 row.
- 🔴 **ADD A REAL POSITIVE CONTROL on the `weekendMould` fixture:**
  `check(mouldBody.includes('On-site assessment'), …)` — *the fixture reached the
  renderer and produced a body*, which is what makes the negative arm meaningful.
- ⚠️ **Red-first rows 3 and 4 become observable for the first time** — see §L-4.

## §L-4 · 🔴🔴 THE REQUEST EMAIL HAS NO MONEY PIN AT ALL

**Six planted defects shipped GREEN with commit (b) fully applied.** Each string
grepped back out before scoring; every run `EXIT=0`, `✓ booking notification
checks passed`.

| ID | Planted | Why it passed |
| --- | --- | --- |
| **A5-G1** | `— $399 + GST, due on the day` | **`$399` has no decimals.** All three money pins are `!/\$[\d,]+\.\d{2}/` |
| **A5-G2** | `— 399.00 CAD plus 5% GST, payable on arrival` | **no dollar sign** |
| **A5-G3** | `AFTER_HOURS_NOTE` verbatim | §L-3's case sensitivity |
| **A5-G4** | `FEE_TERMS_PAYMENT[1]` verbatim — *"…charged at 1.5 times the weekday price."* | 🔴 **red-first row 4's own break** — *"the spelling that has no 'weekend' and no '1.5x' in it"*. **§K-4 installs no such pin** |
| **A5-G5** | a figure re-added to the office arm at `:1214` | 🔴 **red-first row 3's own break (G2's inversion)** |
| 🔴 **A5-G6** | `${tierNameOf(...)}${isRequest ? ' — $699.00 + GST ($733.95 total), payable at the visit' : ''}` | 🔴 **§B-1's OPENING SENTENCE, CHARACTER FOR CHARACTER, on the request email** |

🔴 **THE ROOT CAUSE, and it is one fact:** at commit (b) the file contains
**exactly three money-shape regexes** — `:739` (`free`, the `$0` confirmed arm),
`:795` (§K-3's `noSnapshot`, confirmed) and `:2019` (§D's split, `INSURANCE`,
confirmed). **`INSURANCE.messageType` is `'confirmed'`, so all three are
CONFIRMED-arm fixtures. Nothing pins the request email.**

⚠️ **G1's own row already orders the fix** — *"scope it to `settled === null`
fixtures of **BOTH** message types (review S2)"* — **and §K-3, §K-4 and §D's split
all left it unperformed.** *Same class as PR4-B6/B7, in the revision that
performs those.*

**→ SPECCED: G1's money pin is instantiated on a REQUEST fixture as well as a
confirmed one, and the shapes widen beyond `\$[\d,]+\.\d{2}` to cover a
decimal-free figure and a sign-free amount.** ⚠️ **`BK-53.md` R12-2a measures the
same class on the web side — 14 of 19 money spellings green — so this is one
defect on two surfaces, and the shape list should be shared, not written twice.**

## §L-5 · G0 IS SOUND, WITH ONE HOLE IT DOES NOT CLOSE

✅ **Measured working:** with §I-4's block and wrapper landed, a mid-run throw
gives **`EXIT=2`, `✗ CRASHED after 393 checks (0 had already failed)`** —
legible, and exactly what BK-33 demands. Commit (a) is confirmed additive in
assertion terms (**417** baseline; nothing outside the script consumes its
summary line) while being a structural wrap, as §K-6 says.

🔴 **The hole:** a **module-load** throw — *the shape of BK-53's own commit, which
§I-1's table describes as "a missing export … before one assertion runs"* —
raises **before** the in-module `try` and reports **`EXIT=1` with an EMPTY summary
line and zero `✗`.** §I-4's contract says *"exit 2 is CRASHED, exit 1 is
FAILED"*; **a harness scoring on (exit code, summary line) sees FAILED with
nothing to read.**
**→ Closing it needs a wrapper PROCESS or a dynamic `import()` inside the `try`.
Neither is specced. Recorded, owed, and NOT closed** — *an item whose effect
lands on an artifact nobody has looked at is pending an artifact.*

## §L-6 · THE TRACEABILITY FINDINGS

🔴 **THE ✅ MARKS BELOW ARE CLAIMS, NOT LANDINGS. An independent audit found ten
of fourteen checked destinations untouched, and three that do not exist. See
§L-9 for the OWED list. Do not read a ✅ here as evidence.**


| ID | Disposition |
| --- | --- |
| **T5-1** | ✅ **ACCEPTED — PERFORMED.** All 8 should-fix and 10 nits are **transcribed** above the absorption pass; §F gains **clause 5b**. *The 18 findings existed only as revision 5's own paraphrase — the pre-condition for silent narrowing* |
| **T5-2** | ✅ **ACCEPTED — PERFORMED.** §D's two-state G0 block is **deleted where the block was**, not seventy lines below it |
| **T5-3** | ✅ **ACCEPTED — PERFORMED.** §F now carries **clauses 1c, 5b and 6** |
| **T5-4** | ✅ **ACCEPTED — moot under §L-2's withdrawal**, and the reason is recorded so it is not re-derived |
| **T5-5** | ✅ **ACCEPTED — §L-2.** Routes (d), (e), (f) enumerated; the choice is **deferred to revision 7** |
| **T5-6** | ✅ **ACCEPTED — moot.** §A3 and §C-2 need no amendment under the withdrawal, and §A3's nine-literal convention is **confirmed by execution** |
| **T5-7** | ✅ **ACCEPTED — §L-3.** The `?? ''` fallback means a negative assertion passes on an empty string; **`A-R5-3` is rewritten** *(⛔ ~~A16~~ — renumbered T5-9, two rows below)* |
| **T5-8** | ✅ **ACCEPTED — PERFORMED.** `NEXT-SESSION-PROMPT.md` carries a **superseded banner**; `BK-53.md`'s two surviving *"end-to-end"* mentions are annotated |
| **T5-9** | ✅ **ACCEPTED — PERFORMED.** §K-9's bare `A14/A15/A16` are renumbered **`A-R5-1…3`**, restoring §I-5's convention. *A rule rewrite that drops the case that motivated the rule is a decision nobody made* |
| **T5-10** | ✅ **ACCEPTED.** §J-2's vacuous set is **re-measured against commit (b)**: with `:2043-2046` and `:671-678` it is **eleven, not nine**. §K-0's *"§J-2 STANDS"* is struck |
| **T5-11** | ✅ **ACCEPTED — the five unapplied nit corrections are applied at their destinations** (`check()` `:86-91`; §J-2's `:291`/`:364`; §B-2's `:528-530`; `resend.ts:94-96` ×3; §B-4's `:628-629`) |
| **T5-12** | ✅ **ACCEPTED.** `lastIndexOf` is written into **§D's G6 row**; row 17's positive twin is written into **§D as row 17b**, not only into §I-6 |
| **T5-13** | ✅ **ACCEPTED.** §D's red-first table absorbs §I-3's **8b, 13–16** and §I-6's **17/17b**. 🔴 **Row 13 was double-assigned** (§H-5's G8 row and §I-3's cutoff row) — §H-5's becomes **13b**. §D's row 8 corrected to *"G4's **two** `'none-*'` arms"*. **§D's 6b and §I-6's 17 are the same failure mode under two numbers — 6b is withdrawn** |
| **T5-14** | ✅ **ACCEPTED.** Seven of eight orphaned-comment citations were off; corrected, **and `:1117-1124` — `assessmentSummary`'s own doc comment — is added**, which the enumeration missed entirely |
| **T5-15** | ✅ **ACCEPTED.** §C-2's *"the two producers"* is corrected to **three** (`create.ts:216` is the third, as §C-2's own list already said) |
| **T5-16** | ✅ **ACCEPTED — folded into T5-10's re-measurement** |
| **T5-17** | ✅ **ACCEPTED.** §L-0 carries rows for **§I-0, §I-3, §I-5, §J-1** and for **§I-6's N9 half**. 🔴 **N9's specced assertion has no G-number, no §D row and no red-first row** — it is a specced assertion nothing points to, *routed into §I-6 precisely because it had been dispositioned to a section that did not exist.* → **G12, with a §D row** |
| **T5-18 … T5-28** | ✅ all accepted; citation ranges normalised to the `check(` opening line, `INSURANCE`'s comment to `:130-135`, `verify-cutover.ts:350-357`, §K-3's sibling arms corrected to **two** (`:802-805` **and** `:818-823`) |
| **A5-F5** | ✅ **§K-1's rows 2–7 cite `:2015-2016`; the figures are at `:2018`/`:2019`** (`:2015` is the tier-name check). §D's prose was right and §K-1 was wrong |
| **A5-F8** | ✅ commit (b) leaves **`assessmentQuote` orphaned at `booking-email.ts:75`** (`ts(6133)`); `astro check` still reports 0 errors. Added to §K-2's orphan list |
| **A5-F9** | ✅ §A3's `verify-booking-email.ts:108` literal is reported by the compiler at `:112:7` |


## §L-9 · 🔴 THE OWED LIST — **read this INSTEAD of §L-6's ✅ marks**

⚠️ 🔴 **§L-6 claims roughly twenty-five in-place corrections. AN INDEPENDENT
AUDIT WENT TO THE DESTINATIONS: of fourteen checked, FOUR landed and TEN did
not** — and **three named destinations do not exist in this file at all.** The ✅
marks in §L-6 are **claims, not landings.**

🔴 **THREE OF THE NAMED DESTINATIONS ARE PHANTOMS — `row 13b`, `row 17b` and
`G12` each occur EXACTLY ONCE in this file: inside the disposition that names
them.** ***That is §I-6's own trap, verbatim*** — *a disposition that names a
destination nobody creates is indistinguishable from a dropped finding — recorded
as a lesson by this ticket, and committed again by the revision that cites it.*

⚠️ 🔴 **READ THE LEFT COLUMN FOR THE CURRENT STATE. The last column is
`⛔ WAS:` — the DEFECT AS FOUND, kept so the finding stays legible. It is NOT
the current state.** *A cold-read test misread this table as "ten still open"
because a ✅ row still carried a 🔴 cell — the promissory-note trap reproduced
inside the table built to cure it.*

| # | Owed | NOW | The defect as found |
| --- | --- | --- | --- |
| ✅ **O1 — DONE 2026-09-07** | T5-11's five nit corrections | `check()` `:86-91`; §J-2's `:291`/`:364`; §B-2's `:528-530`; `resend.ts:94-96` ×3; §B-4's `:628-629` | ⛔ **WAS:** **0 of 5 applied** |
| ✅ **O2 — DONE 2026-09-07** | T5-12's `lastIndexOf` into **§D's G6 row** | §D | ⛔ **WAS:** **NOT applied** — G6 still says only *"POSITION, not presence"*. `lastIndexOf` appears nowhere in §D |
| ⇄ **O3 — MOOT 2026-09-07** | T5-12's **row 17b** in §D | §D red-first | ⛔ **WAS:** ⇄ **Revision 7 WITHDREW rows 17 and 17b** (§M-2) — route (e) removes the pointer they tested. **Replaced by rows 18–20 (§M-3), which ARE in §D** |
| ✅ **O4 — DONE 2026-09-07** | T5-13's absorption of §I-3's **8b, 13–16** and §I-6's **17/17b** into §D | §D red-first | ⛔ **WAS:** **NOT applied** — the table still stops at row 12 |
| ✅ **O5 — DONE 2026-09-07** | T5-13's *"§H-5's row 13 becomes **13b**"* | §H-5 | ⛔ **WAS:** **PHANTOM** |
| ✅ **O6 — DONE 2026-09-07** | T5-14's addition of **`:1117-1124`** (`assessmentSummary`'s own doc comment) to the orphan list | §K-2 / §B-4 | ⛔ **WAS:** **NOT applied** — and this was the one orphan the enumeration had MISSED |
| ✅ **O7 — DONE 2026-09-07** | T5-15's correction of §C-2's *"the **two** producers"* → three | §C-2 | ⛔ **WAS:** NOT applied. §K-5 quoted this stale sentence as binding, and §K-5 is now withdrawn — **but the sentence is still stale** |
| ✅ **O8 — DONE 2026-09-07** | T5-17's five new §L-0 rows (§I-0, §I-3, §I-5, §J-1, §I-6's N9) | §L-0 | ⛔ **WAS:** NOT applied. §L-0 has rows only for §K-1…§K-9 — *the absorption pass omits exactly the sections the audit said it omits* |
| ✅ **O9 — DONE 2026-09-07** | T5-17's **G12** for N9's specced assertion | §D pins | ⛔ **WAS:** **PHANTOM.** §D's pins are G0–G9. **N9's assertion still has no G-number, no §D row and no red-first row** |
| ✅ **O10 — DONE 2026-09-07** | T5-10's re-measurement of §J-2 to **eleven** | §J-2; §K-0 | ⛔ **WAS:** NOT applied. §J-2 still titled *"THE VACUOUS SET IS NINE"*; §K-0's *"§J-2 ✅ STANDS"* unstruck |
| ✅ **O11 — DONE 2026-09-07** | T5-18…T5-28's citation normalisation | `INSURANCE` `:130-135` (5 sites); §K-3's siblings → **two** (`:802-805` **and** `:818-823`) | ⛔ **WAS:** **NOT applied** |
| ✅ **O12 — DONE 2026-09-07** | T5-8's annotation of `BK-53.md`'s two surviving *"end-to-end"* mentions | `BK-53.md` | ⛔ **WAS:** NOT applied. One still asserts flatly *"the first time in this ticket's history that a specced repair has been executed end-to-end"* — **the exact reading §K-1 retracted** |
| ✅ **O13 — DONE 2026-09-07** | 🔴 **Four DANGLING citations of the renumbered assumption IDs** | `§K-2` (*"→ Assumption A14"*), `PR4-S7` (*"recorded as A14"*), `A6` (*"load-bearing for A15"*), and **§L-6's own T5-7** (*"A16 is rewritten"*, one row above T5-9 which says A16 no longer exists) | ⛔ **WAS:** **The renumbering to `A-R5-1/2/3` landed at the table and nowhere else** |
| ✅ **O14 — DONE 2026-09-07** | 🔴 **§L-2's heading says *"at least FIVE routes"*; its own table lists SIX (a)–(f)**, and the Status line says *"three of at least six"* | §L-2 | ⛔ **WAS:** **The section correcting an enumeration miscounts its own enumeration** |
| ✅ **O15 — DONE 2026-09-07** | ⚠️ **The Status line asserts `397 checks / 417 baseline` as measured fact; §L-8 says this session reproduced the `✗ 10` and the baseline, NOT the 397/417** | Status vs §L-8 | ⛔ **WAS:** **Status overstates §L-8's own caveat.** Re-measure before registering |

✅ **What DID land, verified at the destination:** T5-1's transcription of all 18
should-fix and nits · T5-2's deletion of §D's two-state G0 block · T5-3's §F
clauses 1c/5b/6 · §L-2's revert of §I-2's producer table · §D's row 8 correction ·
§D's 6b withdrawal · the `A14/A15/A16` renumbering **at the assumption table** ·
the `NEXT-SESSION-PROMPT.md` banner · **and §D's `:2039-2042` row, corrected
in place after the audit** — it had still been ordering the arm §L-3 withdrew.

## §L-7 · ASSUMPTIONS

| # | Assumption | Basis |
| --- | --- | --- |
| **A-R5-1** *(was the colliding bare `A14`)* | Decision 28 removes the **RECOMPUTED** figure, not a settled one; `:1213` keeps `formatCents(input.settled.totalCents)` | Unchanged from §K-9. **No pin covers `:1213`** — recorded, not solved |
| **A-R5-2** *(was `A15`)* | 🔴 **WITHDRAWN.** `confirmationAlreadySent` does not exist | §L-2: the column is stamped by the request path and not by `markPaid` |
| **A-R5-3** *(was `A16`)* | ⇄ **REWRITTEN.** `:2039-2042` takes **G3's CLAIM shape**, case-insensitive, **plus a genuine positive control**; `weekendMould` survives | §L-3: the inverted arm was a second ban, vacuous and case-sensitive |
| **A-R6-1** | 🆕 **The `termsEra` guard is DEFERRED to revision 7**, among routes (d), (e) and (f) | §K-5 chose in one paragraph against three readings and was wrong on a fact the repo had recorded |
| **A-R6-2** | 🆕 **A rule stated in a revision is not thereby applied by that revision.** §F clause 6 requires the sandbox's CONTENTS to be listed and diffed | §L-1b: §K-1 wrote the sandbox rule and violated it in the paragraph below |

## §L-8 · WHAT THIS REVISION DOES **NOT** CLAIM

⚠️ **The `397` and `417` are from ONE agent's run in an isolated worktree.**
This session independently reproduced the **`✗ 10`** and the baseline `exit 0`,
**not the 397/417.** → **Re-measure at implementation before registering.**

⚠️ **The `termsEra` guard is UNSPECCED.** Revision 6 withdraws §K-5 and does not
replace it. **§C, G4's fifth arm and red-first rows 17/17b are all pending
revision 7's route choice**, and route (d) needs a production `SELECT` that §G's
"NOT KNOWN" list has wanted since revision 4.

⚠️ **No red-first row in this ticket has been observed.** Two controls fired
during the adversarial round (row 2 → `✗ 5 checks failed`; row 12 → `EXIT=2`,
`✗ CRASHED after 393 checks`) — **that is evidence about those two rows and
nothing else.**

---

# 🟩 REVISION 5 — 2026-09-06. **THE SPEC for §B-1, §D and §I-4. Amends §B-1, §D, §F, §I-1, §I-4 and §I-6.**

**Status of the claim this revision exists to retract:** ⛔ **"exit 0 after
§I-1's minimum edit" was never measured for commit (b) and is FALSE.**

⚠️ **"Everything else stands" is deliberately NOT written here**, per revision
4's own note. The sections amended are named; anything not named is unexamined,
not endorsed.

## §K-0 · ABSORPTION — revision 4's sections against revision 5

| Revision 4 section | Verdict under revision 5 |
| --- | --- |
| **§I-1 clauses 1 and 2** (the minimum edit) | ✅ **STAND — independently re-executed.** They are correct and sufficient for §B-2's twenty |
| **§I-1's "expected: exit 0"** | 🔴 **STRUCK — measured `✗ 10`.** Replaced by §K-1 |
| **§I-1 clause 3** (leave `mustNotCarry`, record vacuous) | ✅ STANDS |
| **§I-4's registration row "After commit (b): ZERO"** | 🔴 **STRUCK — replaced by §K-1's table.** The row is rewritten where it lives, not here |
| **§I-4's G0 code block** | ⚠️ **AMENDED by §K-6** — it is now the ONLY G0 block; §D's is deleted in place |
| **§I-6's `'prior'` guard** | ⚠️ **AMENDED by §K-5** — the route is now named |
| **§I-6's "G4 gains a fifth arm"** | ⚠️ **PERFORMED by §K-4** — §D's row is rewritten in place, not ordered to be |
| **§I-2, §J** | ✅ STAND |
| **§J-2** | ⛔ ~~STANDS~~ 🔴 **AMENDED revision 7 (O10): the vacuous set is ELEVEN, not nine** |
| **§B-2, §B-5, §C-1, §C-2, §E, §G** | ✅ STAND, unexamined by this round except where §K-5 names §C-2 |
| **§B-1** | 🔴 **AMENDED — §K-2 supplies the `:1214` replacement it never stated** |
| **§D's class table** | 🔴 **AMENDED by §K-3 and §K-4** |
| **§F** | ⚠️ **AMENDED by §K-7** — the gate decision is answered; the union is unchanged |

## §K-1 · 🔴 PR4-B1 — COMMIT (b) MEASURED IN FULL, FOR THE FIRST TIME

**The claim being retracted.** The status header, §I-1 (*"Expected after the
minimum edit: exit 0"*) and §I-4's registration row labelled **"After commit
(b)"** all assert exit 0. **All three were measured against §B-2's deletion
alone.** §B-1 is in the same commit and had never been sandboxed — by either
validation agent, or by the two runs the handoff calls "end-to-end".

🔴 **`HANDOFF-2026-09-05.md:432` has already propagated the stronger reading**
(*"its specced repair has been RUN END-TO-END"*). It is true of §B-2 and false of
commit (b). *The handoff is corrected in the same commit as this revision.*

### The measurements — mine, executed, restored from file backups

**Method, per CLAUDE.md:** every edit asserted landed before scoring; scored on
**(exit code, summary line)**, never a `✗` count; restored from a **file backup**
(`cp`), never `git checkout --`. Working tree verified clean afterwards
(`git status --porcelain` → the pre-existing untracked `.jpeg` only) and the
suite re-run green.

| State | Exit | Summary line |
| --- | --- | --- |
| Baseline | **0** | `✓ booking notification checks passed` |
| **§B-1's `tierLine` fix ALONE** | **1** | 🔴 **`✗ 8 checks failed`** |
| **Full commit (b)** — §B-1 (incl. §K-2's `:1214`) + §B-2 + §I-1 clauses 1–2 | **1** | 🔴 **`✗ 10 checks failed`** |

**`npm run typecheck` on the full commit-(b) state: 0 errors in either file.**
The failure is assertion-level, not compile-level — so unlike BK-53's commit it
**is** scorable (§I-1's own table row stands).

### The ten, enumerated — because a count is what R7-A 🔴2 forbids

| # | Assertion | Line | Block | Owner |
| --- | --- | --- | --- | --- |
| 1 | *a row with no snapshot falls back to the quote line rather than saying nothing* | **`:796-801`** | snapshot-absent fallback | 🔴 **BK-55 — see §K-3** |
| 2–3 | *the customer body states the base `$399.00`* / *the `$418.95` total* | `:2015-2016` | three-body loop `:2009-2024` | **BK-55, Class 1 SPLIT** |
| 4–5 | *the customer text body states…* (same two) | `:2015-2016` | same loop, 2nd iteration | **BK-55, Class 1 SPLIT** |
| 6–7 | 🆕 *the office body states…* (same two) | `:2015-2016` | same loop, 3rd iteration | 🔴 **BK-55 — reached only via §K-2's `:1214`** |
| 8–10 | *`$577.50`* / *`$606.38`* / *says WHY it is higher* | **`:2037`, `:2038`, `:2039-2042`** | `weekendMould` fixture | 🔴 **BK-55 — see §K-4** |

⚠️ **Rows 6–7 are the ones no prior measurement could have produced.** They come
from §K-2's `:1214` replacement — the office message losing its figure under
decision 28. A sandbox of §B-1's `tierLine` line alone yields **8**, not 10; the
office pair only appears once the second caller is resolved. **The 8-vs-10 gap is
the exact size of PR4-B4's unstated spec.**

### The rule this cost, and it is a generalisation of §I-1's own

> 🔴 **A commit is measured by sandboxing THE COMMIT, not by sandboxing the item
> of it that a review happened to be about.** §I-1 measured §B-2 because B5/B6
> were about §B-2. §B-1 sat in the same commit, was never named by a finding, and
> was therefore never run — for **three** rounds, across two validation agents
> and two independent end-to-end runs that all reported green.

**A green sandbox is evidence about what was IN the sandbox.** §F names the
commit's contents; the sandbox must match §F, and the two must be diffed rather
than assumed equal. → **Added to §F's brief as clause 6 (§K-7).**

## §K-2 · 🔴 PR4-B4 — `:1214`'s REPLACEMENT, STATED

§B-1 says *"`assessmentSummary` is DEAD and is deleted with it — decision 28. Its
only other caller was `:1214`."* **It never says what `:1214` becomes.** Deleting
the function without naming the replacement is a compile error, and the obvious
repair does not typecheck: `assessmentTier` is `AssessmentTier | null`
(`:203`) and `tierNameOf` (`:1155-1157`) takes `AssessmentTier`.

**Enumerating the readings before choosing one, because a refutation is a
measurement and so is a spec decision:**

| Reading | Result |
| --- | --- |
| `tierNameOf(input.assessmentTier)` | 🔴 **does not compile** — nullable argument |
| `'Settled'` fallback, mirroring `:1213`'s settled branch | 🔴 **loses `'Not chosen (phone booking — settle with the customer)'`**, the ONLY producer of a string `:2049-2054` pins → an **eleventh** red |
| 🆕 **Inline ternary preserving the prose** | ✅ **compiles, keeps the pin green, removes the figure per decision 28** |

**→ SPECCED. `booking-email.ts:1211-1215` becomes:**

```ts
    row(
      'Assessment',
      input.settled
        ? `${input.assessmentTier ? ASSESSMENT_TIER_NAMES[input.assessmentTier] : 'Settled'} — ${formatCents(input.settled.totalCents)} total incl. GST${input.settled.travelCents > 0 ? ` (incl. ${formatCents(input.settled.travelCents)} travel)` : ''}`
        : input.assessmentTier
          ? ASSESSMENT_TIER_NAMES[input.assessmentTier]
          : 'Not chosen (phone booking — settle with the customer)',
    ),
```

**Verified by execution:** typecheck 0 errors; `:2051`'s *"a NULL tier renders as
'Not chosen' on the office copy, not as a price"* stays **green**; and
`:2055`'s `!/undefined|NaN|\$0\.00/` stays green.

⚠️ **`:1213`'s settled branch is UNCHANGED and still prints
`${formatCents(input.settled.totalCents)} total incl. GST`** (PR4-S7). That is a
**real settled amount**, not a recomputed quote — consistent with §B-5's KEEP
verdicts and with decision 28, which removes the *recomputed* figure. **Decision
28's sentence is unqualified and is hereby read as scoped to the recomputed
quote.** → **Assumption `A-R5-1`** *(renumbered revision 6, T5-9 — a bare `A14` collides with `BK-53.md` R5-11)*. No pin covers `:1213`; recorded, not solved.

🔴 **Orphaned comments.** Deleting `assessmentSummary` orphans five comment
references (`:217`, `:219`, `:454`, `:533`, `:1169`) and two `FEE_TERMS_PAYMENT`
ones (`:1121`, `:1137`). **Delete `:1159-1169`'s function together with the
comment block at `:450-459`** *(the previous review's `:450-458` stops one line
short and leaves an orphan `*/` — §B-1 already records this)*, **and rewrite
`:217-219`, `:533` and `:1121-1137` — 🔴 **AND `:1117-1124`, `assessmentSummary`'s OWN doc comment, which the enumeration missed (O6/T5-14); §B-2 deletes `feeTermsPaymentFor` in the same commit, so BOTH blocks at `:1117-1149` orphan together** — rather than leaving prose that argues for
deleted code** — PR4-S4, which is N1's defect one file over and the same argument
§B-2 makes about `:538-541`.

## §K-3 · 🔴 PR4-B2 — THE SNAPSHOT-ABSENT FALLBACK IS BK-55's, AND IT CERTIFIES THE DEFECT §C REMOVES

`verify-booking-email.ts:794-801`:

```ts
const noSnapshot = planBookingNotifications({ ...INSURANCE, messageType: 'confirmed' }).customer!;
check(
  noSnapshot.html.includes('$399.00') && noSnapshot.html.includes('$418.95'),
  'a row with no snapshot falls back to the quote line rather than saying nothing',
);
```

Its comment at `:790-792` states the old rule outright: *"A pre-BK-32 `confirmed`
row reached through Resend has no amount columns. It falls back to the quote line
rather than to silence."*

🔴 **It appears in no table in this ticket** — `grep` for `noSnapshot`, `798`,
`800`, `snapshot-absent fallback` → **zero hits.** It reads **no `booking-copy.ts`
symbol** (bare string literals), so **seam clause 5 makes it unambiguously
BK-55's**, Class 1. Measured red (row 1 of §K-1's ten).

🔴 **This is the assertion that CERTIFIES the defect §C exists to fix, on exactly
the population §C is about** — the thirteen pre-prepay `confirmed` rows reached
through Resend. **CLAUDE.md's BK-44 trap verbatim:** *a defect old enough gets
encoded in the fixtures, so a green suite is not evidence the defect is absent —
the fixtures may require it.*

**→ SPECCED, and it is a REWRITE, not a deletion** *(deleting is B8's silent
narrowing, in the ticket that names B8 as a rule)*:

```ts
check(
  !/\$[\d,]+\.\d{2}/.test(noSnapshot.html),
  'a row with no snapshot states NO figure — the quote-line fallback priced an assessment nobody is charged for',
);
check(
  noSnapshot.html.includes('On-site assessment'),
  'and still names the tier the customer chose, which is the part that was right',
);
```

⚠️ **The negative pin gets a POSITIVE TWIN in the same breath** — the BK-51 gate
lesson: *every negative pin needs a positive twin.* The first assertion alone
would pass on an empty body.
⚠️ **Its TWO siblings** *(⛔ ~~sibling~~, singular — corrected revision 7, O11)* **— `:802-805`** (`!noSnapshot.html.includes('Total')`) **and `:818-823`** (*"makes NO claim about payment at all"*) **— stay
green and unchanged** — verified.

## §K-4 · 🔴 PR4-B3 — N6 IS FALSE, AND G3's ONLY POSITIVE CONTROL DIES IN G3's OWN COMMIT

**§D's class table currently reads:** *"`:2037-2043` — ⚠️ **N6: it sits INSIDE
`:2013-2045`.** Not a separate arm"*, Class column `—`.

🔴 **N6 is factually wrong, and its wrongness is load-bearing — it is the entire
reason the block got no class.** Verified against the file:

| Claim | Reality |
| --- | --- |
| the three-body loop is `:2013-2045` | 🔴 **It is `:2009-2024`.** Its closing `}` is `:2024` |
| `:2037-2043` sits inside it | 🔴 **No.** `:2029-2046` is a **separate `weekendMould` fixture** |
| some construct spans `:2013-2045` | 🔴 **None does.** The enclosing block is `:2006-2059` |

**Measured red under §B-1** (rows 8–10 of §K-1): `:2037` `$577.50`, `:2038`
`$606.38`, `:2039-2042` `'weekend rate'`.

🔴 **Worse than the red.** These three are the **only** assertions in the suite by
which the **weekend 1.5× multiplier reaches a customer email**. **G3 adds a BAN on
weekend claims in customer arms.** Per the BK-51 gate lesson — *every ban needs a
positive CONTROL matching live copy* — **`:2037-2041` IS G3's control, and §B-1
removes it in the same commit that adds the ban.** That is the gate lesson's
failure mode exactly: a ban certified by a list while its control is deleted
beside it.

**→ SPECCED.** §D's `:2037-2043` row is **rewritten in place** to:

| Arm | Class | Disposition |
| --- | --- | --- |
| `:2037`, `:2038` (the two figures) | **Class 1 SPLIT** | **DELETE** — they assert a recomputed figure decision 28 removes. Registered as expected red in §K-1 |
| `:2039-2042` (`'weekend rate'`) | 🔴 **Class 2 REWRITE** | **INVERT to `!mouldBody.includes('weekend rate')` and keep the `weekendMould` fixture.** It becomes **G3's positive control**: the one fixture that would carry a weekend claim if any did |

⚠️ **The `weekendMould` fixture itself is NOT deleted.** Deleting it removes the
only weekend-reaching input in the file and makes G3's ban vacuous — which is
§J's own definition, applied to the ban this ticket adds.

## §K-5 · 🔴 PR4-B5 — §I-6's `'prior'` GUARD: THE ROUTE IS NAMED

§I-6 requires `'prior'` **AND** `confirmation_sent_at IS NOT NULL` → the pointer;
`'prior'` with NULL → `'none-legacy'`'s line. It explicitly **rejects** a fifth
`termsEra` value. In its own words: *"four `termsEra` values, five renderings."*

**Five renderings from four values means the renderer branches on something
beyond `termsEra`** — which §C-2 forbids (*"Do NOT put a raw `Date | null` on the
type and compare it in the renderer… the comparison happens once"*).

**All three routes enumerated, because a refutation is a measurement:**

| Route | Verdict |
| --- | --- |
| **(a)** a fifth `termsEra` value | 🔴 **Foreclosed** — §I-6 rejects it explicitly |
| **(b)** a second field on `BookingNotificationInput` | ⚠️ **Survives §C-2's letter** (a boolean is not a raw `Date`) but adds nine compiler-forced decisions §A3's enumeration never counts and §C-2's producer table never assigns |
| **(c)** the producer collapses `'prior'` + NULL → `'none-legacy'` | ⚠️ Consistent with A6, but makes `termsEra` mean something other than a terms epoch — **§H-1's own defect**, and §I-6's stated reason for rejecting (a) |

**→ SPECCED: route (b), with the field named and its producers assigned.**

Route (c) is rejected on §H-1's grounds — a value that no longer means what it is
named is the defect §H-1 exists to fix, and re-introducing it one section later
is that defect wearing §I-6's hat. Route (b)'s cost is real and is **paid
explicitly here rather than discovered at implementation**:

```ts
/**
 * Whether a confirmation was ever SENT for this row. `termsEra` says which
 * terms epoch the row belongs to; this says whether there is an earlier
 * message to point at. They are different questions and BK-55 needed both —
 * see §K-5. Resolved at the two producers, never in the renderer (A6).
 */
confirmationAlreadySent: boolean;
```

- **Mechanism verified available:** `db.ts:105` is `confirmation_sent_at: Date | null` ✓; `planForAppointment(row: Appointment, …)` receives the full row ✓.
- **Producers:** `resend.ts:106` and `bk51-gated:booking-payment.ts:1597` — **both**, per §A2. Each sets `confirmationAlreadySent: row.confirmation_sent_at !== null`.
- 🔴 **The nine literals of §A3 become nine + 1.** §A3's enumeration is amended: **each of the nine gains this field too.** *(§A3's list itself is confirmed correct and is not re-derived.)*

🔴 **AND §I-2 IS CORRECTED WHERE IT LIVES.** §I-2 replaced §C-2's closing
paragraph and **restated the producer table "whole so §C-2 can be read alone"** —
and that table says `planForAppointment` computes from *"`row.terms_acked_at` and
`row.source`"*, **with no `confirmation_sent_at`.** §I-2 is at `:1007`, §I-6 at
`:1124`, **same revision.** *The section written to be self-contained is the one
that dropped the guard.* → **§I-2's producer table is rewritten in place** to add
`row.confirmation_sent_at → confirmationAlreadySent`.

## §K-6 · 🔴 PR4-B6 + PR4-B7 — TWO ORDERS ISSUED IN PROSE, NOW PERFORMED

**CLAUDE.md: *an order issued in prose is not performed.*** Both of these are that
trap, and **both occurred inside revision 4 — the revision whose §J recorded the
lesson.** They are executed here, in the text an implementer actually reads.

### PR4-B6 · G4's fifth arm

§I-3 **replaced** §D's G4 row with a four-arm text. §I-6, later in the same
revision, says *"G4 gains a fifth arm"* and **issues no replacement of the row
§I-3 had just written.** An implementer reading §D's pin table builds four arms.

**→ PERFORMED: §D's G4 row is rewritten in place to five arms**, the fifth being
guarded-`'prior'` (`confirmationAlreadySent === true`) rendering the pointer.
**→ And its POSITIVE TWIN, per PR4-S5:** under §K-5, guarded-`'prior'` and
`'none-legacy'` emit the same sentence, and **red-first row 17 is negative-only**
(*"assert the pointer does NOT render"*). **Row 17 gains a twin: assert
`'none-legacy'`'s line IS present on the guarded-`'prior'` fixture.** A
negative-only pin here passes on an empty render.

### PR4-B7 · G0's two code blocks

| Where | Shape | Summary line |
| --- | --- | --- |
| **§D `:503-509`** | two-state, `passes` | `✗ ${passes} passed, ${failures} failed` / `✓ … — ${passes} passed, 0 failed` |
| **§I-4 `:1090-1095`** | three-state, `checksRun` | `✗ ${failures} check(s) failed of ${checksRun} run` / `✓ … — ${checksRun} checks, 0 failed` |

§I-4 says *"G0 lands the counter AND the wrapper"* but **never says §D's block is
replaced**; §I-0's row says "EXTENDED," not replaced. **Both stand.** The ticket's
own rule is *score on (exit code, summary line)* — **two blocks, two formats, one
gate.**

**→ PERFORMED: §D's `:503-509` block is DELETED in place**, replaced by a pointer
to §I-4's three-state block, which is now the only G0 specification.

🔴 **And the registered string is stale against this ticket's own commit order.**
§I-4 registers *"expect `✗ 20 checks failed`"* — the **pre-G0** format (verified:
`verify-booking-email.ts:2063`). **§F step 2 lands G0 as commit (a), BEFORE (b).**
So at the moment of measurement the script emits the three-state format.
**→ The registration is rewritten to the post-G0 format**, and **§K-1's ten are
registered in that format too**:

> **After commit (b), expect `✗ 10 check(s) failed of N run`** — `N` filled at
> implementation from commit (a)'s counter, which cannot be known before it lands
> (§G's "not known" rule, third item, applied to a count instead of a date).

⚠️ **PR4-N6, folded in:** §I-4's block references `didCrash`/`crashed` without
saying where they are set, and `verify-booking-email.ts` has **no top-level
`try`/`catch` and no `main()`** — it is straight-line blocks. **So commit (a) is a
structural change to a 2,066-line script, not the additive three-liner §F calls
it.** Commit (a) remains additive *in assertion terms* (it reddens nothing —
verified: nothing outside the script consumes its summary line), but §F's
description is corrected to say so.

## §K-7 · §F — THE GATE DECISION IS ANSWERED; THE UNION IS UNCHANGED

✅ **The scope question flagged against §F is ANSWERED (BK-53 R11).** The gate
change is a **refinement of BK-53's own machinery** — no surface moves, **no
ticket is split out, BK-57 stays unopened.** Therefore:

- **§F steps 1–5 stand unchanged.** The union is BK-51 + BK-55 + BK-53; one push.
- 🔴 **§I-4's registration table keeps three rows**, the last still *"At the
  branch tip, after BK-53."* **This was the row that would have gone stale** had a
  fourth ticket landed after BK-53 — it does not, so the row is correct as
  written. *Recorded because the next reader will ask.*

**→ §F's brief gains clause 6**, from §K-1's rule:

> **6. Sandbox THE COMMIT, not the finding.** §F names each commit's contents; a
> sandbox is evidence only about what was in it. Diff the sandbox against §F's
> contents before reporting a result, and say which items were in it. *(Three
> rounds and two "end-to-end" runs reported green on a commit whose §B-1 half had
> never been executed.)*

**→ And clause 1c**, from PR4-N8 — *a defect in the reviewer brief itself, which
is what clause 1b already exists for*:

> **1c. `resend.ts` means `src/pages/api/admin/appointments/resend.ts`.** There is
> only one in `src/`, so the bare form is unambiguous *in the repo* — but a
> briefing for this ticket expanded it to **`src/lib/resend.ts`, which does not
> exist**, on the same day 1b was written about the same class of error. **Expand
> every bare filename in the brief.**

## §K-8 · SHOULD-FIX AND NITS — DISPOSITIONED

| ID | Disposition |
| --- | --- |
| **PR4-S1** | ✅ **ACCEPTED.** §D hands `:742-745` to BK-53 on a basis false about the cited lines — the arm is `check(!/\$[\d,]+\.\d{2}/.test(body), …)`, reading no constant. **The verdict stands** (its shape is justified at `:735-741` by `FEE_TERMS_ITEMS`, which BK-53 rewrites) **but the BASIS is rewritten**: assigned by the *surrounding block's* reads, not the arm's. *A later round re-deriving from the stated basis re-derives wrong* |
| **PR4-S2** | ✅ **ACCEPTED.** §B-1's title *"and no figure"* is false at commit (b): `FEE_TERMS_ITEMS` still renders `$399 / $699 / $1,199` (`booking-email.ts:526`/`:606`), and **G1's `!/\$[\d,]+\.\d{2}/` is deliberately blind to them** — no decimals (`:735-741`). **CLAUDE.md's negative-assertion trap by name.** → §B-1 retitled *"names the tier and no COMPUTED figure"*, and the ticket now states **the claim becomes true at BK-53's commit (G9)**, not at its own |
| **PR4-S3** | ✅ **ACCEPTED.** G6 goes red at its own commit unless specified as `lastIndexOf`: §I-1 clause 1 keeps `FEE_TERMS_REFUND` rendering, and `FEE_TERMS_REFUND[4] === CANCEL_LINE` (`booking-copy.ts:509`), so `CANCEL_LINE` still renders **twice**. → **G6 is specced as `lastIndexOf`** |
| **PR4-S4** | ✅ **ACCEPTED — folded into §K-2's orphaned-comment paragraph** |
| **PR4-S5** | ✅ **ACCEPTED — performed in §K-6** (row 17's positive twin) |
| **PR4-S6** | ✅ **ACCEPTED.** Red-first row 11 (*"delete `feeTermsPaymentFor`'s replacement"*) is **not executable at this commit** — clause 2 removes the PAYMENT link, installing no replacement. → **Row 11 is restated as breaking the `ITEMS→REFUND` link**; the replacement is BK-53's per §B-2's own attribution to R7-6b |
| **PR4-S7** | ✅ **ACCEPTED — answered in §K-2 and recorded as `A-R5-1`** *(⛔ ~~A14~~, renumbered T5-9)* |
| **PR4-S8** | ✅ **ACCEPTED — answered in §K-7.** Its factual verifications (the five other `FEE_TERMS_PAYMENT` namers; `verify-cutover.ts:352-356` pinning the string; commit (a) consuming nothing outside the script) are **confirmed and inherited** |
| **PR4-N1** | ✅ `check()` is `:86-91`, not `:86-90`. Corrected where cited |
| **PR4-N2** | ✅ §J-2's citations mix conventions — `:366` is a message line (check opens `:364`), `:290` is blank (check opens `:291`). **All §J-2 citations normalised to the `check(` opening line** |
| **PR4-N3** | ✅ §B-2's `:528` → `:528-530`, matching §I-1 and the review |
| **PR4-N4** | ✅ **ACCEPTED and stated once, per §J's own request.** `:373/:377/:396/:419/:427` are not vacuous by §J's definition — the fact is not gone, its **reachability** is. Disposition unchanged; the distinction is now written down |
| **PR4-N5** | 🔴 **ACCEPTED — and it is the same class as PR4-B1.** §J-2's vacuous set was measured against **§B-2's deletion only**; §B-1 renders `:2043-2046` and `:671-678` vacuous by the same definition. **A-R4-6's "measured in both directions" was applied to one of the commit's two edits.** → §J-2's set is re-measured against **commit (b)**, not against §B-2 |
| **PR4-N6** | ✅ **ACCEPTED — folded into §K-6** |
| **PR4-N7** | ✅ `resend.ts:94` is a two-line block `:94-96`. Immaterial; corrected |
| **PR4-N8** | ✅ **ACCEPTED — performed in §K-7 as §F clause 1c** |
| **PR4-N9** | ✅ §B-4's `:628-630` includes `].join('\n')`; narrowed to `:628-629` |
| **PR4-N10** | ✅ **ACCEPTED — the status header is rewritten, and `HANDOFF-2026-09-05.md:432` is corrected in the same commit** |

## §K-9 · ASSUMPTIONS AMENDED

| # | Assumption | Basis |
| --- | --- | --- |
| **A-R5-1** *(⛔ ~~A14~~ — renumbered revision 6, T5-9: a bare `A14` already denotes R5-11's adversarial finding in `BK-53.md`, and §I-5 established the prefix IN WRITING against this exact number)* | 🆕 **Decision 28 removes the RECOMPUTED figure, not a settled one.** `:1213`'s `input.settled` branch keeps `formatCents(input.settled.totalCents)` | A real settled amount is a real fact; consistent with §B-5's KEEP verdicts. **No pin covers `:1213`** — recorded, not solved (PR4-S7) |
| **A-R5-2** *(⛔ ~~A15~~)* | 🔴 **WITHDRAWN revision 6 (§L-2) — the column cannot answer the question, and the two named producers construct no literal.** ~~`confirmationAlreadySent` is a second boolean field, resolved at both producers~~ | §K-5 route (b). Routes (a) and (c) are foreclosed by §I-6 and §H-1 respectively |
| **A-R5-3** *(⛔ ~~A16~~)* | ⇄ **REWRITTEN revision 6 (§L-3): `:2039-2042` takes G3's CLAIM shape, case-insensitive, PLUS a genuine positive control.** ~~inverts rather than deletes, and `weekendMould` survives as G3's positive control~~ — *a negative assertion over `?? ''` passes on an empty string* | Deleting the only weekend-reaching fixture makes G3's ban vacuous by §J's own definition |
| **A6** | ✅ **UNCHANGED and now load-bearing for A15** — `termsEra` and `confirmationAlreadySent` are both resolved at the producers, never in the renderer |

---

# 🟧 REVISION 4 — 2026-09-06. **Amends §B-2, §B-5, §C-2, §D and §G — plus §I-6 and §J, added after two validation rounds.**

⚠️ **"Everything else stands" is NOT written here deliberately.** Revision 3 used
that phrase and it is how §C-2's stale paragraph and §B-5's exempt-list citation
survived (§I-2, §I-6 T6). **The sections amended are named; anything not named is
unexamined, not endorsed.**

Written against the traceability audit and adversarial pre-read transcribed in
**`BK-53.md` R7-A and R7-B**, and against **`BK-53.md` R8-6**, which executed all
three of the unowned-assertion findings in a sandbox.

🔴 **The headline is that this ticket's commit (b) could not pass its own gate,
and the reason is a seam rule, not a fixture.**

## §I-0 · ABSORPTION — revision 3's sections against revision 4

*(BK-53's standing rule, applied here: enumerate the OLD section's rows against
the new one and state which verdicts changed. Revision 3 amended §C, §D and §G
and never checked what in them it had left behind — §I-2 is what that cost.)*

| Revision 3 section | Now |
| --- | --- |
| **§H-1** four-valued `termsEra`, split on `source` | ✅ **STANDS** — R7-B confirmed it *mechanically sound* and verified `planForPayload` has exactly one caller. ⇄ **§D's G4 row and red-first rows 6–8 still describe THREE values and are corrected — §I-3** |
| **§H-2** A7 reversed to `'current'` | ✅ **STANDS**, and is **necessary but not sufficient**: R7-B sandboxed it and **the 20 reds remain** — §I-1 |
| **§H-3** the third producer; the cutoff | ✅ **STANDS.** ⇄ the cutoff assertion had no red-first row — §I-3 |
| **§H-4** G11, the post-flip hand-charged row | ✅ **STANDS.** ⇄ no red-first row — §I-3 |
| **§H-5** gate and nit corrections | ✅ **STANDS.** ⇄ G10 had no red-first row — §I-3 |
| **§C-2** producers and the type | 🔴 **ITS CLOSING PARAGRAPH STILL ARGUES FOR THE VALUE §H-2 REVERSED — §I-2** |
| **§D · G0** the assertion counter | ⇄ **EXTENDED — §I-4.** It gains a crash state, and the registration becomes per-block |
| **§H-6** assumptions A7, A8, A9, A13 | 🔴 **ADDED 2026-09-06 — THIS TABLE OMITTED IT (§I-6 T4), which is the exact failure the table exists to catch, in the pass added to prevent it.** ✅ **A7** reversed → `'current'`, carried by the §H-2 row · ✅ **A8** four-valued `termsEra` split on `source` — **STANDS**, R7-B confirmed it mechanically sound · ✅ **A9** `create.ts:216` is `'current'` — **STANDS** · ✅ **A13** the cached-page straddle, accepted and unpinnable — **STANDS**, and it is BK-54's structural fix, not this ticket's |

## §I-1 · 🔴 B5/B6 — COMMIT (b) CANNOT PASS ITS OWN GATE, AND A FIXTURE IS NOT THE CAUSE

**Sandboxed twice, independently. §B-2's specced deletion ALONE — drop the `:44`
import, `feeTermsPaymentFor` (`:1150-1152`) and both call sites (`:528-530` HTML,
`:607` text) — produces:**

```
EXIT=1
✗ 20 checks failed
```

**Enumerated, because a count is what R7-A 🔴2 exists to forbid:**

| Source | Count | What |
| --- | --- | --- |
| `:565-570` `carries`, over the list at **`:525-546`** | **12** | the four `FEE_TERMS_PAYMENT` members × 2 parts on `request`, 2 × 2 on `confirmed` |
| `:597-602` **the render-order chain at `:578-603`** | **8** | exactly 2 per iteration × 4 iterations: `PAYMENT`'s `at()` is `-1`, failing the `>` at its own link and the `>= 0` guard at the next |

⚠️ **§H-2 diagnosed this failure mode and blamed the fixture. Reversing A7 does
not touch these 20** — they come from the render deletion, not from
`INSURANCE.termsEra`. **Both are real; only one was the cause.**
⚠️ **N10 is right and now quantified: the anticipated red count was understated
by 8 of 20 — 40% — and the missing 40% is an entire block (`:578-603`) that
appeared in NO class table in either ticket.**

🔴 **And `:525-546` and `:578-603` are BOTH assigned to BK-53** — the first by
§E-2 clause 5, the second by `BK-53.md` R8-6a on the same reasoning. **So this
ticket's commit is red in arms it is forbidden to touch.**

### → SEAM CLAUSE 6, adopted verbatim from `BK-53.md` R8-6d

> **Clause 5 assigns the arm's DESIGN. It does not license a red commit. Where a
> ticket's own change reddens an arm clause 5 gives the other ticket, the
> reddening ticket makes the MINIMUM edit that restores green — deleting the rows
> whose subject its own commit removed, and nothing else — and the owning ticket
> re-derives the arm in its commit. An arm edited twice across two commits is
> correct; a red commit is not.**

**BK-55's minimum edit, stated exactly so the implementer does not improvise:**

1. **`:525-546`** — remove the four `FEE_TERMS_PAYMENT` members from `carries`.
   **Nothing else.** `FEE_TERMS_HEADING`, `FEE_TERMS_ITEMS`, `FEE_TERMS_REFUND`
   and `FEE_TERMS_CREDIT` **stay** — 🔴 **§B-2 deletes only the PAYMENT render**,
   so the refund and credit lines are still rendered at this commit and their
   rows are still true.
2. **`:587-596`** — remove `['PAYMENT', at(FEE_TERMS_PAYMENT[0])]` from `chain`.
   **`REFUND`'s link then re-anchors to `ITEMS`**, which is the correct claim at
   this commit. `INTRO` stays (it is `request`-only and still rendered).
3. 🔴 **`:554-563` `mustNotCarry` — the four confirmed-arm assertions over
   `FEE_TERMS_PAYMENT[2]`/`[3]` go VACUOUS at this commit, NOT "unaffected"**
   (§J; ⚠️ *the first draft said "verified unaffected", and §J replaced it in
   PROSE ONLY — R9-10 U4*). They pass because **nothing payment-related renders
   at all**, not because the arm drops the right two of four. **2 lines × 2 parts
   = 4 checks passing for the wrong reason.** → **Leave them in place, RECORD
   them as vacuous in the commit message, and name `BK-53.md` R8-6a as the owner
   that re-derives them.** **Do not delete them here** — deleting is B8's
   silent-narrowing defect and this ticket does not own the rewrite.

⚠️ **BK-53 then re-derives both arms against `ASSESSMENT_TERMS` in its own
commit** (`BK-53.md` R8-6a). **Two edits to one arm across two commits is the
intended outcome, not drift.**

### ⚠️ The failure class at BK-53's commit is DIFFERENT, and must not be scored the same way

| Commit | Result | Scorable? |
| --- | --- | --- |
| **this ticket's (b)** | exit 1, **`✗ 20 checks failed`** — every other assertion still ran | ✅ **a measurement** |
| **BK-53's** | `astro check`: **TS2459/TS2724 at `:30,:33,:35,:37,:38`.** Under `tsx`: `SyntaxError: … does not provide an export named 'FEE_TERMS_CREDIT'` **before one assertion runs — 0 `✗`, no summary line, all 417 `check()` calls skipped** | 🔴 **NO — CLAUDE.md's BK-33 trap** |

**→ Recorded in §F's brief:** *at BK-53's commit this script does not compile, so
a green-looking `grep -c "✗"` on it means the file never ran.*

## §I-2 · 🔴 🟠21 — §C-2's CLOSING PARAGRAPH STILL ARGUES FOR THE VALUE §H-2 REVERSED

**§C-2's last sentence reads, today:** *"…so **`'prior'` is the honest default**
and it makes the existing arms exercise the interesting branch."*
**§H-2 reversed exactly that, for exactly that reason** — `'prior'` reddens ~22
inherited assertions in a block this ticket does not own.

🔴 **The audit called this *"partly fixed"*. It is not partly anything: the type
and the producers were corrected and the ARGUMENT FOR THE OLD VALUE was left
standing three lines below them**, so a reader who stops at §C-2 implements the
reversed value with a rationale attached. *This is the revision-3 shape of the
same defect §I-0 exists to catch, and it is why the absorption pass is now
mandatory here too.*

**→ §C-2's closing paragraph is REPLACED in place:**

> ⚠️ **The nine sites will be named by `astro check`; the ~20 `{ ...INSURANCE }`
> spreads will NOT.** They inherit whatever `INSURANCE` declares. **Set
> `INSURANCE`'s value deliberately and say why in its comment** — it is
> `messageType: 'confirmed'` with `settled: null`, which models the
> **snapshot-absent** confirmed row. 🔴 **Its value is `'current'` (A7, reversed
> by §H-2): the value that leaves every inherited arm asserting what it asserts
> today.** The interesting branches get their own named fixtures — `LEGACY_WEB`,
> `PHONE_ENTRY`, `PRIOR_ERA` — which is what `INSURANCE`'s own comment at
> `:130-136` says the settled cases did. **A fixture default chosen to make a new
> branch interesting is the BK-44 trap running forward.**

**And the three producers, re-stated whole so §C-2 can be read alone:**

| Producer | Value | Note |
| --- | --- | --- |
| `planForAppointment` (`booking-admin-notify.ts:164-198`) | computed from `row.terms_acked_at` **and `row.source`** — ⛔ ~~AND `row.confirmation_sent_at → confirmationAlreadySent`~~ 🔴 **REVERTED revision 6 (§L-2): the field is withdrawn. This table was RIGHT as revision 4 wrote it, and §K-5 contradicted it** | the resend path **and** the payment path (`booking-payment.ts:1597` **on `bk51-gated`**; `:1567` is `main`) |
| `planForPayload` (`:108-152`) | **`'none-phone'`** | an office entry has accepted nothing; stated beside its existing `settled: null`. **Exactly one caller (verified)** |
| `create.ts:216` | **`'current'`** | the public write path, at insert (§H-3) |

## §I-3 · 🔴 🟠22 — REVISION 3 ADDED FOUR PINS AND NO FAILURE MODES

**A pin with no red-first row is a pin nobody has seen fail.** Revision 3's four
new pins had none. **And §D's G4 row and red-first rows 6–8 still describe a
THREE-valued `termsEra`**, which §H-1 replaced — *the rule-rewrite trap inside
one document, again.*

**→ §D's G4 row is REPLACED:**

> **G4** · The **four** `termsEra` branches, each asserted separately, each
> asserting the other three renderings are ABSENT: `'none-phone'` renders *"This
> booking was taken over the phone…"*; `'none-legacy'` renders *"Our current
> terms are on the website."* **and makes no claim about how it was booked**;
> `'prior'` renders the pointer and NOT the terms block; `'current'` renders the
> terms block and neither sentence. *Two branches pass by suppressing everything,
> which is why each arm carries its own negative.*

**→ Red-first rows, replacing 6–8 and adding four:**

| # | Break the PRODUCTION target | Expect | red-observed |
| --- | --- | --- | --- |
| 6 | Render the pointer for a `'current'` row | **G4** red | ☐ |
| 7 | Render the terms block for a `'prior'` row | **G4** red | ☐ |
| 8 | Write the gate as `terms_acked_at < CUTOFF` with **no NULL branch** | 🔴 **G4's two `'none-*'` arms red.** This one **typechecks** | ☐ |
| **8b** | 🆕 Collapse `'none-phone'` and `'none-legacy'` back to one branch — i.e. ignore `row.source` | 🔴 **G4 red on a `source: 'web'`, NULL-`terms_acked_at` fixture.** *This is §H-1's whole finding: without it, **thirteen real web customers** are told their booking was taken over the phone* | ☐ |
| **13** | 🆕 Set `TERMS_ERA_CUTOFF` more than 24h from the deploy timestamp | **§H-3's cutoff assertion red** — *a guessed literal nothing binds to reality* | ☐ |
| **14** | 🆕 Give a `'current'` row `settled.totalCents > 0` and render the free-mode block | **G11 red** (§H-4) | ☐ |
| **15** | 🆕 Set a **request** fixture to `termsEra: 'prior'` | **G10 red** (§H-5) — *unreachable in production, asserted rather than left true by accident* | ☐ |
| **16** | 🆕 Delete the `You chose:` twin from the **office** arm only | **G2's twin red** — ⚠️ **the loop contributes `plan.internal.html` ONLY; `plan.internal.text` is not in it, and the twin must not assume it is** | ☐ |

⚠️ **Row 12's wording is corrected by §I-4:** with a crash state, a part-way
throw becomes **exit 2 with `✗ CRASHED after N checks`** — *legible*, rather than
a run that must be discarded.

## §I-4 · §D · G0 — THE COUNTER GAINS A CRASH STATE, AND THE REGISTRATION GOES PER BLOCK

🔴 **`verify-booking-email.ts` has no `try`/`catch` and no crash state.** Executed
(`BK-53.md` R8-6a): a missing export exits **1 with zero `✗` and no summary
line** — the BK-33 trap, in the script this ticket's whole gate rests on.
**`verify-booking-admin-db.ts:237-259` and `:5047-5068` already have the
machinery** — `checksRun`, a `try`/`catch`/`finally`, and **three** terminal
states.

**→ G0 lands the counter AND the wrapper, copied from that file:**

```ts
if (didCrash) {
  console.error(crashed);
  console.error(`\n✗ CRASHED after ${checksRun} check${checksRun === 1 ? '' : 's'} (${failures} had already failed)\n`);
  process.exit(2);
}
if (failures > 0) { console.error(`\n✗ ${failures} check${failures === 1 ? '' : 's'} failed of ${checksRun} run\n`); process.exit(1); }
console.log(`\n✓ booking notification checks passed — ${checksRun} checks, 0 failed\n`);
```
⚠️ **exit 2 is CRASHED, exit 1 is FAILED, exit 0 is PASSED.** A red-first row
scored on *"exit non-zero"* alone cannot tell them apart, and they mean opposite
things about whether the run is evidence.

**→ And the pre-registration is PER NAMED BLOCK**, matching `BK-53.md` R8-8a:
a script-wide count — scalar or range — **cannot see a net-zero swap** (an arm
deleted and a `check(true, …)` added). Register **G1…G11 individually**, plus the
script total as a range with each bound's cause.

| | Assertions run | Summary line |
| --- | --- | --- |
| Baseline, today | ☐ record from the G0 run | `✓ booking notification checks passed` *(no number — that is the defect)* |
| After commit (b) | ☐ pre-register **before** editing | ⛔ ~~expect `✗ 20 checks failed` at the specced deletion, and ZERO after §I-1's minimum edit~~ 🔴 **REWRITTEN §K-1/§K-6: `✗ 20` is §B-2 ALONE and is in the PRE-G0 format. Commit (a) lands G0 first, so the real registration is `✗ 10 check(s) failed of N run` after the full commit (b), `N` filled at implementation.** |
| At the branch tip, after BK-53 | ☐ re-register | |

⚠️ **Measured reference points, for the tip registration:** `:565-570` `carries`
runs **54**, `:571-576` `mustNotCarry` **14**, `:597-602` the chain **18** — the
region `:565-602` is **86 assertions**, not the *"2×2"* an earlier reading
implied. Call sites remain **229**; the runtime number is larger.

## §I-5 · ASSUMPTIONS AMENDED

| # | Assumption | Basis |
| --- | --- | --- |
| **A-R4-1** *(prefixed — a bare `A14` already denotes R5-11's adversarial finding in `BK-53.md` and is cited by that ID inside revision 8; R8-10 T18)* | 🆕 Seam **clause 6** subordinates clause 5 wherever clause 5 would produce a red commit | A red commit cannot reach implementation review. Sandboxed: clause 5 alone leaves commit (b) at `✗ 20 checks failed` |
| **A-R4-2** | 🆕 `verify-booking-email.ts` gets a **three-state** exit (0/1/2), not two | A crash and a failure are opposite statements about whether the run is evidence, and today they share exit 1 |
| **A-R4-3** | 🆕 Pre-registration is **per named pin**, not per script | A script-wide count cannot see an arm deleted and a `check(true)` added |

## §I-6 · 🔴 SF6 AND N9 — the two findings dispositioned to a section that did not exist

⚠️ 🔴 **`BK-53.md` R8-10 T3 and R8-10e N9 both routed a finding to "BK-55 §I-6".
No such section was ever written** (R9-10 U3), so both were recorded as
dispositioned and were in fact still open. **This is the section.** *A
disposition that names a destination nobody creates is indistinguishable from a
dropped finding — the failure a traceability pass exists to catch, committed by
the pass that catches it.*

### SF6 · 🔴 `'prior'` POINTS AT AN EMAIL THAT MAY NEVER HAVE BEEN SENT

**R7-B SF6, undispositioned for two revisions.** The `'prior'` arm renders a
pointer — *"The terms you accepted are in your original confirmation email."*
✅ **Verified: `db.ts:105` is `confirmation_sent_at: Date | null`**, and NULL is
a real state — a row can reach `confirmed` without a confirmation email ever
going out. **G4 pins the sentence, so the gate would assert a falsehood.**

**→ The `'prior'` arm is GUARDED, not split into a fifth value:**

> **`termsEra === 'prior'` AND `confirmation_sent_at IS NOT NULL`** → the
> pointer. **`'prior'` with a NULL `confirmation_sent_at`** → render
> **`'none-legacy'`'s line**, *"Our current terms are on the website."*, which
> makes **no claim about what was accepted and none about what was sent**, and is
> true for this population by construction.

⚠️ **A fifth `termsEra` value was considered and REJECTED:** the discriminator is
not the terms era at all, it is whether an artifact exists. **Adding a value
would put an email-delivery fact into a field named for a terms epoch** — which
is §H-1's own defect, a branch named after one of the two populations it covers.
**→ G4 gains a fifth ARM** *(four `termsEra` values, five renderings)* **and
red-first row 17: set a `'prior'` fixture's `confirmation_sent_at` to NULL and
assert the pointer does NOT render.**

🔴 **PERFORMED, revision 5 (§K-6):** this order was issued in prose and §D's G4
row still said four — *§J's own finding, recurring inside the revision that
recorded it.* **§D's G4 row now specifies five arms**, and **§K-5 names the route
the fifth arm is implemented by** (`confirmationAlreadySent`, a second field —
routes (a) and (c) are foreclosed by this very paragraph and by §H-1).

🔴 **AND ROW 17 GAINS ITS POSITIVE TWIN (PR4-S5).** Under the guard, a
`'prior'` row with NULL `confirmation_sent_at` and a `'none-legacy'` row **emit
the same sentence** — so a negative-only pin passes on an empty render.
**→ Row 17b: assert `'none-legacy'`'s line IS present on that same fixture.**
*Every negative pin needs a positive twin — the BK-51 gate lesson, and this
ticket adds a negative pin without one in the section that cites it.*

### N9 · The `source` split rests on a schema DEFAULT, not on an assertion

**R7-B N9.** §H-1 splits NULL `terms_acked_at` on `db.ts:94`'s
`source: 'web' | 'admin'`. **That column's correctness for the thirteen
historical rows rests on its DEFAULT having been right at insert, and nothing
asserts it.**
**→ A one-time gate assertion: every `status = 'confirmed'` row with NULL
`terms_acked_at` has a NON-NULL `source`.** A NULL `source` silently
misclassifies — and `'none-phone'` is the arm that tells a web customer their
booking was taken over the phone. **Cheap, and it is the only thing between
§H-1's fix and the defect it replaced.**

### §I-6 assumption

| # | Assumption | Basis |
| --- | --- | --- |
| **A-R4-5** | 🆕 `'prior'` is guarded on `confirmation_sent_at`, not promoted to a fifth `termsEra` value | The discriminator is artifact existence, not terms epoch; conflating the two is §H-1's own defect |

## §J · 🔴 S6 — CLAUSE 3 CALLED FOUR VACUOUS ASSERTIONS "UNAFFECTED"

*(Adversarial pre-read of revision 4, transcribed in `BK-53.md` R8-A / R9-6 S6.)*

§I-1 clause 3 says *"Nothing in `:554-563` `mustNotCarry` — verified
unaffected."* **"Unaffected" and "vacuous" are different verdicts, and clause 3
gave the wrong one.**

After the minimum edit, the confirmed arm's `mustNotCarry` still asserts
`FEE_TERMS_PAYMENT[2]` and `[3]` are **absent** — and they are, **because
nothing payment-related renders at all**, not because the arm drops the right
two of four. **2 lines × 2 parts = 4 checks passing for the wrong reason**, and
still in the file when BK-53 arrives.

**→ Clause 3 is REPLACED:**

> 3. **`:554-563` `mustNotCarry` — the four confirmed-arm assertions over
>    `FEE_TERMS_PAYMENT[2]`/`[3]` go VACUOUS at this commit, not unaffected.**
>    **Leave them in place and RECORD them as vacuous in the commit message**,
>    with `BK-53.md` R8-6a as the owner that re-derives them against
>    `ASSESSMENT_TERMS`. **Do not delete them here** — a vacuous assertion that
>    is recorded is a handover; a deleted one is BK-53's silent-narrowing defect
>    (B8), and this ticket does not own the rewrite.

⚠️ **The distinction is worth stating once, because it recurs:** an assertion is
**unaffected** when the fact it tests is unchanged; **vacuous** when the fact is
gone and the assertion still passes. **Only the second is a hole**, and only the
second needs an owner.

### §J-2 · 🔴 THE VACUOUS SET IS **ELEVEN** — ⛔ ~~NINE, NOT FOUR~~

🔴 **RE-MEASURED revision 7 (O10/T5-10/PR4-N5): the NINE was measured against
§B-2's deletion ALONE.** §B-1 is in the same commit and renders **`:2043-2046`**
and **`:671-678`** vacuous by the same definition — **neither body can carry a
recomputed figure at all once `assessmentSummary` is deleted.** *A-R4-6's
"measured in both directions" had been applied to one of the commit's two
edits — the same defect as §K-1's sandbox, one section over.*

**The original heading and body follow, as measured against §B-2 alone:**

**Adversarial pre-read of revision 4, round 3 (`BK-53.md` R10-5 S-6). Sandboxed:
§B-2's deletion (asserted landed — `FEE_TERMS_PAYMENT` refs in
`booking-email.ts` **7 → 2**, both comments) → exit 1, `✗ 20 checks failed`;
§I-1's edit as amended by §J → ✅ exit 0. §I-1 reproduces exactly.**

🔴 **But after the edit `FEE_TERMS_PAYMENT` renders in NO email at all, and five
more assertions still pass over it** — in `verify-booking-**email**.ts`:

| Line | What it asserts | After §I-1 |
| --- | --- | --- |
| `:373` | *"names the payment link"* | 🔴 vacuous |
| `:377` | *"nothing is charged when the request is sent"* | 🔴 vacuous |
| `:396` | *"the listed figures are STANDARD rates"* | 🔴 vacuous |
| `:419` | *"states the weekend multiplier"* | 🔴 vacuous |
| `:427` | *"names Saturday and Sunday"* | 🔴 vacuous |

**§J's own definition — *"vacuous when the fact is gone and the assertion still
passes"* — fits all five. §J named four. The set is NINE.**

✅ **And it was measured in the other direction too, which is what makes the
enumeration trustworthy: `:290`, `:366` and `:384` are `terms`-scoped and are
NOT vacuous** — each still passes over the post-§B-2 render. *An enumeration
measured only in the direction that grows it is a guess.*

**→ All nine are recorded as vacuous in commit (b)'s message, with `BK-53.md`
R8-6a as the owner. None is deleted here** (§J's rule, unchanged).
⚠️ **`:373`, `:377`, `:396`, `:419` and `:427` are already in R6-3e's Class 1
list as BK-53's** — so the owner exists and this is a HANDOVER, not a new
assignment. **What was missing was the statement that they go quiet in between.**


### §J-1 · Assumption

| # | Assumption | Basis |
| --- | --- | --- |
| **A-R4-4** | 🆕 A commit that renders an inherited assertion **vacuous** records it and names its owner; it does not delete it | Deleting is B8's silent narrowing; leaving it silent is how a green suite stops being evidence |
| **A-R4-6** | 🆕 A vacuous-set enumeration is **measured in both directions** — which arms go quiet AND which provably do not | §J's first version named four of nine, and the three it would have wrongly added were caught the same way |

---

# 🟥 REVISION 3 — 2026-09-06. **Amends §C, §D and §G. Everything else stands.**

Written against the adversarial pre-read and traceability audit of revision 2.

## §H-1 · 🔴 `termsEra: 'none'` tells THIRTEEN WEB CUSTOMERS their booking was taken over the phone

**§C-1 routes NULL `terms_acked_at` to *"This booking was taken over the phone.
Our current terms are on the website."* NULL covers TWO populations, not one.**

`migrations/005-assessment-terms.ts:16-20` and `db.ts:99-102` both say it:

> *"NULL is correct and permanent for two populations: **every row that predates
> this migration**, and every admin entry from here on."*

🔴 **The pre-005 rows are WEB bookings** — `008-review-lifecycle.ts:162` calls
them *"13 historical appointments"* confirmed *"under a flow that took no money
online"* — they sit at `status = 'confirmed'`, and **`resend.ts:94-96` admits exactly
that status.** They are live for the button §C exists for.

**And G4 PINS the falsehood**: its `'none'` arm asserts that sentence renders.

**→ Split the branch on a column that is already on the row and already passed to
the builder.** ✅ **Verified: `db.ts:94` is `source: 'web' | 'admin'`**, and
`planForAppointment(row: Appointment, …)` receives the full row and does not read
it. *(Do not confuse it with the second `source` at `db.ts:311`, a different
column with different values.)*

| `terms_acked_at` | `source` | Renders |
| --- | --- | --- |
| NULL | `'admin'` | *"This booking was taken over the phone. Our current terms are on the website."* |
| NULL | `'web'` | 🆕 *"Our current terms are on the website."* — **no claim about how it was booked, and none about what was accepted** |
| < `TERMS_ERA_CUTOFF` | any | the pointer to the original confirmation email |
| ≥ `TERMS_ERA_CUTOFF` | any | the terms block, rendered normally |

**→ `termsEra` becomes four-valued: `'none-phone' \| 'none-legacy' \| 'prior' \|
'current'`, and G4 pins all four.**
*(CLAUDE.md's rule-rewrite trap: two populations were enumerated into one branch
and the branch was then named after only one of them.)*

## §H-2 · 🔴 `INSURANCE.termsEra = 'prior'` REDDENS ~22 ASSERTIONS IN A BLOCK THIS TICKET DOES NOT OWN

§G-A7 chose `'prior'` so *"~20 inherited call sites exercise the interesting
branch"*. **`INSURANCE` is `messageType: 'confirmed'`, and
`verify-booking-email.ts:525-546`'s `carries` asserts that message renders
`FEE_TERMS_HEADING`, `...FEE_TERMS_ITEMS`, `FEE_TERMS_PAYMENT[0]`, `[1]`,
`...FEE_TERMS_REFUND`, `FEE_TERMS_CREDIT` — ~11 strings × 2 arms.** Under
`'prior'`, `customerConfirmation` renders the pointer **instead of** the block.
**All of them go red at this ticket's commit (b) — and `:525-546` is BK-53's**
(§E-2, and BK-53 R7-6a now takes it). **So commit (b) cannot pass
`npm run verify:booking:email`, which the gate requires before implementation
review, and the seam forbids this ticket from fixing it.**

⚠️ **A7's rationale was the BK-44 trap running FORWARD:** a fixture default chosen
to make the *new* rule's branch interesting, at the cost of 22 assertions about
*old* behaviour nobody re-derived. CLAUDE.md: *"when a fix reddens existing arms,
establish whether each arm was relying on the old behaviour **before** touching
the rule."*

**→ A7 REVERSED: `INSURANCE.termsEra = 'current'`.** It is the value that leaves
every inherited arm asserting what it asserts today, which is the honest default
for a fixture ~20 call sites spread from. **The interesting branches get their own
named fixtures** — `LEGACY_WEB`, `PHONE_ENTRY`, `PRIOR_ERA` — which is what
`INSURANCE`'s own comment at `:130-135` says the settled cases did.
⚠️ **And `'prior'` would have spread to the REQUEST fixtures** (`:139`, `:149`,
`:1156`), so a brand-new request email would have said *"The terms you accepted
are in your original confirmation email."* **Nothing forbids `'prior'` on a
request — G10 now does.**

## §H-3 · A THIRD producer, and the cutoff nothing binds

- 🔴 **`src/pages/api/booking/create.ts:216` builds a `BookingNotificationInput`
  inline** — the **public write path**, a real customer's first email. §C-2 said
  *"resolved once, by the two producers"* and named only `planForAppointment` and
  `planForPayload`. §A3 listed `create.ts:216` among the nine sites `astro check`
  names, but gave it no verdict. **→ `'current'`. Stated.**
- ⚠️ **`TERMS_ERA_CUTOFF` is a guessed literal nothing binds to reality.** If the
  push slips a day past it, every booking in the gap classifies `'current'` while
  having accepted paid-era terms — **the exact misclassification §C-1 rewrote
  decision 29 to prevent, shipping silently**, and every G4 fixture uses explicit
  dates so no assertion can see it. **→ Gate the push on re-reading the constant,
  and assert it is within 24h of the deploy.**
- ⚠️ **A cached `/book/` page straddling the cutoff** stamps `terms_acked_at ≥
  CUTOFF` for a customer who read pre-flip terms. **Unpinnable; recorded as A13.**

## §H-4 · 🔴 The post-flip hand-charged row is ticked ✅ and still has the defect

§C-1's table marks *"Post-flip hand-charged booking → CURRENT terms ✅ classified
right."* **Classification is right; the harm is not addressed.**
`ASSESSMENT_TERMS` carries **no refund or cancellation-money policy at all**
(decision 19 moved those to `APPROVED_*`), and decision 19's own withdrawal
established that **`resend.ts` never sends `APPROVED_*`.** So Resend on a
post-flip hand-charged `confirmed` row still produces *"the visit is free… a
travel fee may apply"* **beside a real charge with zero refund policy** — §C's
opening sentence, verbatim, for a population §C ticks as correct.

⚠️ **And decision 19's own argument says this population GROWS**: the travel fee
makes hand-charging *expected* rather than rare.

**→ G11: a `'current'` row WITH `settled.totalCents > 0` renders the charged-path
terms, not the free-mode block.** This is the one case where `termsEra` and
`settled` must both be read. **It is also the strongest argument yet for BK-54**
(the terms snapshot). ⚠️ **This previously said *"and it is recorded there"* —
it was not.** It is now, under BK-54's *"Why this is the structural fix"*.

## §H-5 · Gate and nit corrections

- **G1's twin pins `You chose: On-site assessment`, not the bare tier name.**
  The review said the bare name *"is one rewrite away from being satisfied by the
  menu"*; `verify-booking-email.ts:683` already pins the stronger shape.
  ⚠️ **The ID `N8` was re-used in §D for an unrelated `&&`-splitting point, so the
  drop read as a resolution.** The `&&` point stands on its own and is renamed
  **§D-N8b**.
- **G2 names its fixture** — `INSURANCE` with `settled: null`, since `:1214`
  fires only when the snapshot is absent (N10).
- **G8 gets red-first row 13**, or is recorded as unpinnable-by-artifact — the
  review asked for one and revision 2 supplied neither (S5's second half).
  ⚠️ **And the reason to restate `booking-payment.ts:288` is now named:** its
  description carries *"beyond **the free radius**"* — a free claim about a
  mechanism decision 17 retires. Server-side, never in `dist/client`, never in
  `TERMS_TEXT`: **restoring it trips nothing.**
- **§B-2's block order still lists `[paymentClaim]`** after §B-2 itself
  establishes `feeTermsPaymentFor` is deleted. `paymentClaim` (`:441-448`) is a
  *different* thing — `NO_CHARGE_LINE`/`PAID_IN_FULL_LINE` — and it **stays**.
  Corrected wording, not a scope change.
- **§B-5's `NO_CHARGE_LINE` row** cites BK-53's exempt list as *"what stops the
  two rules contradicting"*. **BK-53 R6-3a withdrew that list.** The outcome is
  unchanged — it is simply not in `TERMS_TEXT` — but two same-day revisions
  disagreed about the mechanism. **Corrected.**
- **`'prior'` is unreachable in production for a `request`** (`create.ts` sets it
  at insert). **G10 asserts it**, rather than leaving it true-by-accident.

## §H-6 · Assumptions amended

| # | Assumption | Basis |
| --- | --- | --- |
| A7 | ⇄ **REVERSED — `INSURANCE.termsEra = 'current'`** | `'prior'` reddens ~22 arms in a block this ticket does not own, and would have spread to the request fixtures |
| A8 | `termsEra` is four-valued, split on `source` | Two populations share a NULL; `db.ts:94` already carries the discriminator |
| A9 | `create.ts:216` is `'current'` | It is the public write path, at insert time |
| A13 | 🆕 A cached `/book/` page straddling the cutoff is **accepted and unpinnable** | Recorded rather than solved; BK-54's snapshot is the structural fix |
