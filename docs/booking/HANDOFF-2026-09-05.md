# HANDOFF — session of 2026-09-05. Read this first, then stop and check two things.

**Written at session close. Everything below was verified in code or git at the
time of writing; the two commands in §0 tell you what has changed since.**

## 0 · Before you trust any line in this file

```sh
git log --oneline -1                 # expect a32323c or later
git log --oneline main..bk51-gated   # expect 10 lines: 8 commits + 2 docs syncs
git ls-remote --heads origin         # MUST list exactly 3: main,
                                     # booking/p1-foundation, deploy-2-prepay.
                                     # Fewer, or an error, means the check did
                                     # NOT run — stop. `bk51` must not appear.
```

⚠️ **The third command is deliberately not `| grep bk51`.** A grep that finds
nothing looks identical to a grep that never ran — network down, auth expired,
remote renamed — and reads as *"safe, not pushed."* That is CLAUDE.md's BK-33
trap: **never measure by a marker count being zero.** Count the refs.

⚠️ **`bk51-gated` exists on this machine and nowhere else.** No remote copy. It
carries BK-51's production code — 6 files, +940/−102. If this checkout is lost,
that work is lost.

🛑 **DO NOT PUSH IT ANYWHERE, INCLUDING AS A BACKUP.** ROADMAP's human-blocked
index item 5 gates that on the user confirming **Vercel Preview environment
isolation** — a preview build of this branch pointed at the production Neon
database would allow a **real free approval**: email sent, ICS issued, `paid_at`
stamped, none of the gates. **To protect the work:**
`git bundle create ../bk51-gated.bundle bk51-gated`, or copy the directory.
Never a remote.

⚠️ **`git push origin main` IS the deploy on this project.** Docs-only pushes are
safe; code pushes are live.

## 1 · Where the work actually is

| | State |
| --- | --- |
| **BK-50** | ✅ LIVE since 2026-09-02 |
| **BK-51** | ✅ reviewed, ❌ **NOT LIVE** — on `bk51-gated` only. Its one gate is BK-53 |
| **BK-53** | 🔴 **NOT APPROVED.** 4 plan-review rounds (1, 2, 3, 3B) + 2 validation agents (traceability, adversarial); **3 written revisions (2, 3, 4)**; revision 5 owed **but gated — see §2** |
| **BK-52** | drafted, not started, not blocking |
| **BK-54** | 🆕 named but not written — the terms snapshot (revision 4, R4-3) |

**BK-53 ships ON `bk51-gated`, in ONE push with BK-51, never separately.**
Alone on `main` it produces a priceless site while the approval screen still
pre-fills the tier price — **$399.00 at `[id].astro:1776`, computed at
`:505-512`** — and routes approval to a Checkout Session that charges $418.95.
*(ROADMAP:3315 says "pre-fills $418.95"; that is the GST-inclusive total the
charge becomes, not the field value. Both were wrong the same way.)*

## 2 · ⛔ THE FIRST THING TO DO IS ASK THE USER, NOT TO WRITE REVISION 5

**There is an open recommendation the user has not answered: SPLIT BK-53.**

Four plan-review rounds and three revisions, still not approved. Every round finds real
defects, so the review is working — but the ticket currently owns prices off
five surfaces, the terms-box rewrite, the tier picker, the weekend surcharge,
the resend defect, W20's and W22's surfaces, and a gate rewrite across three
verify scripts. **Every blocker keeps landing in the gate half.**

**Do not start revision 5 before the user answers.** If they say push through,
revision 5's scope is R4-1…R4-6 plus the restored items in revision 4.

## 3 · The client's decisions — the ones that changed everything

📖 **READING ORDER for `tickets/BK-53.md` — it is ~2,000 lines, roughly
newest-first but NOT monotonic, and decisions 17–19 and 20–23 sit 550 lines
apart:**

| Read | Lines |
| --- | --- |
| Status | `:1-34` — ⚠️ **IGNORE the `Tier:` line at `:30`** (*"plain Reviewed, not adversarial… copy-only"*). It was **struck in round 1**; the correction is 316 lines later at `:346-348`. The real tier is **Reviewed + FULL adversarial pass** |
| Decisions 17–19 | `:62-174` |
| Decisions 20–23 | `:611-775` |
| **Revision 4 — CURRENT** | `:175-358` |
| Revision 3, as amended by revision 4 | `:359-610` |
| Everything from `:776` | **superseded review history.** Read only to recover a finding by its ID |

Read **DECISIONS 17–23** in full. The load-bearing ones:

- **17 · The VISIT is free.** Not the assessment unconditionally — **a travel fee
  may apply**, decided by the client at confirmation, handled on the phone and
  **enforced out of the software** (`bk51-gated:review.ts:246` refuses any
  non-zero travel fee). So **every "free" claim needs its qualifier IN THE SAME
  SENTENCE.**
- **20 · NOTHING is handed to the customer in writing at the visit.** The quote
  follows only if they want to proceed, and that is where reports and tiers live.
  ⚠️ **This restored the client's original position** — their 2026-08-14 table
  said *"the on-site assessment. No documents."* **We** added the written scope
  in BK-36. It also **falsified three live pages** — see §5.
- **21 · The weekend surcharge is dead.** Verbatim at last; the original rule
  rested on four words whose question was never recorded.
- **22 · Do NOT promise "never after the visit."** Say the customer is told the
  amount when the booking is confirmed.
- **23 · "No obligation" goes back on** — never beside a bare "free" without the
  qualifier, and the ban is **replaced** by the coupling pin, not struck.

## 4 · The three blockers revision 5 must close

1. **The request email prices the assessment.** `booking-email.ts:1159-1168` —
   computed from `assessmentQuote`, so no constant deletion touches it and no pin
   sees it — **no *deletion* pin, that is; the fixtures assert it in five
   places.** **The fixtures REQUIRE it:** `verify-booking-email.ts:2013-2045`
   asserts `$399.00`, `$418.95`, `$577.50`, `$606.38` and `weekend rate` — and
   `mouldBody` is the **customer's** message.
2. **The form prices every radio.** `BookingForm.svelte:1171-1175`, in its own
   `{#if tierQuotes}` — not the block being deleted — and
   `verify-cutover.ts:594-598` **positively requires it**.
3. **The gate forbids the ticket's own copy.** Round 1's B2, still open after
   three revisions. `FREE_CLAIM_SHAPES` **(proposed, drafted at `BK-53.md:475` —
   it does not exist in the repo)** anchors on *"assessment"* while the copy
   standard mandates *"visit"*; `BANNED_SHAPES` fails the build for a string the
   positive twin requires present; decision 23 binds the pin to a file §6 defers.

## 5 · Unowned, live, and NOT BK-53's — needs a ticket

**Three surfaces say the assessment produces a written document. Decision 20
makes that false.** In `ROADMAP.md` Known traps, severity HIGH:
`insurance-claims.astro:70` (*"At the assessment we give you a written scope
with real costs"* — worst: claim-filing context, and *"real costs"* is the $699
tier), `services.ts:389`, `services.ts:291`.
✅ **Do NOT over-sweep:** *"a written scope **before work starts**"* stays TRUE
on the **nine** surfaces enumerated in ROADMAP Known traps — the quote precedes
the work.

🛑 **DO NOT FIX THESE INLINE IN BK-53** (ROADMAP's own imperative). They are
deliverable claims on unrelated pages; BK-53 owns the booking funnel's price
claims and already carries its own blockers. **And remember `git push origin
main` is the deploy** — an urgent-feeling fix here is one command from an
unreviewed production release.

Also unowned: `new.astro:192` (*"Payment comes before dispatch — client
policy"*, which decision 17 reverses).

⚠️ ~~and the Travel field having no guidance on `bk51-gated`~~ **— STRUCK,
VERIFIED FALSE 2026-09-05.** `bk51-gated:[id].astro:1858-1866` carries **more**
guidance than `main`: *"Free in Edmonton, Spruce Grove, St. Albert, Beaumont and
Leduc. Further out, a flat $150 may apply — decide it on the confirmation call
and tell the customer then… Settled outside this system."* And
`TRAVEL_FEE_CENTS_PER_KM`/`_FREE_RADIUS_KM` **survive on both branches**
(`booking-pricing.ts:123-124`); what the branch removed is `[id].astro`'s two
*references*. **The live defect is on `main`**, whose `:1804` still states the
retired per-km rule. *This finding was inherited from a review (R3B-E3) and
restated without verification — the copy-inventory trap in mirror image:
derived from "where does the constant render", it missed the sentence stating
the same thing in its own words.*

## 6 · Blocked on the user

1. **Split BK-53, or push through?** — §2. Blocks everything.
2. **Read the Ads landing pages.** **ATTEMPTED TWICE, 2026-09-05. Partial
   progress, then blocked. Do not repeat the dead ends:**
   - ✅ **The Chrome extension DOES work** (it was briefly not-connected earlier;
     re-test rather than inherit a "dead" line). **Google Ads is logged in**, and
     the account is **673-094-6254** — matching `CONVERSION` §3A.
   - 🔴 **There is NO "Landing pages" report in this account's nav.** Verified in
     the UI: *Insights and reports* offers Insights, Brand report, Auction
     insights, Search terms, When and where ads showed, Channel performance,
     Stores, Report editor, Dashboards — **no Landing pages.** Expected: the
     campaign is **Performance Max**, which has asset groups, not ad groups.
     **So the manual path recorded earlier in this project was wrong.**
   - 🔴 `/aw/landingpages` **redirects to Overview**; `/aw/assetgroups` **404s.**
     Do not guess Ads URLs.
   - 🔴 The campaigns table **is not exposed to the accessibility tree** and the
     UI sat on its loading spinner through repeated waits. The remaining route is
     screenshot-and-click into the campaign → Asset groups → the asset group.
   - ⚠️ **GA4 is still the better instrument and is still dead** (`invalid_grant`,
     re-tested). It reports where traffic **went**; an asset-group final URL only
     says where it was **configured** to go, and **PMax expands beyond its
     configured URLs** — so even a successful asset-group read would not fully
     answer the question. **Re-authing GA4 (W9) is the cheaper and better fix,
     and it also unblocks W5/W6.** Playwright
   cannot substitute — no Google session. **GA4 is the better instrument** (it
   reports where traffic *went*, not where it was *configured* to go, and the
   campaign is PMax). Manual: Ads → the campaign → **Landing pages**, read-only.
   Until answered, the four `[service].astro` pages are **not** in BK-53.
3. **May the $150 be SHOWN to the customer, or only spoken on the call?**
   ⚠️ The *amount* is already decided — **a flat $150** (decision 8;
   `PREPAY-PLAN:86`, `ROADMAP:846`, and `bk51-gated:[id].astro:1861` renders it
   to the office today). Do not re-ask what the number is; ROADMAP index item 8
   struck a near-identical question as the answered-question-still-open trap,
   fourth instance. **What decision 22 leaves open is whether it appears in
   customer-facing copy at all.**
4. **Sign-off on the drafted copy** in BK-53 revision 3 §1, as amended by rev 4.

## 7 · Traps this session paid for — all four are new

- **A revision is not a rewrite.** Every BK-53 revision dropped findings from the
  round it answered; revision 3 dropped **21**, and 13 findings now exist only as
  digits in review headers. **After any revision, run a traceability audit as a
  separate fresh agent** — its only question is *"what is in a review and absent
  from the revision with no stated reason?"* It found what four rounds missed.
- **Pair it with an adversarial pre-read** — *"assuming this ships as written,
  what still ships green?"* The two converged independently on the same three
  blockers.
- **Brief every reviewer on the TARGET BRANCH.** Two of four reasoned from
  `main` while reviewing a plan for `bk51-gated` and produced findings false on
  the branch. Stale *semantics* are worse than stale line numbers: a wrong line
  fails loudly, a wrong premise reads as a confident finding.
- **Reviews must enumerate their own nits**, not count them in a header.

## 8 · What this session shipped

Docs only — **no production code changed** (`git diff a3a3f0c..main` touches
only `docs/`). **17 commits**, all on `origin/main`.
Highlights: the human-blocked index refreshed with the user's answers; six stale
ticket `Status:` lines corrected; BK-52's false *"BK-51 is committed"*; the
*"TWO gates"* row; **there are FOUR service landing pages, not six**, and the
claim that they are the Ads landing pages is **one unsourced sentence**; W22
given an owner; and BK-53 taken draft → planned → 4 reviews → revisions 2, 3, 4.
