# HANDOFF — session of 2026-09-05. Read this first, then stop and check two things.

**Written at session close. Everything below was verified in code or git at the
time of writing; the two commands in §0 tell you what has changed since.**

## 0 · Before you trust any line in this file

```sh
git log --oneline -1                 # expect a32323c or later
git log --oneline main..bk51-gated   # expect 10 lines: 8 commits + 2 docs syncs
git ls-remote --heads origin         # MUST list exactly 3: main,
                                     # booking/p1-foundation, deploy-2-prepay.
                                     # Fewer, or an error, means the check did
                                     # NOT run — stop. `bk51` must not appear.
```

⚠️ **The third command is deliberately not `| grep bk51`.** A grep that finds
nothing looks identical to a grep that never ran — network down, auth expired,
remote renamed — and reads as *"safe, not pushed."* That is CLAUDE.md's BK-33
trap: **never measure by a marker count being zero.** Count the refs.

⚠️ **`bk51-gated` exists on this machine and nowhere else.** No remote copy. It
carries BK-51's production code — 6 files, +940/−102. If this checkout is lost,
that work is lost.

🛑 **DO NOT PUSH IT ANYWHERE, INCLUDING AS A BACKUP.** ROADMAP's human-blocked
index item 5 gates that on the user confirming **Vercel Preview environment
isolation** — a preview build of this branch pointed at the production Neon
database would allow a **real free approval**: email sent, ICS issued, `paid_at`
stamped, none of the gates. **To protect the work:**
`git bundle create ../bk51-gated.bundle bk51-gated`, or copy the directory.
Never a remote.

⚠️ **`git push origin main` IS the deploy on this project.** Docs-only pushes are
safe; code pushes are live.

## 1 · Where the work actually is

| | State |
| --- | --- |
| **BK-50** | ✅ LIVE since 2026-09-02 |
| **BK-51** | ✅ reviewed, ❌ **NOT LIVE** — on `bk51-gated` only. Its one gate is BK-53 |
| **BK-53** | 🔴 **NOT APPROVED.** 4 plan-review rounds (1, 2, 3, 3B) + 2 validation agents (traceability, adversarial); **3 written revisions (2, 3, 4)**; revision 5 owed **but gated — see §2** |
| **BK-52** | drafted, not started, not blocking |
| **BK-55** | 🆕 **written 2026-09-05** — the email half of the split. Reviewed + adversarial |
| **BK-54** | 🆕 named, not written — the terms snapshot at insert (revision 4, R4-3) |
| **BK-56** | 🆕 named, not written — let the owner add a line to the customer's confirmation email. **Decision 25 requires this channel and it does not exist** |

**BK-53 ships ON `bk51-gated`, in ONE push with BK-51, never separately.**
Alone on `main` it produces a priceless site while the approval screen still
pre-fills the tier price — **$399.00 at `[id].astro:1776`, computed at
`:505-512`** — and routes approval to a Checkout Session that charges $418.95.
*(ROADMAP:3315 says "pre-fills $418.95"; that is the GST-inclusive total the
charge becomes, not the field value. Both were wrong the same way.)*

## 2 · ✅ ANSWERED — SPLIT (decision 24). Gate DISCHARGED. ~~**Nothing is blocked on the user.**~~

🔴 **THE STRUCK CLAUSE, 2026-09-06 (§11): ONE THING IS BLOCKED ON THE USER
AGAIN — and it is a DIFFERENT SPLIT.** Decision 24's split (by surface group,
BK-53 web / BK-55 email) is answered and shipped, and everything below this line
about it stands. **§11 opens a second, unrelated question about the GATE's
machinery.** *Two "split BK-53" questions in one file; do not conflate them.*

**2026-09-05: the user answered — SPLIT.** BK-53 is now the **web** half;
🆕 **BK-55** is the **email** half and carries the money, the resend defect and
the adversarial pass. ⚠️ **The split is by SURFACE GROUP, not
copy-ticket / gate-ticket** — the latter would break the one-commit rule
(*pin first and the build goes red; copy first and there is an unpinned
window*). See `BK-53.md` decision 24.
**Revision 5 is no longer owed as such; each half now needs its own spec.**

~~There is an open recommendation the user has not answered: SPLIT BK-53.~~

Four plan-review rounds and three revisions, still not approved. Every round finds real
defects, so the review is working — but the ticket currently owns prices off
five surfaces, the terms-box rewrite, the tier picker, the weekend surcharge,
the resend defect, W20's and W22's surfaces, and a gate rewrite across three
verify scripts. **Every blocker keeps landing in the gate half.**

**Do not start revision 5 before the user answers.** If they say push through,
revision 5's scope is R4-1…R4-6 plus the restored items in revision 4.

## 3 · The client's decisions — the ones that changed everything

🔴 **THIS FILE NO LONGER GIVES A LINE TABLE FOR `BK-53.md`, DELIBERATELY.**
It gave one when that file was ~2,000 lines; it is now ~4,500 and has been
revised seven times, so every line number here went stale within a day and the
correction lived *inside* the file this table was sent you to navigate.

**→ Use the ticket's own `## Reading order` section** (near the top of
`BK-53.md`), which is updated in the same commit as each new revision header.
**→ And read its `Status:` line first — it is the only current answer to "which
revision is the spec".**

Read **DECISIONS 17–23** in full. The load-bearing ones:

- **17 · The VISIT is free.** Not the assessment unconditionally — **a travel fee
  may apply**, decided by the client at confirmation, handled on the phone and
  **enforced out of the software** (`bk51-gated:review.ts:246` refuses any
  non-zero travel fee). So **every "free" claim needs its qualifier IN THE SAME
  SENTENCE.**
- **20 · NOTHING is handed to the customer in writing at the visit.** The quote
  follows only if they want to proceed, and that is where reports and tiers live.
  ⚠️ **This restored the client's original position** — their 2026-08-14 table
  said *"the on-site assessment. No documents."* **We** added the written scope
  in BK-36. It also **falsified three live pages** — see §5.
- **21 · The weekend surcharge is dead.** Verbatim at last; the original rule
  rested on four words whose question was never recorded.
- **22 · Do NOT promise "never after the visit."** Say the customer is told the
  amount when the booking is confirmed.
- **23 · "No obligation" goes back on** — never beside a bare "free" without the
  qualifier, and the ban is **replaced** by the coupling pin, not struck.

## 4 · The three blockers revision 5 must close

1. **The request email prices the assessment.** `booking-email.ts:1159-1168` —
   computed from `assessmentQuote`, so no constant deletion touches it and no pin
   sees it — **no *deletion* pin, that is; the fixtures assert it in five
   places.** **The fixtures REQUIRE it:** `verify-booking-email.ts:2013-2045`
   asserts `$399.00`, `$418.95`, `$577.50`, `$606.38` and `weekend rate` — and
   `mouldBody` is the **customer's** message.
2. **The form prices every radio.** `BookingForm.svelte:1171-1175`, in its own
   `{#if tierQuotes}` — not the block being deleted — and
   `verify-cutover.ts:594-598` **positively requires it**.
3. **The gate forbids the ticket's own copy.** Round 1's B2, still open after
   three revisions. `FREE_CLAIM_SHAPES` **(proposed, drafted at `BK-53.md:475` —
   it does not exist in the repo)** anchors on *"assessment"* while the copy
   standard mandates *"visit"*; `BANNED_SHAPES` fails the build for a string the
   positive twin requires present; decision 23 binds the pin to a file §6 defers.

## 5 · Unowned, live, and NOT BK-53's — needs a ticket

**Three surfaces say the assessment produces a written document. Decision 20
makes that false.** In `ROADMAP.md` Known traps, severity HIGH:
`insurance-claims.astro:70` (*"At the assessment we give you a written scope
with real costs"* — worst: claim-filing context, and *"real costs"* is the $699
tier), `services.ts:389`, `services.ts:291`.
✅ **Do NOT over-sweep:** *"a written scope **before work starts**"* stays TRUE
on the **nine** surfaces enumerated in ROADMAP Known traps — the quote precedes
the work.

🛑 **DO NOT FIX THESE INLINE IN BK-53** (ROADMAP's own imperative). They are
deliverable claims on unrelated pages; BK-53 owns the booking funnel's price
claims and already carries its own blockers. **And remember `git push origin
main` is the deploy** — an urgent-feeling fix here is one command from an
unreviewed production release.

Also unowned: `new.astro:192` (*"Payment comes before dispatch — client
policy"*, which decision 17 reverses).

⚠️ ~~and the Travel field having no guidance on `bk51-gated`~~ **— STRUCK,
VERIFIED FALSE 2026-09-05.** `bk51-gated:[id].astro:1858-1866` carries **more**
guidance than `main`: *"Free in Edmonton, Spruce Grove, St. Albert, Beaumont and
Leduc. Further out, a flat $150 may apply — decide it on the confirmation call
and tell the customer then… Settled outside this system."* And
`TRAVEL_FEE_CENTS_PER_KM`/`_FREE_RADIUS_KM` **survive on both branches**
(`booking-pricing.ts:123-124`); what the branch removed is `[id].astro`'s two
*references*. **The live defect is on `main`**, whose `:1804` still states the
retired per-km rule. *This finding was inherited from a review (R3B-E3) and
restated without verification — the copy-inventory trap in mirror image:
derived from "where does the constant render", it missed the sentence stating
the same thing in its own words.*

## 6 · Blocked on the user

1. **Split BK-53, or push through?** — §2. Blocks everything.
2. ✅ **DONE 2026-09-05 — MEASURED, and the long-standing premise was FALSE.**
   GA4 works (property **543677881**). Paid sessions 2026-07-01→09-05:
   **`/` homepage 1,097 (~92%)**, `/contact` 32, `/mold-removal` 18, `/book` 6,
   `/water-damage-restoration` 3, `/sewage-cleanup` 2, **`/fire-smoke-damage` 0.**
   **The four service pages take 23 of ~1,204 — 1.9%. The HOMEPAGE is the ad
   entry point**, and it is already in scope (`index.astro:66` renders
   `ContactSection` in the CTA arm the terms pins target).
   ⚠️ **GA4 now uses SERVICE-ACCOUNT IMPERSONATION, not user credentials.**
   `ga4-mcp@project-2671172b-3f2b-433f-9b1.iam.gserviceaccount.com`, Viewer on
   the property. **A key file is impossible — org policy
   `disableServiceAccountKeyCreation` blocks it, and the attempt leaves a
   0-byte stub. Never retry `--scopes=…analytics.readonly`; Google refuses it.**
   The MCP server needs a reconnect to pick up credentials, **or query the Data
   API directly with `gcloud auth print-access-token --impersonate-service-account=…`,
   which is what actually produced the numbers above.**

3. ✅ **ANSWERED — decision 25: the $150 NEVER appears on the website.** Spoken
   on the call, or in an owner-triggered confirmation email — **a channel that
   does not exist yet; that is BK-56.** ~~May the $150 be SHOWN…~~
   ⚠️ The *amount* is already decided — **a flat $150** (decision 8;
   `PREPAY-PLAN:86`, `ROADMAP:846`, and `bk51-gated:[id].astro:1861` renders it
   to the office today). Do not re-ask what the number is; ROADMAP index item 8
   struck a near-identical question as the answered-question-still-open trap,
   fourth instance. **What decision 22 leaves open is whether it appears in
   customer-facing copy at all.**
4. ✅ **ANSWERED — decision 26: the copy is APPROVED**, subject to decision 25
   (no number on any web surface) and revision 4's corrections.

~~✅ **§6 IS EMPTY. Nothing is blocked on the user.**~~
~~🙋 **SUPERSEDED 2026-09-05 (later session). ONE item is now blocked on the
user** … **does the client want a stated cancellation-notice expectation?**~~
⛔ **STRUCK 2026-09-06 (§11). CLOSED, and it was closed the same day it was
written here** — `BK-53.md` **decision 27**: NO stated cancellation-notice
expectation, delegated and recorded with reasoning so it can be overturned on
its merits. ⚠️ **NOT a decision that the client does not want one — `ROADMAP.md`
human-blocked item 11 says ASK AT THE END OF SEPTEMBER.** *Struck here because
a cold reader working backwards from §11 would otherwise meet an answered
question presented as live — the trap this file has now paid for six times.*
🔴 **§11 opens a DIFFERENT item on the user; it is not this one and it is not
decision 24's split either. See §11.**

~~Original text, kept for provenance:~~ `CANCEL_LINE` survives and says **how** to
cancel; the refund block was the only place that said **how much notice**.
Policy, not judgment — and the terms box is where the answer lives, so it blocks
**shipping** the box, not writing the spec. See `BK-53.md` R5-10, last row.

---

# 9 · WHAT THE NEXT SESSION DID — 2026-09-05, later

**§0's three checks were re-run and all passed** (`d3c1647` has `a32323c` as an
ancestor; `main..bk51-gated` is 10; `git ls-remote --heads origin` returns
**exactly 3** refs, counted not grepped). **A `git bundle` of `bk51-gated` was
taken before anything was touched. Nothing was pushed.**

| | |
| --- | --- |
| **BK-53** | **Revision 5 written** — rescoped to the web half per decision 24. Folds in R4-2/4-4/4-5/4-6; drops R4-1/R4-3 to BK-55. **Then validated the same day** by a traceability audit (**R5-10**) and an adversarial pre-read (**R5-11**), both fresh agents. Still **NOT APPROVED**; plan review owed |
| **BK-55** | **Planned**, and its inherited fix corrected — see below. Amended by R5-10/R5-11 |
| **BK-54, BK-56** | 🆕 **Written.** Both were named as destinations by decisions 24/25 and revision 4 and **neither had a file** |
| **ROADMAP** | `new.astro:192` actually recorded in Known traps — revision 4 promised it there and never wrote it |

## The findings that were not inherited

1. 🔴 **The split has a SEAM (R5-1).** Decision 24 gave *"the terms box"* to
   BK-53 and *"`FEE_TERMS_ITEMS` as it reaches email"* to BK-55 — **the same
   constants.** `booking-email.ts` imports nine `booking-copy` symbols. So a
   literal reading puts a constant's edit in one ticket and its email pin in the
   other, which is **the unpinned window decision 24 chose this axis to
   prevent.** Resolved by a seam rule, a fixed commit order (**BK-55 first**),
   and **one adversarial pass at the branch TIP over the union.**
2. 🔴 **No gate script emits a count of assertions RUN (R5-2).** `check()`
   increments only on failure; the success line is a bare *"checks passed"* with
   no number. **The only number any of them prints is the `✗` count — exactly
   what BK-33 forbids scoring on** — so the ticket's own pre-registration rule
   was unfalsifiable. Fixed in three lines per script, landing first.
3. 🔴 **Decision 25 had NO pin at all (R5-11 A1)** — and the new coupling pin
   made adding `$150` read as **more** compliant, because the sentence then
   carries both a free claim and the qualifier. The user's one **absolute**
   instruction was unenforced. → `WEB_AMOUNT_BAN`.
4. 🔴 **Deleting a `FEE_TERMS_PAYMENT` member makes an assertion pass on
   `undefined` and crashes its sibling (R5-11 A5).** `verify-booking-email.ts:554-563`
   indexes `[2]` and `[3]`; `noUncheckedIndexedAccess` is not set, so typecheck
   is green, the text arm tests `includes("undefined")` and passes having tested
   nothing, and the HTML arm throws inside `escapeHtml` — **a crash with no `✗`
   and no summary line.**
5. 🔴 **BK-55's inherited fix was wrong.** R4-1 said `return name;`;
   `tierNameOf` already exists three lines above, and `assessmentSummary` has a
   **second caller at `:1214` — the office's message**, which BK-46 deliberately
   gives the settled figure and which the office reads to decide the travel fee.
   The fix is one line in `tierLine` at `:460`.
6. **Nine inherited citations were wrong**, including one restated across three
   revisions that **does not exist in the file**: revision 3 justified keeping
   `verify-cutover.ts:440-453` by *"its own comment records SHIPPED GREEN
   ONCE"*. Grepped in every casing — no such comment. Also
   `ContactSection.astro` is in **`src/sections/`**, and `booking-form.ts:276`
   is the **`termsAck`** check, not the tier one.

## ⚠️ Two traps this session paid for

- **A revision that asserts it dropped nothing is how the fourth one did it
  anyway.** R5-3 was written with the headline *"Nothing below is dropped. Every
  item of revision 4 has a row."* **It was false when written — six items had no
  row**, and the audit found them the same day. **Never write that sentence
  again; write the enumeration instead.**
- **A validator briefed to verify branch claims will still re-raise a struck
  one.** The traceability audit re-raised **R3B-E3** (*"the admin Travel field
  has no guidance on `bk51-gated`"*) — which §5 of this file had already struck
  as **verified false**, and which is false again:
  `bk51-gated:[id].astro:1857-1867` carries *"Free in Edmonton, Spruce Grove, St.
  Albert, Beaumont and Leduc. Further out, a flat $150 may apply — decide it on
  the confirmation call and tell the customer then."* **Fifth instance of the
  answered-question-still-open trap, and the first where the re-raiser had been
  told how to check.** Recorded in BK-53 R5-10 T13 with the quotation, so the
  next refutation is a paste rather than a re-derivation.

## 7 · Traps this session paid for — all four are new

- **A revision is not a rewrite.** Every BK-53 revision dropped findings from the
  round it answered; revision 3 dropped **21**, and 13 findings now exist only as
  digits in review headers. **After any revision, run a traceability audit as a
  separate fresh agent** — its only question is *"what is in a review and absent
  from the revision with no stated reason?"* It found what four rounds missed.
- **Pair it with an adversarial pre-read** — *"assuming this ships as written,
  what still ships green?"* The two converged independently on the same three
  blockers.
- **Brief every reviewer on the TARGET BRANCH.** Two of four reasoned from
  `main` while reviewing a plan for `bk51-gated` and produced findings false on
  the branch. Stale *semantics* are worse than stale line numbers: a wrong line
  fails loudly, a wrong premise reads as a confident finding.
- **Reviews must enumerate their own nits**, not count them in a header.

## 8 · What this session shipped

Docs only — **no production code changed** (`git diff a3a3f0c..main` touches
only `docs/`). All on `origin/main`. **Count them rather than trusting a
number here:** `git log --oneline a3a3f0c..origin/main | wc -l`.
Highlights: the human-blocked index refreshed with the user's answers; six stale
ticket `Status:` lines corrected; BK-52's false *"BK-51 is committed"*; the
*"TWO gates"* row; **there are FOUR service landing pages, not six**, and the
claim that they are the Ads landing pages is **one unsourced sentence**; W22
given an owner; and BK-53 taken draft → planned → 4 reviews → revisions 2, 3, 4.

---

# 10 · SESSION 2 — 2026-09-05 into 2026-09-06

**§0's three checks were re-run and passed. `bk51-gated` was bundled before
anything was touched. NOTHING WAS PUSHED — `git ls-remote --heads origin` still
returns exactly 3.**

## Where the two tickets are

| | State |
| --- | --- |
| **BK-53** | **Revision 7** — web half. ⛔ NOT APPROVED, **and revision 7 is NOT YET VALIDATED** |
| **BK-55** | **Revision 3** — email half. ⛔ NOT APPROVED, **not yet validated** |
| **BK-54, BK-56** | 🆕 written — both were named as destinations and neither had a file |

**Owed before either can be approved:** traceability audit + adversarial pre-read
of revisions 7/3, then a plan review. **Each a fresh agent, each briefed per
BK-53 R5-8 / BK-55 §F.**

## What each round cost, and why the rounds were worth it

| Round | Found |
| --- | --- |
| Revision 5 + 2 validators | The split's **seam** (the terms box is web AND email); **no gate script emits a count of assertions run** |
| Plan review ×2 | 15 blockers. **Decision 25 had NO pin at all** — and the coupling pin made adding `$150` read as *more* compliant |
| Revision 6 + 2 validators | **`verify-cutover.ts` stays green while the prices vanish from `dist/`** — its terms pins match identifier NAMES, never values. **Measured in a sandbox** |
| Revision 7 + 3 | **The gate has no refund or credit ban**, and the ticket's title is *"stop promising a refund"*. **The radio prices had been dropped.** **Red-first rows 1 and 2 were green** |

⚠️ **Every round found real defects, and roughly half of each round's findings
were in the FIX for the previous round.** That is not thrash — the findings get
narrower each time — but **budget a validation cycle for revision 7; do not
assume it is clean because revision 6 was heavily reviewed.**

## 🔴 The three facts most likely to be re-derived wrongly

1. **Booking #36 is `cancelled`, not `confirmed`.** `resend.ts:94` refuses
   anything else, so **#36 cannot be resent** and is not an example of the resend
   defect. Four documents used it as the worked example carrying that entire
   argument. **The defect is real for any charged `confirmed` row; #36 is not
   one.** `ROADMAP:387`, `:418`.
2. **There are TWO producers of the confirmed customer message**, not one:
   `resend.ts:106` and **`booking-payment.ts:1597` on `bk51-gated`** (`:1567` is
   the `main` line — that file is one of the six divergent ones). A fix written
   inside `resend.ts` covers one of two.
3. **`booking-form.ts` is only the CLIENT validator.** The server's 422 comes from
   `booking-payload.ts:277-286`, and only when `entry === 'public'`. Any change to
   what the form requires touches two files.

## The standing rule this session paid for

> **When a section is replaced wholesale, enumerate the OLD section's rows
> against the new one and state which verdicts changed.**

CLAUDE.md's rule-rewrite trap, one level up: applied to **sections**, not rules.
Revision 6 replaced the copy and gate sections and dispositioned the *review's*
findings faithfully — **and lost six rows of the replaced sections' own content**,
because the disposition table never claimed to cover them. `BK-53.md` **R7-0** is
that enumeration and is the pattern to copy.

**Corollary, and it is cheap:** a finding's ID is its handle. **Do not re-use an
ID** — BK-55 revision 2 re-used `N8` for a different point, and the original
dropped finding read as resolved.
---

# 11 · SESSION 3 — 2026-09-06. **THREE REVISIONS, THREE VALIDATION ROUNDS, ONE OPEN DECISION.**

**§0's three checks were re-run and passed. `bk51-gated` was bundled before
anything was touched (`../bk51-gated-2026-09-06.bundle`; the 09-05 one also
survives). NOTHING WAS PUSHED — `git ls-remote --heads origin` returns exactly
3, counted. Docs only: `git diff --name-only 2385a7f..HEAD` touches only
`docs/`.**

⚠️ **`git diff main..bk51-gated` now lists SIXTEEN files — six code and ten
docs — because `main` has advanced. The standing "six files differ" fact means
six NON-DOCS files.** *An agent briefed with a bare "six" who runs the command
finds the brief wrong, which is trap 4 below.*

## ⛔ ~~🙋 THE ONE THING BLOCKED ON THE USER~~ — **CLOSED 2026-09-07. ANSWERED TWICE.**

🔴 **STRUCK — see §12.** The user chose the **BLOCK** unit and then **WITHDREW it the
same day after measurement.** **The standing answer is KEEP REVISION 10's SENTENCE
UNIT**, with `MAX_UNIT`/`MIN_UNITS`/residue/`A-R10-4` restored and **both bans kept.**
*Struck here because a cold reader meeting an answered question presented as live is
the trap this file has now paid for SEVEN times.* **Everything below this line is
provenance for how the question was framed, not a live question.**

~~## 🙋 THE ONE THING BLOCKED ON THE USER — NOT approved, and NOT decision 24's split~~

🔴 **A SCOPE RECOMMENDATION WAS MADE, THEN SUBSTANTIALLY REFUTED BY THE AGENT
SENT TO ATTACK IT, AND THE USER HAS NOT ANSWERED THE CORRECTED VERSION.**

⚠️ **This is NOT §2's split.** Decision 24's *"split BK-53 by surface group"* is
**ANSWERED and shipped** — BK-53 is web, BK-55 is email. **§2's *"nothing is
blocked on the user"* line no longer holds, and this is why.** The new question
is about the GATE's machinery, not the ticket's surfaces.

**Read `BK-53.md` `R10-8` in full — it carries the corrected argument, the
counter-argument and the three calls. Its FIRST DRAFT WAS WRONG BY THIRTEEN and
is corrected in place.**

**The three calls, each on its own evidence:**

| | |
| --- | --- |
| ✅ **KEEP — they earned it** | the coupling pin + three-tier rule, `verify-booking-ics.ts`'s `BANNED_SHAPES` + arity, the `WEB_CONSTANTS` closure. **All three caught defects that existed independently of the pin** |
| 🔴 **REPLACE** | the `dist/` extractor → a **threshold-free FILE-LEVEL coupling assertion**: *every built file matching a free shape must also contain `TRAVEL_QUALIFIER`*. **The thresholds are the problem** — three honest implementations **87 characters apart**, and A-R10-4 already concedes the ticket cannot state its own numbers |
| ⚠️ **JUSTIFY OR DROP** | the amount ban and the money-promise ban. **Zero catches, ~16 self-inflicted findings between them.** They may still be right — decision 25 is absolute — **but nobody has made that argument** |

🔴 **WHAT I GOT WRONG, recorded because the next reader will otherwise inherit
it:** I proposed *"revert R6-3c's whole-file free-claim ban — strictly better,
no extractor."* **Executed: it is RED on `ASSESSMENT_TERMS[0]`, the eyebrow and
the meta description — three of this ticket's own approved sentences — and GREEN
on `llms.txt`, the surface tier A exists for.** The ban was retired at **R4-5**
because it is **mutually exclusive with the copy the ticket ships**, two
revisions before the per-sentence rule existed. **Executing my recommendation as
written produces a red gate on day one.**

⚠️ **The counter-argument, which is strong and is NOT mine:** shrinking removes
the only production-artifact assertion from a ticket about what the production
artifact says — **BK-29's and BK-34a's shape, both in CLAUDE.md**. The source
corpora provably do not reach the customer (`RECEIVED_EMAILED_LINE` is in **0**
built HTML files; `BookingConfirmation.svelte:174`/`:220`'s prose is in **no**
corpus). And this project's recorded failure mode is **under**-gating: BK-51,
eleven breaks, eleven green.

## Where the two tickets are

| | State |
| --- | --- |
| **BK-53** | ⇄ **REVISION 12, 2026-09-06.** ⛔ **NOT APPROVED — validation and plan review both owed.** ✅ **The gate question is ANSWERED — TWICE.** The user first chose the block unit (revision 11) and **withdrew it the same day after measurement**; the standing answer is **KEEP REVISION 10's SENTENCE UNIT**, restore `MAX_UNIT`/`MIN_UNITS`/residue/`A-R10-4`, keep BOTH bans. **No ticket split out — BK-57 unopened.** 🔴 **The gate DOES NOT EXIST YET** (its constants return zero hits in `src/` and `scripts/`), so every gate measurement in revisions 10–12 is a SIMULATION. 🔴 **Two REAL breaks against the EXISTING scripts both shipped GREEN — rows 25 and 57** |
| **BK-55** | ⇄ **REVISION 5, 2026-09-06.** ⛔ **NOT APPROVED — validation and plan review both owed.** ~~Revision 4 … only plan review is owed~~ 🔴 **STRUCK: plan review of revision 4 returned 7 blockers.** ⚠️ **AND THE "RUN END-TO-END" CLAIM WAS FALSE — it is true of §B-2's deletion and false of COMMIT (b).** §B-1 is in the same commit and had **never been sandboxed**, through three rounds and two validation agents. **Measured 2026-09-06: full commit (b) → `exit 1, ✗ 10 checks failed`** (BK-55 §K-1, all ten enumerated). ✅ §I-1's twenty reds and its clauses 1–2 **are** correct and independently re-executed — the repair is right, the claim about what it finishes was wrong. ✅ **§F's gate flag is DISCHARGED** — no ticket is split out, the union and the push are unchanged |
| **BK-51** | unchanged — reviewed, on `bk51-gated` only |
| 🆕 **THE DAY'S LESSON** | 🔴 **A CONSTANT WITH NO RED-FIRST ROW IS UNTESTED, NOT WORTHLESS.** `MAX_UNIT` was deleted for "catching nothing" and is the ONLY catcher of the mutation that **restores B4's vacuity at 19,178 characters**; the residue rule is the ONLY catcher of an **entity-encoded price** (`&dollar;150`). **Before deleting an assertion for having no catch record, write the row that would catch its failure and RUN it** |
| **GATE 1** | ⇄ **TWO TICKETS since decision 24 — BK-53 (web) AND BK-55 (email).** *`ROADMAP.md` said "BK-53 is the only blocker" in two places until 2026-09-06* |
| **BK-52 · BK-54 · BK-56** | unchanged, none blocking. **BK-56 is required by decision 25 and is still unwritten. BK-57 is the next free number** |

## 🔴 Finding the spec inside 6,700 lines — do not read from the top

**`BK-53.md`'s reading-order table is near the top and is updated with every
revision. Use it.** Revision 10 is THE SPEC; revisions 9, 8 and 7 are each
marked *"⛔ NOT the spec"* **and are each still current for named sections**.
**A cold read from the top lands in revision-6 text superseded three times.**
⚠️ **The tickets do NOT exist in usable form on `bk51-gated`** — `git show
bk51-gated:docs/booking/tickets/BK-53.md` is a **212-line draft** and
`BK-55.md` **does not exist there at all**. **Read them from `main`.** The
branch has a docs-sync precedent (`8ba8a96`, `8a97a07`).
⚠️ **`dist/` is from 2026-09-05 and is not committed.** Every arrival colour,
the 4-of-42 and the ten in-scope pages were measured against it. **`npm run
build` before re-measuring anything.**

## What three rounds cost, and what each found

| Round | Wrote | Found |
| --- | --- | --- |
| 1 | **rev 8** / **rev 4** | The bans scanned a corpus containing none of the copy — **and the proposed fix was blind the other way.** 🔴 **The only COPY blockers this ticket has had were here** — B1 forced two approved sentences to change |
| 2 | **rev 9** | **7🔴 + 5🔴.** Self-checks **red on arrival on `llms.txt`, with R8-2a's own table containing the proof**; the amount ban **could not see a constant value**; `join('\n')` **welds members** |
| 3 | **rev 10** | **6🔴 + 6🔴.** Revision 9's headline fix wrong **three ways**; two landmarks **red on arrival**; adjacency unsound **exactly where welding was** |

**24 blockers across rounds 2 and 3 — 18 in the gate, 6 in this ticket's own
bookkeeping, 0 in the copy.** ⚠️ **The zero is CONVERGENCE from round 1, not
evidence about the gate**, and R10-8's first draft used it as evidence.
⚠️ **Rounds 2 and 3 were the same class at finer granularity.**

## 🔴 Five traps this session paid for — all new

- **A REFUTATION IS A MEASUREMENT.** Revision 8 refuted a reviewer's *"34
  matches"* with *"no combination produces 34."* **34 reproduces exactly** —
  `stripForUse` + phone + `(19|20)\d{2}`. One reading of *"the four-digit year"*
  was measured and **no reading** was concluded. **A wrong refutation retires a
  TRUE finding with the authority of a measurement.** *Enumerate the readings, or
  do not refute.* Memory: `refutation-is-a-measurement`.
- **AND IT HAPPENED AGAIN, ONE LEVEL UP.** The scope recommendation above rested
  on a misread of R6-3c that survived into a handoff draft. **It was caught only
  because a fresh agent was sent to attack the recommendation itself.** *Send
  one. The argument for a decision gets the same audit as the decision.*
- **A DISPOSITION THAT NAMES A DESTINATION NOBODY CREATES IS A DROPPED
  FINDING** — **twice, and the second was worse.** Two findings routed to
  *"BK-55 §I-6"*, cited 5×, no such section *(now written)*. Then **`R9-10` cited
  NINETEEN times and `R9-A` three, with no heading either** — in the ticket that
  had just recorded the lesson. *(Both now written.)* **→ After writing a
  disposition that names a section, `grep` for that section's heading.**
- **AN ORDER ISSUED IN PROSE IS NOT PERFORMED.** Four instances, each ordered at
  least twice: the withdrawn exempt list, R6-3d's carve-out sentence, seam rule
  3's `slice(0,2)`, §J's clause-3 replacement.
- **THE REVIEWER BRIEF ITSELF CAN BE WRONG.** Four briefings expanded the bare
  `review.ts:246` to **`src/lib/booking-review.ts:246`, which does not exist**
  (171 lines on `main`, 209 on the branch). It is
  **`src/pages/api/admin/appointments/review.ts:246`**. *Found by a reviewer, in
  the brief that was warning about wrong premises.* **Fixed in BK-53 R5-8 AND
  BK-55 §F.**

## 🆕 The gate lesson, worth more than the ticket

**Every ban was written from the spellings its author had in front of them, and
the live spelling was a different one** — five times: `1.5×`/`1.5x` while the
site says **`1.5 times`**; a `credit` shape needing a noun while the live eyebrow
is *"Credited in full"*; **`no fee`** missing on a site whose whole vocabulary is
*fee*.

🔴 **The fix that FAILED:** revision 9 added a control per **BAN**. **Deleting
any one shape leaves it green 8-of-8, 15-of-16, 9-of-10**, and five controls
exercised **5 of 34 shapes.** *A rule that certifies the list cannot see the
shape.* **And its criterion — *"a string that exists in the repo today"* —
describes a CORPUS: three of five named controls are strings the ticket
deletes.**
✅ **What replaced it (R10-1):** **every SHAPE carries its own control — a
literal HAZARD SENTENCE beside it in the verify script, not a repo string —
asserted as a table whose LENGTH is pinned.** Memory: `gate-design-lesson`.

## Unowned, live, still needing a ticket

- **The blog** — client-authored copy surface, **no gate at all**. ROADMAP Known
  traps, MEDIUM, latent, verified clean three times.
- 🆕 **`/upload/`** — **non-prerendered**, so `dist/client/upload` does not exist
  and it is invisible to every corpus **and to the out-of-scope ACCOUNTING**,
  which was derived from what `dist/` contains. Same class: `ContactForm.svelte`,
  `NextOpening.svelte`. ROADMAP Known traps.
- **The three decision-20 pages** — `insurance-claims.astro:70`,
  **`src/data/services.ts`**`:389` and `:291` — unchanged from §5, still HIGH,
  still **not BK-53's**.

---

# 12 · SESSION 4 — 2026-09-06 into 2026-09-07. **THE GATE QUESTION ANSWERED TWICE; BOTH TICKETS ADVANCED TWO REVISIONS; THE OWED LISTS WORKED THROUGH.**

**§0's three checks were re-run and passed. NOTHING WAS PUSHED —
`git ls-remote --heads origin` returns exactly 3, counted. `bk51-gated` is
untouched at 10 commits ahead of `main`, still 6 NON-DOCS files divergent.
Zero production files were modified this session.**

⚠️ **`main` is well ahead of `origin/main` and every one of those commits touches
only `docs/`.** 🔴 **The COUNT is not the check — it moves with every commit and a
stale one invites a false alarm.** The check is
`git diff --name-only origin/main..main | grep -v '^docs/'` **being EMPTY**.
**So a `git push origin main` today would ship nothing live.** The standing
"push IS the deploy" rule is unchanged — it just is not currently blocking.

**Session commits, all docs-only:** `06655c6` · `561950b` · `959110f` · `0104f9f`
· `9a13611` · and this one. ⚠️ 🔴 **An earlier version of this line said "three
commits, `959110f` (HEAD)" — it omitted the commit that wrote it, and every one
after.** *A document cannot state its own commit count; check with
`git log --oneline 64fe386..HEAD`.* **The invariant that matters:
`git diff --name-only 64fe386..HEAD | grep -v '^docs/'` is EMPTY.**

## 🔴 THE ONE THING TO READ FIRST

⭐ **`BK-53.md` now opens with `## THE SPEC` (S1–S8). To implement, read ONLY
that.** Twelve revisions and nine "still current for…" pointers had made the
ticket unreadable in one pass. Nothing was deleted; the revisions remain as
provenance, and **where the spec and a revision disagree, the spec is newer and
wins.**

## Where the two tickets are

| | State |
| --- | --- |
| **BK-53** | **Revision 12 + `THE SPEC`.** ⛔ **NOT APPROVED** — validation round and plan review both owed. **`R12-9`'s owed list: 9 of 9 now landed** — ⚠️ 🔴 **but O9 was MARKED done and never performed, and O8 only half-landed; both were caught by a fact-check and fixed on 2026-09-07. RE-CHECK THE DESTINATIONS RATHER THAN THE MARKS** |
| **BK-55** | **Revision 7.** ⛔ **NOT APPROVED** — validation and plan review owed. **`§L-9`'s owed list is CLOSED — 14 done, 1 moot** |
| **BK-51** | unchanged — reviewed, `bk51-gated` only, waits on both |
| **BK-52 · BK-54 · BK-56** | unchanged, none blocking. **BK-56 is still required by decision 25 and is OPENED-BUT-UNPLANNED** *(the file exists, 43 lines, "not planned, not started" — "unwritten" was imprecise)*. **BK-57 is unopened and the next free number** |

## ✅ THE THREE DECISIONS THE USER MADE — do not re-open these

| | Decision |
| --- | --- |
| **The gate's coupling unit** | 🔴 **ANSWERED TWICE.** The user chose a **BLOCK** unit (revision 11) and **withdrew it the same day after measurement**. **STANDING ANSWER: KEEP REVISION 10's SENTENCE UNIT**, with the three-tier same-or-next adjacency. `MAX_UNIT`, `MIN_UNITS`, the residue rule and `A-R10-4` are **all restored**. **Both bans are kept.** *Two "change the unit" proposals have now failed; a third needs new evidence, not new argument* |
| **BK-55's `termsEra` guard** | ✅ **ROUTE (e), 2026-09-07: the SENTENCE changes so no guard is needed.** `TERMS_PRIOR_LINE` — *"You accepted our terms when you booked. Ask us and we'll send you a copy."* ⚠️ **NEW customer-facing copy, NOT covered by decision 26 — the client signs it off** |
| **BK-53's shape** | ✅ **Add `THE SPEC` at the top, delete nothing.** A full re-baseline was rejected: rewrites are how this project loses rows |

## 🆕 THE MEASURED CONSTANTS — `A-R10-4` is discharged for the numbers it named

**A throwaway instrument was built and run against a fresh build on 2026-09-07.
It was NOT committed.** *(Rebuild `dist/` before re-measuring: `npm run build`.)*

| | |
| --- | --- |
| `body` max unit | **273** — 🔴 **but that 273 is a CLIENT-AUTHORED BLOG POST. Max over pages this ticket owns is 222** |
| min units per file | **63** (`book/confirmed/`); max 309. *A floor of 40 has headroom* |
| residue | ✅ **ZERO entities survive a 23-entry table** — the earlier "red on arrival" was an incomplete table, not a broken rule |
| arrival colour | **6 unqualified free claims**, ✅ **matching R8-2f's independent count exactly** |

✅ **AND THE RULE WAS RUN AGAINST THE APPROVED COPY.** Five approved shapes
green — **including arm `e`, the `travel <span>fee</span>` tag-split false red
that killed BOTH rejected proposals** — and both hazards red. **This is the
evidence the ticket had been arguing about since revision 8.**

🔴 **`MAX_UNIT` MUST BE MEASURED OVER OWNED PAGES ONLY, or the blog leaves the
corpus.** *This is R10-5 S-2's hazard — "a client blog post can redden BK-53's
gate" — found for `llms.txt` length and equally true of `MAX_UNIT`. Nobody had
noticed.*

## 🔴 What is TRUE TODAY and is why BK-53 exists

**A price and an unqualified free claim planted in `ContactSection.astro:287`
reach `dist/client/index.html`, and `verify-cutover.ts` exits `0`** with zero
`✗`. **The homepage — 92% of paid ad sessions.** *(Executed and restored from a
file backup; red-first row 57.)*

⚠️ **AND BK-53's GATE DOES NOT EXIST IN CODE.** `WEB_AMOUNT_BAN`,
`MONEY_PROMISE`, `WEB_CONSTANTS`, `MAX_UNIT`, `MIN_UNITS`, `WEB_PROSE` and
`ASSESSMENT_TERMS` return **zero hits** across `src/` and `scripts/`. **Every
gate measurement in revisions 10–12 is a SIMULATION of a proposed gate**, except
the instrument runs above and the two real breaks — **both of which shipped
green** against the EXISTING scripts.

## 🔴 Still open before either ticket can be approved

| | |
| --- | --- |
| **BK-53** | **`WEB_AMOUNT_BAN` misses NINE of the THIRTEEN spellings tested** — *"The travel fee is 150."* ships green. **The shapes must widen before the ban can be said to enforce decision 25.** And **`head:title` and `attr` have NO red-first row** — the two classes R10-3 found red on arrival |
| **BK-55** | 🔴 **AT COMMIT (b) NOTHING PINS THE REQUEST EMAIL.** All three money regexes are `!/\$[\d,]+\.\d{2}/` on **confirmed**-arm fixtures. **Six planted defects shipped green**, including §B-1's own opening sentence, and **red-first rows 3 and 4 are not observable.** G1's row ordered *"BOTH message types"* and nothing performed it |
| **Both** | one validation round *(traceability + adversarial, separate fresh agents, adversary RUNS the breaks)*, then plan review |
| **Client** | the new `TERMS_PRIOR_LINE` sentence needs sign-off |

## 🔴 Five traps this session paid for — all new, all measured

- **A CONSTANT WITH NO RED-FIRST ROW IS UNTESTED, NOT WORTHLESS.** `MAX_UNIT`
  was deleted for "catching nothing" and is **the only** catcher of the
  `<script>`-leak mutation that restores B4's vacuity at **19,178** characters;
  the residue rule is **the only** catcher of an entity-encoded price
  (`&dollar;150`). Neither had a row, which is why both looked worthless.
  **Before deleting an assertion for an empty catch record, write the row that
  would catch its failure and RUN it.** Memory: `untested-not-worthless`.
- **A SCRIPTED EDIT'S OWN `OK` IS NOT EVIDENCE IT LANDED.** Edit batches print a
  per-edit `OK` and write the file **once, at the end**; a batch that raises
  later **discards every earlier edit while its `OK` lines stay on screen.** It
  happened twice this session. Cost the first time: a reading-order table left
  pointing at a **withdrawn** revision as THE SPEC, and a strikethrough with an
  unmatched `~~` that rendered the dead text as live. **Grep the DESTINATION.**
  Memory: `edit-landed-not-announced`.
- 🔴 **AND IT HAPPENED A THIRD TIME, IN THE FIX.** Closing `R12-9` I marked O2–O9
  done **in a loop** — a `for` over the row labels — having performed only some of
  their substance. **O9 was never done at all** and O8 half-landed. Caught by a
  fact-check agent, not by me. **Never mark a checklist complete with the same
  edit that is supposed to complete it.** *Marking is not doing, at any scale.*
- **A DISPOSITION TABLE IS A PROMISSORY NOTE, NOT A RECEIPT.** An audit of the
  two revisions found **6 of 11** and **10 of 14** claimed in-place corrections
  untouched, and **three named destinations that did not exist.** *"✅ applied in
  place" is the same act as "→ PERFORMED".* **The fix is to make the claim
  checkable in one grep** — which is what `R12-9` and `§L-9` are.
- **SANDBOX THE COMMIT, NOT THE FINDING.** BK-55 reported green three times on a
  commit whose §B-1 half had never been executed — then the retraction
  **over-corrected**, registering a red for a commit that actually reaches
  `✓ 397 checks, 0 failed`, because §K-1's sandbox omitted three of commit (b)'s
  own items. **A rule stated in a revision is not thereby applied by it.**
  Memory: `sandbox-the-commit`.
- **A NUMBER PROPAGATES FASTER THAN IT IS CHECKED.** *"14 of 19 spellings"* was
  inherited from an agent report and written to four places; its own enumeration
  says **nine of thirteen**. *"53 red-first rows"* was the highest label, not a
  count; the real figure is **59 entries, 58 live, across FIVE segments**. **Read
  the list printed beside the number.**

## ⚠️ The instrument was written TWICE, and that is the lesson in miniature

The first version segmented on **words** instead of blocks. It produced
confident, plausible numbers. **It was caught only because two of its own outputs
contradicted each other** — a blog post reported as both the largest and a small
unit. *Three times this session a cross-check caught something that would
otherwise have been reported as fact. Not once did re-reading my own work catch
anything.*

## Unowned, live, still needing a ticket — unchanged and still true

- 🔴 **The three decision-20 pages** — `insurance-claims.astro:70`,
  `services.ts:389`, `services.ts:291` — say the assessment produces a written
  document. **Decision 20 makes that false. HIGH, customer-facing, wrong today,
  owned by nobody.** *(All three verified verbatim by an independent fact-check.)*
  ⚠️ **Plus `new.astro:192`, which is ADMIN-facing** — `src/pages/admin/appointments/new.astro`
  is an office form, so it is a different severity, not a fourth customer page. 🛑 **Do NOT fix inline in BK-53.**
- **The blog** — client-authored, no gate at all. **And it now sets `MAX_UNIT`.**
- **`/upload/`** — non-prerendered, invisible to every corpus.

---

# 13 · SESSION 5 — 2026-09-09. **THE VALIDATION ROUND RAN AND CHANGED BOTH TICKETS' DESIGN. BK-57 SHIPPED. DECISION 30 COLLAPSED A BRANCH.**

**§0's checks were re-run and passed.** `bk51-gated` is untouched at 10 commits
ahead, **still 6 non-docs files divergent, and STILL NOT PUSHED** —
`git ls-remote --heads origin` returns exactly 3, counted.

🔴 **THIS SESSION PUSHED TO PRODUCTION — the first production deploy in several
sessions.** `699b099`, BK-57, **three sentences on three pages.** Verified
against the live artifact. **`main` and `origin/main` are now IN SYNC**, so the
`grep -v '^docs/'` check is empty because everything is pushed, not because
nothing is stageable. *That check's meaning changed; the prompt says so.*

## What happened, in order

1. **Four fresh agents** — traceability audit + adversarial pre-read, per ticket.
2. **The findings were landed at their DESTINATIONS**, not written as revision 13
   of BK-53. `§T` (BK-53) and `§N` (BK-55) hold the evidence.
3. **The user answered both blocked items**, and the first answer turned out to
   be bigger than the question asked.
4. **BK-57 opened, implemented, gated, pushed and verified live.**
5. **BK-55 revision 8** — decision 30, with every case re-derived.

## 🔴 THE THREE FINDINGS THAT CHANGE BK-53's GATE DESIGN

**All three are already in `S2`. `§T` is the evidence.**

| | |
| --- | --- |
| **The `.js` arm** | A hydration-rendered price is in **no HTML file**. `verify-cutover.ts:1029-1030` is **already** `.html \|\| .js`, so the specced gate was a **REGRESSION against a hole this repo closed once**. 🔴 **THREE independent instances found, NONE PLANTED** — including **every FAQ answer on the site** via `services.*.js`. *An HTML-only corpus misses the largest body of prose the site has* |
| **The corpus CLOSURE** | `export const prerender = false;` removes a page from `dist/client` in **one line**. The gate's output goes **byte-identical to the honest baseline**, `verify-cutover` exits `0`, and a `$150` plus a bare free claim ship live on `/contact/`. **`MIN_UNITS`, `MAX_UNIT` and all seven landmarks are per-file and structurally cannot see a file that is absent.** *R10-5 S-7 filed this as "latent." It is not* |
| **The SCOPE LIST** | Without it the gate is **red on arrival on the client's own marketing** — six findings on the blog and `insurance-claims`, including *"white mineral deposits"* and a *"$500 to $2,500"* deductible explainer. **R9-6 S10 excludes those eight pages and `THE SPEC` dropped the exclusion** |

✅ **AND `S3`/`S4` ARE CONFIRMED** — a second independent implementation
reproduced 273/222, 17 files, residue 0, and coupling 6 **on exactly `S4`'s
surfaces.** *First time a number in that ticket has been reproduced by someone
who did not write it.*

## 🔴 BK-55: NINETEEN OF THIRTY BREAKS GREEN

**Fifteen put a real price, a false promise or a false status in a customer's
inbox.** `§N-3` lists each with the assertion owed **and its positive twin.**

- 🔴 **`§L-4` is UNDERSTATED, and it changes the fix.** `$399.00 + GST ($418.95
  total)` — **the exact string the regex was written to catch** — ships green on
  the request email. **Not a shape problem: no money regex EVER runs against a
  request body.** Widening shapes without instantiating pins fixes none of it.
- **`subject` is checked for money nowhere in the file, on any message type.**
- **The ICS attachment bans policy, claim and insurer — and no amount.**
- **`150 dollars` walks past the `$0` guard** — BK-48's defect in the pin BK-48
  wrote.
- **`Your booking is confirmed.` ships on the REQUEST email** — five of six
  `BOOKED_CLAIM_SHAPES` run on the confirmed message only. *BK-36's trap, on the
  surface BK-36 was filed about.*
- **Nothing in the file bans a refund promise on any customer message.**
- 🔴 **`397 / 417` CANNOT EXIST.** The script prints **no number**: `check()`
  increments only `failures` and the success path is a bare `console.log`.
  Baseline is `EXIT=0`, `✓ booking notification checks passed`. **`O15` and
  `§L-8` are closed in a direction neither expected.**

## ✅ DECISION 30 — the paid-era branch collapses

**User, verbatim:** *"we dont have to worry about existing bookings made on
current terms which we are about to change."* ⚠️ **Three readings were put to
them rather than one assumed**; they chose **collapse the branch.**

**Deletes:** `TERMS_PRIOR_LINE` · `G4a/G4b/G4c` · red-first rows 18–20 ·
`TERMS_ERA_CUTOFF` *(a placeholder date nobody had filled in, gating a
customer-facing branch)*. **`G4` → THREE arms** on a field **renamed `termsAck`**,
because §I-6 required `termsEra` to mean an epoch and the collapse makes it stop
meaning that.

✅ 🔴 **BK-55 NOW ADDS NO CUSTOMER-FACING COPY, SO THE CLIENT IS NOT BLOCKED.**

🔴 **`§P-1` re-derives ALL 23 cases decided under the old rule.** **Two would
otherwise have vanished:** §H-1 *(13 web customers told their booking was taken
over the phone)* survives untouched because it keys on `source`, not the era; and
§H-5's `G10` is **restated**, not deleted — *a request fixture must never render
the historical arm* is the same invariant with a new name.

🙋 **AND ITS COST, FLAGGED FOR THE USER — `§P-4`:** a customer who accepted the
**paid** terms is shown the **free** terms block on a resend, and **the refund
entitlement is stated only in that block.** ⚠️ **Not a new defect class — it
ENLARGES `§H-4`'s population.** **The size of that population has never been
measured** and no database was connected to this session.

## ✅ BK-57 — opened, shipped, verified

Three live pages said the free assessment produces a written document, which
decision 20 makes false. **The list of three was handed over, so it was
RE-DERIVED** by grepping the claim in both grammars: **30 hits, no fourth unowned
surface.** One near-miss cleared and recorded *(the `$699` report tier's name is
true)*. **The fix stays silent on price and never writes "free"** — BK-53 has not
shipped, the site still quotes `$399`, and three new unqualified free claims are
the exact defect BK-53 exists to prevent.

## 🔴 FIVE TRAPS THIS SESSION PAID FOR — three of them the same trap

- 🔴 **A NEGATIVE CHECK WITH NO POSITIVE TWIN PASSES ON AN EMPTY DOCUMENT —
  THREE INSTANCES IN ONE SESSION.** `G4b` **measured vacuous** *(row 20 empties
  the body; its twin fires and `G4b` stays silent)*. BK-57's `V4` **satisfiable
  by deleting every "written scope" on the site** — proven, because red-first row
  2 left `V4` green. And BK-57's **live probe read "old claim gone" off a 301
  REDIRECT STUB** because `curl` had no `-L`; **both counts were zero, and a
  negative-only probe would have PASSED.** *Always pair the negative with a
  positive: the positive column cannot be satisfied by a stub.*
- 🔴 **A WORKTREE IS NOT `main`, AND SOURCE PARITY HIDES IT.** Both adversarial
  worktrees were **32 and 34 commits stale**: one's BK-53 had **no `THE SPEC`
  section at all**, the other's BK-55 was a **135-line revision-1 stub**. **The
  source was byte-identical**, so every code measurement stayed valid and only
  the spec was wrong. **Brief agents to read docs via `git show main:<path>`.**
  Memory: `worktree-reads-stale-docs`.
- 🔴 **AN ASSERTION MAY BE DELETED WHEN ITS SUBJECT IS GONE — THAT IS NOT THE
  FORBIDDEN DELETION.** `untested-not-worthless` forbids deleting for an **empty
  catch record**. `§P-2` states the distinction and makes it checkable: the
  banned string **never existed in `src/`**. *Without stating it, decision 30
  would have looked like the mistake the project already paid for.*
- 🔴 **READ THE LIST BESIDE THE NUMBER — again.** BK-57's first `V4` returned
  **five** hits and **not one was its own**: two were the still-true job-scoped
  claim, three were BK-53's. *The loose proximity shape was matching a class it
  should have excluded.*
- ✅ **AND THE EDIT-BATCH TRAP DID NOT LAND THIS TIME.** A batch raised on a
  duplicate anchor and **wrote nothing** — assert-then-write-once, and the
  destination greps proved it. *The pattern works; it is the pattern to keep.*

## 🛑 A HAZARD THE DEPLOY CREATED — `bk51-gated` IS NOW STALE RELATIVE TO PRODUCTION

🔴 **The non-docs divergence between `main` and `bk51-gated` went from SIX to
EIGHT, and every earlier brief in this repo says six.** The two new ones are
`src/data/services.ts` and `src/pages/insurance-claims.astro` — **BK-57's files,
which `bk51-gated` has never touched.** It is behind, not conflicting:
`git diff --quiet $(git merge-base main bk51-gated)..bk51-gated -- <file>`
confirms the branch made no change to either.

⚠️ **So `bk51-gated` still carries the three sentences BK-57 REMOVED from the
live site.** **Merging or cherry-picking it without rebasing would REVERT a
shipped customer-facing fix — and no gate would notice**, because
`verify-cutover.ts` passed with those sentences live for weeks. **Rebase onto
`main`, or confirm those two files come from `main`, before the final push.**

*This is the first time a production deploy and a long-lived local branch have
coexisted in this area. The count is now a moving number and must be re-counted,
never inherited.*

## Where the tickets are

| | |
| --- | --- |
| **BK-53** | **Revision 12 + `THE SPEC` + `§T`.** ⛔ **NOT APPROVED — plan review owed.** `S2` corrected; `§T-7` is the owed list, `T1` done |
| **BK-55** | **Revision 8 (`§P`) + `§N`.** ⛔ **NOT APPROVED — plan review owed.** `§N-3`'s nineteen breaks are the real work |
| **BK-57** | ✅ **DEPLOYED AND VERIFIED** (`699b099`) |
| **BK-51** | unchanged — `bk51-gated` only, never pushed |
| **BK-52 · BK-54 · BK-56** | unchanged, none blocking. **Next free number is BK-58** |
